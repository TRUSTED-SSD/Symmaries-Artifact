static java.nio.file.DirectoryStream java.nio.file.Files:newDirectoryStream(java.nio.file.Path r0)
{
java.nio.file.spi.FileSystemProvider $r1;
  java.nio.file.Files$AcceptAllFilter $r2;
  java.nio.file.DirectoryStream $r3;
  $r1 = java.nio.file.Files.provider(r0);
 $r2=java.nio.file.Files$AcceptAllFilter.FILTER;
 $r3 = $r1.newDirectoryStream(r0,(java.nio.file.DirectoryStream$Filter)$r2);
 return  $r3;
 }  

