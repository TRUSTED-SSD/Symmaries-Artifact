static boolean java.nio.file.Files:isSameFile(java.nio.file.Path r0,java.nio.file.Path r1)
{
java.nio.file.spi.FileSystemProvider $r2;
  boolean $z0;
  $r2 = java.nio.file.Files.provider(r0);
 $z0 = $r2.isSameFile(r0,r1);
 return  $z0;
 }  

