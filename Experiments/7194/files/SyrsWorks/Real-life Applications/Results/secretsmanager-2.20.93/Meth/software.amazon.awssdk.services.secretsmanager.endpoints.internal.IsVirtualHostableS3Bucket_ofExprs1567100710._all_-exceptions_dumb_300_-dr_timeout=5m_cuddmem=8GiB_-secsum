static software.amazon.awssdk.services.secretsmanager.endpoints.internal.IsVirtualHostableS3Bucket software.amazon.awssdk.services.secretsmanager.endpoints.internal.IsVirtualHostableS3Bucket:ofExprs (
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr
    @expr,
  boolean allowDots) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(@expr) ⊔ level(allowDots) ⊔ obj_level(@expr);
  ret(obj_level(@expr)) = level(@expr) ⊔ level(allowDots) ⊔ obj_level(@expr);
}