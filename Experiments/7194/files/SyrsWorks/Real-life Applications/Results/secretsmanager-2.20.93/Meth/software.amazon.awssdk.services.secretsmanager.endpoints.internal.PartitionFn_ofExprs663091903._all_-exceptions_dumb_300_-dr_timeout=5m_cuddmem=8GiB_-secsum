static software.amazon.awssdk.services.secretsmanager.endpoints.internal.PartitionFn software.amazon.awssdk.services.secretsmanager.endpoints.internal.PartitionFn:ofExprs (
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr expr) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(expr) ⊔ obj_level(expr);
  ret(obj_level(expr)) = level(expr) ⊔ obj_level(expr);
}