software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value software.amazon.awssdk.services.secretsmanager.endpoints.internal.IsVirtualHostableS3Bucket:eval(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Scope scope)
{
java.lang.String hostLabel;
  software.amazon.awssdk.utils.Pair $stack3;
  java.lang.Object $stack4;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr $stack5;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value $stack6;
  boolean $stack8;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value$Bool $stack10;
  $stack3 = this.expectTwoArgs();
 $stack4 = $stack3.left();
 $stack5=(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr) $stack4;
 $stack6 = $stack5.eval(scope);
 hostLabel = $stack6.expectString();
 $stack8 = this#software.amazon.awssdk.services.secretsmanager.endpoints.internal.IsVirtualHostableS3Bucket:allowDots(scope);
 if ($stack8==0) goto label0;
 $stack8 = hostLabel.matches("[a-z\\d][a-z\\d\\-.]{1,61}[a-z\\d]");
 if ($stack8==0) goto label1;
 $stack8 = hostLabel.matches("(\\d+\\.){3}\\d+");
 if ($stack8!=0) goto label1;
 $stack8 = hostLabel.matches(".*[.-]{2}.*");
 if ($stack8!=0) goto label1;
 $stack8=1;
 goto label2;
 label1:$stack8=0;
 label2:$stack10 = software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value.fromBool($stack8);
 return  $stack10;
 label0:$stack8 = hostLabel.matches("[a-z\\d][a-z\\d\\-]{1,61}[a-z\\d]");
 $stack10 = software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value.fromBool($stack8);
 return  $stack10;
 } 

