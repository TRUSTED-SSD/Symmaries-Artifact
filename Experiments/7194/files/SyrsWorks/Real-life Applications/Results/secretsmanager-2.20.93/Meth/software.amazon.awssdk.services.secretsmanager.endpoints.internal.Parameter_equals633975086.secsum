boolean software.amazon.awssdk.services.secretsmanager.endpoints.internal.Parameter:equals (
  java.lang.Object o) {
  guard = tt;
  ret(level()) = pc ⊔ level(o) ⊔ level(this) ⊔ obj_level(o);
  ret(obj_level(o)) = if obj_level(o) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(this) = ⊤ then obj_level(o) = ⊥ else level(o) = ⊤ ∧ level(this) = ⊥ ∧ obj_level(o) = ⊥) then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(o) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(this) = ⊤ then obj_level(o) = ⊥ else level(o) = ⊤ ∧ level(this) = ⊥ ∧ obj_level(o) = ⊥) then ⊤ else ⊥;
}