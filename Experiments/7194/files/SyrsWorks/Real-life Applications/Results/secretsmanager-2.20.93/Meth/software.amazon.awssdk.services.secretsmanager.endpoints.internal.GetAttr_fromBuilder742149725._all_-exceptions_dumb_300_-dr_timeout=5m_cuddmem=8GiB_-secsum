static software.amazon.awssdk.services.secretsmanager.endpoints.internal.GetAttr software.amazon.awssdk.services.secretsmanager.endpoints.internal.GetAttr:fromBuilder (
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.GetAttr$Builder
    builder) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(builder) ⊔ obj_level(builder);
  ret(obj_level(builder)) = level(builder) ⊔ obj_level(builder);
}