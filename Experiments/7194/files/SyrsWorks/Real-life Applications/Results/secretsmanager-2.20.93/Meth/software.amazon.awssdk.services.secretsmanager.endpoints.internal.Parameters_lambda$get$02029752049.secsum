static boolean software.amazon.awssdk.services.secretsmanager.endpoints.internal.Parameters:lambda$get$0 (
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Identifier
    name,
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Parameter
    param) {
  guard = tt;
  ret(level()) = pc ⊔ level(name) ⊔ level(param) ⊔ obj_level(name) ⊔ obj_level(param);
  ret(obj_level(name)) = if obj_level(name) = ⊤ ∨ pc ≠ ⊤ ∧ (if obj_level(param) = ⊤ then obj_level(name) = ⊥ else obj_level(name) = ⊥ ∧ obj_level(param) = ⊥ ∧ level(name) ⊔ level(param) = ⊤) then ⊤ else ⊥;
  ret(obj_level(param)) = if obj_level(name) ⊔ obj_level(param) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(name) = ⊥ ∧ obj_level(param) = ⊥ ∧ level(name) ⊔ level(param) = ⊤ then ⊤ else ⊥;
}