static software.amazon.awssdk.services.secretsmanager.endpoints.internal.Not software.amazon.awssdk.services.secretsmanager.endpoints.internal.Not:ofExpr (
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr expr) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(expr) ⊔ obj_level(expr);
  ret(obj_level(expr)) = level(expr) ⊔ obj_level(expr);
}