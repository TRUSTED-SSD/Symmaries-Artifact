static void software.amazon.awssdk.services.secretsmanager.DefaultSecretsManagerClient:lambda$updateSecretVersionStage$20(software.amazon.awssdk.metrics.MetricCollector apiCallMetricCollector,software.amazon.awssdk.metrics.MetricPublisher p)
{
software.amazon.awssdk.metrics.MetricCollection $stack2;
  $stack2 = apiCallMetricCollector.collect();
 p.publish($stack2);
 return;
 } 

