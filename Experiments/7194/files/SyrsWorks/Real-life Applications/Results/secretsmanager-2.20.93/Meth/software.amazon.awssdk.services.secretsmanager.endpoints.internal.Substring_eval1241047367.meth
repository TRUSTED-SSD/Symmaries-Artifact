software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value software.amazon.awssdk.services.secretsmanager.endpoints.internal.Substring:eval(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Scope scope)
{
java.util.List args;
  java.lang.String str;
  int startIndex;
  int stopIndex;
  boolean reverse;
  java.lang.Object $stack10;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr $stack11;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value $stack12;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value$None $stack26;
  int $stack27;
  software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value$Str $stack30;
  java.lang.StringBuilder $stack31;
  args = this.expectVariableArgs(4);
 $stack10 = args.get(0);
 $stack11=(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr) $stack10;
 $stack12 = $stack11.eval(scope);
 str = $stack12.expectString();
 $stack10 = args.get(1);
 $stack11=(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr) $stack10;
 $stack12 = $stack11.eval(scope);
 startIndex = $stack12.expectInt();
 $stack10 = args.get(2);
 $stack11=(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr) $stack10;
 $stack12 = $stack11.eval(scope);
 stopIndex = $stack12.expectInt();
 $stack10 = args.get(3);
 $stack11=(software.amazon.awssdk.services.secretsmanager.endpoints.internal.Expr) $stack10;
 $stack12 = $stack11.eval(scope);
 reverse = $stack12.expectBool();
 if (startIndex>=stopIndex) goto label0;
 $stack27 = str.length();
 $stack27=($stack27-1);
 if ($stack27>=stopIndex) goto label1;
 label0:$stack26=new software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value$None;
 $stack26#software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value$None();
 return  $stack26;
 label1:if (reverse==0) goto label2;
 $stack31=new java.lang.StringBuilder;
 $stack31#java.lang.StringBuilder(str);
 $stack31 = $stack31.reverse();
 str = $stack31.toString();
 $stack31=new java.lang.StringBuilder;
 str = str.substring(startIndex,stopIndex);
 $stack31#java.lang.StringBuilder(str);
 $stack31 = $stack31.reverse();
 str = $stack31.toString();
 goto label3;
 label2:str = str.substring(startIndex,stopIndex);
 label3:$stack30 = software.amazon.awssdk.services.secretsmanager.endpoints.internal.Value.fromStr(str);
 return  $stack30;
 } 

