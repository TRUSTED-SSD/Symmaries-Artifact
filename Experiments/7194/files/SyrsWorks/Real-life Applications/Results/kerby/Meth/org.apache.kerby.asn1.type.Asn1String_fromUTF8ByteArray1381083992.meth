static java.lang.String org.apache.kerby.asn1.type.Asn1String:fromUTF8ByteArray(byte[] bytes)
{
int i;
  int length;
  char[] cs;
  char ch;
  int codePoint;
  java.lang.String $stack11;
  byte $stack12;
  short $stack13;
  int $stack30;
  i=0;
 length=0;
 label0:codePoint=#bytes;
 if (i>=codePoint) goto label1;
 length=(length+1);
 $stack12=bytes[i];
 $stack13=($stack12 & 240);
 if ($stack13!=240) goto label2;
 length=(length+1);
 i=(i+4);
 goto label0;
 label2:$stack12=bytes[i];
 $stack13=($stack12 & 224);
 if ($stack13!=224) goto label3;
 i=(i+3);
 goto label0;
 label3:$stack12=bytes[i];
 $stack13=($stack12 & 192);
 if ($stack13!=192) goto label4;
 i=(i+2);
 goto label0;
 label4:i=(i+1);
 goto label0;
 label1:cs=new char[length];
 i=0;
 length=0;
 label5:codePoint=#bytes;
 if (i>=codePoint) goto label6;
 $stack12=bytes[i];
 $stack13=($stack12 & 240);
 if ($stack13!=240) goto label7;
 $stack12=bytes[i];
 $stack12=($stack12 & 3);
 codePoint=($stack12<<18);
 $stack30=(i+1);
 $stack12=bytes[$stack30];
 $stack12=($stack12 & 63);
 $stack30=($stack12<<12);
 codePoint=(codePoint | $stack30);
 $stack30=(i+2);
 $stack12=bytes[$stack30];
 $stack12=($stack12 & 63);
 $stack30=($stack12<<6);
 $stack30=(codePoint | $stack30);
 codePoint=(i+3);
 $stack12=bytes[codePoint];
 $stack12=($stack12 & 63);
 codePoint=($stack30 | $stack12);
 codePoint=(codePoint-65536);
 $stack30=(codePoint>>10);
 $stack30=(55296 | $stack30);
 ch=(char) $stack30;
 codePoint=(codePoint & 1023);
 codePoint=(56320 | codePoint);
 $stack30=length;
 length=(length+1);
 cs[$stack30]=ch;
 ch=(char) codePoint;
 i=(i+4);
 goto label8;
 label7:$stack12=bytes[i];
 $stack13=($stack12 & 224);
 if ($stack13!=224) goto label9;
 $stack12=bytes[i];
 $stack12=($stack12 & 15);
 codePoint=($stack12<<12);
 $stack30=(i+1);
 $stack12=bytes[$stack30];
 $stack12=($stack12 & 63);
 $stack30=($stack12<<6);
 $stack30=(codePoint | $stack30);
 codePoint=(i+2);
 $stack12=bytes[codePoint];
 $stack12=($stack12 & 63);
 codePoint=($stack30 | $stack12);
 ch=(char) codePoint;
 i=(i+3);
 goto label8;
 label9:$stack12=bytes[i];
 $stack13=($stack12 & 208);
 if ($stack13!=208) goto label10;
 $stack12=bytes[i];
 $stack12=($stack12 & 31);
 $stack30=($stack12<<6);
 codePoint=(i+1);
 $stack12=bytes[codePoint];
 $stack12=($stack12 & 63);
 codePoint=($stack30 | $stack12);
 ch=(char) codePoint;
 i=(i+2);
 goto label8;
 label10:$stack12=bytes[i];
 $stack13=($stack12 & 192);
 if ($stack13!=192) goto label11;
 $stack12=bytes[i];
 $stack12=($stack12 & 31);
 $stack30=($stack12<<6);
 codePoint=(i+1);
 $stack12=bytes[codePoint];
 $stack12=($stack12 & 63);
 codePoint=($stack30 | $stack12);
 ch=(char) codePoint;
 i=(i+2);
 goto label8;
 label11:$stack12=bytes[i];
 $stack13=($stack12 & 255);
 ch=(char) $stack13;
 i=(i+1);
 label8:codePoint=length;
 length=(length+1);
 cs[codePoint]=ch;
 goto label5;
 label6:$stack11=new java.lang.String;
 $stack11#java.lang.String(cs);
 return  $stack11;
 } 

