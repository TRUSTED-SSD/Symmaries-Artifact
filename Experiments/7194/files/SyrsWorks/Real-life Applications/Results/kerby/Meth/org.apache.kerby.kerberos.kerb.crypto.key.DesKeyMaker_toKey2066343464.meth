byte[] org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:toKey(java.lang.String string,java.lang.String salt)
{
byte[] bytes;
  byte[] fanFoldedKey;
  bytes = org.apache.kerby.kerberos.kerb.crypto.key.AbstractKeyMaker.makePasswdSalt(string,salt);
 bytes = org.apache.kerby.kerberos.kerb.crypto.util.BytesUtil.padding(bytes,8);
 fanFoldedKey = org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.fanFold(string,salt,bytes);
 fanFoldedKey = org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.intermediateKey(fanFoldedKey);
 bytes = this#org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:desEncryptedKey(fanFoldedKey,bytes);
 org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.keyCorrection(bytes);
 return  bytes;
 } 

