java.nio.ByteBuffer org.apache.kerby.asn1.parse.Asn1ParseResult:getBodyBuffer()
{
java.nio.ByteBuffer result;
  int end;
  int $stack8;
  result=this.buffer;
 result = result.duplicate();
 end=this.bodyStart;
 result.position(end);
 end = this.getBodyEnd();
 $stack8=this.bodyStart;
 if (end<$stack8) goto label0;
 result.limit(end);
 label0:return  result;
 } 

