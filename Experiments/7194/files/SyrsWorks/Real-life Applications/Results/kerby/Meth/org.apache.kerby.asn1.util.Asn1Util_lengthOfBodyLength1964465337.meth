static int org.apache.kerby.asn1.util.Asn1Util:lengthOfBodyLength(int bodyLength)
{
int length;
  length=1;
 if (bodyLength<=127) goto label0;
 label1:if (bodyLength==0) goto label0;
 bodyLength=(bodyLength>>8);
 length=(length+1);
 goto label1;
 label0:return  length;
 } 

