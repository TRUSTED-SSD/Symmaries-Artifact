static byte org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:pstep(byte x,int step)
{
int $stack2;
  $stack2 = org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.smask(step);
 $stack2=(x & $stack2);
 x=(x>>step);
 step = org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.smask(step);
 step=(x & step);
 step=(!$stack2 & step) |(!step & $stack2);
 x=(byte) step;
 return  x;
 } 

