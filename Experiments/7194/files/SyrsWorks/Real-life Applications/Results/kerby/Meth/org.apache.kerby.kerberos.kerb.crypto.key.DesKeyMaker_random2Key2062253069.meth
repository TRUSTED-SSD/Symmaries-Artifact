byte[] org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:random2Key(byte[] @randomBits)
{
int $stack3;
  org.apache.kerby.kerberos.kerb.crypto.enc.EncryptProvider $stack4;
  int $stack5;
  org.apache.kerby.kerberos.kerb.KrbException $stack7;
  byte[] randomBits;
randomBits=@randomBits;
$stack3=#randomBits;
 $stack4 = this.encProvider();
 $stack5 = $stack4.keyInputSize();
 if ($stack3==$stack5) goto label0;
 $stack7=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack7#org.apache.kerby.kerberos.kerb.KrbException("Invalid random bits, not of correct bytes size");
 throw $stack7;
 label0:randomBits = org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.getEightBits(randomBits);
 org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.addParity(randomBits);
 org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.keyCorrection(randomBits);
 return  randomBits;
 } 

