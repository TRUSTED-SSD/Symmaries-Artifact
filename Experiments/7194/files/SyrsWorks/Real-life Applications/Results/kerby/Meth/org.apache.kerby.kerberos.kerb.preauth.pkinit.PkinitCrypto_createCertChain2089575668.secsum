static java.security.cert.X509Certificate[] org.apache.kerby.kerberos.kerb.preauth.pkinit.PkinitCrypto:createCertChain (
  org.apache.kerby.kerberos.kerb.preauth.pkinit.PkinitPlgCryptoContext
    cryptoContext) {
  guard = pc = ⊥;
  ret(level()) = ⊥;
  ret(obj_level()) = ⊥;
  ret(obj_level(cryptoContext)) = obj_level(cryptoContext);
}