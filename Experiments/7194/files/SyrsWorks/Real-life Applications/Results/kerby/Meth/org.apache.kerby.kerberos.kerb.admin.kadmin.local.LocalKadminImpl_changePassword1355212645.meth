void org.apache.kerby.kerberos.kerb.admin.kadmin.local.LocalKadminImpl:changePassword(java.lang.String @principal,java.lang.String newPassword)
{
org.apache.kerby.kerberos.kerb.request.KrbIdentity identity;
  java.util.List keys;
  org.apache.kerby.kerberos.kerb.identity.backend.IdentityBackend $stack6;
  org.apache.kerby.kerberos.kerb.server.KdcConfig $stack8;
  org.apache.kerby.kerberos.kerb.KrbException $stack13;
  java.lang.StringBuilder $stack14;
  java.lang.String principal;
principal=@principal;
principal = this#org.apache.kerby.kerberos.kerb.admin.kadmin.local.LocalKadminImpl:fixPrincipal(principal);
 $stack6=this.backend;
 identity = $stack6.getIdentity(principal);
 if (identity!=null) goto label0;
 $stack13=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack14=new java.lang.StringBuilder;
 $stack14#java.lang.StringBuilder();
 $stack14 = $stack14.append("Principal ");
 $stack14 = $stack14.append(principal);
 $stack14 = $stack14.append(" was not found. Please check the input and try again");
 principal = $stack14.toString();
 $stack13#org.apache.kerby.kerberos.kerb.KrbException(principal);
 throw $stack13;
 label0:$stack8 = this.getKdcConfig();
 keys = $stack8.getEncryptionTypes();
 keys = org.apache.kerby.kerberos.kerb.common.EncryptionUtil.generateKeys(principal,newPassword,keys);
 identity.addKeys(keys);
 $stack6=this.backend;
 $stack6.updateIdentity(identity);
 return;
 } 

