void org.apache.kerby.kerberos.kerb.ccache.CredCacheOutputStream:writeAuthzData(org.apache.kerby.kerberos.kerb.type.ad.AuthorizationData authData)
{
java.util.Iterator l2;
  org.apache.kerby.kerberos.kerb.type.ad.AuthorizationDataEntry entry;
  java.util.List $stack4;
  boolean $stack6;
  java.lang.Object $stack7;
  org.apache.kerby.kerberos.kerb.type.ad.AuthorizationType $stack8;
  int $stack9;
  byte[] $stack10;
  if (authData!=null) goto label0;
 this.writeInt(0);
 goto label1;
 label0:$stack4 = authData.getElements();
 l2 = $stack4.iterator();
 label2:$stack6 = l2.hasNext();
 if ($stack6==0) goto label1;
 $stack7 = l2.next();
 entry=(org.apache.kerby.kerberos.kerb.type.ad.AuthorizationDataEntry) $stack7;
 $stack8 = entry.getAuthzType();
 $stack9 = $stack8.getValue();
 dummyClass.lowSink($stack9);
this.write($stack9);
 $stack10 = entry.getAuthzData();
 $stack9=#$stack10;
 dummyClass.lowSink($stack9);
this.write($stack9);
 $stack10 = entry.getAuthzData();
 dummyClass.lowSink($stack10);
this.write($stack10);
 goto label2;
 label1:return;
 } 

