static java.nio.ByteBuffer org.apache.kerby.kerberos.kerb.admin.message.KadminCode:encodeMessage(org.apache.kerby.kerberos.kerb.admin.message.AdminMessage adminMessage)
{
int length;
  java.nio.ByteBuffer buffer;
  int $stack4;
  java.nio.ByteBuffer $stack6;
  length = adminMessage.encodingLength();
 $stack4=(length+4);
 buffer = java.nio.ByteBuffer.allocate($stack4);
 buffer.putInt(length);
 $stack6 = adminMessage.getMessageBuffer();
 buffer.put($stack6);
 buffer.flip();
 return  buffer;
 } 

