byte[] org.apache.kerby.xdr.type.AbstractXdrType:encode()
{
int len;
  java.nio.ByteBuffer byteBuffer;
  byte[] $stack6;
  len = this.encodingLength();
 byteBuffer = java.nio.ByteBuffer.allocate(len);
 this.encode(byteBuffer);
 byteBuffer.flip();
 $stack6 = byteBuffer.array();
 return  $stack6;
 } 

