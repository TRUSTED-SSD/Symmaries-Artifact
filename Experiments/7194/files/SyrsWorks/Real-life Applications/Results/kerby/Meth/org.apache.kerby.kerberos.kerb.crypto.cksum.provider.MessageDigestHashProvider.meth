org.apache.kerby.kerberos.kerb.crypto.cksum.provider.MessageDigestHashProvider(int hashSize,int blockSize,java.lang.String algorithm)
{
this#org.apache.kerby.kerberos.kerb.crypto.cksum.provider.AbstractHashProvider(hashSize,blockSize);
 this.algorithm=algorithm;
 this.init();
 return;
 } 

