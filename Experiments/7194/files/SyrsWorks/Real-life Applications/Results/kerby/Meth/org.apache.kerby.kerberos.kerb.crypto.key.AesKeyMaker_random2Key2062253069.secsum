byte[] org.apache.kerby.kerberos.kerb.crypto.key.AesKeyMaker:random2Key (
  byte[] randomBits) {
  guard = tt;
  ret(level()) = pc ⊔ level(randomBits);
  ret(obj_level()) = obj_level(randomBits);
  ret(obj_level(randomBits)) = obj_level(randomBits);
  ret(obj_level(this)) = obj_level(this);
}