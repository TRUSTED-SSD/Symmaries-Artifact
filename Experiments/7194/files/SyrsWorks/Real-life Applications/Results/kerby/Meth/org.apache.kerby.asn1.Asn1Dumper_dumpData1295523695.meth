org.apache.kerby.asn1.Asn1Dumper org.apache.kerby.asn1.Asn1Dumper:dumpData(java.lang.String @hexData)
{
byte range;
  int pos;
  int $stack4;
  java.io.PrintStream $stack5;
  java.lang.String $stack11;
  java.lang.String hexData;
hexData=@hexData;
range=100;
 pos=100;
 label0:$stack4 = hexData.length();
 if (pos>=$stack4) goto label1;
 $stack5=java.lang.System.out;
 $stack4=(pos-100);
 $stack11 = hexData.substring($stack4,pos);
 dummyClass.lowSink($stack11);
$stack5.println($stack11);
 pos=(pos+100);
 goto label0;
 label1:$stack5=java.lang.System.out;
 pos=(pos-100);
 $stack4 = hexData.length();
 hexData = hexData.substring(pos,$stack4);
 dummyClass.lowSink(hexData);
$stack5.println(hexData);
 return  this;
 } 

