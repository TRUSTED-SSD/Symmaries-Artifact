void org.apache.kerby.kerberos.kerb.server.request.KdcRequest:checkPolicy()
{
org.apache.kerby.kerberos.kerb.request.KrbIdentity entry;
  org.slf4j.Logger $stack3;
  boolean $stack4;
  org.apache.kerby.kerberos.kerb.type.KerberosTime $stack6;
  java.util.Date $stack7;
  long $stack8;
  org.apache.kerby.kerberos.kerb.KrbException $stack10;
  org.apache.kerby.kerberos.kerb.KrbErrorCode $stack11;
  java.lang.StringBuilder $stack12;
  java.lang.String $stack15;
  entry = this.getClientEntry();
 if (entry==null) goto label0;
 $stack4 = entry.isDisabled();
 if ($stack4==0) goto label1;
 $stack3=org.apache.kerby.kerberos.kerb.server.request.KdcRequest.LOG;
 $stack12=new java.lang.StringBuilder;
 $stack12#java.lang.StringBuilder();
 $stack12 = $stack12.append("Client entry ");
 $stack15 = entry.getPrincipalName();
 $stack12 = $stack12.append($stack15);
 $stack12 = $stack12.append(" is disabled.");
 $stack15 = $stack12.toString();
 $stack3.warn($stack15);
 $stack10=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack11=org.apache.kerby.kerberos.kerb.KrbErrorCode.KDC_ERR_CLIENT_REVOKED;
 $stack10#org.apache.kerby.kerberos.kerb.KrbException($stack11);
 throw $stack10;
 label1:$stack4 = entry.isLocked();
 if ($stack4==0) goto label2;
 $stack3=org.apache.kerby.kerberos.kerb.server.request.KdcRequest.LOG;
 $stack12=new java.lang.StringBuilder;
 $stack12#java.lang.StringBuilder();
 $stack12 = $stack12.append("Client entry ");
 $stack15 = entry.getPrincipalName();
 $stack12 = $stack12.append($stack15);
 $stack12 = $stack12.append(" is expired.");
 $stack15 = $stack12.toString();
 $stack3.warn($stack15);
 $stack10=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack11=org.apache.kerby.kerberos.kerb.KrbErrorCode.KDC_ERR_CLIENT_REVOKED;
 $stack10#org.apache.kerby.kerberos.kerb.KrbException($stack11);
 throw $stack10;
 label2:$stack6 = entry.getExpireTime();
 $stack7=new java.util.Date;
 $stack7#java.util.Date();
 $stack8 = $stack7.getTime();
 $stack4 = $stack6.lessThan($stack8);
 if ($stack4==0) goto label3;
 $stack10=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack11=org.apache.kerby.kerberos.kerb.KrbErrorCode.KDC_ERR_CLIENT_REVOKED;
 $stack10#org.apache.kerby.kerberos.kerb.KrbException($stack11);
 throw $stack10;
 label0:$stack3=org.apache.kerby.kerberos.kerb.server.request.KdcRequest.LOG;
 dummyClass.lowSink("Client entry is empty, token preauth or cross realm.");
$stack3.info("Client entry is empty, token preauth or cross realm.");
 label3:return;
 } 

