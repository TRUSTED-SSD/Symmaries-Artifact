byte[] org.apache.kerby.kerberos.kerb.crypto.cksum.AbstractCheckSumTypeHandler:checksum(byte[] @data)
{
int $stack2;
  byte[] data;
data=@data;
$stack2=#data;
 data = this.checksum(data,0,$stack2);
 return  data;
 } 

