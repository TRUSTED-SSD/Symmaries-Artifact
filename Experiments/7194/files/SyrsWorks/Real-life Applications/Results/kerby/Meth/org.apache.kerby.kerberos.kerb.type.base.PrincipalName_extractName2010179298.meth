static java.lang.String org.apache.kerby.kerberos.kerb.type.base.PrincipalName:extractName(java.lang.String @principal)
{
int pos;
  java.lang.String principal;
principal=@principal;
pos = principal.indexOf(64);
 if (pos>=0) goto label0;
 return  principal;
 label0:principal = principal.substring(0,pos);
 return  principal;
 } 

