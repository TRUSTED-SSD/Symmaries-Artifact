static void org.apache.kerby.kerberos.kerb.crypto.util.Cmac:encryptBlock(org.apache.kerby.kerberos.kerb.crypto.enc.EncryptProvider encProvider,byte[] key,byte[] @cipherState,byte[] block)
{
boolean $stack4;
  int $stack6;
  byte[] cipherState;
cipherState=@cipherState;
if (cipherState!=null) goto label0;
 $stack6 = encProvider.blockSize();
 cipherState=new byte[$stack6];
 label0:$stack4 = encProvider.supportCbcMac();
 if ($stack4==0) goto label1;
 encProvider.cbcMac(key,cipherState,block);
 goto label2;
 label1:encProvider.encrypt(key,cipherState,block);
 label2:return;
 } 

