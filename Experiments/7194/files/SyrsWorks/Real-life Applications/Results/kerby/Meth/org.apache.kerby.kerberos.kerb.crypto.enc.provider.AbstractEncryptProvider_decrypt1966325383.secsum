void org.apache.kerby.kerberos.kerb.crypto.enc.provider.AbstractEncryptProvider:decrypt (
  byte[] key, byte[] cipherState, byte[] data) {
  guard = tt;
  ret(obj_level(cipherState)) = obj_level(cipherState);
  ret(obj_level(data)) = obj_level(data);
  ret(obj_level(key)) = obj_level(key);
  ret(obj_level(this)) = obj_level(this);
}