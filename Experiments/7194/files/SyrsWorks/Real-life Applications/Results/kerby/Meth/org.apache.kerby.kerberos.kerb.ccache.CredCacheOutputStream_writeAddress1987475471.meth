void org.apache.kerby.kerberos.kerb.ccache.CredCacheOutputStream:writeAddress(org.apache.kerby.kerberos.kerb.type.base.HostAddress address)
{
org.apache.kerby.kerberos.kerb.type.base.HostAddrType $stack2;
  int $stack3;
  byte[] $stack4;
  byte[] $stack8;
  $stack2 = address.getAddrType();
 $stack3 = $stack2.getValue();
 dummyClass.lowSink($stack3);
this.write($stack3);
 $stack4 = address.getAddress();
 $stack3=#$stack4;
 dummyClass.lowSink($stack3);
this.write($stack3);
 $stack4 = address.getAddress();
 $stack8 = address.getAddress();
 $stack3=#$stack8;
 dummyClass.lowSink($stack4);
this.write($stack4,0,$stack3);
 return;
 } 

