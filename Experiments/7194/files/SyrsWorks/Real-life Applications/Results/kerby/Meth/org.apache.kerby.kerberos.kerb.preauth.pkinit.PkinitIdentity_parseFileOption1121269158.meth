static void org.apache.kerby.kerberos.kerb.preauth.pkinit.PkinitIdentity:parseFileOption(org.apache.kerby.kerberos.kerb.preauth.pkinit.IdentityOpts identityOpts,java.lang.String @residual)
{
java.lang.String[] parts;
  java.lang.String keyName;
  int $stack6;
  java.lang.String residual;
residual=@residual;
parts = residual.split(",");
 residual=parts[0];
 keyName=null;
 $stack6=#parts;
 if ($stack6<=1) goto label0;
 keyName=parts[1];
 label0:identityOpts.setCertFile(residual);
 identityOpts.setKeyFile(keyName);
 return;
 } 

