byte[] org.apache.kerby.kerberos.kerb.crypto.dh.DiffieHellmanClient:decrypt(byte[] @cipherText,org.apache.kerby.kerberos.kerb.type.base.KeyUsage usage)
{
org.apache.kerby.kerberos.kerb.crypto.EncTypeHandler encType;
  org.apache.kerby.kerberos.kerb.type.base.EncryptionKey $stack4;
  org.apache.kerby.kerberos.kerb.type.base.EncryptionType $stack5;
  byte[] $stack8;
  int $stack9;
  byte[] cipherText;
cipherText=@cipherText;
$stack4=this.clientKey;
 $stack5 = $stack4.getKeyType();
 encType = org.apache.kerby.kerberos.kerb.crypto.EncryptionHandler.getEncHandler($stack5);
 $stack4=this.clientKey;
 $stack8 = $stack4.getKeyData();
 $stack9 = usage.getValue();
 cipherText = encType.decrypt(cipherText,$stack8,$stack9);
 return  cipherText;
 } 

