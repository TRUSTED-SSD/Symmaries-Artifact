java.lang.String org.apache.kerby.kerberos.kerb.admin.kadmin.remote.command.RemoteDeletePrincipalCommand:getReply(java.util.Scanner scanner,java.lang.String @prompt)
{
java.io.PrintStream $stack3;
  java.lang.String prompt;
prompt=@prompt;
$stack3=java.lang.System.out;
 dummyClass.lowSink(prompt);
$stack3.println(prompt);
 prompt = scanner.nextLine();
dummyClass.highSourceMut(prompt);
 prompt = prompt.trim();
 return  prompt;
 } 

