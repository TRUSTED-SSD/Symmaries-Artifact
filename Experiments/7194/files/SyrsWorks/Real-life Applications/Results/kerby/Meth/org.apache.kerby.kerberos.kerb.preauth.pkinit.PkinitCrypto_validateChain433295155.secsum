static void org.apache.kerby.kerberos.kerb.preauth.pkinit.PkinitCrypto:validateChain (
  java.util.List certificateList, java.security.cert.X509Certificate anchor) {
  guard = tt;
  ret(obj_level(anchor)) = ⊤;
  ret(obj_level(certificateList)) = ⊤;
}