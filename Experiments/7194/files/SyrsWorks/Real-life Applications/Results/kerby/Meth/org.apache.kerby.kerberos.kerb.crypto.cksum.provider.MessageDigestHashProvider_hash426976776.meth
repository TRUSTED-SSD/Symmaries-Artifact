void org.apache.kerby.kerberos.kerb.crypto.cksum.provider.MessageDigestHashProvider:hash(byte[] data,int start,int len)
{
java.security.MessageDigest $stack4;
  $stack4=this.messageDigest;
 $stack4.update(data,start,len);
 return;
 } 

