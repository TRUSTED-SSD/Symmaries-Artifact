java.lang.String org.apache.kerby.kerberos.kerb.common.Krb5Parser:insertEntries(java.lang.String @line,java.io.BufferedReader br,java.util.Map entries)
{
java.lang.String[] kv;
  java.util.IdentityHashMap meValue;
  java.util.ArrayList values;
  boolean $stack7;
  java.lang.String $stack25;
  java.lang.String line;
line=@line;
if (line!=null) goto label0;
 return  line;
 label0:$stack7 = line.startsWith("[");
 if ($stack7==0) goto label1;
 return  line;
 label1:$stack7 = line.startsWith("}");
 if ($stack7==0) goto label2;
 line = br.readLine();
dummyClass.highSourceMut(line);
 if (line==null) goto label3;
 line = line.trim();
 label3:return  line;
 label2:$stack7 = line.isEmpty();
 if ($stack7!=0) goto label4;
 $stack7 = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:isComment(line);
 if ($stack7==0) goto label5;
 label4:line = br.readLine();
dummyClass.highSourceMut(line);
 if (line==null) goto label6;
 line = line.trim();
 line = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:insertEntries(line,br,entries);
 label6:return  line;
 label5:kv = line.split("=",2);
 line=kv[0];
 line = line.trim();
 kv[0]=line;
 line=kv[1];
 line = line.trim();
 kv[1]=line;
 line=kv[1];
 $stack7 = line.startsWith("{");
 if ($stack7==0) goto label7;
 meValue=new java.util.IdentityHashMap;
 meValue#java.util.IdentityHashMap();
 line = br.readLine();
dummyClass.highSourceMut(line);
 label8:if (line==null) goto label9;
 $stack25 = line.trim();
 $stack7 = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:isComment($stack25);
 if ($stack7!=0) goto label10;
 $stack25 = line.trim();
 $stack7 = $stack25.isEmpty();
 if ($stack7==0) goto label9;
 label10:line = br.readLine();
dummyClass.highSourceMut(line);
 goto label8;
 label9:if (line==null) goto label11;
 line = line.trim();
 $stack7 = line.contains((java.lang.CharSequence)"=");
 if ($stack7!=0) goto label12;
 $stack7 = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:isComment(line);
 if ($stack7!=0) goto label12;
 $stack7 = line.isEmpty();
 if ($stack7!=0) goto label12;
 values=new java.util.ArrayList;
 values#java.util.ArrayList();
 label13:if (line==null) goto label14;
 $stack7 = line.contains((java.lang.CharSequence)"}");
 if ($stack7!=0) goto label14;
 line = line.trim();
 $stack7 = line.isEmpty();
 if ($stack7!=0) goto label15;
 $stack7 = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:isComment(line);
 if ($stack7!=0) goto label15;
 line = line.trim();
 values.add((java.lang.Object)line);
 label15:line = br.readLine();
dummyClass.highSourceMut(line);
 goto label13;
 label14:line=kv[0];
 entries.put((java.lang.Object)line,(java.lang.Object)values);
 line = br.readLine();
dummyClass.highSourceMut(line);
 if (line==null) goto label16;
 line = line.trim();
 label16:goto label17;
 label12:line = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:insertEntries(line,br,(java.util.Map)meValue);
 $stack25=kv[0];
 entries.put((java.lang.Object)$stack25,(java.lang.Object)meValue);
 label17:line = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:insertEntries(line,br,entries);
 label11:goto label18;
 label7:$stack25=kv[0];
 line=kv[1];
 entries.put((java.lang.Object)$stack25,(java.lang.Object)line);
 line = br.readLine();
dummyClass.highSourceMut(line);
 if (line==null) goto label18;
 line = line.trim();
 line = this#org.apache.kerby.kerberos.kerb.common.Krb5Parser:insertEntries(line,br,entries);
 label18:return  line;
 } 

