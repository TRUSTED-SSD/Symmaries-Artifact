byte org.apache.kerby.kerberos.kerb.crypto.util.CamelliaKey:lRot8(byte v,int rot)
{
short $stack4;
  int $stack6;
  $stack6=(v<<rot);
 $stack4=(v & 255);
 rot=(8-rot);
 $stack4=($stack4>>>rot);
 rot=($stack6 | $stack4);
 v=(byte) rot;
 return  v;
 } 

