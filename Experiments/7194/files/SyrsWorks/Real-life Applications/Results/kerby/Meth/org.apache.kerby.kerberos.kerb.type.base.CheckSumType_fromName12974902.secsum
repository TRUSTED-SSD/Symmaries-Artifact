static org.apache.kerby.kerberos.kerb.type.base.CheckSumType org.apache.kerby.kerberos.kerb.type.base.CheckSumType:fromName (
  java.lang.String name) {
  guard = tt;
  ret(level()) = pc ⊔ level(name) ⊔ obj_level(name);
  ret(obj_level()) = pc ⊔ level(name) ⊔ obj_level(name);
  ret(obj_level(name)) = if obj_level(name) = ⊤ ∨ pc ≠ ⊤ ∧ level(name) = ⊤ ∧ obj_level(name) = ⊥ then ⊤ else ⊥;
}