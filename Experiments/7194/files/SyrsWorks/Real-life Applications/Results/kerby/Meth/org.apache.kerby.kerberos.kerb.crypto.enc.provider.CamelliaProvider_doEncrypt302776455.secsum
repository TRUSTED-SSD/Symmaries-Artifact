void org.apache.kerby.kerberos.kerb.crypto.enc.provider.CamelliaProvider:doEncrypt (
  byte[] data, byte[] key, byte[] cipherState, boolean encrypt) {
  guard = tt;
  ret(obj_level(cipherState)) = obj_level(cipherState);
  ret(obj_level(data)) = obj_level(cipherState);
  ret(obj_level(key)) = obj_level(cipherState);
  ret(obj_level(this)) = obj_level(this);
}