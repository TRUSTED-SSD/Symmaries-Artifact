java.lang.String org.apache.kerby.asn1.UniversalTag:toStr()
{
java.lang.String typeStr;
  typeStr = this.toString();
 typeStr = typeStr.replace((char)95,(char)32);
 typeStr = typeStr.toLowerCase();
 return  typeStr;
 } 

