static byte[] org.apache.kerby.kerberos.kerb.crypto.util.Hmac:hmac(org.apache.kerby.kerberos.kerb.crypto.cksum.HashProvider hashProvider,byte[] @key,byte[] data,int outputSize)
{
int $stack4;
  byte[] key;
key=@key;
$stack4=#data;
 key = org.apache.kerby.kerberos.kerb.crypto.util.Hmac.hmac(hashProvider,key,data,0,$stack4,outputSize);
 return  key;
 } 

