static byte[] org.apache.kerby.util.Util:fileToBytes(java.io.File f)
{
java.nio.file.Path $stack1;
  java.nio.file.OpenOption[] $stack2;
  java.io.InputStream $stack3;
  byte[] $stack4;
  $stack1 = f.toPath();
 $stack2=new java.nio.file.OpenOption[0];
 $stack3 = java.nio.file.Files.newInputStream($stack1,$stack2);
 $stack4 = org.apache.kerby.util.Util.streamToBytes($stack3);
 return  $stack4;
 } 

