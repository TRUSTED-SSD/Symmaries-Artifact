void org.apache.kerby.kerberos.kerb.crypto.enc.DesCbcEnc:encryptWith (
  byte[] workBuffer, int[] workLens, byte[] key, byte[] iv, int usage,
  boolean raw) {
  guard = tt;
  ret(obj_level(iv)) = obj_level(iv);
  ret(obj_level(key)) = obj_level(iv);
  ret(obj_level(this)) = if if pc = ⊥ then level(raw) ⊓ (obj_level(iv) ⊔ obj_level(workLens)) = ⊥ else pc = ⊤ then obj_level(this) else if pc = ⊥ ∧ level(raw) ⊓ (obj_level(iv) ⊔ obj_level(workLens)) = ⊤ then ⊤ else ⊥;
  ret(obj_level(workBuffer)) = obj_level(iv);
  ret(obj_level(workLens)) = obj_level(workLens);
}