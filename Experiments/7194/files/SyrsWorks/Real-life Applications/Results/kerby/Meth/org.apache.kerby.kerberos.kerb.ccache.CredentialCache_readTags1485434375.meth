java.util.List org.apache.kerby.kerberos.kerb.ccache.CredentialCache:readTags(org.apache.kerby.kerberos.kerb.ccache.CredCacheInputStream ccis)
{
int len;
  java.util.ArrayList tags;
  short tag;
  short tagLen;
  int time;
  int usec;
  org.apache.kerby.kerberos.kerb.ccache.Tag $stack14;
  byte[] $stack18;
  java.io.IOException $stack24;
  len = ccis.readShort();
dummyClass.highSourceMut(len);
 tags=new java.util.ArrayList;
 tags#java.util.ArrayList();
 label0:if (len<=0) goto label1;
 tag = ccis.readShort();
dummyClass.highSourceMut(tag);
 tagLen = ccis.readShort();
dummyClass.highSourceMut(tagLen);
 switch [tag]
if 0 goto label2;
goto label3;
 label2:time = ccis.readInt();
dummyClass.highSourceMut(time);
 usec = ccis.readInt();
dummyClass.highSourceMut(usec);
 $stack14=new org.apache.kerby.kerberos.kerb.ccache.Tag;
 $stack14#org.apache.kerby.kerberos.kerb.ccache.Tag(tag,time,usec);
 tags.add((java.lang.Object)$stack14);
 goto label4;
 label3:$stack18=new byte[tagLen];
 dummyClass.highSourceMut($stack18);
time = ccis.read($stack18,0,(int)tagLen);
 if (time!=-1) goto label4;
 $stack24=new java.io.IOException;
 $stack24#java.io.IOException();
 throw $stack24;
 label4:time=(4+tagLen);
 len=(len-time);
 goto label0;
 label1:return  tags;
 } 

