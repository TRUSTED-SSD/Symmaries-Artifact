byte[] org.apache.kerby.asn1.type.Asn1Encodeable:taggedEncode(org.apache.kerby.asn1.TaggingOption taggingOption)
{
int len;
  java.nio.ByteBuffer byteBuffer;
  byte[] $stack7;
  len = this.taggedEncodingLength(taggingOption);
 byteBuffer = java.nio.ByteBuffer.allocate(len);
 this.taggedEncode(byteBuffer,taggingOption);
 byteBuffer.flip();
 $stack7 = byteBuffer.array();
 return  $stack7;
 } 

