static byte[] org.apache.kerby.kerberos.kerb.crypto.util.Cmac:cmac(org.apache.kerby.kerberos.kerb.crypto.enc.EncryptProvider encProvider,byte[] @key,byte[] data,int outputSize)
{
int $stack4;
  byte[] key;
key=@key;
$stack4=#data;
 key = org.apache.kerby.kerberos.kerb.crypto.util.Cmac.cmac(encProvider,key,data,0,$stack4,outputSize);
 return  key;
 } 

