void org.apache.kerby.kerberos.kerb.admin.kadmin.local.LocalKadminImpl:modifyPrincipal(java.lang.String @principal,org.apache.kerby.KOptions kOptions)
{
org.apache.kerby.kerberos.kerb.request.KrbIdentity identity;
  org.apache.kerby.kerberos.kerb.identity.backend.IdentityBackend $stack5;
  org.apache.kerby.kerberos.kerb.KrbException $stack9;
  java.lang.StringBuilder $stack10;
  java.lang.String principal;
principal=@principal;
principal = this#org.apache.kerby.kerberos.kerb.admin.kadmin.local.LocalKadminImpl:fixPrincipal(principal);
 $stack5=this.backend;
 identity = $stack5.getIdentity(principal);
 if (identity!=null) goto label0;
 $stack9=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack10=new java.lang.StringBuilder;
 $stack10#java.lang.StringBuilder();
 $stack10 = $stack10.append("Principal \"");
 $stack10 = $stack10.append(principal);
 $stack10 = $stack10.append("\" does not exist.");
 principal = $stack10.toString();
 $stack9#org.apache.kerby.kerberos.kerb.KrbException(principal);
 throw $stack9;
 label0:org.apache.kerby.kerberos.kerb.admin.kadmin.local.AdminHelper.updateIdentity(identity,kOptions);
 $stack5=this.backend;
 $stack5.updateIdentity(identity);
 return;
 } 

