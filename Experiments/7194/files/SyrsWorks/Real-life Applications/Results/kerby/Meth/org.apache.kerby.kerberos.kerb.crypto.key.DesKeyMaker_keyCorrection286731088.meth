static void org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:keyCorrection(byte[] key)
{
int $stack1;
  org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.addParity(key);
 $stack1=#key;
 org.apache.kerby.kerberos.kerb.crypto.util.Des.fixKey(key,0,$stack1);
 return;
 } 

