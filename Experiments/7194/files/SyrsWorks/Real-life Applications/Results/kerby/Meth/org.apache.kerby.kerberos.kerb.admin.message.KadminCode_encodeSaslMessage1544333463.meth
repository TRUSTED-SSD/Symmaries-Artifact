static java.nio.ByteBuffer org.apache.kerby.kerberos.kerb.admin.message.KadminCode:encodeSaslMessage(byte[] bytes,org.apache.kerby.kerberos.kerb.admin.kadmin.remote.NegotiationStatus status)
{
int length;
  java.nio.ByteBuffer buffer;
  int $stack5;
  length=#bytes;
 length=(length+4);
 $stack5=(length+4);
 buffer = java.nio.ByteBuffer.allocate($stack5);
 buffer.putInt(length);
 length = status.getValue();
 buffer.putInt(length);
 buffer.put(bytes);
 buffer.flip();
 return  buffer;
 } 

