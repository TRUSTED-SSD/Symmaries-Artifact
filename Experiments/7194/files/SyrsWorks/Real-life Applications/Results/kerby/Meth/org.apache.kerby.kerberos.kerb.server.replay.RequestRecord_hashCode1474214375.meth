int org.apache.kerby.kerberos.kerb.server.replay.RequestRecord:hashCode()
{
int result;
  java.lang.String $stack2;
  int $stack6;
  long $stack7;
  long $stack9;
  $stack2=this.clientPrincipal;
 result = $stack2.hashCode();
 result=(31 * result);
 $stack2=this.serverPrincipal;
 $stack6 = $stack2.hashCode();
 result=(result+$stack6);
 $stack6=(31 * result);
 $stack9=this.requestTime;
 $stack7=this.requestTime;
 $stack7=($stack7>>>32);
 $stack9=(!$stack9 & $stack7) |(!$stack7 & $stack9);
 result=(int) $stack9;
 result=($stack6+result);
 $stack6=(31 * result);
 result=this.microseconds;
 result=($stack6+result);
 return  result;
 } 

