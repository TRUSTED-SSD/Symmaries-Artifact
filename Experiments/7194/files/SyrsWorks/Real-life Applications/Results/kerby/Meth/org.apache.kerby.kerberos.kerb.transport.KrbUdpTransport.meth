org.apache.kerby.kerberos.kerb.transport.KrbUdpTransport(java.net.InetSocketAddress remoteAddress)
{
java.nio.channels.DatagramChannel tmpChannel;
  java.nio.ByteBuffer $stack6;
  this#org.apache.kerby.kerberos.kerb.transport.AbstractKrbTransport();
 this.remoteAddress=remoteAddress;
 tmpChannel = java.nio.channels.DatagramChannel.open();
 tmpChannel.configureBlocking((boolean)1);
 tmpChannel.connect((java.net.SocketAddress)remoteAddress);
 this.setChannel(tmpChannel);
 $stack6 = java.nio.ByteBuffer.allocate(65507);
 this.recvBuffer=$stack6;
 return;
 } 

