boolean org.apache.kerby.kerberos.kerb.type.KerberosTime:isInClockSkew(long clockSkew)
{
long delta;
  long $stack6;
  byte $stack9;
  boolean $stack10;
  delta = this.getTime();
 $stack6 = java.lang.System.currentTimeMillis();
 delta=(delta-$stack6);
 delta = java.lang.Math.abs(delta);
 $stack9=(delta cmp clockSkew);
 if ($stack9>=0) goto label0;
 $stack10=1;
 goto label1;
 label0:$stack10=0;
 label1:return  $stack10;
 } 

