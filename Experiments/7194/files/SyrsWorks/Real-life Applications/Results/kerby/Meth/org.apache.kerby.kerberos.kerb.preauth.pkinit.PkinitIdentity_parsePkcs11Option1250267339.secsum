static void org.apache.kerby.kerberos.kerb.preauth.pkinit.PkinitIdentity:parsePkcs11Option (
  org.apache.kerby.kerberos.kerb.preauth.pkinit.IdentityOpts identityOpts,
  java.lang.String residual) {
  guard = tt;
  ret(obj_level(identityOpts)) = obj_level(identityOpts);
  ret(obj_level(residual)) = obj_level(residual);
}