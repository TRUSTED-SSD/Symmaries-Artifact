void org.apache.kerby.kerberos.kerb.ccache.CredCacheOutputStream:writeTicket(org.apache.kerby.kerberos.kerb.type.ticket.Ticket t)
{
byte[] bytes;
  int $stack4;
  if (t!=null) goto label0;
 this.writeInt(0);
 goto label1;
 label0:bytes = t.encode();
 $stack4=#bytes;
 this.writeInt($stack4);
 dummyClass.lowSink(bytes);
this.write(bytes);
 label1:return;
 } 

