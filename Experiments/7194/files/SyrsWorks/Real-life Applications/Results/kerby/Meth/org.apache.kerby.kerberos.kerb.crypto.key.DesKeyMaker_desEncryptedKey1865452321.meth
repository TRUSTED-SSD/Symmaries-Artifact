byte[] org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:desEncryptedKey(byte[] intermediateKey,byte[] originalBytes)
{
byte[] resultKey;
  org.apache.kerby.kerberos.kerb.crypto.enc.EncryptProvider $stack4;
  boolean $stack5;
  org.apache.kerby.kerberos.kerb.KrbException $stack6;
  java.lang.StringBuilder $stack7;
  java.lang.Class $stack10;
  java.lang.String $stack12;
  resultKey=null;
 $stack4 = this.encProvider();
 $stack5 = $stack4.supportCbcMac();
 if ($stack5==0) goto label0;
 $stack4 = this.encProvider();
 resultKey = $stack4.cbcMac(intermediateKey,intermediateKey,originalBytes);
 goto label1;
 label0:$stack6=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack7=new java.lang.StringBuilder;
 $stack7#java.lang.StringBuilder();
 $stack7 = $stack7.append("cbcMac should be supported by the provider: ");
 $stack4 = this.encProvider();
 $stack10 = $stack4.getClass();
 $stack7 = $stack7.append((java.lang.Object)$stack10);
 $stack12 = $stack7.toString();
 $stack6#org.apache.kerby.kerberos.kerb.KrbException($stack12);
 throw $stack6;
 label1:org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.keyCorrection(resultKey);
 return  resultKey;
 } 

