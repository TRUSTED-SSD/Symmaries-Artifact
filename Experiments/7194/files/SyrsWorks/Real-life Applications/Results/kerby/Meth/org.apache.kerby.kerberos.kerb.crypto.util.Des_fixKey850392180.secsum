static void org.apache.kerby.kerberos.kerb.crypto.util.Des:fixKey (byte[] key,
                                                                   int offset,
                                                                   int len) {
  guard = tt;
  ret(obj_level(key)) = pc ⊔ level(key) ⊔ level(len) ⊔ level(offset) ⊔ obj_level(key);
}