byte[][] org.apache.kerby.asn1.type.Asn1ObjectIdentifier:group(byte[] bytes)
{
int count;
  int i;
  int[] countArr;
  byte[][] bytesGroups;
  int k;
  int $stack11;
  byte $stack13;
  byte[] $stack14;
  short $stack16;
  count=0;
 i=#bytes;
 countArr=new int[i];
 i=0;
 label0:count=#countArr;
 if (i>=count) goto label1;
 countArr[i]=0;
 i=(i+1);
 goto label0;
 label1:count=0;
 i=0;
 label2:k=#bytes;
 if (i>=k) goto label3;
 $stack13=bytes[i];
 $stack16=($stack13 & 128);
 if ($stack16==0) goto label4;
 k=countArr[count];
 k=(k+1);
 countArr[count]=k;
 goto label5;
 label4:k=count;
 count=(count+1);
 $stack11=countArr[k];
 $stack11=($stack11+1);
 countArr[k]=$stack11;
 label5:i=(i+1);
 goto label2;
 label3:bytesGroups=new byte[][count];
 i=0;
 label6:if (i>=count) goto label7;
 k=countArr[i];
 $stack14=new byte[k];
 bytesGroups[i]=$stack14;
 i=(i+1);
 goto label6;
 label7:k=0;
 count=0;
 i=0;
 label8:$stack11=#bytes;
 if (i>=$stack11) goto label9;
 $stack14=bytesGroups[count];
 $stack11=k;
 k=(k+1);
 $stack13=bytes[i];
 $stack14[$stack11]=$stack13;
 $stack13=bytes[i];
 $stack16=($stack13 & 128);
 if ($stack16!=0) goto label10;
 count=(count+1);
 k=0;
 label10:i=(i+1);
 goto label8;
 label9:return  bytesGroups;
 } 

