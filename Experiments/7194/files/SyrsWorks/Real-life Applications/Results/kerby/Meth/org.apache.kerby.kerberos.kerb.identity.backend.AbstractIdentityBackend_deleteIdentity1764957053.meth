void org.apache.kerby.kerberos.kerb.identity.backend.AbstractIdentityBackend:deleteIdentity(java.lang.String @principalName)
{
org.slf4j.Logger $stack2;
  org.apache.kerby.kerberos.kerb.request.KrbIdentity $stack3;
  java.lang.StringBuilder $stack4;
  org.apache.kerby.kerberos.kerb.KrbException $stack10;
  java.lang.IllegalArgumentException $stack11;
  java.lang.String principalName;
principalName=@principalName;
$stack2=org.apache.kerby.kerberos.kerb.identity.backend.AbstractIdentityBackend.logger;
 dummyClass.lowSink("deleteIdentity called, principalName = {}");
$stack2.debug("deleteIdentity called, principalName = {}",(java.lang.Object)principalName);
 if (principalName!=null) goto label0;
 $stack11=new java.lang.IllegalArgumentException;
 $stack11#java.lang.IllegalArgumentException("null identity to remove");
 throw $stack11;
 label0:$stack3 = this.doGetIdentity(principalName);
 if ($stack3!=null) goto label1;
 $stack2=org.apache.kerby.kerberos.kerb.identity.backend.AbstractIdentityBackend.logger;
 $stack4=new java.lang.StringBuilder;
 $stack4#java.lang.StringBuilder();
 $stack4 = $stack4.append("Error occurred while deleting identity, principal ");
 $stack4 = $stack4.append(principalName);
 $stack4 = $stack4.append(" does not exist.");
 principalName = $stack4.toString();
 $stack2.error(principalName);
 $stack10=new org.apache.kerby.kerberos.kerb.KrbException;
 $stack10#org.apache.kerby.kerberos.kerb.KrbException("Principal does not exist.");
 throw $stack10;
 label1:this.doDeleteIdentity(principalName);
 return;
 } 

