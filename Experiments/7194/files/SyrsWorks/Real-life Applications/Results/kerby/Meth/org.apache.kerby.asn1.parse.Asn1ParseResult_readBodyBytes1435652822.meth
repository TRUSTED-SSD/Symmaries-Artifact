byte[] org.apache.kerby.asn1.parse.Asn1ParseResult:readBodyBytes()
{
java.nio.ByteBuffer bodyBuffer;
  byte[] result;
  int $stack4;
  bodyBuffer = this.getBodyBuffer();
 $stack4 = bodyBuffer.remaining();
 result=new byte[$stack4];
 bodyBuffer.get(result);
 return  result;
 } 

