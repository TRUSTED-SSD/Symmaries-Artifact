byte[] org.apache.kerby.kerberos.kerb.crypto.key.Des3KeyMaker:str2key(java.lang.String string,java.lang.String salt,byte[] @param)
{
int keyInputSize;
  org.apache.kerby.kerberos.kerb.crypto.enc.EncryptProvider $stack8;
  byte[] $stack12;
  byte[] param;
param=@param;
param = org.apache.kerby.kerberos.kerb.crypto.key.AbstractKeyMaker.makePasswdSalt(string,salt);
 $stack8 = this.encProvider();
 keyInputSize = $stack8.keyInputSize();
 param = org.apache.kerby.kerberos.kerb.crypto.util.Nfold.nfold(param,keyInputSize);
 param = this.random2Key(param);
 $stack12=org.apache.kerby.kerberos.kerb.crypto.key.AbstractKeyMaker.KERBEROS_CONSTANT;
 param = this.dk(param,$stack12);
 return  param;
 } 

