static byte[] org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:addParityBits(byte[] bits56)
{
int i;
  byte $stack2;
  int $stack3;
  i=0;
 label0:if (i>=8) goto label1;
 $stack2=bits56[i];
 $stack3=($stack2<<1);
 $stack2=(byte) $stack3;
 bits56[i]=$stack2;
 i=(i+1);
 goto label0;
 label1:org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker.addParity(bits56);
 return  bits56;
 } 

