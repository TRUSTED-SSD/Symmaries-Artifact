void org.apache.kerby.kerberos.kerb.keytab.Keytab:writeVersion(org.apache.kerby.kerberos.kerb.keytab.KeytabOutputStream kos)
{
byte[] bytes;
  int $stack3;
  byte $stack4;
  bytes=new byte[2];
 bytes[0]=5;
 $stack3=this.version;
 if ($stack3!=1282) goto label0;
 $stack4=2;
 goto label1;
 label0:$stack4=1;
 label1:bytes[1]=$stack4;
 dummyClass.lowSink(bytes);
kos.write(bytes);
 return;
 } 

