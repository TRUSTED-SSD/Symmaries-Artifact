static boolean org.apache.kerby.kerberos.kerb.crypto.key.DesKeyMaker:checkKeyParity (
  byte[] key) {
  guard = tt;
  ret(level()) = pc ⊔ level(key) ⊔ obj_level(key);
  ret(obj_level(key)) = obj_level(key);
}