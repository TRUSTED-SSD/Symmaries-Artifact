static byte[] org.apache.kerby.kerberos.kerb.crypto.util.Random:makeBytes(int size)
{
byte[] data;
  java.security.SecureRandom $stack2;
  data=new byte[size];
 $stack2=org.apache.kerby.kerberos.kerb.crypto.util.Random.instance;
 dummyClass.highSourceMut(data);
$stack2.nextBytes(data);
 return  data;
 } 

