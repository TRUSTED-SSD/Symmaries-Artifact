java.nio.ByteBuffer org.apache.kerby.kerberos.kerb.transport.KrbTcpTransport:receiveMessage()
{
int msgLen;
  java.io.DataInputStream $stack2;
  byte[] $stack4;
  int $stack5;
  java.nio.ByteBuffer $stack9;
  java.io.IOException $stack10;
  $stack2=this.inputStream;
 msgLen = $stack2.readInt();
dummyClass.highSourceMut(msgLen);
 if (msgLen<=0) goto label0;
 $stack4=this.messageBuffer;
 $stack5=#$stack4;
 if (msgLen<=$stack5) goto label1;
 $stack10=new java.io.IOException;
 $stack10#java.io.IOException("Recv buffer overflowed, too large message?");
 throw $stack10;
 label1:$stack2=this.inputStream;
 $stack4=this.messageBuffer;
 dummyClass.highSourceMut($stack4);
$stack2.readFully($stack4,0,msgLen);
 $stack4=this.messageBuffer;
 $stack9 = java.nio.ByteBuffer.wrap($stack4,0,msgLen);
 return  $stack9;
 label0:return  null;
 } 

