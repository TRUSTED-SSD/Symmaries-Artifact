int org.apache.kerby.kerberos.kerb.crypto.enc.DesCbcEnc:paddingLength(int inputLen)
{
int payloadLen;
  int $stack5;
  payloadLen = this.confounderSize();
 $stack5 = this.checksumSize();
 payloadLen=(payloadLen+$stack5);
 payloadLen=(payloadLen+inputLen);
 inputLen = this.paddingSize();
 if (inputLen==0) goto label0;
 $stack5=(payloadLen%inputLen);
 if ($stack5!=0) goto label1;
 label0:return  0;
 label1:payloadLen=(payloadLen%inputLen);
 inputLen=(inputLen-payloadLen);
 return  inputLen;
 } 

