byte[] org.apache.kerby.kerberos.kerb.crypto.cksum.ConfounderedDesCheckSum:deriveKey(byte[] @key)
{
byte[] key;
key=@key;
key = this.xorKey(key);
 key = this#org.apache.kerby.kerberos.kerb.crypto.cksum.ConfounderedDesCheckSum:fixKey(key);
 return  key;
 } 

