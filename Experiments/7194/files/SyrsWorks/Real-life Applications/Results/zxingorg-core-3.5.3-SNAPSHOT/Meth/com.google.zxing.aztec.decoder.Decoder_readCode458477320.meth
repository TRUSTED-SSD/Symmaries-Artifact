static int com.google.zxing.aztec.decoder.Decoder:readCode(boolean[] rawbits,int startIndex,int length)
{
int res;
  int i;
  int $stack5;
  boolean $stack6;
  res=0;
 i=startIndex;
 label0:$stack5=(startIndex+length);
 if (i>=$stack5) goto label1;
 res=(res<<1);
 $stack6=rawbits[i];
 if ($stack6==0) goto label2;
 res=(res | 1);
 label2:i=(i+1);
 goto label0;
 label1:return  res;
 } 

