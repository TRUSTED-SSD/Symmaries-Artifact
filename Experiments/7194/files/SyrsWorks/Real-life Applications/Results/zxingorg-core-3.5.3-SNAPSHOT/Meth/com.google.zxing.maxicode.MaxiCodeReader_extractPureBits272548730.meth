static com.google.zxing.common.BitMatrix com.google.zxing.maxicode.MaxiCodeReader:extractPureBits(com.google.zxing.common.BitMatrix image)
{
int[] enclosingRectangle;
  int left;
  int top;
  int width;
  int height;
  com.google.zxing.common.BitMatrix bits;
  int y;
  int iy;
  int x;
  int ix;
  int $stack22;
  boolean $stack31;
  com.google.zxing.NotFoundException $stack34;
  enclosingRectangle = image.getEnclosingRectangle();
 if (enclosingRectangle!=null) goto label0;
 $stack34 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack34;
 label0:left=enclosingRectangle[0];
 top=enclosingRectangle[1];
 width=enclosingRectangle[2];
 height=enclosingRectangle[3];
 bits=new com.google.zxing.common.BitMatrix;
 bits#com.google.zxing.common.BitMatrix(30,33);
 y=0;
 label1:if (y>=33) goto label2;
 x=(y * height);
 iy=(height/2);
 iy=(x+iy);
 x=(iy/33);
 iy=(height-1);
 iy = java.lang.Math.min(x,iy);
 iy=(top+iy);
 x=0;
 label3:if (x>=30) goto label4;
 $stack22=(x * width);
 ix=(width/2);
 ix=($stack22+ix);
 $stack22=(y & 1);
 $stack22=($stack22 * width);
 $stack22=($stack22/2);
 ix=(ix+$stack22);
 $stack22=(ix/30);
 ix=(width-1);
 ix = java.lang.Math.min($stack22,ix);
 ix=(left+ix);
 $stack31 = image.get(ix,iy);
 if ($stack31==0) goto label5;
 bits.set(x,y);
 label5:x=(x+1);
 goto label3;
 label4:y=(y+1);
 goto label1;
 label2:return  bits;
 } 

