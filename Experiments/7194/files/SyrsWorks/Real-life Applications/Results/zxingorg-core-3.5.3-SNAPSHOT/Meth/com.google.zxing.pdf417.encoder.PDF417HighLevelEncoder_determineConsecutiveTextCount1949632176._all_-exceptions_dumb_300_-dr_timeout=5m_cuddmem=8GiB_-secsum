static int com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder:determineConsecutiveTextCount (
  com.google.zxing.common.ECIInput input, int startpos) {
  guard = tt;
  ret(level()) = level(startpos);
  ret(obj_level(input)) = level(input) ⊔ level(startpos) ⊔ obj_level(input);
}