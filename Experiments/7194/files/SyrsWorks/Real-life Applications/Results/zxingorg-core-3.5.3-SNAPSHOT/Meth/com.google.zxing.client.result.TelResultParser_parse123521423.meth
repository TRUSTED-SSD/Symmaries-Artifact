com.google.zxing.client.result.TelParsedResult com.google.zxing.client.result.TelResultParser:parse(com.google.zxing.Result result)
{
java.lang.String rawText;
  java.lang.String telURI;
  int queryStart;
  boolean $stack7;
  com.google.zxing.client.result.TelParsedResult $stack11;
  java.lang.StringBuilder $stack16;
  rawText = com.google.zxing.client.result.ResultParser.getMassagedText(result);
 $stack7 = rawText.startsWith("tel:");
 if ($stack7!=0) goto label0;
 $stack7 = rawText.startsWith("TEL:");
 if ($stack7!=0) goto label0;
 return  null;
 label0:$stack7 = rawText.startsWith("TEL:");
 if ($stack7==0) goto label1;
 $stack16=new java.lang.StringBuilder;
 $stack16#java.lang.StringBuilder();
 $stack16 = $stack16.append("tel:");
 telURI = rawText.substring(4);
 $stack16 = $stack16.append(telURI);
 telURI = $stack16.toString();
 goto label2;
 label1:telURI=rawText;
 label2:queryStart = rawText.indexOf(63,4);
 if (queryStart>=0) goto label3;
 rawText = rawText.substring(4);
 goto label4;
 label3:rawText = rawText.substring(4,queryStart);
 label4:$stack11=new com.google.zxing.client.result.TelParsedResult;
 $stack11#com.google.zxing.client.result.TelParsedResult(rawText,telURI,null);
 return  $stack11;
 } 

