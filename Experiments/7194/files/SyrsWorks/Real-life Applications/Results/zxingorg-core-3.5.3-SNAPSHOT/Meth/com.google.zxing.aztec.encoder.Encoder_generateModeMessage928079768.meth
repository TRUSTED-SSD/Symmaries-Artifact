static com.google.zxing.common.BitArray com.google.zxing.aztec.encoder.Encoder:generateModeMessage(boolean compact,int layers,int messageSizeInWords)
{
com.google.zxing.common.BitArray modeMessage;
  modeMessage=new com.google.zxing.common.BitArray;
 modeMessage#com.google.zxing.common.BitArray();
 if (compact==0) goto label0;
 layers=(layers-1);
 modeMessage.appendBits(layers,2);
 layers=(messageSizeInWords-1);
 modeMessage.appendBits(layers,6);
 modeMessage = com.google.zxing.aztec.encoder.Encoder.generateCheckWords(modeMessage,28,4);
 goto label1;
 label0:layers=(layers-1);
 modeMessage.appendBits(layers,5);
 layers=(messageSizeInWords-1);
 modeMessage.appendBits(layers,11);
 modeMessage = com.google.zxing.aztec.encoder.Encoder.generateCheckWords(modeMessage,40,4);
 label1:return  modeMessage;
 } 

