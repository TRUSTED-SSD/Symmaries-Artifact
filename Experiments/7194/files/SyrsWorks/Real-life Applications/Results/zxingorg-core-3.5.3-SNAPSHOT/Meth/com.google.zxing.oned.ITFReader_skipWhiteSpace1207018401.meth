static int com.google.zxing.oned.ITFReader:skipWhiteSpace(com.google.zxing.common.BitArray row)
{
int width;
  int endStart;
  com.google.zxing.NotFoundException $stack5;
  width = row.getSize();
 endStart = row.getNextSet(0);
 if (endStart!=width) goto label0;
 $stack5 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack5;
 label0:return  endStart;
 } 

