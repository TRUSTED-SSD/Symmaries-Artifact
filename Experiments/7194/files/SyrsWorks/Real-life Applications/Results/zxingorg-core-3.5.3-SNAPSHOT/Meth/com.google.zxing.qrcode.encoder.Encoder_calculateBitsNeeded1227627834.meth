static int com.google.zxing.qrcode.encoder.Encoder:calculateBitsNeeded(com.google.zxing.qrcode.decoder.Mode mode,com.google.zxing.common.BitArray headerBits,com.google.zxing.common.BitArray dataBits,com.google.zxing.qrcode.decoder.Version version)
{
int $stack4;
  int $stack5;
  $stack4 = headerBits.getSize();
 $stack5 = mode.getCharacterCountBits(version);
 $stack4=($stack4+$stack5);
 $stack5 = dataBits.getSize();
 $stack4=($stack4+$stack5);
 return  $stack4;
 } 

