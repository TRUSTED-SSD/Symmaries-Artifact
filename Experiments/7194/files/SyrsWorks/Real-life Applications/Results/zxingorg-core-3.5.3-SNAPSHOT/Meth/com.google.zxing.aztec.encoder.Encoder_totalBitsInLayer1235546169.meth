static int com.google.zxing.aztec.encoder.Encoder:totalBitsInLayer(int layers,boolean compact)
{
int $stack2;
  byte $stack7;
  if (compact==0) goto label0;
 $stack7=88;
 goto label1;
 label0:$stack7=112;
 label1:$stack2=(16 * layers);
 $stack2=($stack7+$stack2);
 layers=($stack2 * layers);
 return  layers;
 } 

