boolean com.google.zxing.qrcode.detector.AlignmentPatternFinder:foundPatternCross(int[] stateCount)
{
float moduleSize;
  float maxVariance;
  int i;
  int $stack5;
  float $stack6;
  byte $stack9;
  moduleSize=this.moduleSize;
 maxVariance=(moduleSize/2.0F);
 i=0;
 label0:if (i>=3) goto label1;
 $stack5=stateCount[i];
 $stack6=(float) $stack5;
 $stack6=(moduleSize-$stack6);
 $stack6 = java.lang.Math.abs($stack6);
 $stack9=($stack6 cmpl maxVariance);
 if ($stack9<0) goto label2;
 return  0;
 label2:i=(i+1);
 goto label0;
 label1:return  1;
 } 

