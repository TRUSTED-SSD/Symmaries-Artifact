static int com.google.zxing.aztec.encoder.Encoder:totalBitsInLayer (int
                                                                    layers,
                                                                    boolean
                                                                    compact) {
  guard = tt;
  ret(level()) = level(layers);
}