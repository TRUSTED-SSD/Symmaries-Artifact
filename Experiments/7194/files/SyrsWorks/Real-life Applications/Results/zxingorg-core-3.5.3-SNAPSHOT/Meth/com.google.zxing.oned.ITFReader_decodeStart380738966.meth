int[] com.google.zxing.oned.ITFReader:decodeStart(com.google.zxing.common.BitArray row)
{
int endStart;
  int[] startPattern;
  int $stack8;
  endStart = com.google.zxing.oned.ITFReader.skipWhiteSpace(row);
 startPattern=com.google.zxing.oned.ITFReader.START_PATTERN;
 startPattern = com.google.zxing.oned.ITFReader.findGuardPattern(row,endStart,startPattern);
 $stack8=startPattern[1];
 endStart=startPattern[0];
 endStart=($stack8-endStart);
 endStart=(endStart/4);
 this.narrowLineWidth=endStart;
 endStart=startPattern[0];
 this#com.google.zxing.oned.ITFReader:validateQuietZone(row,endStart);
 return  startPattern;
 } 

