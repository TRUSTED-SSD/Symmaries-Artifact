static void com.google.zxing.qrcode.encoder.MatrixUtil:makeTypeInfoBits(com.google.zxing.qrcode.decoder.ErrorCorrectionLevel ecLevel,int maskPattern,com.google.zxing.common.BitArray bits)
{
com.google.zxing.common.BitArray maskBits;
  boolean $stack6;
  int $stack7;
  com.google.zxing.WriterException $stack12;
  java.lang.StringBuilder $stack13;
  java.lang.String $stack17;
  $stack6 = com.google.zxing.qrcode.encoder.QRCode.isValidMaskPattern(maskPattern);
 if ($stack6!=0) goto label0;
 $stack12=new com.google.zxing.WriterException;
 $stack12#com.google.zxing.WriterException("Invalid mask pattern");
 throw $stack12;
 label0:$stack7 = ecLevel.getBits();
 $stack7=($stack7<<3);
 maskPattern=($stack7 | maskPattern);
 bits.appendBits(maskPattern,5);
 maskPattern = com.google.zxing.qrcode.encoder.MatrixUtil.calculateBCHCode(maskPattern,1335);
 bits.appendBits(maskPattern,10);
 maskBits=new com.google.zxing.common.BitArray;
 maskBits#com.google.zxing.common.BitArray();
 maskBits.appendBits(21522,15);
 bits.xor(maskBits);
 maskPattern = bits.getSize();
 if (maskPattern==15) goto label1;
 $stack12=new com.google.zxing.WriterException;
 $stack13=new java.lang.StringBuilder;
 $stack13#java.lang.StringBuilder();
 $stack13 = $stack13.append("should not happen but we got: ");
 maskPattern = bits.getSize();
 $stack13 = $stack13.append(maskPattern);
 $stack17 = $stack13.toString();
 $stack12#com.google.zxing.WriterException($stack17);
 throw $stack12;
 label1:return;
 } 

