static int com.google.zxing.oned.rss.expanded.decoders.GeneralAppIdDecoder:extractNumericValueFromBitArray (
  com.google.zxing.common.BitArray information, int pos, int bits) {
  guard = tt;
  ret(level()) = level(bits);
  ret(obj_level(information)) = obj_level(information);
}