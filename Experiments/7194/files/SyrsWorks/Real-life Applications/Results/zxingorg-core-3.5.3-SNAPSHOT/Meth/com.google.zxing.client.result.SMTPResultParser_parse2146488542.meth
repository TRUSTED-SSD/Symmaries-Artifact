com.google.zxing.client.result.EmailAddressParsedResult com.google.zxing.client.result.SMTPResultParser:parse(com.google.zxing.Result result)
{
java.lang.String rawText;
  java.lang.String subject;
  java.lang.String body;
  int colon;
  boolean $stack8;
  com.google.zxing.client.result.EmailAddressParsedResult $stack11;
  java.lang.String[] $stack12;
  int $stack13;
  rawText = com.google.zxing.client.result.ResultParser.getMassagedText(result);
 $stack8 = rawText.startsWith("smtp:");
 if ($stack8!=0) goto label0;
 $stack8 = rawText.startsWith("SMTP:");
 if ($stack8!=0) goto label0;
 return  null;
 label0:rawText = rawText.substring(5);
 subject=null;
 body=null;
 colon = rawText.indexOf(58);
 if (colon<0) goto label1;
 $stack13=(colon+1);
 subject = rawText.substring($stack13);
 rawText = rawText.substring(0,colon);
 colon = subject.indexOf(58);
 if (colon<0) goto label1;
 $stack13=(colon+1);
 body = subject.substring($stack13);
 subject = subject.substring(0,colon);
 label1:$stack11=new com.google.zxing.client.result.EmailAddressParsedResult;
 $stack12=new java.lang.String[1];
 $stack12[0]=rawText;
 $stack11#com.google.zxing.client.result.EmailAddressParsedResult($stack12,null,null,subject,body);
 return  $stack11;
 } 

