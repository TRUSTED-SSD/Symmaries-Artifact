void com.google.zxing.pdf417.decoder.DetectionResultRowIndicatorColumn:adjustIncompleteIndicatorColumnRowNumbers(com.google.zxing.pdf417.decoder.BarcodeMetadata barcodeMetadata)
{
com.google.zxing.pdf417.decoder.BoundingBox boundingBox;
  com.google.zxing.ResultPoint top;
  int firstRow;
  int lastRow;
  com.google.zxing.pdf417.decoder.Codeword[] codewords;
  int barcodeRow;
  int maxRowHeight;
  int currentRowHeight;
  com.google.zxing.pdf417.decoder.Codeword codeword;
  int rowDifference;
  boolean $stack15;
  com.google.zxing.ResultPoint $stack18;
  float $stack19;
  int $stack29;
  boundingBox = this.getBoundingBox();
 $stack15=this.isLeft;
 if ($stack15==0) goto label0;
 top = boundingBox.getTopLeft();
 goto label1;
 label0:top = boundingBox.getTopRight();
 label1:$stack15=this.isLeft;
 if ($stack15==0) goto label2;
 $stack18 = boundingBox.getBottomLeft();
 goto label3;
 label2:$stack18 = boundingBox.getBottomRight();
 label3:$stack19 = top.getY();
 lastRow=(int) $stack19;
 firstRow = this.imageRowToCodewordIndex(lastRow);
 $stack19 = $stack18.getY();
 lastRow=(int) $stack19;
 lastRow = this.imageRowToCodewordIndex(lastRow);
 codewords = this.getCodewords();
 barcodeRow=-1;
 maxRowHeight=1;
 currentRowHeight=0;
 label4:if (firstRow>=lastRow) goto label5;
 codeword=codewords[firstRow];
 if (codeword!=null) goto label6;
 goto label7;
 label6:codeword=codewords[firstRow];
 codeword.setRowNumberAsRowIndicatorColumn();
 rowDifference = codeword.getRowNumber();
 rowDifference=(rowDifference-barcodeRow);
 if (rowDifference!=0) goto label8;
 currentRowHeight=(currentRowHeight+1);
 goto label7;
 label8:if (rowDifference!=1) goto label9;
 maxRowHeight = java.lang.Math.max(maxRowHeight,currentRowHeight);
 currentRowHeight=1;
 barcodeRow = codeword.getRowNumber();
 goto label7;
 label9:rowDifference = codeword.getRowNumber();
 $stack29 = barcodeMetadata.getRowCount();
 if (rowDifference<$stack29) goto label10;
 codewords[firstRow]=null;
 goto label7;
 label10:barcodeRow = codeword.getRowNumber();
 currentRowHeight=1;
 label7:firstRow=(firstRow+1);
 goto label4;
 label5:return;
 } 

