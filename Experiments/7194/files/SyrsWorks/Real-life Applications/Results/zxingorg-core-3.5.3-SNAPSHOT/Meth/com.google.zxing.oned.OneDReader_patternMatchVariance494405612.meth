static float com.google.zxing.oned.OneDReader:patternMatchVariance(int[] counters,int[] pattern,float maxIndividualVariance)
{
int numCounters;
  int total;
  int patternLength;
  float i;
  float totalVariance;
  int counter;
  float scaledPattern;
  int $stack16;
  float $stack18;
  byte $stack19;
  boolean tmp$1960714367;
  numCounters=#counters;
 total=0;
 patternLength=0;
 tmp$1960714367=0;
 totalVariance=(float) 0;
 i=totalVariance;
 label0:counter=(int) i;
 if (counter>=numCounters) goto label1;
 counter=(int) i;
 counter=counters[counter];
 total=(total+counter);
 counter=(int) i;
 counter=pattern[counter];
 patternLength=(patternLength+counter);
 i=(i+1);
 goto label0;
 label1:if (total>=patternLength) goto label2;
 return  #InfinityF;
 label2:i=(float) total;
 totalVariance=(float) patternLength;
 i=(i/totalVariance);
 maxIndividualVariance=(maxIndividualVariance * i);
 totalVariance=0.0F;
 patternLength=0;
 label3:if (patternLength>=numCounters) goto label4;
 counter=counters[patternLength];
 $stack16=pattern[patternLength];
 scaledPattern=(float) $stack16;
 scaledPattern=(scaledPattern * i);
 $stack18=(float) counter;
 $stack19=($stack18 cmpl scaledPattern);
 if ($stack19<=0) goto label5;
 $stack18=(float) counter;
 scaledPattern=($stack18-scaledPattern);
 goto label6;
 label5:$stack18=(float) counter;
 scaledPattern=(scaledPattern-$stack18);
 label6:$stack19=(scaledPattern cmpl maxIndividualVariance);
 if ($stack19<=0) goto label7;
 return  #InfinityF;
 label7:totalVariance=(totalVariance+scaledPattern);
 patternLength=(patternLength+1);
 goto label3;
 label4:maxIndividualVariance=(float) total;
 maxIndividualVariance=(totalVariance/maxIndividualVariance);
 return  maxIndividualVariance;
 } 

