static int com.google.zxing.datamatrix.encoder.HighLevelEncoder:determineConsecutiveDigitCount(java.lang.CharSequence msg,int startpos)
{
int len;
  int idx;
  char $stack6;
  boolean $stack7;
  len = msg.length();
 idx=startpos;
 label0:if (idx>=len) goto label1;
 $stack6 = msg.charAt(idx);
 $stack7 = com.google.zxing.datamatrix.encoder.HighLevelEncoder.isDigit($stack6);
 if ($stack7==0) goto label1;
 idx=(idx+1);
 goto label0;
 label1:startpos=(idx-startpos);
 return  startpos;
 } 

