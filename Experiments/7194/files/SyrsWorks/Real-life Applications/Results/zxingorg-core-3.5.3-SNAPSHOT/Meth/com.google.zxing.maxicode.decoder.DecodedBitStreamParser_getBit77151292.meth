static int com.google.zxing.maxicode.decoder.DecodedBitStreamParser:getBit(int bit,byte[] bytes)
{
int $stack2;
  byte $stack6;
  bit=(bit+-1);
 $stack2=(bit/6);
 $stack6=bytes[$stack2];
 bit=(bit%6);
 bit=(5-bit);
 bit=(1<<bit);
 bit=($stack6 & bit);
 if (bit!=0) goto label0;
 $stack6=0;
 goto label1;
 label0:$stack6=1;
 label1:return  $stack6;
 } 

