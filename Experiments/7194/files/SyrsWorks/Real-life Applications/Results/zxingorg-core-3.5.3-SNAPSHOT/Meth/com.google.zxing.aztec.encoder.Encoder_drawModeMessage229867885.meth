static void com.google.zxing.aztec.encoder.Encoder:drawModeMessage(com.google.zxing.common.BitMatrix matrix,boolean compact,int matrixSize,com.google.zxing.common.BitArray modeMessage)
{
int i;
  int offset;
  int $stack9;
  matrixSize=(matrixSize/2);
 if (compact==0) goto label0;
 i=0;
 label1:if (i>=7) goto label2;
 offset=(matrixSize-3);
 offset=(offset+i);
 compact = modeMessage.get(i);
 if (compact==0) goto label3;
 $stack9=(matrixSize-5);
 matrix.set(offset,$stack9);
 label3:$stack9=(i+7);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label4;
 $stack9=(matrixSize+5);
 matrix.set($stack9,offset);
 label4:$stack9=(20-i);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label5;
 $stack9=(matrixSize+5);
 matrix.set(offset,$stack9);
 label5:$stack9=(27-i);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label6;
 $stack9=(matrixSize-5);
 matrix.set($stack9,offset);
 label6:i=(i+1);
 goto label1;
 label2:goto label7;
 label0:i=0;
 label8:if (i>=10) goto label7;
 offset=(matrixSize-5);
 $stack9=(offset+i);
 offset=(i/5);
 offset=($stack9+offset);
 compact = modeMessage.get(i);
 if (compact==0) goto label9;
 $stack9=(matrixSize-7);
 matrix.set(offset,$stack9);
 label9:$stack9=(i+10);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label10;
 $stack9=(matrixSize+7);
 matrix.set($stack9,offset);
 label10:$stack9=(29-i);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label11;
 $stack9=(matrixSize+7);
 matrix.set(offset,$stack9);
 label11:$stack9=(39-i);
 compact = modeMessage.get($stack9);
 if (compact==0) goto label12;
 $stack9=(matrixSize-7);
 matrix.set($stack9,offset);
 label12:i=(i+1);
 goto label8;
 label7:return;
 } 

