static int com.google.zxing.oned.rss.AbstractRSSReader:parseFinderValue (
  int[] counters, int[][] finderPatterns) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(counters)) = obj_level(counters);
  ret(obj_level(finderPatterns)) = obj_level(finderPatterns);
}