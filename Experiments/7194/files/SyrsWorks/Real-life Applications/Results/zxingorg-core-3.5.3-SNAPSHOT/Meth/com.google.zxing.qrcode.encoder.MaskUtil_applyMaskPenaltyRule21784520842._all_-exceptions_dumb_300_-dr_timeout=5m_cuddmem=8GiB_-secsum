static int com.google.zxing.qrcode.encoder.MaskUtil:applyMaskPenaltyRule2 (
  com.google.zxing.qrcode.encoder.ByteMatrix matrix) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(matrix)) = obj_level(matrix);
}