boolean com.google.zxing.qrcode.encoder.MinimalEncoder:canEncode (com.google.zxing.qrcode.decoder.Mode
                                                                    mode,
                                                                  char c) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(mode)) = level(mode) ⊔ obj_level(mode);
  ret(obj_level(this)) = level(mode) ⊔ obj_level(mode) ⊔ obj_level(this);
}