static int com.google.zxing.oned.UPCEANExtension5Support:extensionChecksum(java.lang.CharSequence s)
{
int length;
  int sum;
  int i;
  char $stack6;
  int $stack10;
  length = s.length();
 sum=0;
 i=(length-2);
 label0:if (i<0) goto label1;
 $stack6 = s.charAt(i);
 $stack10=($stack6-48);
 sum=(sum+$stack10);
 i=(i+-2);
 goto label0;
 label1:sum=(sum * 3);
 i=(length-1);
 label2:if (i<0) goto label3;
 $stack6 = s.charAt(i);
 length=($stack6-48);
 sum=(sum+length);
 i=(i+-2);
 goto label2;
 label3:sum=(sum * 3);
 sum=(sum%10);
 return  sum;
 } 

