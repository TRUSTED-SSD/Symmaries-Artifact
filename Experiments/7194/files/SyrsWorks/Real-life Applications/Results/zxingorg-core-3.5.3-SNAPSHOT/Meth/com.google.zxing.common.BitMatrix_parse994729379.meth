static com.google.zxing.common.BitMatrix com.google.zxing.common.BitMatrix:parse(boolean[][] image)
{
int height;
  int width;
  com.google.zxing.common.BitMatrix bits;
  int i;
  boolean[] imageI;
  int j;
  boolean $stack11;
  height=#image;
 imageI=image[0];
 width=#imageI;
 bits=new com.google.zxing.common.BitMatrix;
 bits#com.google.zxing.common.BitMatrix(width,height);
 i=0;
 label0:if (i>=height) goto label1;
 imageI=image[i];
 j=0;
 label2:if (j>=width) goto label3;
 $stack11=imageI[j];
 if ($stack11==0) goto label4;
 bits.set(j,i);
 label4:j=(j+1);
 goto label2;
 label3:i=(i+1);
 goto label0;
 label1:return  bits;
 } 

