static void com.google.zxing.oned.rss.expanded.RSSExpandedReader:reverseCounters(int[] counters)
{
int length;
  int i;
  int tmp;
  int $stack5;
  length=#counters;
 i=0;
 label0:tmp=(length/2);
 if (i>=tmp) goto label1;
 tmp=counters[i];
 $stack5=(length-i);
 $stack5=($stack5-1);
 $stack5=counters[$stack5];
 counters[i]=$stack5;
 $stack5=(length-i);
 $stack5=($stack5-1);
 counters[$stack5]=tmp;
 i=(i+1);
 goto label0;
 label1:return;
 } 

