void com.google.zxing.datamatrix.encoder.DefaultPlacement:setBit(int col,int row,boolean bit)
{
int $stack4;
  byte $stack6;
  byte[] $stack8;
  $stack8=this.bits;
 $stack4=this.numcols;
 row=(row * $stack4);
 col=(row+col);
 if (bit==0) goto label0;
 bit=1;
 goto label1;
 label0:bit=0;
 label1:$stack6=(byte) bit;
 $stack8[col]=$stack6;
 return;
 } 

