static void com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder:encodeMultiECIBinary(com.google.zxing.common.ECIInput input,int startpos,int count,int startmode,java.lang.StringBuilder sb)
{
int localStart;
  int localEnd;
  int localCount;
  byte[] $stack12;
  boolean $stack20;
  count=(startpos+count);
 localStart = input.length();
 count = java.lang.Math.min(count,localStart);
 localStart=startpos;
 label0:if (localStart>=count) goto label1;
 $stack20 = input.isECI(localStart);
 if ($stack20==0) goto label1;
 localEnd = input.getECIValue(localStart);
 com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder.encodingECI(localEnd,sb);
 localStart=(localStart+1);
 goto label0;
 label1:localEnd=localStart;
 label2:if (localEnd>=count) goto label3;
 $stack20 = input.isECI(localEnd);
 if ($stack20!=0) goto label3;
 localEnd=(localEnd+1);
 goto label2;
 label3:localCount=(localEnd-localStart);
 if (localCount>0) goto label4;
 goto label5;
 label4:$stack12 = com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder.subBytes(input,localStart,localEnd);
 if (localStart!=startpos) goto label6;
 localStart=startmode;
 goto label7;
 label6:localStart=1;
 label7:com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder.encodeBinary($stack12,0,localCount,localStart,sb);
 localStart=localEnd;
 goto label0;
 label5:return;
 } 

