static com.google.zxing.qrcode.decoder.Mode com.google.zxing.qrcode.encoder.Encoder:chooseMode (
  java.lang.String content, java.nio.charset.Charset encoding) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = ⊥;
  ret(obj_level(content)) = level(content) ⊔ obj_level(content);
  ret(obj_level(encoding)) = level(content) ⊔ obj_level(content) ⊔ obj_level(encoding);
}