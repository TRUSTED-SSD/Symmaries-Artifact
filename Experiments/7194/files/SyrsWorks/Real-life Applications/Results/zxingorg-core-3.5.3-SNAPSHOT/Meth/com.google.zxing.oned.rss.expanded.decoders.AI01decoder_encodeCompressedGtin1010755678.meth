void com.google.zxing.oned.rss.expanded.decoders.AI01decoder:encodeCompressedGtin(java.lang.StringBuilder buf,int currentPos)
{
int initialPosition;
  buf.append("(01)");
 initialPosition = buf.length();
 buf.append((char)57);
 this.encodeCompressedGtinWithoutAI(buf,currentPos,initialPosition);
 return;
 } 

