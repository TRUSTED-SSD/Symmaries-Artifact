static com.google.zxing.common.BitArray com.google.zxing.oned.rss.expanded.BitArrayBuilder:buildBitArray (
  java.util.List pairs) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(pairs) ⊔ obj_level(pairs);
  ret(obj_level(pairs)) = level(pairs) ⊔ obj_level(pairs);
}