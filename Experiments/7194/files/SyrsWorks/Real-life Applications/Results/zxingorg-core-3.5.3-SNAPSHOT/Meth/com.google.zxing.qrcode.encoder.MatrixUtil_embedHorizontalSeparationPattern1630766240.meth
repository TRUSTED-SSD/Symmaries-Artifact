static void com.google.zxing.qrcode.encoder.MatrixUtil:embedHorizontalSeparationPattern(int xStart,int yStart,com.google.zxing.qrcode.encoder.ByteMatrix matrix)
{
int x;
  int $stack4;
  byte $stack5;
  boolean $stack6;
  com.google.zxing.WriterException $stack10;
  x=0;
 label0:if (x>=8) goto label1;
 $stack4=(xStart+x);
 $stack5 = matrix.get($stack4,yStart);
 $stack6 = com.google.zxing.qrcode.encoder.MatrixUtil.isEmpty((int)$stack5);
 if ($stack6!=0) goto label2;
 $stack10=new com.google.zxing.WriterException;
 $stack10#com.google.zxing.WriterException();
 throw $stack10;
 label2:$stack4=(xStart+x);
 matrix.set($stack4,yStart,0);
 x=(x+1);
 goto label0;
 label1:return;
 } 

