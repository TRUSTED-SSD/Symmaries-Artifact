static void com.google.zxing.qrcode.encoder.MatrixUtil:embedDataBits(com.google.zxing.common.BitArray dataBits,int maskPattern,com.google.zxing.qrcode.encoder.ByteMatrix matrix)
{
int bitIndex;
  byte direction;
  int x;
  int y;
  int i;
  int xx;
  boolean bit;
  com.google.zxing.WriterException $stack13;
  java.lang.StringBuilder $stack14;
  java.lang.String $stack20;
  byte $stack29;
  int $stack31;
  boolean $stack34;
  bitIndex=0;
 direction=-1;
 x = matrix.getWidth();
 x=(x-1);
 y = matrix.getHeight();
 y=(y-1);
 label0:if (x<=0) goto label1;
 if (x!=6) goto label2;
 x=(x+-1);
 label2:if (y<0) goto label3;
 i = matrix.getHeight();
 if (y>=i) goto label3;
 i=0;
 label4:if (i>=2) goto label5;
 xx=(x-i);
 $stack29 = matrix.get(xx,y);
 bit = com.google.zxing.qrcode.encoder.MatrixUtil.isEmpty((int)$stack29);
 if (bit!=0) goto label6;
 goto label7;
 label6:$stack31 = dataBits.getSize();
 if (bitIndex>=$stack31) goto label8;
 bit = dataBits.get(bitIndex);
 bitIndex=(bitIndex+1);
 goto label9;
 label8:bit=0;
 label9:if (maskPattern==-1) goto label10;
 $stack34 = com.google.zxing.qrcode.encoder.MaskUtil.getDataMaskBit(maskPattern,xx,y);
 if ($stack34==0) goto label10;
 if (bit!=0) goto label11;
 bit=1;
 goto label10;
 label11:bit=0;
 label10:matrix.set(xx,y,bit);
 label7:i=(i+1);
 goto label4;
 label5:y=(y+direction);
 goto label2;
 label3:direction= ! (direction);
 y=(y+direction);
 x=(x+-2);
 goto label0;
 label1:maskPattern = dataBits.getSize();
 if (bitIndex==maskPattern) goto label12;
 $stack13=new com.google.zxing.WriterException;
 $stack14=new java.lang.StringBuilder;
 $stack14#java.lang.StringBuilder();
 $stack14 = $stack14.append("Not all bits consumed: ");
 $stack14 = $stack14.append(bitIndex);
 $stack14 = $stack14.append((char)47);
 maskPattern = dataBits.getSize();
 $stack14 = $stack14.append(maskPattern);
 $stack20 = $stack14.toString();
 $stack13#com.google.zxing.WriterException($stack20);
 throw $stack13;
 label12:return;
 } 

