static com.google.zxing.common.BitMatrix com.google.zxing.pdf417.PDF417Writer:bitMatrixFromBitArray(byte[][] input,int margin)
{
com.google.zxing.common.BitMatrix output;
  int y;
  int yOutput;
  byte[] inputY;
  int x;
  byte[] $stack18;
  int $stack19;
  byte $stack22;
  output=new com.google.zxing.common.BitMatrix;
 inputY=input[0];
 yOutput=#inputY;
 y=(2 * margin);
 y=(yOutput+y);
 x=#input;
 yOutput=(2 * margin);
 yOutput=(x+yOutput);
 output#com.google.zxing.common.BitMatrix(y,yOutput);
 output.clear();
 y=0;
 yOutput = output.getHeight();
 yOutput=(yOutput-margin);
 yOutput=(yOutput-1);
 label0:x=#input;
 if (y>=x) goto label1;
 inputY=input[y];
 x=0;
 label2:$stack18=input[0];
 $stack19=#$stack18;
 if (x>=$stack19) goto label3;
 $stack22=inputY[x];
 if ($stack22!=1) goto label4;
 $stack19=(x+margin);
 output.set($stack19,yOutput);
 label4:x=(x+1);
 goto label2;
 label3:y=(y+1);
 yOutput=(yOutput+-1);
 goto label0;
 label1:return  output;
 } 

