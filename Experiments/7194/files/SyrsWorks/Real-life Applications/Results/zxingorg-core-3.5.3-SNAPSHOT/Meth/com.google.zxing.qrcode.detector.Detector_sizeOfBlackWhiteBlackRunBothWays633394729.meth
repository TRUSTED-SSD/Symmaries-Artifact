float com.google.zxing.qrcode.detector.Detector:sizeOfBlackWhiteBlackRunBothWays(int fromX,int fromY,int toX,int toY)
{
float result;
  float scale;
  com.google.zxing.common.BitMatrix $stack11;
  int $stack12;
  float $stack14;
  float $stack16;
  result = this#com.google.zxing.qrcode.detector.Detector:sizeOfBlackWhiteBlackRun(fromX,fromY,toX,toY);
 scale=1.0F;
 toX=(toX-fromX);
 toX=(fromX-toX);
 if (toX>=0) goto label0;
 $stack16=(float) fromX;
 toX=(fromX-toX);
 scale=(float) toX;
 scale=($stack16/scale);
 toX=0;
 goto label1;
 label0:$stack11=this.image;
 $stack12 = $stack11.getWidth();
 if (toX<$stack12) goto label1;
 $stack11=this.image;
 $stack12 = $stack11.getWidth();
 $stack12=($stack12-1);
 $stack12=($stack12-fromX);
 $stack16=(float) $stack12;
 toX=(toX-fromX);
 scale=(float) toX;
 scale=($stack16/scale);
 $stack11=this.image;
 toX = $stack11.getWidth();
 toX=(toX-1);
 label1:$stack16=(float) fromY;
 toY=(toY-fromY);
 $stack14=(float) toY;
 scale=($stack14 * scale);
 scale=($stack16-scale);
 toY=(int) scale;
 scale=1.0F;
 if (toY>=0) goto label2;
 $stack16=(float) fromY;
 toY=(fromY-toY);
 scale=(float) toY;
 scale=($stack16/scale);
 toY=0;
 goto label3;
 label2:$stack11=this.image;
 $stack12 = $stack11.getHeight();
 if (toY<$stack12) goto label3;
 $stack11=this.image;
 $stack12 = $stack11.getHeight();
 $stack12=($stack12-1);
 $stack12=($stack12-fromY);
 $stack16=(float) $stack12;
 toY=(toY-fromY);
 scale=(float) toY;
 scale=($stack16/scale);
 $stack11=this.image;
 toY = $stack11.getHeight();
 toY=(toY-1);
 label3:$stack16=(float) fromX;
 toX=(toX-fromX);
 $stack14=(float) toX;
 scale=($stack14 * scale);
 scale=($stack16+scale);
 toX=(int) scale;
 scale = this#com.google.zxing.qrcode.detector.Detector:sizeOfBlackWhiteBlackRun(fromX,fromY,toX,toY);
 result=(result+scale);
 result=(result-1.0F);
 return  result;
 } 

