static com.google.zxing.common.BitMatrix com.google.zxing.qrcode.QRCodeReader:extractPureBits(com.google.zxing.common.BitMatrix image)
{
int[] leftTopBlack;
  int[] rightBottomBlack;
  float moduleSize;
  int top;
  int bottom;
  int left;
  int right;
  int matrixWidth;
  int matrixHeight;
  int nudge;
  com.google.zxing.common.BitMatrix bits;
  com.google.zxing.NotFoundException $stack19;
  float $stack26;
  int $stack36;
  boolean $stack56;
  leftTopBlack = image.getTopLeftOnBit();
 rightBottomBlack = image.getBottomRightOnBit();
 if (leftTopBlack==null) goto label0;
 if (rightBottomBlack!=null) goto label1;
 label0:$stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label1:moduleSize = com.google.zxing.qrcode.QRCodeReader.moduleSize(leftTopBlack,image);
 top=leftTopBlack[1];
 bottom=rightBottomBlack[1];
 left=leftTopBlack[0];
 right=rightBottomBlack[0];
 if (left>=right) goto label2;
 if (top<bottom) goto label3;
 label2:$stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label3:matrixHeight=(bottom-top);
 matrixWidth=(right-left);
 if (matrixHeight==matrixWidth) goto label4;
 matrixWidth=(bottom-top);
 right=(left+matrixWidth);
 matrixWidth = image.getWidth();
 if (right<matrixWidth) goto label4;
 $stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label4:matrixWidth=(right-left);
 matrixWidth=(matrixWidth+1);
 $stack26=(float) matrixWidth;
 $stack26=($stack26/moduleSize);
 matrixWidth = java.lang.Math.round($stack26);
 matrixHeight=(bottom-top);
 matrixHeight=(matrixHeight+1);
 $stack26=(float) matrixHeight;
 $stack26=($stack26/moduleSize);
 matrixHeight = java.lang.Math.round($stack26);
 if (matrixWidth<=0) goto label5;
 if (matrixHeight>0) goto label6;
 label5:$stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label6:if (matrixHeight==matrixWidth) goto label7;
 $stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label7:$stack26=(moduleSize/2.0F);
 nudge=(int) $stack26;
 top=(top+nudge);
 left=(left+nudge);
 $stack36=(matrixWidth-1);
 $stack26=(float) $stack36;
 $stack26=($stack26 * moduleSize);
 $stack36=(int) $stack26;
 $stack36=(left+$stack36);
 right=($stack36-right);
 if (right<=0) goto label8;
 if (right<=nudge) goto label9;
 $stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label9:left=(left-right);
 label8:right=(matrixHeight-1);
 $stack26=(float) right;
 $stack26=($stack26 * moduleSize);
 right=(int) $stack26;
 right=(top+right);
 bottom=(right-bottom);
 if (bottom<=0) goto label10;
 if (bottom<=nudge) goto label11;
 $stack19 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack19;
 label11:top=(top-bottom);
 label10:bits=new com.google.zxing.common.BitMatrix;
 bits#com.google.zxing.common.BitMatrix(matrixWidth,matrixHeight);
 bottom=0;
 label12:if (bottom>=matrixHeight) goto label13;
 $stack26=(float) bottom;
 $stack26=($stack26 * moduleSize);
 right=(int) $stack26;
 right=(top+right);
 nudge=0;
 label14:if (nudge>=matrixWidth) goto label15;
 $stack26=(float) nudge;
 $stack26=($stack26 * moduleSize);
 $stack36=(int) $stack26;
 $stack36=(left+$stack36);
 $stack56 = image.get($stack36,right);
 if ($stack56==0) goto label16;
 bits.set(nudge,bottom);
 label16:nudge=(nudge+1);
 goto label14;
 label15:bottom=(bottom+1);
 goto label12;
 label13:return  bits;
 } 

