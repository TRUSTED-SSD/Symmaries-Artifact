static void com.google.zxing.qrcode.encoder.MatrixUtil:embedTimingPatterns(com.google.zxing.qrcode.encoder.ByteMatrix matrix)
{
int i;
  int bit;
  byte $stack6;
  boolean $stack7;
  i=8;
 label0:bit = matrix.getWidth();
 bit=(bit-8);
 if (i>=bit) goto label1;
 bit=(i+1);
 bit=(bit%2);
 $stack6 = matrix.get(i,6);
 $stack7 = com.google.zxing.qrcode.encoder.MatrixUtil.isEmpty((int)$stack6);
 if ($stack7==0) goto label2;
 matrix.set(i,6,bit);
 label2:$stack6 = matrix.get(6,i);
 $stack7 = com.google.zxing.qrcode.encoder.MatrixUtil.isEmpty((int)$stack6);
 if ($stack7==0) goto label3;
 matrix.set(6,i,bit);
 label3:i=(i+1);
 goto label0;
 label1:return;
 } 

