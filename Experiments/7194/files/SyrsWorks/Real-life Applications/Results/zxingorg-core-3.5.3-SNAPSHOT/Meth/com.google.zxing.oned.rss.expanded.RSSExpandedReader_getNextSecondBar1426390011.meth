static int com.google.zxing.oned.rss.expanded.RSSExpandedReader:getNextSecondBar(com.google.zxing.common.BitArray row,int initialPos)
{
boolean $stack3;
  $stack3 = row.get(initialPos);
 if ($stack3==0) goto label0;
 initialPos = row.getNextUnset(initialPos);
 initialPos = row.getNextSet(initialPos);
 goto label1;
 label0:initialPos = row.getNextSet(initialPos);
 initialPos = row.getNextUnset(initialPos);
 label1:return  initialPos;
 } 

