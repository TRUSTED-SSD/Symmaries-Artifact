static int com.google.zxing.datamatrix.DataMatrixReader:moduleSize(int[] leftTopBlack,com.google.zxing.common.BitMatrix image)
{
int width;
  int x;
  int y;
  com.google.zxing.NotFoundException $stack8;
  boolean $stack10;
  width = image.getWidth();
 x=leftTopBlack[0];
 y=leftTopBlack[1];
 label0:if (x>=width) goto label1;
 $stack10 = image.get(x,y);
 if ($stack10==0) goto label1;
 x=(x+1);
 goto label0;
 label1:if (x!=width) goto label2;
 $stack8 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack8;
 label2:width=leftTopBlack[0];
 x=(x-width);
 if (x!=0) goto label3;
 $stack8 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack8;
 label3:return  x;
 } 

