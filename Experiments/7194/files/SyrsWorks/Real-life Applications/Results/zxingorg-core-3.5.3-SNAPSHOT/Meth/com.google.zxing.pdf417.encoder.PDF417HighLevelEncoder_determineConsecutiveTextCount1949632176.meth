static int com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder:determineConsecutiveTextCount(com.google.zxing.common.ECIInput input,int startpos)
{
int len;
  int idx;
  int numericCount;
  boolean $stack7;
  char $stack10;
  len = input.length();
 idx=startpos;
 label0:if (idx>=len) goto label1;
 numericCount=0;
 label2:if (numericCount>=13) goto label3;
 if (idx>=len) goto label3;
 $stack7 = input.isECI(idx);
 if ($stack7!=0) goto label3;
 $stack10 = input.charAt(idx);
 $stack7 = com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder.isDigit($stack10);
 if ($stack7==0) goto label3;
 numericCount=(numericCount+1);
 idx=(idx+1);
 goto label2;
 label3:if (numericCount<13) goto label4;
 startpos=(idx-startpos);
 startpos=(startpos-numericCount);
 return  startpos;
 label4:if (numericCount<=0) goto label5;
 goto label0;
 label5:$stack7 = input.isECI(idx);
 if ($stack7!=0) goto label1;
 $stack10 = input.charAt(idx);
 $stack7 = com.google.zxing.pdf417.encoder.PDF417HighLevelEncoder.isText($stack10);
 if ($stack7!=0) goto label6;
 goto label1;
 label6:idx=(idx+1);
 goto label0;
 label1:startpos=(idx-startpos);
 return  startpos;
 } 

