static void com.google.zxing.aztec.encoder.HighLevelEncoder:updateStateForPair(com.google.zxing.aztec.encoder.State @state,int index,int pairCode,java.util.Collection result)
{
com.google.zxing.aztec.encoder.State stateNoBinary;
  com.google.zxing.aztec.encoder.State $stack7;
  int $stack9;
  com.google.zxing.aztec.encoder.State state;
state=@state;
stateNoBinary = state.endBinaryShift(index);
 $stack7 = stateNoBinary.latchAndAppend(4,pairCode);
 result.add((java.lang.Object)$stack7);
 $stack9 = state.getMode();
 if ($stack9==4) goto label0;
 $stack7 = stateNoBinary.shiftAndAppend(4,pairCode);
 result.add((java.lang.Object)$stack7);
 label0:if (pairCode==3) goto label1;
 if (pairCode!=4) goto label2;
 label1:pairCode=(16-pairCode);
 stateNoBinary = stateNoBinary.latchAndAppend(2,pairCode);
 stateNoBinary = stateNoBinary.latchAndAppend(2,1);
 result.add((java.lang.Object)stateNoBinary);
 label2:pairCode = state.getBinaryShiftByteCount();
 if (pairCode<=0) goto label3;
 state = state.addBinaryShiftChar(index);
 index=(index+1);
 state = state.addBinaryShiftChar(index);
 result.add((java.lang.Object)state);
 label3:return;
 } 

