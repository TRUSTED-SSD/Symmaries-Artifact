static void com.google.zxing.qrcode.encoder.MatrixUtil:embedVerticalSeparationPattern(int xStart,int yStart,com.google.zxing.qrcode.encoder.ByteMatrix matrix)
{
int y;
  int $stack4;
  byte $stack5;
  boolean $stack6;
  com.google.zxing.WriterException $stack10;
  y=0;
 label0:if (y>=7) goto label1;
 $stack4=(yStart+y);
 $stack5 = matrix.get(xStart,$stack4);
 $stack6 = com.google.zxing.qrcode.encoder.MatrixUtil.isEmpty((int)$stack5);
 if ($stack6!=0) goto label2;
 $stack10=new com.google.zxing.WriterException;
 $stack10#com.google.zxing.WriterException();
 throw $stack10;
 label2:$stack4=(yStart+y);
 matrix.set(xStart,$stack4,0);
 y=(y+1);
 goto label0;
 label1:return;
 } 

