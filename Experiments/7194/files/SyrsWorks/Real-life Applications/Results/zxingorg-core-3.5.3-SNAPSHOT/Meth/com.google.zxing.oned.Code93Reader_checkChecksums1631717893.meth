static void com.google.zxing.oned.Code93Reader:checkChecksums(java.lang.CharSequence result)
{
int length;
  int $stack3;
  length = result.length();
 $stack3=(length-2);
 com.google.zxing.oned.Code93Reader.checkOneChecksum(result,$stack3,20);
 length=(length-1);
 com.google.zxing.oned.Code93Reader.checkOneChecksum(result,length,15);
 return;
 } 

