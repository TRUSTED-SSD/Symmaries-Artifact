void com.google.zxing.datamatrix.encoder.C40Encoder:handleEOD(com.google.zxing.datamatrix.encoder.EncoderContext context,java.lang.StringBuilder buffer)
{
int unwritten;
  int rest;
  int $stack10;
  com.google.zxing.datamatrix.encoder.SymbolInfo $stack11;
  java.lang.IllegalStateException $stack13;
  boolean $stack15;
  rest = buffer.length();
 rest=(rest/3);
 unwritten=(rest * 2);
 rest = buffer.length();
 rest=(rest%3);
 $stack10 = context.getCodewordCount();
 unwritten=($stack10+unwritten);
 context.updateSymbolInfo(unwritten);
 $stack11 = context.getSymbolInfo();
 $stack10 = $stack11.getDataCapacity();
 unwritten=($stack10-unwritten);
 if (rest!=2) goto label0;
 buffer.append((char)0);
 label1:rest = buffer.length();
 if (rest<3) goto label2;
 com.google.zxing.datamatrix.encoder.C40Encoder.writeNextTriplet(context,buffer);
 goto label1;
 label2:$stack15 = context.hasMoreCharacters();
 if ($stack15==0) goto label3;
 context.writeCodeword((char)254);
 goto label3;
 label0:if (unwritten!=1) goto label4;
 if (rest!=1) goto label4;
 label5:rest = buffer.length();
 if (rest<3) goto label6;
 com.google.zxing.datamatrix.encoder.C40Encoder.writeNextTriplet(context,buffer);
 goto label5;
 label6:$stack15 = context.hasMoreCharacters();
 if ($stack15==0) goto label7;
 context.writeCodeword((char)254);
 label7:rest=context.pos;
 rest=(rest-1);
 context.pos=rest;
 goto label3;
 label4:if (rest!=0) goto label8;
 label9:rest = buffer.length();
 if (rest<3) goto label10;
 com.google.zxing.datamatrix.encoder.C40Encoder.writeNextTriplet(context,buffer);
 goto label9;
 label10:if (unwritten>0) goto label11;
 $stack15 = context.hasMoreCharacters();
 if ($stack15==0) goto label3;
 label11:context.writeCodeword((char)254);
 goto label3;
 label8:$stack13=new java.lang.IllegalStateException;
 $stack13#java.lang.IllegalStateException("Unexpected case. Please report!");
 throw $stack13;
 label3:context.signalEncoderChange(0);
 return;
 } 

