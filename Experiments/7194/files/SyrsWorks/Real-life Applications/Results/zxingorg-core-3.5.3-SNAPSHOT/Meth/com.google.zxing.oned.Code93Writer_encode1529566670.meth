boolean[] com.google.zxing.oned.Code93Writer:encode(java.lang.String @contents)
{
int length;
  int codeWidth;
  boolean[] result;
  int i;
  int indexInString;
  int[] $stack17;
  java.lang.StringBuilder $stack20;
  char $stack23;
  java.lang.String $stack32;
  java.lang.IllegalArgumentException $stack40;
  java.lang.String contents;
contents=@contents;
contents = com.google.zxing.oned.Code93Writer.convertToExtended(contents);
 length = contents.length();
 if (length<=80) goto label0;
 $stack40=new java.lang.IllegalArgumentException;
 $stack20=new java.lang.StringBuilder;
 $stack20#java.lang.StringBuilder();
 $stack20 = $stack20.append("Requested contents should be less than 80 digits long after converting to extended encoding, but got ");
 $stack20 = $stack20.append(length);
 contents = $stack20.toString();
 $stack40#java.lang.IllegalArgumentException(contents);
 throw $stack40;
 label0:codeWidth = contents.length();
 codeWidth=(codeWidth+2);
 codeWidth=(codeWidth+2);
 codeWidth=(codeWidth * 9);
 codeWidth=(codeWidth+1);
 result=new boolean[codeWidth];
 codeWidth=com.google.zxing.oned.Code93Reader.ASTERISK_ENCODING;
 codeWidth = com.google.zxing.oned.Code93Writer.appendPattern(result,0,codeWidth);
 i=0;
 label1:if (i>=length) goto label2;
 $stack32="0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ-. $/+%abcd*";
 $stack23 = contents.charAt(i);
 indexInString = $stack32.indexOf((int)$stack23);
 $stack17=com.google.zxing.oned.Code93Reader.CHARACTER_ENCODINGS;
 indexInString=$stack17[indexInString];
 indexInString = com.google.zxing.oned.Code93Writer.appendPattern(result,codeWidth,indexInString);
 codeWidth=(codeWidth+indexInString);
 i=(i+1);
 goto label1;
 label2:i = com.google.zxing.oned.Code93Writer.computeChecksumIndex(contents,20);
 $stack17=com.google.zxing.oned.Code93Reader.CHARACTER_ENCODINGS;
 length=$stack17[i];
 length = com.google.zxing.oned.Code93Writer.appendPattern(result,codeWidth,length);
 codeWidth=(codeWidth+length);
 $stack20=new java.lang.StringBuilder;
 $stack20#java.lang.StringBuilder();
 $stack20 = $stack20.append(contents);
 contents="0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ-. $/+%abcd*";
 $stack23 = contents.charAt(i);
 $stack20 = $stack20.append($stack23);
 contents = $stack20.toString();
 indexInString = com.google.zxing.oned.Code93Writer.computeChecksumIndex(contents,15);
 $stack17=com.google.zxing.oned.Code93Reader.CHARACTER_ENCODINGS;
 length=$stack17[indexInString];
 length = com.google.zxing.oned.Code93Writer.appendPattern(result,codeWidth,length);
 codeWidth=(codeWidth+length);
 length=com.google.zxing.oned.Code93Reader.ASTERISK_ENCODING;
 length = com.google.zxing.oned.Code93Writer.appendPattern(result,codeWidth,length);
 codeWidth=(codeWidth+length);
 result[codeWidth]=1;
 return  result;
 } 

