com.google.zxing.ResultPoint[] com.google.zxing.datamatrix.detector.Detector:shiftToModuleCenter(com.google.zxing.ResultPoint[] @points)
{
com.google.zxing.ResultPoint pointA;
  com.google.zxing.ResultPoint pointB;
  com.google.zxing.ResultPoint pointC;
  com.google.zxing.ResultPoint pointD;
  int dimH;
  int dimV;
  com.google.zxing.ResultPoint pointAs;
  com.google.zxing.ResultPoint pointCs;
  float centerX;
  float centerY;
  com.google.zxing.ResultPoint pointBs;
  int $stack22;
  float $stack32;
  com.google.zxing.ResultPoint[] points;
points=@points;
pointA=points[0];
 pointB=points[1];
 pointC=points[2];
 pointD=points[3];
 dimH = this#com.google.zxing.datamatrix.detector.Detector:transitionsBetween(pointA,pointD);
 dimH=(dimH+1);
 dimV = this#com.google.zxing.datamatrix.detector.Detector:transitionsBetween(pointC,pointD);
 dimV=(dimV+1);
 dimV=(dimV * 4);
 pointAs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointA,pointB,dimV);
 dimH=(dimH * 4);
 pointCs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointC,pointB,dimH);
 dimH = this#com.google.zxing.datamatrix.detector.Detector:transitionsBetween(pointAs,pointD);
 dimH=(dimH+1);
 dimV = this#com.google.zxing.datamatrix.detector.Detector:transitionsBetween(pointCs,pointD);
 dimV=(dimV+1);
 $stack22=(dimH & 1);
 if ($stack22!=1) goto label0;
 dimH=(dimH+1);
 label0:$stack22=(dimV & 1);
 if ($stack22!=1) goto label1;
 dimV=(dimV+1);
 label1:centerX = pointA.getX();
 centerY = pointB.getX();
 centerX=(centerX+centerY);
 centerY = pointC.getX();
 centerX=(centerX+centerY);
 centerY = pointD.getX();
 centerX=(centerX+centerY);
 centerX=(centerX/4.0F);
 centerY = pointA.getY();
 $stack32 = pointB.getY();
 centerY=(centerY+$stack32);
 $stack32 = pointC.getY();
 centerY=(centerY+$stack32);
 $stack32 = pointD.getY();
 centerY=(centerY+$stack32);
 centerY=(centerY/4.0F);
 pointA = com.google.zxing.datamatrix.detector.Detector.moveAway(pointA,centerX,centerY);
 pointB = com.google.zxing.datamatrix.detector.Detector.moveAway(pointB,centerX,centerY);
 pointC = com.google.zxing.datamatrix.detector.Detector.moveAway(pointC,centerX,centerY);
 pointD = com.google.zxing.datamatrix.detector.Detector.moveAway(pointD,centerX,centerY);
 $stack22=(dimV * 4);
 pointAs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointA,pointB,$stack22);
 $stack22=(dimH * 4);
 pointAs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointAs,pointD,$stack22);
 $stack22=(dimV * 4);
 pointBs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointB,pointA,$stack22);
 $stack22=(dimH * 4);
 pointBs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointBs,pointC,$stack22);
 $stack22=(dimV * 4);
 pointCs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointC,pointD,$stack22);
 $stack22=(dimH * 4);
 pointCs = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointCs,pointB,$stack22);
 dimV=(dimV * 4);
 pointB = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointD,pointC,dimV);
 dimH=(dimH * 4);
 pointA = com.google.zxing.datamatrix.detector.Detector.shiftPoint(pointB,pointA,dimH);
 points=new com.google.zxing.ResultPoint[4];
 points[0]=pointAs;
 points[1]=pointBs;
 points[2]=pointCs;
 points[3]=pointA;
 return  points;
 } 

