static int com.google.zxing.qrcode.encoder.MaskUtil:applyMaskPenaltyRule1Internal (
  com.google.zxing.qrcode.encoder.ByteMatrix matrix, boolean isHorizontal) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(matrix)) = obj_level(matrix);
}