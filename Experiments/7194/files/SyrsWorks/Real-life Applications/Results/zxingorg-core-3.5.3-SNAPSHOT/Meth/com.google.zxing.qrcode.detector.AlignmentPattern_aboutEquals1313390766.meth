boolean com.google.zxing.qrcode.detector.AlignmentPattern:aboutEquals(float moduleSize,float i,float j)
{
float $stack5;
  byte $stack8;
  boolean $stack20;
  $stack5 = this.getY();
 i=(i-$stack5);
 i = java.lang.Math.abs(i);
 $stack8=(i cmpg moduleSize);
 if ($stack8>0) goto label0;
 i = this.getX();
 i=(j-i);
 i = java.lang.Math.abs(i);
 $stack8=(i cmpg moduleSize);
 if ($stack8>0) goto label0;
 i=this.estimatedModuleSize;
 moduleSize=(moduleSize-i);
 moduleSize = java.lang.Math.abs(moduleSize);
 $stack8=(moduleSize cmpg 1.0F);
 if ($stack8<=0) goto label1;
 i=this.estimatedModuleSize;
 $stack8=(moduleSize cmpg i);
 if ($stack8>0) goto label2;
 label1:$stack20=1;
 goto label3;
 label2:$stack20=0;
 label3:return  $stack20;
 label0:return  0;
 } 

