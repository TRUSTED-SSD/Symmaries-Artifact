int[] com.google.zxing.pdf417.encoder.PDF417:determineDimensions(int sourceCodeWords,int errorCorrectionCodeWords)
{
float ratio;
  int[] dimension;
  int cols;
  int rows;
  float newRatio;
  com.google.zxing.WriterException $stack9;
  int $stack18;
  float $stack23;
  float $stack31;
  byte $stack33;
  ratio=0.0F;
 dimension=null;
 cols=this.minCols;
 label0:rows=this.maxCols;
 if (cols>rows) goto label1;
 rows = com.google.zxing.pdf417.encoder.PDF417.calculateNumberOfRows(sourceCodeWords,errorCorrectionCodeWords,cols);
 $stack18=this.minRows;
 if (rows>=$stack18) goto label2;
 goto label1;
 label2:$stack18=this.maxRows;
 if (rows<=$stack18) goto label3;
 goto label4;
 label3:$stack18=(17 * cols);
 $stack18=($stack18+69);
 newRatio=(float) $stack18;
 newRatio=(newRatio * 0.357F);
 $stack23=(float) rows;
 $stack23=($stack23 * 2.0F);
 newRatio=(newRatio/$stack23);
 if (dimension==null) goto label5;
 $stack23=(newRatio-3.0F);
 $stack23 = java.lang.Math.abs($stack23);
 $stack31=(ratio-3.0F);
 $stack31 = java.lang.Math.abs($stack31);
 $stack33=($stack23 cmpl $stack31);
 if ($stack33<=0) goto label5;
 goto label4;
 label5:ratio=newRatio;
 dimension=new int[2];
 dimension[0]=cols;
 dimension[1]=rows;
 label4:cols=(cols+1);
 goto label0;
 label1:if (dimension!=null) goto label6;
 cols=this.minCols;
 cols = com.google.zxing.pdf417.encoder.PDF417.calculateNumberOfRows(sourceCodeWords,errorCorrectionCodeWords,cols);
 sourceCodeWords=this.minRows;
 if (cols>=sourceCodeWords) goto label6;
 dimension=new int[2];
 sourceCodeWords=this.minCols;
 dimension[0]=sourceCodeWords;
 sourceCodeWords=this.minRows;
 dimension[1]=sourceCodeWords;
 label6:if (dimension!=null) goto label7;
 $stack9=new com.google.zxing.WriterException;
 $stack9#com.google.zxing.WriterException("Unable to fit message in columns");
 throw $stack9;
 label7:return  dimension;
 } 

