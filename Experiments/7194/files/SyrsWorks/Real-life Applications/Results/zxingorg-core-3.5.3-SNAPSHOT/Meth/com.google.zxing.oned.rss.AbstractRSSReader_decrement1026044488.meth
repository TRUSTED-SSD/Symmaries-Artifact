static void com.google.zxing.oned.rss.AbstractRSSReader:decrement(int[] array,float[] errors)
{
int index;
  float biggestError;
  int i;
  int $stack5;
  float $stack8;
  byte $stack9;
  index=0;
 biggestError=errors[0];
 i=1;
 label0:$stack5=#array;
 if (i>=$stack5) goto label1;
 $stack8=errors[i];
 $stack9=($stack8 cmpg biggestError);
 if ($stack9>=0) goto label2;
 biggestError=errors[i];
 index=i;
 label2:i=(i+1);
 goto label0;
 label1:i=array[index];
 i=(i-1);
 array[index]=i;
 return;
 } 

