int com.google.zxing.oned.rss.expanded.decoders.GeneralAppIdDecoder:extractNumericValueFromBitArray (
  int pos, int bits) {
  guard = tt;
  ret(level()) = level(bits);
  ret(obj_level(this)) = obj_level(this);
}