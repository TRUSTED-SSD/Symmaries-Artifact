com.google.zxing.common.MinimalECIInput(java.lang.String stringToEncode,java.nio.charset.Charset priorityCharset,int fnc1)
{
com.google.zxing.common.ECIEncoderSet encoderSet;
  int i;
  char c;
  int[] $stack9;
  int $stack13;
  this#java.lang.Object();
 this.fnc1=fnc1;
 encoderSet=new com.google.zxing.common.ECIEncoderSet;
 encoderSet#com.google.zxing.common.ECIEncoderSet(stringToEncode,priorityCharset,fnc1);
 i = encoderSet.length();
 if (i!=1) goto label0;
 i = stringToEncode.length();
 $stack9=new int[i];
 this.bytes=$stack9;
 i=0;
 label1:$stack9=this.bytes;
 $stack13=#$stack9;
 if (i>=$stack13) goto label2;
 c = stringToEncode.charAt(i);
 $stack9=this.bytes;
 $stack13=i;
 if (c!=fnc1) goto label3;
 c=1000;
 goto label3;
 label3:$stack9[$stack13]=c;
 i=(i+1);
 goto label1;
 label2:goto label4;
 label0:$stack9 = com.google.zxing.common.MinimalECIInput.encodeMinimally(stringToEncode,encoderSet,fnc1);
 this.bytes=$stack9;
 label4:return;
 } 

