void com.google.zxing.datamatrix.encoder.DefaultPlacement:place()
{
int pos;
  int row;
  int col;
  int $stack4;
  boolean $stack21;
  pos=0;
 row=4;
 col=0;
 label0:$stack4=this.numrows;
 if (row!=$stack4) goto label1;
 if (col!=0) goto label1;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:corner1($stack4);
 label1:$stack4=this.numrows;
 $stack4=($stack4-2);
 if (row!=$stack4) goto label2;
 if (col!=0) goto label2;
 $stack4=this.numcols;
 $stack4=($stack4%4);
 if ($stack4==0) goto label2;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:corner2($stack4);
 label2:$stack4=this.numrows;
 $stack4=($stack4-2);
 if (row!=$stack4) goto label3;
 if (col!=0) goto label3;
 $stack4=this.numcols;
 $stack4=($stack4%8);
 if ($stack4!=4) goto label3;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:corner3($stack4);
 label3:$stack4=this.numrows;
 $stack4=($stack4+4);
 if (row!=$stack4) goto label4;
 if (col!=2) goto label4;
 $stack4=this.numcols;
 $stack4=($stack4%8);
 if ($stack4!=0) goto label4;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:corner4($stack4);
 label4:$stack4=this.numrows;
 if (row>=$stack4) goto label5;
 if (col<0) goto label5;
 $stack21 = this#com.google.zxing.datamatrix.encoder.DefaultPlacement:noBit(col,row);
 if ($stack21==0) goto label5;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:utah(row,col,$stack4);
 label5:row=(row+-2);
 col=(col+2);
 if (row<0) goto label6;
 $stack4=this.numcols;
 if (col<$stack4) goto label4;
 label6:row=(row+1);
 col=(col+3);
 label7:if (row<0) goto label8;
 $stack4=this.numcols;
 if (col>=$stack4) goto label8;
 $stack21 = this#com.google.zxing.datamatrix.encoder.DefaultPlacement:noBit(col,row);
 if ($stack21==0) goto label8;
 $stack4=pos;
 pos=(pos+1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:utah(row,col,$stack4);
 label8:row=(row+2);
 col=(col+-2);
 $stack4=this.numrows;
 if (row>=$stack4) goto label9;
 if (col>=0) goto label7;
 label9:row=(row+3);
 col=(col+1);
 $stack4=this.numrows;
 if (row<$stack4) goto label0;
 $stack4=this.numcols;
 if (col<$stack4) goto label0;
 pos=this.numcols;
 pos=(pos-1);
 row=this.numrows;
 row=(row-1);
 $stack21 = this#com.google.zxing.datamatrix.encoder.DefaultPlacement:noBit(pos,row);
 if ($stack21==0) goto label10;
 pos=this.numcols;
 pos=(pos-1);
 row=this.numrows;
 row=(row-1);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:setBit(pos,row,(boolean)1);
 pos=this.numcols;
 pos=(pos-2);
 row=this.numrows;
 row=(row-2);
 this#com.google.zxing.datamatrix.encoder.DefaultPlacement:setBit(pos,row,(boolean)1);
 label10:return;
 } 

