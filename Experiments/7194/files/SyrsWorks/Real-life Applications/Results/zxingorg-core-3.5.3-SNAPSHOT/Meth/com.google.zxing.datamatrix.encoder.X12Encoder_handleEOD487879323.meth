void com.google.zxing.datamatrix.encoder.X12Encoder:handleEOD(com.google.zxing.datamatrix.encoder.EncoderContext context,java.lang.StringBuilder buffer)
{
int available;
  int count;
  com.google.zxing.datamatrix.encoder.SymbolInfo $stack5;
  int $stack9;
  context.updateSymbolInfo();
 $stack5 = context.getSymbolInfo();
 available = $stack5.getDataCapacity();
 count = context.getCodewordCount();
 available=(available-count);
 count = buffer.length();
 $stack9=context.pos;
 count=($stack9-count);
 context.pos=count;
 count = context.getRemainingCharacters();
 if (count>1) goto label0;
 if (available>1) goto label0;
 count = context.getRemainingCharacters();
 if (count==available) goto label1;
 label0:context.writeCodeword((char)254);
 label1:available = context.getNewEncoding();
 if (available>=0) goto label2;
 context.signalEncoderChange(0);
 label2:return;
 } 

