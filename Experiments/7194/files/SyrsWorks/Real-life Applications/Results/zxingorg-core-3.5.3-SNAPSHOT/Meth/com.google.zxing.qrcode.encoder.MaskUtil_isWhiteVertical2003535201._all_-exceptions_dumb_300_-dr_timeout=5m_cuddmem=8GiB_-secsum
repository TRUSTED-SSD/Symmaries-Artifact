static boolean com.google.zxing.qrcode.encoder.MaskUtil:isWhiteVertical (
  byte[][] array, int col, int from, int to) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(array)) = obj_level(array);
}