static void com.google.zxing.qrcode.encoder.Encoder:appendECI (com.google.zxing.common.CharacterSetECI
                                                                 eci,
                                                               com.google.zxing.common.BitArray
                                                                 bits) {
  guard = tt;
  ret(obj_level(bits)) = level(bits) ⊔ obj_level(bits);
  ret(obj_level(eci)) = level(bits) ⊔ obj_level(bits) ⊔ obj_level(eci);
}