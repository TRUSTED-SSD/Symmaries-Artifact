byte[] com.google.zxing.pdf417.encoder.BarcodeRow:getScaledRow(int scale)
{
byte[] output;
  int i;
  int $stack7;
  byte[] $stack9;
  byte $stack10;
  output=this.row;
 i=#output;
 i=(i * scale);
 output=new byte[i];
 i=0;
 label0:$stack7=#output;
 if (i>=$stack7) goto label1;
 $stack9=this.row;
 $stack7=(i/scale);
 $stack10=$stack9[$stack7];
 output[i]=$stack10;
 i=(i+1);
 goto label0;
 label1:return  output;
 } 

