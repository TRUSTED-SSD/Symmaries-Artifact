void com.google.zxing.common.BitArray:appendBit(boolean bit)
{
int $stack2;
  int[] $stack8;
  int $stack9;
  int $stack12;
  $stack2=this.size;
 $stack2=($stack2+1);
 this#com.google.zxing.common.BitArray($stack2);
 if (bit==0) goto label0;
 $stack8=this.bits;
 $stack2=this.size;
 $stack2=($stack2/32);
 $stack12=$stack8[$stack2];
 $stack9=this.size;
 $stack9=($stack9 & 31);
 $stack9=(1<<$stack9);
 $stack12=($stack12 | $stack9);
 $stack8[$stack2]=$stack12;
 label0:$stack2=this.size;
 $stack2=($stack2+1);
 this.size=$stack2;
 return;
 } 

