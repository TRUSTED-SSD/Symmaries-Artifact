static void com.google.zxing.qrcode.encoder.Encoder:terminateBits (int
                                                                    numDataBytes,
                                                                   com.google.zxing.common.BitArray
                                                                    bits) {
  guard = tt;
  ret(obj_level(bits)) = level(bits) ⊔ obj_level(bits);
}