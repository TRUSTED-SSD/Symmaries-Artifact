int[] com.google.zxing.oned.rss.RSS14Reader:findFinderPattern(com.google.zxing.common.BitArray row,boolean rightFinderPattern)
{
int[] counters;
  int width;
  boolean isWhite;
  int rowOffset;
  int counterPosition;
  int patternStart;
  com.google.zxing.NotFoundException $stack12;
  int $stack18;
  int $stack19;
  counters = this.getDecodeFinderCounters();
 counters[0]=0;
 counters[1]=0;
 counters[2]=0;
 counters[3]=0;
 width = row.getSize();
 isWhite=0;
 rowOffset=0;
 label0:if (rowOffset>=width) goto label1;
 isWhite = row.get(rowOffset);
 if (isWhite!=0) goto label2;
 isWhite=1;
 goto label3;
 label2:isWhite=0;
 label3:if (rightFinderPattern!=isWhite) goto label4;
 goto label1;
 label4:rowOffset=(rowOffset+1);
 goto label0;
 label1:counterPosition=0;
 patternStart=rowOffset;
 label5:if (rowOffset>=width) goto label6;
 rightFinderPattern = row.get(rowOffset);
 if (rightFinderPattern==isWhite) goto label7;
 $stack18=counters[counterPosition];
 $stack18=($stack18+1);
 counters[counterPosition]=$stack18;
 goto label8;
 label7:if (counterPosition!=3) goto label9;
 rightFinderPattern = com.google.zxing.oned.rss.AbstractRSSReader.isFinderPattern(counters);
 if (rightFinderPattern==0) goto label10;
 counters=new int[2];
 counters[0]=patternStart;
 counters[1]=rowOffset;
 return  counters;
 label10:$stack19=counters[0];
 $stack18=counters[1];
 $stack18=($stack19+$stack18);
 patternStart=(patternStart+$stack18);
 $stack18=counters[2];
 counters[0]=$stack18;
 $stack18=counters[3];
 counters[1]=$stack18;
 counters[2]=0;
 counters[3]=0;
 counterPosition=(counterPosition+-1);
 goto label11;
 label9:counterPosition=(counterPosition+1);
 label11:counters[counterPosition]=1;
 if (isWhite!=0) goto label12;
 rightFinderPattern=1;
 goto label13;
 label12:rightFinderPattern=0;
 label13:isWhite=rightFinderPattern;
 label8:rowOffset=(rowOffset+1);
 goto label5;
 label6:$stack12 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack12;
 } 

