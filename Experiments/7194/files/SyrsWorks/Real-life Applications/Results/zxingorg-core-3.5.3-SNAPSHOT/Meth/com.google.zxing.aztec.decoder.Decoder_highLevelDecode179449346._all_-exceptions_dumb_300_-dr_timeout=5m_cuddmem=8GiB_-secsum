static java.lang.String com.google.zxing.aztec.decoder.Decoder:highLevelDecode (
  boolean[] correctedBits) {
  guard = tt;
  ret(level()) = level(correctedBits) ⊔ obj_level(correctedBits);
  ret(obj_level()) = level(correctedBits) ⊔ obj_level(correctedBits);
  ret(obj_level(correctedBits)) = obj_level(correctedBits);
}