static int com.google.zxing.qrcode.encoder.Encoder:calculateBitsNeeded (
  com.google.zxing.qrcode.decoder.Mode mode,
  com.google.zxing.common.BitArray headerBits,
  com.google.zxing.common.BitArray dataBits,
  com.google.zxing.qrcode.decoder.Version version) {
  guard = tt;
  ret(level()) = level(dataBits) ⊔ level(headerBits) ⊔ level(mode) ⊔ obj_level(headerBits) ⊔ obj_level(mode);
  ret(obj_level(dataBits)) = obj_level(dataBits);
  ret(obj_level(headerBits)) = obj_level(headerBits);
  ret(obj_level(mode)) = obj_level(mode);
  ret(obj_level(version)) = obj_level(version);
}