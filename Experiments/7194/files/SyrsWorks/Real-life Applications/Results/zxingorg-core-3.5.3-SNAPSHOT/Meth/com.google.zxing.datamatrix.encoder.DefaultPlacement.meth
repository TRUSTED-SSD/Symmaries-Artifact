com.google.zxing.datamatrix.encoder.DefaultPlacement(java.lang.CharSequence codewords,int numcols,int numrows)
{
byte[] $stack5;
  this#java.lang.Object();
 this.codewords=codewords;
 this.numcols=numcols;
 this.numrows=numrows;
 numcols=(numcols * numrows);
 $stack5=new byte[numcols];
 this.bits=$stack5;
 $stack5=this.bits;
 java.util.Arrays.fill($stack5,(byte)-1);
 return;
 } 

