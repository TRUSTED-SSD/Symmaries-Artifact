static void com.google.zxing.oned.rss.expanded.RSSExpandedReader:removePartialRows (
  java.util.Collection pairs, java.util.Collection rows) {
  guard = tt;
  ret(obj_level(pairs)) = level(pairs) ⊔ level(rows) ⊔ obj_level(pairs);
  ret(obj_level(rows)) = level(pairs) ⊔ level(rows) ⊔ obj_level(pairs);
}