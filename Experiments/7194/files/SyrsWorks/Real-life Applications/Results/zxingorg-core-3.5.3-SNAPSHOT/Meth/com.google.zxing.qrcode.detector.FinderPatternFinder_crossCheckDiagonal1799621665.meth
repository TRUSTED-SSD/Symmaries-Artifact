boolean com.google.zxing.qrcode.detector.FinderPatternFinder:crossCheckDiagonal(int centerI,int centerJ)
{
int[] stateCount;
  int i;
  int maxI;
  int maxJ;
  com.google.zxing.common.BitMatrix $stack11;
  int $stack15;
  boolean $stack20;
  int $stack25;
  stateCount = this#com.google.zxing.qrcode.detector.FinderPatternFinder:getCrossCheckStateCount();
 i=0;
 label0:if (centerI<i) goto label1;
 if (centerJ<i) goto label1;
 $stack11=this.image;
 maxJ=(centerJ-i);
 maxI=(centerI-i);
 $stack20 = $stack11.get(maxJ,maxI);
 if ($stack20==0) goto label1;
 maxI=stateCount[2];
 maxI=(maxI+1);
 stateCount[2]=maxI;
 i=(i+1);
 goto label0;
 label1:maxI=stateCount[2];
 if (maxI!=0) goto label2;
 return  0;
 label2:if (centerI<i) goto label3;
 if (centerJ<i) goto label3;
 $stack11=this.image;
 maxJ=(centerJ-i);
 maxI=(centerI-i);
 $stack20 = $stack11.get(maxJ,maxI);
 if ($stack20!=0) goto label3;
 maxI=stateCount[1];
 maxI=(maxI+1);
 stateCount[1]=maxI;
 i=(i+1);
 goto label2;
 label3:maxI=stateCount[1];
 if (maxI!=0) goto label4;
 return  0;
 label4:if (centerI<i) goto label5;
 if (centerJ<i) goto label5;
 $stack11=this.image;
 maxJ=(centerJ-i);
 maxI=(centerI-i);
 $stack20 = $stack11.get(maxJ,maxI);
 if ($stack20==0) goto label5;
 maxI=stateCount[0];
 maxI=(maxI+1);
 stateCount[0]=maxI;
 i=(i+1);
 goto label4;
 label5:i=stateCount[0];
 if (i!=0) goto label6;
 return  0;
 label6:$stack11=this.image;
 maxI = $stack11.getHeight();
 $stack11=this.image;
 maxJ = $stack11.getWidth();
 i=1;
 label7:$stack15=(centerI+i);
 if ($stack15>=maxI) goto label8;
 $stack15=(centerJ+i);
 if ($stack15>=maxJ) goto label8;
 $stack11=this.image;
 $stack25=(centerJ+i);
 $stack15=(centerI+i);
 $stack20 = $stack11.get($stack25,$stack15);
 if ($stack20==0) goto label8;
 $stack15=stateCount[2];
 $stack15=($stack15+1);
 stateCount[2]=$stack15;
 i=(i+1);
 goto label7;
 label8:$stack15=(centerI+i);
 if ($stack15>=maxI) goto label9;
 $stack15=(centerJ+i);
 if ($stack15>=maxJ) goto label9;
 $stack11=this.image;
 $stack25=(centerJ+i);
 $stack15=(centerI+i);
 $stack20 = $stack11.get($stack25,$stack15);
 if ($stack20!=0) goto label9;
 $stack15=stateCount[3];
 $stack15=($stack15+1);
 stateCount[3]=$stack15;
 i=(i+1);
 goto label8;
 label9:$stack15=stateCount[3];
 if ($stack15!=0) goto label10;
 return  0;
 label10:$stack15=(centerI+i);
 if ($stack15>=maxI) goto label11;
 $stack15=(centerJ+i);
 if ($stack15>=maxJ) goto label11;
 $stack11=this.image;
 $stack25=(centerJ+i);
 $stack15=(centerI+i);
 $stack20 = $stack11.get($stack25,$stack15);
 if ($stack20==0) goto label11;
 $stack15=stateCount[4];
 $stack15=($stack15+1);
 stateCount[4]=$stack15;
 i=(i+1);
 goto label10;
 label11:centerI=stateCount[4];
 if (centerI!=0) goto label12;
 return  0;
 label12:$stack20 = com.google.zxing.qrcode.detector.FinderPatternFinder.foundPatternDiagonal(stateCount);
 return  $stack20;
 } 

