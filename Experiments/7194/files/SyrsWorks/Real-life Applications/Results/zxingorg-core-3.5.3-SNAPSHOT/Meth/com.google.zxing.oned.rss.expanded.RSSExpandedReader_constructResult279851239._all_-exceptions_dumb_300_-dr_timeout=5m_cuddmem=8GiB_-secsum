static com.google.zxing.Result com.google.zxing.oned.rss.expanded.RSSExpandedReader:constructResult (
  java.util.List pairs) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(pairs) ⊔ obj_level(pairs);
  ret(obj_level(pairs)) = level(pairs) ⊔ obj_level(pairs);
}