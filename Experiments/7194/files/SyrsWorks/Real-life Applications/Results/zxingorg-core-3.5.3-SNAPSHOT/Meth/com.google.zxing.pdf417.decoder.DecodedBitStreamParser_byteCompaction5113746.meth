static int com.google.zxing.pdf417.decoder.DecodedBitStreamParser:byteCompaction(int mode,int[] codewords,int codeIndex,com.google.zxing.common.ECIStringBuilder result)
{
boolean end;
  long value;
  int count;
  int $stack17;
  long $stack20;
  byte $stack28;
  end=0;
 label0:count=codewords[0];
 if (codeIndex>=count) goto label1;
 if (end!=0) goto label1;
 label2:count=codewords[0];
 if (codeIndex>=count) goto label3;
 count=codewords[codeIndex];
 if (count!=927) goto label3;
 codeIndex=(codeIndex+1);
 count=codewords[codeIndex];
 result.appendECI(count);
 codeIndex=(codeIndex+1);
 goto label2;
 label3:count=codewords[0];
 if (codeIndex>=count) goto label4;
 count=codewords[codeIndex];
 if (count<900) goto label5;
 label4:end=1;
 goto label0;
 label5:value=0L;
 count=0;
 label6:$stack20=(900L * value);
 $stack17=codeIndex;
 codeIndex=(codeIndex+1);
 $stack17=codewords[$stack17];
 value=(long) $stack17;
 value=($stack20+value);
 count=(count+1);
 if (count>=5) goto label7;
 $stack17=codewords[0];
 if (codeIndex>=$stack17) goto label7;
 $stack17=codewords[codeIndex];
 if ($stack17<900) goto label6;
 label7:if (count!=5) goto label8;
 if (mode==924) goto label9;
 $stack17=codewords[0];
 if (codeIndex>=$stack17) goto label8;
 $stack17=codewords[codeIndex];
 if ($stack17>=900) goto label8;
 label9:count=0;
 label10:if (count>=6) goto label11;
 $stack17=(5-count);
 $stack17=(8 * $stack17);
 $stack20=(value>>$stack17);
 $stack17=(int) $stack20;
 $stack28=(byte) $stack17;
 result.append($stack28);
 count=(count+1);
 goto label10;
 label11:goto label12;
 label8:codeIndex=(codeIndex-count);
 label13:count=codewords[0];
 if (codeIndex>=count) goto label12;
 if (end!=0) goto label12;
 count=codeIndex;
 codeIndex=(codeIndex+1);
 count=codewords[count];
 if (count>=900) goto label14;
 $stack28=(byte) count;
 result.append($stack28);
 goto label15;
 label14:if (count!=927) goto label16;
 count=codeIndex;
 codeIndex=(codeIndex+1);
 count=codewords[count];
 result.appendECI(count);
 goto label15;
 label16:codeIndex=(codeIndex+-1);
 end=1;
 label15:goto label13;
 label12:goto label0;
 label1:return  codeIndex;
 } 

