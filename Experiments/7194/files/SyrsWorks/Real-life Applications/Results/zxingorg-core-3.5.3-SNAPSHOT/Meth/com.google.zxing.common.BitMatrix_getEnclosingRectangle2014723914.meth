int[] com.google.zxing.common.BitMatrix:getEnclosingRectangle()
{
int left;
  int top;
  int right;
  int bottom;
  int y;
  int x32;
  int theBits;
  int bit;
  int[] $stack10;
  int $stack28;
  left=this.width;
 top=this.height;
 right=-1;
 bottom=-1;
 y=0;
 label0:x32=this.height;
 if (y>=x32) goto label1;
 x32=0;
 label2:theBits=this.rowSize;
 if (x32>=theBits) goto label3;
 $stack10=this.bits;
 theBits=this.rowSize;
 theBits=(y * theBits);
 theBits=(theBits+x32);
 theBits=$stack10[theBits];
 if (theBits==0) goto label4;
 if (y>=top) goto label5;
 top=y;
 label5:if (y<=bottom) goto label6;
 bottom=y;
 label6:bit=(x32 * 32);
 if (bit>=left) goto label7;
 bit=0;
 label8:$stack28=(31-bit);
 $stack28=(theBits<<$stack28);
 if ($stack28!=0) goto label9;
 bit=(bit+1);
 goto label8;
 label9:$stack28=(x32 * 32);
 $stack28=($stack28+bit);
 if ($stack28>=left) goto label7;
 left=(x32 * 32);
 left=(left+bit);
 label7:bit=(x32 * 32);
 bit=(bit+31);
 if (bit<=right) goto label4;
 bit=31;
 label10:$stack28=(theBits>>>bit);
 if ($stack28!=0) goto label11;
 bit=(bit+-1);
 goto label10;
 label11:theBits=(x32 * 32);
 theBits=(theBits+bit);
 if (theBits<=right) goto label4;
 right=(x32 * 32);
 right=(right+bit);
 label4:x32=(x32+1);
 goto label2;
 label3:y=(y+1);
 goto label0;
 label1:if (right<left) goto label12;
 if (bottom>=top) goto label13;
 label12:return  null;
 label13:$stack10=new int[4];
 $stack10[0]=left;
 $stack10[1]=top;
 y=(right-left);
 y=(y+1);
 $stack10[2]=y;
 top=(bottom-top);
 top=(top+1);
 $stack10[3]=top;
 return  $stack10;
 } 

