static void com.google.zxing.qrcode.encoder.MatrixUtil:embedTypeInfo (
  com.google.zxing.qrcode.decoder.ErrorCorrectionLevel ecLevel,
  int maskPattern, com.google.zxing.qrcode.encoder.ByteMatrix matrix) {
  guard = tt;
  ret(obj_level(ecLevel)) = obj_level(ecLevel);
  ret(obj_level(matrix)) = level(matrix) ⊔ obj_level(matrix);
}