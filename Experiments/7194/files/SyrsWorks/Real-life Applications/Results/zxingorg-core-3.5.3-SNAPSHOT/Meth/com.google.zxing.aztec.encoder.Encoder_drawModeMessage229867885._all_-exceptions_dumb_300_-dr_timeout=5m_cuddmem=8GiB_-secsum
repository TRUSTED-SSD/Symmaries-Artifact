static void com.google.zxing.aztec.encoder.Encoder:drawModeMessage (com.google.zxing.common.BitMatrix
                                                                    matrix,
                                                                    boolean
                                                                    compact,
                                                                    int
                                                                    matrixSize,
                                                                    com.google.zxing.common.BitArray
                                                                    modeMessage) {
  guard = tt;
  ret(obj_level(matrix)) = level(matrix) ⊔ level(matrixSize) ⊔ obj_level(matrix);
  ret(obj_level(modeMessage)) = level(matrix) ⊔ level(matrixSize) ⊔ obj_level(matrix) ⊔ obj_level(modeMessage);
}