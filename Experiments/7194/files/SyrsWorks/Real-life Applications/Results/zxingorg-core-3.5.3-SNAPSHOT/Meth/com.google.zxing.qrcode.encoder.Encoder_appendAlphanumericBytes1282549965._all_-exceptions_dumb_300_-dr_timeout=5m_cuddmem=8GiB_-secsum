static void com.google.zxing.qrcode.encoder.Encoder:appendAlphanumericBytes (
  java.lang.CharSequence content, com.google.zxing.common.BitArray bits) {
  guard = tt;
  ret(obj_level(bits)) = level(bits) ⊔ obj_level(bits);
  ret(obj_level(content)) = level(content) ⊔ obj_level(content);
}