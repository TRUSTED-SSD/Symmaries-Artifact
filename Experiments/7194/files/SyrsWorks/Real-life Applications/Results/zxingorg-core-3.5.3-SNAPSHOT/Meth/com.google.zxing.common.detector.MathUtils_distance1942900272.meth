static float com.google.zxing.common.detector.MathUtils:distance(int aX,int aY,int bX,int bY)
{
double xDiff;
  double yDiff;
  float $stack14;
  aX=(aX-bX);
 xDiff=(double) aX;
 aX=(aY-bY);
 yDiff=(double) aX;
 xDiff=(xDiff * xDiff);
 yDiff=(yDiff * yDiff);
 yDiff=(xDiff+yDiff);
 yDiff = java.lang.Math.sqrt(yDiff);
 $stack14=(float) yDiff;
 return  $stack14;
 } 

