static int com.google.zxing.pdf417.encoder.PDF417:calculateNumberOfRows(int m,int k,int c)
{
int r;
  int $stack10;
  r=(m+1);
 r=(r+k);
 r=(r/c);
 r=(r+1);
 $stack10=(c * r);
 m=(m+1);
 m=(m+k);
 m=(m+c);
 if ($stack10<m) goto label0;
 r=(r+-1);
 label0:return  r;
 } 

