static void com.google.zxing.qrcode.encoder.Encoder:appendLengthInfo (
  int numLetters, com.google.zxing.qrcode.decoder.Version version,
  com.google.zxing.qrcode.decoder.Mode mode,
  com.google.zxing.common.BitArray bits) {
  guard = tt;
  ret(obj_level(bits)) = level(bits) ⊔ obj_level(bits);
  ret(obj_level(mode)) = level(bits) ⊔ level(mode) ⊔ level(numLetters) ⊔ obj_level(bits) ⊔ obj_level(mode);
  ret(obj_level(version)) = level(bits) ⊔ obj_level(bits) ⊔ obj_level(version);
}