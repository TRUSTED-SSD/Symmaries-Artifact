static boolean com.google.zxing.pdf417.decoder.PDF417ScanningDecoder:isValidBarcodeColumn (
  com.google.zxing.pdf417.decoder.DetectionResult detectionResult,
  int barcodeColumn) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(detectionResult)) = obj_level(detectionResult);
}