static int com.google.zxing.aztec.decoder.Decoder:readCode (boolean[] rawbits,
                                                            int startIndex,
                                                            int length) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(rawbits)) = obj_level(rawbits);
}