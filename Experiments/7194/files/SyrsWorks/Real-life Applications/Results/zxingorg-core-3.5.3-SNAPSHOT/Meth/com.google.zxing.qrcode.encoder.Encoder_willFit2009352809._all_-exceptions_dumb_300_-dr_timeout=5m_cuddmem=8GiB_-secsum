static boolean com.google.zxing.qrcode.encoder.Encoder:willFit (int
                                                                  numInputBits,
                                                                com.google.zxing.qrcode.decoder.Version
                                                                  version,
                                                                com.google.zxing.qrcode.decoder.ErrorCorrectionLevel
                                                                  ecLevel) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(ecLevel)) = level(ecLevel) ⊔ obj_level(ecLevel);
  ret(obj_level(version)) = obj_level(version);
}