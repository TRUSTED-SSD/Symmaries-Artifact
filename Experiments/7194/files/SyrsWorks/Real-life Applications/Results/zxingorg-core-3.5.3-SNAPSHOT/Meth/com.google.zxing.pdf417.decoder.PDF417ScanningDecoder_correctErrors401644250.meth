static int com.google.zxing.pdf417.decoder.PDF417ScanningDecoder:correctErrors(int[] codewords,int[] erasures,int numECCodewords)
{
com.google.zxing.ChecksumException $stack3;
  com.google.zxing.pdf417.decoder.ec.ErrorCorrection $stack4;
  int $stack6;
  int $stack8;
  if (erasures==null) goto label0;
 $stack8=#erasures;
 $stack6=(numECCodewords/2);
 $stack6=($stack6+3);
 if ($stack8>$stack6) goto label1;
 label0:if (numECCodewords<0) goto label1;
 if (numECCodewords<=512) goto label2;
 label1:$stack3 = com.google.zxing.ChecksumException.getChecksumInstance();
 throw $stack3;
 label2:$stack4=com.google.zxing.pdf417.decoder.PDF417ScanningDecoder.errorCorrection;
 numECCodewords = $stack4.decode(codewords,numECCodewords,erasures);
 return  numECCodewords;
 } 

