static com.google.zxing.common.CharacterSetECI com.google.zxing.common.CharacterSetECI:getCharacterSetECI (
  java.nio.charset.Charset charset) {
  guard = tt;
  ret(level()) = level(charset) ⊔ obj_level(charset);
  ret(obj_level()) = level(charset) ⊔ obj_level(charset);
  ret(obj_level(charset)) = level(charset) ⊔ obj_level(charset);
}