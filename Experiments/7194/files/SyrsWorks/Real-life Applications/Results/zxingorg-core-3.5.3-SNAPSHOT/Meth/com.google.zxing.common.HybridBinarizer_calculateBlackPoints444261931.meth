static int[][] com.google.zxing.common.HybridBinarizer:calculateBlackPoints(byte[] luminances,int subWidth,int subHeight,int width,int height)
{
int maxXOffset;
  int[][] blackPoints;
  int y;
  int yoffset;
  int x;
  int xoffset;
  int sum;
  short min;
  short max;
  int yy;
  int xx;
  short pixel;
  int[] $stack23;
  int $stack51;
  byte $stack52;
  height=(height-8);
 maxXOffset=(width-8);
 blackPoints= new int[subHeight][subWidth];
 y=0;
 label0:if (y>=subHeight) goto label1;
 yoffset=(y<<3);
 if (yoffset<=height) goto label2;
 yoffset=height;
 label2:x=0;
 label3:if (x>=subWidth) goto label4;
 xoffset=(x<<3);
 if (xoffset<=maxXOffset) goto label5;
 xoffset=maxXOffset;
 label5:sum=0;
 min=255;
 max=0;
 yy=0;
 xx=(yoffset * width);
 xoffset=(xx+xoffset);
 label6:if (yy>=8) goto label7;
 xx=0;
 label8:if (xx>=8) goto label9;
 $stack51=(xoffset+xx);
 $stack52=luminances[$stack51];
 pixel=($stack52 & 255);
 sum=(sum+pixel);
 if (pixel>=min) goto label10;
 min=pixel;
 label10:if (pixel<=max) goto label11;
 max=pixel;
 label11:xx=(xx+1);
 goto label8;
 label9:xx=(max-min);
 if (xx<=24) goto label12;
 yy=(yy+1);
 xoffset=(xoffset+width);
 label13:if (yy>=8) goto label12;
 xx=0;
 label14:if (xx>=8) goto label15;
 $stack51=(xoffset+xx);
 $stack52=luminances[$stack51];
 pixel=($stack52 & 255);
 sum=(sum+pixel);
 xx=(xx+1);
 goto label14;
 label15:yy=(yy+1);
 xoffset=(xoffset+width);
 goto label13;
 label12:yy=(yy+1);
 xoffset=(xoffset+width);
 goto label6;
 label7:yy=(sum>>6);
 sum=(max-min);
 if (sum>24) goto label16;
 yy=(min/2);
 if (y<=0) goto label16;
 if (x<=0) goto label16;
 sum=(y-1);
 $stack23=blackPoints[sum];
 sum=$stack23[x];
 $stack23=blackPoints[y];
 xoffset=(x-1);
 xoffset=$stack23[xoffset];
 xoffset=(2 * xoffset);
 sum=(sum+xoffset);
 xoffset=(y-1);
 $stack23=blackPoints[xoffset];
 xoffset=(x-1);
 xoffset=$stack23[xoffset];
 sum=(sum+xoffset);
 xoffset=(sum/4);
 if (min>=xoffset) goto label16;
 yy=xoffset;
 label16:$stack23=blackPoints[y];
 $stack23[x]=yy;
 x=(x+1);
 goto label3;
 label4:y=(y+1);
 goto label0;
 label1:return  blackPoints;
 } 

