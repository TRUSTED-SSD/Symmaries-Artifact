com.google.zxing.common.BitArray com.google.zxing.common.BitMatrix:getRow(int y,com.google.zxing.common.BitArray @row)
{
int x;
  int $stack8;
  int $stack9;
  int[] $stack10;
  com.google.zxing.common.BitArray row;
row=@row;
if (row==null) goto label0;
 x = row.getSize();
 $stack8=this.width;
 if (x>=$stack8) goto label1;
 label0:row=new com.google.zxing.common.BitArray;
 x=this.width;
 row#com.google.zxing.common.BitArray(x);
 goto label2;
 label1:row.clear();
 label2:x=this.rowSize;
 y=(y * x);
 x=0;
 label3:$stack8=this.rowSize;
 if (x>=$stack8) goto label4;
 $stack8=(x * 32);
 $stack10=this.bits;
 $stack9=(y+x);
 $stack9=$stack10[$stack9];
 row.setBulk($stack8,$stack9);
 x=(x+1);
 goto label3;
 label4:return  row;
 } 

