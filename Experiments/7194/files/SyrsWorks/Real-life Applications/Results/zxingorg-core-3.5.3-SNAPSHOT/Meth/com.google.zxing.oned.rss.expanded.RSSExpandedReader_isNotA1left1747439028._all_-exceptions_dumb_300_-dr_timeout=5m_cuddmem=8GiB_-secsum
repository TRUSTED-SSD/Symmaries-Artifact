static boolean com.google.zxing.oned.rss.expanded.RSSExpandedReader:isNotA1left (
  com.google.zxing.oned.rss.FinderPattern pattern, boolean isOddPattern,
  boolean leftChar) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(pattern)) = obj_level(pattern);
}