static int com.google.zxing.qrcode.decoder.DecodedBitStreamParser:parseECIValue (
  com.google.zxing.common.BitSource bits) {
  guard = tt;
  ret(level()) = level(bits) ⊔ obj_level(bits);
  ret(obj_level(bits)) = level(bits) ⊔ obj_level(bits);
}