static int com.google.zxing.datamatrix.DataMatrixReader:moduleSize (int[]
                                                                    leftTopBlack,
                                                                    com.google.zxing.common.BitMatrix
                                                                    image) {
  guard = tt;
  ret(level()) = pc ⊔ level(image) ⊔ level(leftTopBlack) ⊔ obj_level(image) ⊔ obj_level(leftTopBlack);
  ret(obj_level(image)) = if obj_level(image) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(image) = ⊥ ∧ obj_level(leftTopBlack) = ⊤ then ⊤ else ⊥;
  ret(obj_level(leftTopBlack)) = obj_level(leftTopBlack);
}