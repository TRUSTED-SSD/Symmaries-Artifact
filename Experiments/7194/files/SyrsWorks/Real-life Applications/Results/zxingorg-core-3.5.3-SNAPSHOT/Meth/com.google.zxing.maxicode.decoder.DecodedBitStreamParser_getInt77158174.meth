static int com.google.zxing.maxicode.decoder.DecodedBitStreamParser:getInt(byte[] bytes,byte[] x)
{
int val;
  int i;
  int $stack4;
  byte $stack5;
  int $stack7;
  val=0;
 i=0;
 label0:$stack4=#x;
 if (i>=$stack4) goto label1;
 $stack5=x[i];
 $stack4 = com.google.zxing.maxicode.decoder.DecodedBitStreamParser.getBit((int)$stack5,bytes);
 $stack7=#x;
 $stack7=($stack7-i);
 $stack7=($stack7-1);
 $stack4=($stack4<<$stack7);
 val=(val+$stack4);
 i=(i+1);
 goto label0;
 label1:return  val;
 } 

