static int[] com.google.zxing.aztec.encoder.Encoder:bitsToWords (com.google.zxing.common.BitArray
                                                                   stuffedBits,
                                                                 int wordSize,
                                                                 int
                                                                   totalWords) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(totalWords) ⊔ level(wordSize);
  ret(obj_level(stuffedBits)) = level(wordSize) ⊔ obj_level(stuffedBits);
}