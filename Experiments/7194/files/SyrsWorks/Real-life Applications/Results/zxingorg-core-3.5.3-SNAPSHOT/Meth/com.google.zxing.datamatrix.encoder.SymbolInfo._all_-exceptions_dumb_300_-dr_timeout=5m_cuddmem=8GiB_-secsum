com.google.zxing.datamatrix.encoder.SymbolInfo (boolean rectangular,
                                                int dataCapacity,
                                                int errorCodewords,
                                                int matrixWidth,
                                                int matrixHeight,
                                                int dataRegions) {
  guard = tt;
  ret(obj_level(this)) = obj_level(this);
}