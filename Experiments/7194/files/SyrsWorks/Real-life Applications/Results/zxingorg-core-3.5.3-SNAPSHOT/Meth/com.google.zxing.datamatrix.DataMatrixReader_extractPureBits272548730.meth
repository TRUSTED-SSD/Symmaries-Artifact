static com.google.zxing.common.BitMatrix com.google.zxing.datamatrix.DataMatrixReader:extractPureBits(com.google.zxing.common.BitMatrix image)
{
int[] leftTopBlack;
  int[] rightBottomBlack;
  int moduleSize;
  int top;
  int bottom;
  int left;
  int right;
  int nudge;
  com.google.zxing.common.BitMatrix bits;
  int iOffset;
  int x;
  com.google.zxing.NotFoundException $stack17;
  int $stack28;
  boolean $stack30;
  leftTopBlack = image.getTopLeftOnBit();
 rightBottomBlack = image.getBottomRightOnBit();
 if (leftTopBlack==null) goto label0;
 if (rightBottomBlack!=null) goto label1;
 label0:$stack17 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack17;
 label1:moduleSize = com.google.zxing.datamatrix.DataMatrixReader.moduleSize(leftTopBlack,image);
 top=leftTopBlack[1];
 bottom=rightBottomBlack[1];
 left=leftTopBlack[0];
 right=rightBottomBlack[0];
 right=(right-left);
 right=(right+1);
 right=(right/moduleSize);
 bottom=(bottom-top);
 bottom=(bottom+1);
 bottom=(bottom/moduleSize);
 if (right<=0) goto label2;
 if (bottom>0) goto label3;
 label2:$stack17 = com.google.zxing.NotFoundException.getNotFoundInstance();
 throw $stack17;
 label3:nudge=(moduleSize/2);
 top=(top+nudge);
 left=(left+nudge);
 bits=new com.google.zxing.common.BitMatrix;
 bits#com.google.zxing.common.BitMatrix(right,bottom);
 nudge=0;
 label4:if (nudge>=bottom) goto label5;
 iOffset=(nudge * moduleSize);
 iOffset=(top+iOffset);
 x=0;
 label6:if (x>=right) goto label7;
 $stack28=(x * moduleSize);
 $stack28=(left+$stack28);
 $stack30 = image.get($stack28,iOffset);
 if ($stack30==0) goto label8;
 bits.set(x,nudge);
 label8:x=(x+1);
 goto label6;
 label7:nudge=(nudge+1);
 goto label4;
 label5:return  bits;
 } 

