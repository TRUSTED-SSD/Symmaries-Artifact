static com.google.zxing.ResultPoint[] com.google.zxing.aztec.detector.Detector:expandSquare(com.google.zxing.ResultPoint[] @cornerPoints,int oldSide,int newSide)
{
float ratio;
  float dx;
  float dy;
  float centerx;
  float centery;
  com.google.zxing.ResultPoint $stack15;
  float $stack31;
  float $stack35;
  com.google.zxing.ResultPoint $stack38;
  com.google.zxing.ResultPoint $stack43;
  com.google.zxing.ResultPoint $stack66;
  com.google.zxing.ResultPoint[] cornerPoints;
cornerPoints=@cornerPoints;
ratio=(float) newSide;
 dx=(float) oldSide;
 dx=(2.0F * dx);
 ratio=(ratio/dx);
 $stack15=cornerPoints[0];
 dx = $stack15.getX();
 $stack15=cornerPoints[2];
 dy = $stack15.getX();
 dx=(dx-dy);
 $stack15=cornerPoints[0];
 dy = $stack15.getY();
 $stack15=cornerPoints[2];
 centerx = $stack15.getY();
 dy=(dy-centerx);
 $stack15=cornerPoints[0];
 centerx = $stack15.getX();
 $stack15=cornerPoints[2];
 centery = $stack15.getX();
 centerx=(centerx+centery);
 centerx=(centerx/2.0F);
 $stack15=cornerPoints[0];
 centery = $stack15.getY();
 $stack15=cornerPoints[2];
 $stack31 = $stack15.getY();
 centery=(centery+$stack31);
 centery=(centery/2.0F);
 $stack15=new com.google.zxing.ResultPoint;
 $stack31=(ratio * dx);
 $stack31=(centerx+$stack31);
 $stack35=(ratio * dy);
 $stack35=(centery+$stack35);
 $stack15#com.google.zxing.ResultPoint($stack31,$stack35);
 $stack38=new com.google.zxing.ResultPoint;
 dx=(ratio * dx);
 dx=(centerx-dx);
 dy=(ratio * dy);
 dy=(centery-dy);
 $stack38#com.google.zxing.ResultPoint(dx,dy);
 $stack43=cornerPoints[1];
 dx = $stack43.getX();
 $stack43=cornerPoints[3];
 dy = $stack43.getX();
 dx=(dx-dy);
 $stack43=cornerPoints[1];
 dy = $stack43.getY();
 $stack43=cornerPoints[3];
 centerx = $stack43.getY();
 dy=(dy-centerx);
 $stack43=cornerPoints[1];
 centerx = $stack43.getX();
 $stack43=cornerPoints[3];
 centery = $stack43.getX();
 centerx=(centerx+centery);
 centerx=(centerx/2.0F);
 $stack43=cornerPoints[1];
 centery = $stack43.getY();
 $stack43=cornerPoints[3];
 $stack31 = $stack43.getY();
 centery=(centery+$stack31);
 centery=(centery/2.0F);
 $stack43=new com.google.zxing.ResultPoint;
 $stack31=(ratio * dx);
 $stack31=(centerx+$stack31);
 $stack35=(ratio * dy);
 $stack35=(centery+$stack35);
 $stack43#com.google.zxing.ResultPoint($stack31,$stack35);
 $stack66=new com.google.zxing.ResultPoint;
 dx=(ratio * dx);
 dx=(centerx-dx);
 ratio=(ratio * dy);
 ratio=(centery-ratio);
 $stack66#com.google.zxing.ResultPoint(dx,ratio);
 cornerPoints=new com.google.zxing.ResultPoint[4];
 cornerPoints[0]=$stack15;
 cornerPoints[1]=$stack43;
 cornerPoints[2]=$stack38;
 cornerPoints[3]=$stack66;
 return  cornerPoints;
 } 

