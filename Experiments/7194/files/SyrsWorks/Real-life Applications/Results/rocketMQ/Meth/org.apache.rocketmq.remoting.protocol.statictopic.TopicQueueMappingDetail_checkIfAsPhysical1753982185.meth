static boolean org.apache.rocketmq.remoting.protocol.statictopic.TopicQueueMappingDetail:checkIfAsPhysical(org.apache.rocketmq.remoting.protocol.statictopic.TopicQueueMappingDetail mappingDetail,java.lang.Integer globalId)
{
java.util.List mappingItems;
  int $stack4;
  boolean $stack5;
  java.lang.Object $stack6;
  org.apache.rocketmq.remoting.protocol.statictopic.LogicQueueMappingItem $stack7;
  long $stack8;
  byte $stack9;
  mappingItems = org.apache.rocketmq.remoting.protocol.statictopic.TopicQueueMappingDetail.getMappingInfo(mappingDetail,globalId);
 if (mappingItems==null) goto label0;
 $stack4 = mappingItems.size();
 if ($stack4!=1) goto label1;
 $stack6 = mappingItems.get(0);
 $stack7=(org.apache.rocketmq.remoting.protocol.statictopic.LogicQueueMappingItem) $stack6;
 $stack8 = $stack7.getLogicOffset();
 $stack9=($stack8 cmp 0L);
 if ($stack9!=0) goto label1;
 label0:$stack5=1;
 goto label2;
 label1:$stack5=0;
 label2:return  $stack5;
 } 

