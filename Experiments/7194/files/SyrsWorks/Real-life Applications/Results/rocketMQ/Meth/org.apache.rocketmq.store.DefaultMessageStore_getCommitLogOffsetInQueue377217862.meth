long org.apache.rocketmq.store.DefaultMessageStore:getCommitLogOffsetInQueue(java.lang.String topic,int queueId,long consumeQueueOffset)
{
org.apache.rocketmq.store.queue.ConsumeQueueInterface consumeQueue;
  org.apache.rocketmq.store.queue.ReferredIterator bufferConsumeQueue;
  java.lang.Throwable l11;
  boolean $stack15;
  java.lang.Object $stack16;
  org.apache.rocketmq.store.queue.CqUnit $stack17;
  consumeQueue = this.findConsumeQueue(topic,queueId);
 if (consumeQueue==null) goto label0;
 bufferConsumeQueue = consumeQueue.iterateFrom(consumeQueueOffset);
 if (bufferConsumeQueue==null) goto label0;
 label1:$stack15 = bufferConsumeQueue.hasNext();
 if ($stack15==0) goto label2;
 $stack16 = bufferConsumeQueue.next();
 $stack17=(org.apache.rocketmq.store.queue.CqUnit) $stack16;
 consumeQueueOffset = $stack17.getPos();
 label3:bufferConsumeQueue.release();
 return  consumeQueueOffset;
 label2:bufferConsumeQueue.release();
 goto label0;
 label4:l11 = catch;
 label5:bufferConsumeQueue.release();
 throw l11;
 label0:return  0L;
 catch java.lang.Throwable: label1 - label3 : label4;
catch java.lang.Throwable: label4 - label5 : label4;
} 

