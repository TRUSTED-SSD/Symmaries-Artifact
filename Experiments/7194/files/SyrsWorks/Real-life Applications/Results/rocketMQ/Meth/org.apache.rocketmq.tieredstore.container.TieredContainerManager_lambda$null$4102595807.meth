void org.apache.rocketmq.tieredstore.container.TieredContainerManager:lambda$null$4(org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer container,long expiredTimeStamp)
{
java.lang.Throwable l4;
  java.util.concurrent.locks.ReentrantLock $stack5;
  byte $stack7;
  org.apache.rocketmq.common.message.MessageQueue $stack9;
  $stack5 = container.getQueueLock();
 $stack5.lock();
 label0:container.cleanExpiredFile(expiredTimeStamp);
 container.destroyExpiredFile();
 expiredTimeStamp = container.getConsumeQueueBaseOffset();
 $stack7=(expiredTimeStamp cmp -1L);
 if ($stack7!=0) goto label1;
 $stack9 = container.getMessageQueue();
 this.destroyContainer($stack9);
 label1:$stack5 = container.getQueueLock();
 $stack5.unlock();
 goto label2;
 label3:l4 = catch;
 label4:$stack5 = container.getQueueLock();
 $stack5.unlock();
 throw l4;
 label2:return;
 catch java.lang.Throwable: label0 - label1 : label3;
catch java.lang.Throwable: label3 - label4 : label3;
} 

