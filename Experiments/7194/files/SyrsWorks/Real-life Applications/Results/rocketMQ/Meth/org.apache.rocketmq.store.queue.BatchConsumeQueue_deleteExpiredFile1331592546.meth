int org.apache.rocketmq.store.queue.BatchConsumeQueue:deleteExpiredFile(long minCommitLogPos)
{
int cnt;
  org.apache.rocketmq.store.MappedFileQueue $stack4;
  $stack4=this.mappedFileQueue;
 cnt = $stack4.deleteExpiredFileByOffset(minCommitLogPos,46);
 this.correctMinOffset(minCommitLogPos);
 return  cnt;
 } 

