static java.net.SocketAddress org.apache.rocketmq.common.utils.NetworkUtil:string2SocketAddress (
  java.lang.String @addr) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(@addr) ⊔ obj_level(@addr);
  ret(obj_level(@addr)) = level(@addr) ⊔ obj_level(@addr);
}