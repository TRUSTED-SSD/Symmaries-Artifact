static int org.apache.rocketmq.common.utils.MessageUtils:getShardingKeyIndex (
  java.lang.String shardingKey, int indexSize) {
  guard = tt;
  ret(level()) = level(indexSize) ⊔ level(shardingKey) ⊔ obj_level(shardingKey);
  ret(obj_level(shardingKey)) = level(shardingKey) ⊔ obj_level(shardingKey);
}