void org.apache.rocketmq.store.ConsumeQueue:recover()
{
java.util.List mappedFiles;
  int index;
  int mappedFileSizeLogics;
  org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  java.nio.ByteBuffer byteBuffer;
  long processOffset;
  long mappedFileOffset;
  long maxExtAddr;
  int i;
  long offset;
  int size;
  long tagsCode;
  org.apache.rocketmq.store.MappedFileQueue $stack18;
  boolean $stack20;
  java.lang.Object $stack22;
  byte $stack26;
  java.lang.StringBuilder $stack27;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack28;
  java.lang.String $stack30;
  org.apache.rocketmq.store.ConsumeQueueExt $stack40;
  java.lang.Long $stack43;
  int $stack100;
  long $stack101;
  $stack18=this.mappedFileQueue;
 mappedFiles = $stack18.getMappedFiles();
 $stack20 = mappedFiles.isEmpty();
 if ($stack20!=0) goto label0;
 index = mappedFiles.size();
 index=(index-3);
 if (index>=0) goto label1;
 index=0;
 label1:mappedFileSizeLogics=this.mappedFileSize;
 $stack22 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack22;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 maxExtAddr=1L;
 label2:i=0;
 label3:if (i>=mappedFileSizeLogics) goto label4;
 offset = byteBuffer.getLong();
 size = byteBuffer.getInt();
 tagsCode = byteBuffer.getLong();
 $stack26=(offset cmp 0L);
 if ($stack26<0) goto label5;
 if (size<=0) goto label5;
 $stack100=(i+20);
 mappedFileOffset=(long) $stack100;
 $stack101=(long) size;
 offset=(offset+$stack101);
 this.maxPhysicOffset=offset;
 $stack20 = this.isExtAddr(tagsCode);
 if ($stack20==0) goto label6;
 maxExtAddr=tagsCode;
 goto label6;
 label5:$stack28=org.apache.rocketmq.store.ConsumeQueue.log;
 $stack27=new java.lang.StringBuilder;
 $stack27#java.lang.StringBuilder();
 $stack27 = $stack27.append("recover current consume queue file over,  ");
 $stack30 = mappedFile.getFileName();
 $stack27 = $stack27.append($stack30);
 $stack27 = $stack27.append(" ");
 $stack27 = $stack27.append(offset);
 $stack27 = $stack27.append(" ");
 $stack27 = $stack27.append(size);
 $stack27 = $stack27.append(" ");
 $stack27 = $stack27.append(tagsCode);
 $stack30 = $stack27.toString();
 $stack28.info($stack30);
 goto label4;
 label6:i=(i+20);
 goto label3;
 label4:tagsCode=(long) mappedFileSizeLogics;
 $stack26=(mappedFileOffset cmp tagsCode);
 if ($stack26!=0) goto label7;
 index=(index+1);
 i = mappedFiles.size();
 if (index<i) goto label8;
 $stack28=org.apache.rocketmq.store.ConsumeQueue.log;
 $stack27=new java.lang.StringBuilder;
 $stack27#java.lang.StringBuilder();
 $stack27 = $stack27.append("recover last consume queue file over, last mapped file ");
 $stack30 = mappedFile.getFileName();
 $stack27 = $stack27.append($stack30);
 $stack30 = $stack27.toString();
 $stack28.info($stack30);
 goto label9;
 label8:$stack22 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack22;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 $stack28=org.apache.rocketmq.store.ConsumeQueue.log;
 $stack27=new java.lang.StringBuilder;
 $stack27#java.lang.StringBuilder();
 $stack27 = $stack27.append("recover next consume queue file, ");
 $stack30 = mappedFile.getFileName();
 $stack27 = $stack27.append($stack30);
 $stack30 = $stack27.toString();
 $stack28.info($stack30);
 goto label2;
 label7:$stack28=org.apache.rocketmq.store.ConsumeQueue.log;
 $stack27=new java.lang.StringBuilder;
 $stack27#java.lang.StringBuilder();
 $stack27 = $stack27.append("recover current consume queue over ");
 $stack30 = mappedFile.getFileName();
 $stack27 = $stack27.append($stack30);
 $stack27 = $stack27.append(" ");
 tagsCode=(processOffset+mappedFileOffset);
 $stack27 = $stack27.append(tagsCode);
 $stack30 = $stack27.toString();
 $stack28.info($stack30);
 goto label9;
 label9:processOffset=(processOffset+mappedFileOffset);
 $stack18=this.mappedFileQueue;
 $stack18.setFlushedWhere(processOffset);
 $stack18=this.mappedFileQueue;
 $stack18.setCommittedWhere(processOffset);
 $stack18=this.mappedFileQueue;
 $stack18.truncateDirtyFiles(processOffset);
 $stack20 = this.isExtReadEnable();
 if ($stack20==0) goto label0;
 $stack40=this.consumeQueueExt;
 $stack40.recover();
 $stack28=org.apache.rocketmq.store.ConsumeQueue.log;
 $stack43 = java.lang.Long.valueOf(maxExtAddr);
 $stack28.info("Truncate consume queue extend file by max {}",(java.lang.Object)$stack43);
 $stack40=this.consumeQueueExt;
 $stack40.truncateByMaxAddress(maxExtAddr);
 label0:return;
 } 

