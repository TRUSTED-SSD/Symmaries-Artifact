static java.lang.String org.apache.rocketmq.remoting.common.RemotingHelper:parseChannelRemoteAddr (
  io.netty.channel.Channel channel) {
  guard = tt;
  ret(level()) = level(channel) ⊔ obj_level(channel);
  ret(obj_level()) = ⊤;
  ret(obj_level(channel)) = level(channel) ⊔ obj_level(channel);
}