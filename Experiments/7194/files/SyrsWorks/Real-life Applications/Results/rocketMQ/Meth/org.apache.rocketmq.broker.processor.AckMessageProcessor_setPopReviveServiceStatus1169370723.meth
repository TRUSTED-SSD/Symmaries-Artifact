void org.apache.rocketmq.broker.processor.AckMessageProcessor:setPopReviveServiceStatus(boolean shouldStart)
{
org.apache.rocketmq.broker.processor.PopReviveService[] l2;
  int l3;
  int l4;
  org.apache.rocketmq.broker.processor.PopReviveService popReviveService;
  l2=this.popReviveServices;
 l3=#l2;
 l4=0;
 label0:if (l4>=l3) goto label1;
 popReviveService=l2[l4];
 popReviveService.setShouldRunPopRevive(shouldStart);
 l4=(l4+1);
 goto label0;
 label1:return;
 } 

