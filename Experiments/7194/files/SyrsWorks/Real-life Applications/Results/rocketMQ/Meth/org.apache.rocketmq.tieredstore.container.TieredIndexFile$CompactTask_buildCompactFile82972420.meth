void org.apache.rocketmq.tieredstore.container.TieredIndexFile$CompactTask:buildCompactFile()
{
java.nio.MappedByteBuffer originMappedByteBuffer;
  java.nio.MappedByteBuffer compactMappedByteBuffer;
  int rePutSlotValue;
  int i;
  int slotOffset;
  int slotValue;
  int indexTotalSize;
  int rePutIndexOffset;
  org.apache.rocketmq.store.logfile.MappedFile $stack11;
  long $stack17;
  int $stack56;
  int $stack58;
  $stack11=this.originFile;
 originMappedByteBuffer = $stack11.getMappedByteBuffer();
 $stack11=this.compactFile;
 compactMappedByteBuffer = $stack11.getMappedByteBuffer();
 compactMappedByteBuffer.putInt(0,-1127939447);
 $stack17 = originMappedByteBuffer.getLong(4);
 compactMappedByteBuffer.putLong(4,$stack17);
 $stack17 = originMappedByteBuffer.getLong(12);
 compactMappedByteBuffer.putLong(12,$stack17);
 rePutSlotValue=this.maxHashSlotNum;
 compactMappedByteBuffer.putInt(20,rePutSlotValue);
 rePutSlotValue = originMappedByteBuffer.getInt(24);
 compactMappedByteBuffer.putInt(24,rePutSlotValue);
 rePutSlotValue=this.maxHashSlotNum;
 rePutSlotValue=(rePutSlotValue * 8);
 rePutSlotValue=(28+rePutSlotValue);
 i=0;
 label0:indexTotalSize=this.maxHashSlotNum;
 if (i>=indexTotalSize) goto label1;
 indexTotalSize=(i * 8);
 slotOffset=(28+indexTotalSize);
 slotValue = originMappedByteBuffer.getInt(slotOffset);
 if (slotValue==-1) goto label2;
 indexTotalSize=0;
 label3:if (slotValue<0) goto label4;
 rePutIndexOffset=this.maxIndexNum;
 if (slotValue>=rePutIndexOffset) goto label4;
 rePutIndexOffset=this.maxHashSlotNum;
 rePutIndexOffset=(rePutIndexOffset * 8);
 rePutIndexOffset=(28+rePutIndexOffset);
 slotValue=(slotValue * 32);
 slotValue=(rePutIndexOffset+slotValue);
 rePutIndexOffset=(rePutSlotValue+indexTotalSize);
 $stack56 = originMappedByteBuffer.getInt(slotValue);
 compactMappedByteBuffer.putInt(rePutIndexOffset,$stack56);
 $stack56=(rePutIndexOffset+4);
 $stack58=(slotValue+4);
 $stack58 = originMappedByteBuffer.getInt($stack58);
 compactMappedByteBuffer.putInt($stack56,$stack58);
 $stack56=(rePutIndexOffset+4);
 $stack56=($stack56+4);
 $stack58=(slotValue+4);
 $stack58=($stack58+4);
 $stack58 = originMappedByteBuffer.getInt($stack58);
 compactMappedByteBuffer.putInt($stack56,$stack58);
 $stack56=(rePutIndexOffset+4);
 $stack56=($stack56+4);
 $stack56=($stack56+4);
 $stack58=(slotValue+4);
 $stack58=($stack58+4);
 $stack58=($stack58+4);
 $stack17 = originMappedByteBuffer.getLong($stack58);
 compactMappedByteBuffer.putLong($stack56,$stack17);
 $stack56=(rePutIndexOffset+4);
 $stack56=($stack56+4);
 $stack56=($stack56+4);
 $stack56=($stack56+8);
 $stack58=(slotValue+4);
 $stack58=($stack58+4);
 $stack58=($stack58+4);
 $stack58=($stack58+8);
 $stack58 = originMappedByteBuffer.getInt($stack58);
 compactMappedByteBuffer.putInt($stack56,$stack58);
 rePutIndexOffset=(rePutIndexOffset+4);
 rePutIndexOffset=(rePutIndexOffset+4);
 rePutIndexOffset=(rePutIndexOffset+4);
 rePutIndexOffset=(rePutIndexOffset+8);
 rePutIndexOffset=(rePutIndexOffset+4);
 $stack56=(slotValue+4);
 $stack56=($stack56+4);
 $stack56=($stack56+4);
 $stack56=($stack56+8);
 $stack56=($stack56+4);
 $stack56 = originMappedByteBuffer.getInt($stack56);
 compactMappedByteBuffer.putInt(rePutIndexOffset,$stack56);
 indexTotalSize=(indexTotalSize+28);
 slotValue=(slotValue+4);
 slotValue=(slotValue+4);
 slotValue=(slotValue+4);
 slotValue=(slotValue+8);
 slotValue=(slotValue+4);
 slotValue=(slotValue+4);
 slotValue = originMappedByteBuffer.getInt(slotValue);
 goto label3;
 label4:compactMappedByteBuffer.putInt(slotOffset,rePutSlotValue);
 slotOffset=(slotOffset+4);
 compactMappedByteBuffer.putInt(slotOffset,indexTotalSize);
 rePutSlotValue=(rePutSlotValue+indexTotalSize);
 label2:i=(i+1);
 goto label0;
 label1:compactMappedByteBuffer.putInt(0,-1127939451);
 compactMappedByteBuffer.putInt(rePutSlotValue,-1127939447);
 rePutSlotValue=(rePutSlotValue+4);
 compactMappedByteBuffer.limit(rePutSlotValue);
 return;
 } 

