byte[] org.apache.rocketmq.proxy.remoting.common.RemotingConverter:convertMsgToBytes(org.apache.rocketmq.common.message.MessageExt msg)
{
java.lang.String $stack2;
  int $stack3;
  byte[] $stack4;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack5;
  msg.setStoreSize(0);
 $stack2 = msg.getTopic();
 $stack3 = $stack2.length();
 if ($stack3<=127) goto label0;
 $stack5=org.apache.rocketmq.proxy.remoting.common.RemotingConverter.log;
 $stack2 = msg.getTopic();
 $stack5.warn("Topic length is too long, topic: {}",(java.lang.Object)$stack2);
 label0:$stack4 = org.apache.rocketmq.common.message.MessageDecoder.encode(msg,(boolean)0);
 return  $stack4;
 } 

