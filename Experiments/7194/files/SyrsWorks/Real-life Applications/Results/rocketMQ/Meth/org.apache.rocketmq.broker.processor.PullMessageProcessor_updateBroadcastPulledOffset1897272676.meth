void org.apache.rocketmq.broker.processor.PullMessageProcessor:updateBroadcastPulledOffset(java.lang.String topic,java.lang.String group,int queueId,org.apache.rocketmq.remoting.protocol.header.PullMessageRequestHeader requestHeader,io.netty.channel.Channel channel,org.apache.rocketmq.remoting.protocol.RemotingCommand response,long nextBeginOffset)
{
boolean proxyPullBroadcast;
  org.apache.rocketmq.broker.client.ConsumerGroupInfo consumerGroupInfo;
  long offset;
  java.lang.String clientId;
  org.apache.rocketmq.broker.client.ClientChannelInfo clientChannelInfo;
  org.apache.rocketmq.broker.BrokerController $stack15;
  org.apache.rocketmq.common.BrokerConfig $stack16;
  org.apache.rocketmq.remoting.protocol.RequestSource $stack18;
  int $stack19;
  java.lang.Integer $stack20;
  java.lang.Integer $stack21;
  org.apache.rocketmq.broker.client.ConsumerManager $stack24;
  boolean $stack26;
  java.lang.Long $stack27;
  org.apache.rocketmq.broker.offset.BroadcastOffsetManager $stack34;
  if (response==null) goto label0;
 $stack15=this.brokerController;
 $stack16 = $stack15.getBrokerConfig();
 proxyPullBroadcast = $stack16.isEnableBroadcastOffsetStore();
 if (proxyPullBroadcast!=0) goto label1;
 label0:return;
 label1:$stack18=org.apache.rocketmq.remoting.protocol.RequestSource.PROXY_FOR_BROADCAST;
 $stack19 = $stack18.getValue();
 $stack20 = java.lang.Integer.valueOf($stack19);
 $stack21 = requestHeader.getRequestSource();
 proxyPullBroadcast = java.util.Objects.equals((java.lang.Object)$stack20,(java.lang.Object)$stack21);
 $stack15=this.brokerController;
 $stack24 = $stack15.getConsumerManager();
 consumerGroupInfo = $stack24.getConsumerGroupInfo(group);
 $stack26 = this#org.apache.rocketmq.broker.processor.PullMessageProcessor:isBroadcast(proxyPullBroadcast,consumerGroupInfo);
 if ($stack26==0) goto label2;
 $stack27 = requestHeader.getQueueOffset();
 offset = $stack27.longValue();
 $stack19 = response.getCode();
 if (21!=$stack19) goto label3;
 offset=nextBeginOffset;
 label3:if (proxyPullBroadcast==0) goto label4;
 clientId = requestHeader.getProxyFrowardClientId();
 goto label5;
 label4:clientChannelInfo = consumerGroupInfo.findChannel(channel);
 if (clientChannelInfo!=null) goto label6;
 return;
 label6:clientId = clientChannelInfo.getClientId();
 label5:$stack15=this.brokerController;
 $stack34 = $stack15.getBroadcastOffsetManager();
 $stack34#org.apache.rocketmq.broker.offset.BroadcastOffsetManager(topic,group,queueId,offset,clientId,proxyPullBroadcast);
 label2:return;
 } 

