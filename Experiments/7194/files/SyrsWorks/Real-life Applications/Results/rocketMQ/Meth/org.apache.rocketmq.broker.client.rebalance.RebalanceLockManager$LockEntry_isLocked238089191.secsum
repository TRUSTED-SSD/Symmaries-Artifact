boolean org.apache.rocketmq.broker.client.rebalance.RebalanceLockManager$LockEntry:isLocked (
  java.lang.String clientId) {
  guard = tt;
  ret(level()) = pc ⊔ level(this) ⊔ obj_level(clientId) ⊔ obj_level(this);
  ret(obj_level(clientId)) = if obj_level(clientId) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(clientId) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(clientId) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
}