boolean org.apache.rocketmq.store.CommitLog:resetOffset(long offset)
{
org.apache.rocketmq.store.MappedFileQueue $stack3;
  boolean $stack4;
  $stack3=this.mappedFileQueue;
 $stack4 = $stack3.resetOffset(offset);
 return  $stack4;
 } 

