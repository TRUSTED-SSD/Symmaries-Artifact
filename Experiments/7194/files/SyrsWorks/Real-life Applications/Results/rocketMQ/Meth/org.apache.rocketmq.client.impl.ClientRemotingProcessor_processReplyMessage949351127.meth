void org.apache.rocketmq.client.impl.ClientRemotingProcessor:processReplyMessage(org.apache.rocketmq.common.message.MessageExt replyMsg)
{
java.lang.String correlationId;
  org.apache.rocketmq.client.producer.RequestResponseFuture requestResponseFuture;
  java.lang.String bornHost;
  org.apache.rocketmq.client.producer.RequestFutureHolder $stack6;
  java.util.concurrent.ConcurrentHashMap $stack7;
  java.lang.Object $stack8;
  java.lang.Object[] $stack10;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack11;
  org.apache.rocketmq.client.producer.RequestCallback $stack16;
  correlationId = replyMsg.getUserProperty("CORRELATION_ID");
 $stack6 = org.apache.rocketmq.client.producer.RequestFutureHolder.getInstance();
 $stack7 = $stack6.getRequestFutureTable();
 $stack8 = $stack7.get((java.lang.Object)correlationId);
 requestResponseFuture=(org.apache.rocketmq.client.producer.RequestResponseFuture) $stack8;
 if (requestResponseFuture==null) goto label0;
 requestResponseFuture.putResponseMessage((org.apache.rocketmq.common.message.Message)replyMsg);
 $stack6 = org.apache.rocketmq.client.producer.RequestFutureHolder.getInstance();
 $stack7 = $stack6.getRequestFutureTable();
 $stack7.remove((java.lang.Object)correlationId);
 $stack16 = requestResponseFuture.getRequestCallback();
 if ($stack16==null) goto label1;
 $stack16 = requestResponseFuture.getRequestCallback();
 $stack16.onSuccess((org.apache.rocketmq.common.message.Message)replyMsg);
 goto label1;
 label0:bornHost = replyMsg.getBornHostString();
 $stack11=this.logger;
 $stack10=new java.lang.Object[2];
 $stack10[0]=correlationId;
 $stack10[1]=bornHost;
 correlationId = java.lang.String.format("receive reply message, but not matched any request, CorrelationId: %s , reply from host: %s",$stack10);
 $stack11.warn(correlationId);
 label1:return;
 } 

