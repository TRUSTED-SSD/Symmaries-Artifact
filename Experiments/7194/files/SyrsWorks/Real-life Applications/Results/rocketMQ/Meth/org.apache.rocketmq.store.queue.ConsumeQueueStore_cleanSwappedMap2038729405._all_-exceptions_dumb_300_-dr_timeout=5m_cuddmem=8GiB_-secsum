void org.apache.rocketmq.store.queue.ConsumeQueueStore:cleanSwappedMap (
  org.apache.rocketmq.store.queue.ConsumeQueueInterface consumeQueue,
  long forceCleanSwapIntervalMs) {
  guard = tt;
  ret(obj_level(consumeQueue)) = level(consumeQueue) ⊔ level(this) ⊔ obj_level(consumeQueue) ⊔ obj_level(this);
  ret(obj_level(this)) = level(consumeQueue) ⊔ level(this) ⊔ obj_level(consumeQueue) ⊔ obj_level(this);
}