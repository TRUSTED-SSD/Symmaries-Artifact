org.apache.rocketmq.store.dledger.DLedgerCommitLog$DLedgerSelectMappedBufferResult(io.openmessaging.storage.dledger.store.file.SelectMmapBufferResult sbr)
{
long $stack2;
  java.nio.ByteBuffer $stack3;
  int $stack4;
  $stack2 = sbr.getStartOffset();
 $stack3 = sbr.getByteBuffer();
 $stack4 = sbr.getSize();
 this#org.apache.rocketmq.store.SelectMappedBufferResult($stack2,$stack3,$stack4,null);
 this.sbr=sbr;
 return;
 } 

