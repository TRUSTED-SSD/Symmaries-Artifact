boolean org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:isEmpty(int pos)
{
java.nio.ByteBuffer $stack2;
  long $stack3;
  byte $stack4;
  int $stack5;
  boolean $stack9;
  $stack2=this.dataBytes;
 $stack3 = $stack2.getLong(pos);
 $stack4=($stack3 cmp 0L);
 if ($stack4!=0) goto label0;
 $stack2=this.dataBytes;
 $stack5=(pos+8);
 $stack3 = $stack2.getLong($stack5);
 $stack4=($stack3 cmp 0L);
 if ($stack4!=0) goto label0;
 $stack2=this.dataBytes;
 pos=(pos+16);
 $stack3 = $stack2.getLong(pos);
 $stack4=($stack3 cmp 0L);
 if ($stack4!=0) goto label0;
 $stack9=1;
 goto label1;
 label0:$stack9=0;
 label1:return  $stack9;
 } 

