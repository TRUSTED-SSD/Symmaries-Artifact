static org.apache.rocketmq.common.attribute.CleanupPolicy org.apache.rocketmq.common.utils.CleanupPolicyUtils:getDeletePolicy (
  java.util.Optional topicConfig) {
  guard = tt;
  ret(level()) = obj_level(topicConfig);
  ret(obj_level()) = obj_level(topicConfig);
  ret(obj_level(topicConfig)) = obj_level(topicConfig);
}