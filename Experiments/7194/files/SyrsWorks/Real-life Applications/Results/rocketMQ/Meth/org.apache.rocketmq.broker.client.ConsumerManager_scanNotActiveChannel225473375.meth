void org.apache.rocketmq.broker.client.ConsumerManager:scanNotActiveChannel()
{
java.util.Iterator it;
  java.util.Map$Entry next;
  java.lang.String group;
  org.apache.rocketmq.broker.client.ConsumerGroupInfo consumerGroupInfo;
  java.util.concurrent.ConcurrentMap channelInfoTable;
  java.util.Iterator itChannel;
  org.apache.rocketmq.broker.client.ClientChannelInfo clientChannelInfo;
  long diff;
  java.util.Set $stack12;
  boolean $stack14;
  java.lang.Object $stack15;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack24;
  long $stack28;
  byte $stack30;
  io.netty.channel.Channel $stack34;
  java.lang.String $stack35;
  java.lang.Object[] $stack36;
  org.apache.rocketmq.broker.client.ConsumerGroupEvent $stack37;
  channelInfoTable=this.consumerTable;
 $stack12 = channelInfoTable.entrySet();
 it = $stack12.iterator();
 label0:$stack14 = it.hasNext();
 if ($stack14==0) goto label1;
 $stack15 = it.next();
 next=(java.util.Map$Entry) $stack15;
 $stack15 = next.getKey();
 group=(java.lang.String) $stack15;
 $stack15 = next.getValue();
 consumerGroupInfo=(org.apache.rocketmq.broker.client.ConsumerGroupInfo) $stack15;
 channelInfoTable = consumerGroupInfo.getChannelInfoTable();
 $stack12 = channelInfoTable.entrySet();
 itChannel = $stack12.iterator();
 label2:$stack14 = itChannel.hasNext();
 if ($stack14==0) goto label3;
 $stack15 = itChannel.next();
 next=(java.util.Map$Entry) $stack15;
 $stack15 = next.getValue();
 clientChannelInfo=(org.apache.rocketmq.broker.client.ClientChannelInfo) $stack15;
 diff = java.lang.System.currentTimeMillis();
 $stack28 = clientChannelInfo.getLastUpdateTimestamp();
 diff=(diff-$stack28);
 $stack28=this.channelExpiredTimeout;
 $stack30=(diff cmp $stack28);
 if ($stack30<=0) goto label4;
 $stack24=org.apache.rocketmq.broker.client.ConsumerManager.LOGGER;
 $stack34 = clientChannelInfo.getChannel();
 $stack35 = org.apache.rocketmq.remoting.common.RemotingHelper.parseChannelRemoteAddr($stack34);
 $stack24.warn("SCAN: remove expired channel from ConsumerManager consumerTable. channel={}, consumerGroup={}",(java.lang.Object)$stack35,(java.lang.Object)group);
 $stack37=org.apache.rocketmq.broker.client.ConsumerGroupEvent.CLIENT_UNREGISTER;
 $stack36=new java.lang.Object[2];
 $stack36[0]=clientChannelInfo;
 $stack12 = consumerGroupInfo.getSubscribeTopics();
 $stack36[1]=$stack12;
 this.callConsumerIdsChangeListener($stack37,group,$stack36);
 $stack34 = clientChannelInfo.getChannel();
 org.apache.rocketmq.remoting.common.RemotingHelper.closeChannel($stack34);
 itChannel.remove();
 label4:goto label2;
 label3:$stack14 = channelInfoTable.isEmpty();
 if ($stack14==0) goto label5;
 $stack24=org.apache.rocketmq.broker.client.ConsumerManager.LOGGER;
 $stack24.warn("SCAN: remove expired channel from ConsumerManager consumerTable, all clear, consumerGroup={}",(java.lang.Object)group);
 it.remove();
 label5:goto label0;
 label1:this#org.apache.rocketmq.broker.client.ConsumerManager();
 return;
 } 

