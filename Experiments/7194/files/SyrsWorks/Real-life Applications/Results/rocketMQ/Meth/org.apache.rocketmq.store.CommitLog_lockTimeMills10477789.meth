long org.apache.rocketmq.store.CommitLog:lockTimeMills()
{
long diff;
  long begin;
  byte $stack5;
  org.apache.rocketmq.store.DefaultMessageStore $stack8;
  diff=0L;
 begin=this.beginTimeInLock;
 $stack5=(begin cmp 0L);
 if ($stack5<=0) goto label0;
 $stack8=this.defaultMessageStore;
 diff = $stack8.now();
 diff=(diff-begin);
 label0:$stack5=(diff cmp 0L);
 if ($stack5>=0) goto label1;
 diff=0L;
 label1:return  diff;
 } 

