static java.io.BufferedReader org.apache.rocketmq.common.utils.IOTinyUtils:toBufferedReader (
  java.io.Reader reader) {
  guard = tt;
  ret(level()) = level(reader);
  ret(obj_level()) = level(reader) ⊔ obj_level(reader);
  ret(obj_level(reader)) = level(reader) ⊔ obj_level(reader);
}