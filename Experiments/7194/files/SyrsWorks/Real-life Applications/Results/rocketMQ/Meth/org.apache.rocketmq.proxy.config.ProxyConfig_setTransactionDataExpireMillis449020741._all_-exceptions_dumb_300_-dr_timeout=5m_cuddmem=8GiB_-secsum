void org.apache.rocketmq.proxy.config.ProxyConfig:setTransactionDataExpireMillis (
  long transactionDataExpireMillis) {
  guard = tt;
  ret(obj_level(this)) = level(transactionDataExpireMillis) ⊔ obj_level(this);
}