org.apache.rocketmq.store.SelectMappedBufferResult org.apache.rocketmq.store.dledger.DLedgerCommitLog:getData(long offset)
{
long $stack3;
  byte $stack4;
  org.apache.rocketmq.store.SelectMappedBufferResult $stack6;
  boolean $stack8;
  $stack3=this.dividedCommitlogOffset;
 $stack4=(offset cmp $stack3);
 if ($stack4>=0) goto label0;
 $stack6 = this#org.apache.rocketmq.store.CommitLog:getData(offset);
 return  $stack6;
 label0:$stack4=(offset cmp 0L);
 if ($stack4!=0) goto label1;
 $stack8=1;
 goto label2;
 label1:$stack8=0;
 label2:$stack6 = this.getData(offset,$stack8);
 return  $stack6;
 } 

