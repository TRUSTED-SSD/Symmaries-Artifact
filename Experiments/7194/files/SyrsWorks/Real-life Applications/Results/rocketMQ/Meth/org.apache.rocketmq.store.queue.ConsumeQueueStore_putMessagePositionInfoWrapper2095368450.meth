void org.apache.rocketmq.store.queue.ConsumeQueueStore:putMessagePositionInfoWrapper(org.apache.rocketmq.store.DispatchRequest dispatchRequest)
{
org.apache.rocketmq.store.queue.ConsumeQueueInterface cq;
  java.lang.String $stack3;
  int $stack4;
  $stack3 = dispatchRequest.getTopic();
 $stack4 = dispatchRequest.getQueueId();
 cq = this.findOrCreateConsumeQueue($stack3,$stack4);
 this#org.apache.rocketmq.store.queue.ConsumeQueueStore(cq,dispatchRequest);
 return;
 } 

