static java.lang.String org.apache.rocketmq.acl.plain.PlainAccessResource:getGroupFromRetryTopic(java.lang.String @retryTopic)
{
java.lang.String $stack1;
  int $stack2;
  java.lang.String retryTopic;
retryTopic=@retryTopic;
if (retryTopic!=null) goto label0;
 return  null;
 label0:$stack1="%RETRY%";
 $stack2 = $stack1.length();
 retryTopic = retryTopic.substring($stack2);
 return  retryTopic;
 } 

