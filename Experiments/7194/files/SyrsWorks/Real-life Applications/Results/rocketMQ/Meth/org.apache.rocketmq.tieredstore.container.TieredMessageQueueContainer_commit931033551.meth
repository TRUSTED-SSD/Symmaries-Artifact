void org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:commit(boolean sync)
{
org.apache.rocketmq.tieredstore.container.TieredCommitLog $stack2;
  org.apache.rocketmq.tieredstore.container.TieredConsumeQueue $stack3;
  $stack2=this.commitLog;
 $stack2.commit(sync);
 $stack3=this.consumeQueue;
 $stack3.commit(sync);
 return;
 } 

