void org.apache.rocketmq.store.queue.BatchConsumeQueue:cleanSwappedMap(long forceCleanSwapIntervalMs)
{
org.apache.rocketmq.store.MappedFileQueue $stack3;
  $stack3=this.mappedFileQueue;
 $stack3.cleanSwappedMap(forceCleanSwapIntervalMs);
 return;
 } 

