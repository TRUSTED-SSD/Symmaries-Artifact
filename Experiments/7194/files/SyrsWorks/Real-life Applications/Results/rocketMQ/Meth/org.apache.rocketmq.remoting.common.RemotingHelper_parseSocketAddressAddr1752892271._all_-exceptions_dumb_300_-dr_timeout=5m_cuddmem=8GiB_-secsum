static java.lang.String org.apache.rocketmq.remoting.common.RemotingHelper:parseSocketAddressAddr (
  java.net.SocketAddress socketAddress) {
  guard = tt;
  ret(level()) = obj_level(socketAddress);
  ret(obj_level()) = obj_level(socketAddress);
  ret(obj_level(socketAddress)) = obj_level(socketAddress);
}