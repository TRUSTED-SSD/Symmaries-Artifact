int org.apache.rocketmq.tieredstore.container.TieredFileQueue:getSegmentIndexByOffset(long offset)
{
int left;
  int right;
  int mid;
  long firstSegmentOffset;
  long lastSegmentOffset;
  long midSegmentOffset;
  java.lang.Throwable l13;
  java.util.concurrent.locks.ReentrantReadWriteLock $stack14;
  java.util.concurrent.locks.ReentrantReadWriteLock$ReadLock $stack15;
  java.util.List $stack16;
  java.lang.Object $stack22;
  org.apache.rocketmq.tieredstore.provider.TieredFileSegment $stack23;
  byte $stack33;
  int $stack37;
  $stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.lock();
 label0:$stack16=this.fileSegmentList;
 left = $stack16.size();
 if (left>0) goto label1;
 left=-1;
 label2:$stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.unlock();
 return  -1;
 label1:left=0;
 $stack16=this.fileSegmentList;
 right = $stack16.size();
 right=(right-1);
 mid=(0+right);
 mid=(mid/2);
 $stack16=this.fileSegmentList;
 $stack22 = $stack16.get(0);
 $stack23=(org.apache.rocketmq.tieredstore.provider.TieredFileSegment) $stack22;
 firstSegmentOffset = $stack23.getBaseOffset();
 $stack16=this.fileSegmentList;
 $stack22 = $stack16.get(right);
 $stack23=(org.apache.rocketmq.tieredstore.provider.TieredFileSegment) $stack22;
 lastSegmentOffset = $stack23.getCommitOffset();
 $stack16=this.fileSegmentList;
 $stack22 = $stack16.get(mid);
 $stack23=(org.apache.rocketmq.tieredstore.provider.TieredFileSegment) $stack22;
 midSegmentOffset = $stack23.getBaseOffset();
 $stack33=(offset cmp firstSegmentOffset);
 if ($stack33<0) goto label3;
 $stack33=(offset cmp lastSegmentOffset);
 if ($stack33<=0) goto label4;
 label3:left=-1;
 label5:$stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.unlock();
 return  -1;
 label4:$stack37=(right-1);
 if (left>=$stack37) goto label6;
 $stack33=(offset cmp midSegmentOffset);
 if ($stack33!=0) goto label7;
 left=mid;
 label8:$stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.unlock();
 return  mid;
 label7:$stack33=(offset cmp midSegmentOffset);
 if ($stack33>=0) goto label9;
 right=mid;
 goto label10;
 label9:left=mid;
 label10:mid=(left+right);
 mid=(mid/2);
 $stack16=this.fileSegmentList;
 $stack22 = $stack16.get(mid);
 $stack23=(org.apache.rocketmq.tieredstore.provider.TieredFileSegment) $stack22;
 midSegmentOffset = $stack23.getBaseOffset();
 goto label4;
 label6:$stack16=this.fileSegmentList;
 $stack22 = $stack16.get(right);
 $stack23=(org.apache.rocketmq.tieredstore.provider.TieredFileSegment) $stack22;
 firstSegmentOffset = $stack23.getBaseOffset();
 $stack33=(offset cmp firstSegmentOffset);
 if ($stack33>=0) goto label11;
 left=mid;
 goto label12;
 label11:left=right;
 label12:$stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.unlock();
 return  left;
 label13:l13 = catch;
 label14:$stack14=this.fileSegmentLock;
 $stack15 = $stack14.readLock();
 $stack15.unlock();
 throw l13;
 catch java.lang.Throwable: label0 - label2 : label13;
catch java.lang.Throwable: label1 - label5 : label13;
catch java.lang.Throwable: label4 - label8 : label13;
catch java.lang.Throwable: label7 - label12 : label13;
catch java.lang.Throwable: label13 - label14 : label13;
} 

