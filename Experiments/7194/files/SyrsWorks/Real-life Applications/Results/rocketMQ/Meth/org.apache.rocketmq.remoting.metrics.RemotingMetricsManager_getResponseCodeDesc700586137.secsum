static java.lang.String org.apache.rocketmq.remoting.metrics.RemotingMetricsManager:getResponseCodeDesc (
  int code) {
  guard = tt;
  ret(level()) = pc ⊔ level(code);
  ret(obj_level()) = pc ⊔ level(code);
}