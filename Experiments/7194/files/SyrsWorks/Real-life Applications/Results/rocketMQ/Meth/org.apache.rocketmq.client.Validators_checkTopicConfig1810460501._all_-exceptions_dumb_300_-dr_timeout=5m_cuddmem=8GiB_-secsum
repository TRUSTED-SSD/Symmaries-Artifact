static void org.apache.rocketmq.client.Validators:checkTopicConfig (org.apache.rocketmq.common.TopicConfig
                                                                    topicConfig) {
  guard = tt;
  ret(obj_level(topicConfig)) = level(topicConfig) ⊔ obj_level(topicConfig);
}