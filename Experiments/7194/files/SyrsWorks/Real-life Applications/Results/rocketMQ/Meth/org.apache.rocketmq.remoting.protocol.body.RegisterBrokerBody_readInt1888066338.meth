static int org.apache.rocketmq.remoting.protocol.body.RegisterBrokerBody:readInt(java.util.zip.InflaterInputStream inflaterInputStream)
{
byte[] buffer;
  java.nio.ByteBuffer byteBuffer;
  int $stack5;
  buffer = org.apache.rocketmq.remoting.protocol.body.RegisterBrokerBody.readBytes(inflaterInputStream,4);
 byteBuffer = java.nio.ByteBuffer.wrap(buffer);
 $stack5 = byteBuffer.getInt();
 return  $stack5;
 } 

