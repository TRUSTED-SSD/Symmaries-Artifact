long org.apache.rocketmq.store.timer.TimerMessageStore:formatTimeMs(long timeMs)
{
int $stack3;
  long $stack4;
  $stack3=this.precisionMs;
 $stack4=(long) $stack3;
 $stack4=(timeMs/$stack4);
 $stack3=this.precisionMs;
 timeMs=(long) $stack3;
 timeMs=($stack4 * timeMs);
 return  timeMs;
 } 

