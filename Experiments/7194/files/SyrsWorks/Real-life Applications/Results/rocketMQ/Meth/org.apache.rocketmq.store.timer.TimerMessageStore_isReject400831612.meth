boolean org.apache.rocketmq.store.timer.TimerMessageStore:isReject(long deliverTimeMs)
{
org.apache.rocketmq.store.timer.TimerWheel $stack5;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack7;
  int $stack8;
  long $stack9;
  byte $stack10;
  java.util.Random $stack16;
  double $stack19;
  double $stack26;
  double $stack28;
  $stack5=this.timerWheel;
 deliverTimeMs = $stack5.getNum(deliverTimeMs);
 $stack7=this.storeConfig;
 $stack8 = $stack7.getTimerCongestNumEachSlot();
 $stack9=(long) $stack8;
 $stack10=(deliverTimeMs cmp $stack9);
 if ($stack10>0) goto label0;
 return  0;
 label0:$stack7=this.storeConfig;
 $stack8 = $stack7.getTimerCongestNumEachSlot();
 $stack9=(long) $stack8;
 $stack9=($stack9 * 2L);
 $stack10=(deliverTimeMs cmp $stack9);
 if ($stack10<0) goto label1;
 return  1;
 label1:$stack16=org.apache.rocketmq.store.timer.TimerMessageStore.RANDOM;
 $stack8 = $stack16.nextInt(1000);
 $stack19=(double) $stack8;
 $stack7=this.storeConfig;
 $stack8 = $stack7.getTimerCongestNumEachSlot();
 $stack9=(long) $stack8;
 deliverTimeMs=(deliverTimeMs-$stack9);
 deliverTimeMs=(1000L * deliverTimeMs);
 $stack26=(double) deliverTimeMs;
 $stack7=this.storeConfig;
 $stack8 = $stack7.getTimerCongestNumEachSlot();
 $stack28=(double) $stack8;
 $stack28=($stack28+0.1);
 $stack26=($stack26/$stack28);
 $stack10=($stack19 cmpl $stack26);
 if ($stack10<=0) goto label2;
 return  1;
 label2:return  0;
 } 

