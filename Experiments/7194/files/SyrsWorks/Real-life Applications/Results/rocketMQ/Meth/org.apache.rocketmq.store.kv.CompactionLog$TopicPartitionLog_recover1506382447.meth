void org.apache.rocketmq.store.kv.CompactionLog$TopicPartitionLog:recover()
{
long maxCqPhysicOffset;
  org.apache.rocketmq.store.queue.SparseConsumeQueue $stack3;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack5;
  java.lang.Object[] $stack6;
  java.lang.String $stack10;
  int $stack13;
  java.lang.Integer $stack14;
  java.lang.Long $stack16;
  byte $stack17;
  org.apache.rocketmq.store.MappedFileQueue $stack18;
  $stack3=this.consumeQueue;
 maxCqPhysicOffset = $stack3.getMaxPhyOffsetInLog();
 $stack5 = org.apache.rocketmq.store.kv.CompactionLog.access$100();
 $stack6=new java.lang.Object[3];
 $stack3=this.consumeQueue;
 $stack10 = $stack3.getTopic();
 $stack6[0]=$stack10;
 $stack3=this.consumeQueue;
 $stack13 = $stack3.getQueueId();
 $stack14 = java.lang.Integer.valueOf($stack13);
 $stack6[1]=$stack14;
 $stack16 = java.lang.Long.valueOf(maxCqPhysicOffset);
 $stack6[2]=$stack16;
 $stack5.info("{}:{} max physical offset in compaction log is {}",$stack6);
 $stack17=(maxCqPhysicOffset cmp 0L);
 if ($stack17<=0) goto label0;
 $stack18=this.mappedFileQueue;
 $stack18.setFlushedWhere(maxCqPhysicOffset);
 $stack18=this.mappedFileQueue;
 $stack18.setCommittedWhere(maxCqPhysicOffset);
 $stack18=this.mappedFileQueue;
 $stack18.truncateDirtyFiles(maxCqPhysicOffset);
 label0:return;
 } 

