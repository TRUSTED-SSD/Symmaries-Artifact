static boolean org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:lambda$getActiveGroupCount$2 (
  long minOffset, long maxOffset, java.lang.Long offset) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(offset)) = level(offset) ⊔ obj_level(offset);
}