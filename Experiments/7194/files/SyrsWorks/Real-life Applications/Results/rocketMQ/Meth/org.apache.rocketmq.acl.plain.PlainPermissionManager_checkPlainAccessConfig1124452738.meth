void org.apache.rocketmq.acl.plain.PlainPermissionManager:checkPlainAccessConfig(org.apache.rocketmq.common.PlainAccessConfig plainAccessConfig)
{
java.lang.String $stack2;
  org.apache.rocketmq.acl.common.AclException $stack3;
  java.lang.Object[] $stack4;
  int $stack16;
  $stack2 = plainAccessConfig.getAccessKey();
 if ($stack2==null) goto label0;
 $stack2 = plainAccessConfig.getSecretKey();
 if ($stack2==null) goto label0;
 $stack2 = plainAccessConfig.getAccessKey();
 $stack16 = $stack2.length();
 if ($stack16<=6) goto label0;
 $stack2 = plainAccessConfig.getSecretKey();
 $stack16 = $stack2.length();
 if ($stack16>6) goto label1;
 label0:$stack3=new org.apache.rocketmq.acl.common.AclException;
 $stack4=new java.lang.Object[2];
 $stack2 = plainAccessConfig.getAccessKey();
 $stack4[0]=$stack2;
 $stack2 = plainAccessConfig.getSecretKey();
 $stack4[1]=$stack2;
 $stack2 = java.lang.String.format("The accessKey=%s and secretKey=%s cannot be null and length should longer than 6",$stack4);
 $stack3#org.apache.rocketmq.acl.common.AclException($stack2);
 throw $stack3;
 label1:return;
 } 

