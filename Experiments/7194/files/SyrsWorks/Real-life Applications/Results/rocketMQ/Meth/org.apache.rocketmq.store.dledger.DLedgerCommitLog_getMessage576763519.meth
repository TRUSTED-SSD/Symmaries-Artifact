org.apache.rocketmq.store.SelectMappedBufferResult org.apache.rocketmq.store.dledger.DLedgerCommitLog:getMessage(long offset,int size)
{
int mappedFileSize;
  io.openmessaging.storage.dledger.store.file.MmapFile mappedFile;
  long $stack7;
  byte $stack8;
  io.openmessaging.storage.dledger.DLedgerServer $stack9;
  io.openmessaging.storage.dledger.DLedgerConfig $stack10;
  io.openmessaging.storage.dledger.store.file.MmapFileList $stack13;
  io.openmessaging.storage.dledger.store.file.SelectMmapBufferResult $stack17;
  org.apache.rocketmq.store.SelectMappedBufferResult $stack18;
  boolean $stack20;
  $stack7=this.dividedCommitlogOffset;
 $stack8=(offset cmp $stack7);
 if ($stack8>=0) goto label0;
 $stack18 = this#org.apache.rocketmq.store.CommitLog:getMessage(offset,size);
 return  $stack18;
 label0:$stack9=this.dLedgerServer;
 $stack10 = $stack9.getdLedgerConfig();
 mappedFileSize = $stack10.getMappedFileSizeForEntryData();
 $stack13=this.dLedgerFileList;
 $stack7=offset;
 $stack8=(offset cmp 0L);
 if ($stack8!=0) goto label1;
 $stack20=1;
 goto label2;
 label1:$stack20=0;
 label2:mappedFile = $stack13.findMappedFileByOffset($stack7,$stack20);
 if (mappedFile==null) goto label3;
 $stack7=(long) mappedFileSize;
 offset=(offset%$stack7);
 mappedFileSize=(int) offset;
 $stack17 = mappedFile.selectMappedBuffer(mappedFileSize,size);
 $stack18 = this.convertSbr($stack17);
 return  $stack18;
 label3:return  null;
 } 

