void org.apache.rocketmq.filter.util.BitsArray:not(int bitPos)
{
boolean $stack2;
  this.checkBitPosition(bitPos,this);
 $stack2 = this.getBit(bitPos);
 if ($stack2!=0) goto label0;
 $stack2=1;
 goto label1;
 label0:$stack2=0;
 label1:this.setBit(bitPos,$stack2);
 return;
 } 

