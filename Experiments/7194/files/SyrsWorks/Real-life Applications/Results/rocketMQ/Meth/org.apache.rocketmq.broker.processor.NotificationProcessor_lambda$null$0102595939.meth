static void org.apache.rocketmq.broker.processor.NotificationProcessor:lambda$null$0(org.apache.rocketmq.broker.longpolling.NotificationRequest request,org.apache.rocketmq.remoting.protocol.RemotingCommand response,io.netty.util.concurrent.Future future)
{
boolean $stack3;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack4;
  io.netty.channel.Channel $stack6;
  java.net.SocketAddress $stack7;
  java.lang.Throwable $stack8;
  java.lang.String $stack10;
  $stack3 = future.isSuccess();
 if ($stack3!=0) goto label0;
 $stack4=org.apache.rocketmq.broker.processor.NotificationProcessor.POP_LOGGER;
 $stack6 = request.getChannel();
 $stack7 = $stack6.remoteAddress();
 $stack8 = future.cause();
 $stack4.error("ProcessRequestWrapper response to {} failed",(java.lang.Object)$stack7,(java.lang.Object)$stack8);
 $stack4=org.apache.rocketmq.broker.processor.NotificationProcessor.POP_LOGGER;
 $stack10 = request.toString();
 $stack4.error($stack10);
 $stack4=org.apache.rocketmq.broker.processor.NotificationProcessor.POP_LOGGER;
 $stack10 = response.toString();
 $stack4.error($stack10);
 label0:return;
 } 

