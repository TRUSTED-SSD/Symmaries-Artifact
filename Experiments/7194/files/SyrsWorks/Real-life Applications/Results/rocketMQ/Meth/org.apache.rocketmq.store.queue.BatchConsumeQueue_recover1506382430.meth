void org.apache.rocketmq.store.queue.BatchConsumeQueue:recover()
{
java.util.List mappedFiles;
  int index;
  int mappedFileSizeLogics;
  org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  java.nio.ByteBuffer byteBuffer;
  long processOffset;
  long mappedFileOffset;
  int i;
  long offset;
  int size;
  long msgBaseOffset;
  short batchSize;
  org.apache.rocketmq.store.MappedFileQueue $stack17;
  boolean $stack19;
  java.lang.Object $stack21;
  byte $stack25;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack26;
  java.lang.String $stack28;
  java.lang.Long $stack30;
  java.lang.StringBuilder $stack38;
  java.lang.Object[] $stack68;
  java.lang.Integer $stack76;
  java.lang.Short $stack80;
  $stack17=this.mappedFileQueue;
 mappedFiles = $stack17.getMappedFiles();
 $stack19 = mappedFiles.isEmpty();
 if ($stack19!=0) goto label0;
 index = mappedFiles.size();
 index=(index-3);
 if (index>=0) goto label1;
 index=0;
 label1:mappedFileSizeLogics=this.mappedFileSize;
 $stack21 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack21;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 label2:i=0;
 label3:if (i>=mappedFileSizeLogics) goto label4;
 byteBuffer.position(i);
 offset = byteBuffer.getLong();
 size = byteBuffer.getInt();
 byteBuffer.getLong();
 byteBuffer.getLong();
 msgBaseOffset = byteBuffer.getLong();
 batchSize = byteBuffer.getShort();
 $stack25=(offset cmp 0L);
 if ($stack25<0) goto label5;
 if (size<=0) goto label5;
 $stack25=(msgBaseOffset cmp 0L);
 if ($stack25<0) goto label5;
 if (batchSize<=0) goto label5;
 size=(i+46);
 mappedFileOffset=(long) size;
 this.maxMsgPhyOffsetInCommitLog=offset;
 goto label6;
 label5:$stack26=org.apache.rocketmq.store.queue.BatchConsumeQueue.log;
 $stack68=new java.lang.Object[6];
 $stack28 = mappedFile.getFileName();
 $stack68[0]=$stack28;
 $stack30 = java.lang.Long.valueOf(offset);
 $stack68[1]=$stack30;
 $stack76 = java.lang.Integer.valueOf(size);
 $stack68[2]=$stack76;
 $stack30 = java.lang.Long.valueOf(msgBaseOffset);
 $stack68[3]=$stack30;
 $stack80 = java.lang.Short.valueOf(batchSize);
 $stack68[4]=$stack80;
 $stack30 = java.lang.Long.valueOf(mappedFileOffset);
 $stack68[5]=$stack30;
 $stack26.info("Recover current batch consume queue file over, file:{} offset:{} size:{} msgBaseOffset:{} batchSize:{} mappedFileOffset:{}",$stack68);
 goto label4;
 label6:i=(i+46);
 goto label3;
 label4:offset=(long) mappedFileSizeLogics;
 $stack25=(mappedFileOffset cmp offset);
 if ($stack25!=0) goto label7;
 index=(index+1);
 i = mappedFiles.size();
 if (index<i) goto label8;
 $stack26=org.apache.rocketmq.store.queue.BatchConsumeQueue.log;
 $stack28 = mappedFile.getFileName();
 $stack26.info("Recover last batch consume queue file over, last mapped file:{} ",(java.lang.Object)$stack28);
 goto label9;
 label8:$stack21 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack21;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 $stack26=org.apache.rocketmq.store.queue.BatchConsumeQueue.log;
 $stack38=new java.lang.StringBuilder;
 $stack38#java.lang.StringBuilder();
 $stack38 = $stack38.append("Recover next batch consume queue file: ");
 $stack28 = mappedFile.getFileName();
 $stack38 = $stack38.append($stack28);
 $stack28 = $stack38.toString();
 $stack26.info($stack28);
 goto label2;
 label7:$stack26=org.apache.rocketmq.store.queue.BatchConsumeQueue.log;
 $stack28 = mappedFile.getFileName();
 offset=(processOffset+mappedFileOffset);
 $stack30 = java.lang.Long.valueOf(offset);
 $stack26.info("Recover current batch consume queue file over:{} processOffset:{}",(java.lang.Object)$stack28,(java.lang.Object)$stack30);
 goto label9;
 label9:processOffset=(processOffset+mappedFileOffset);
 $stack17=this.mappedFileQueue;
 $stack17.setFlushedWhere(processOffset);
 $stack17=this.mappedFileQueue;
 $stack17.setCommittedWhere(processOffset);
 $stack17=this.mappedFileQueue;
 $stack17.truncateDirtyFiles(processOffset);
 this.reviseMaxAndMinOffsetInQueue();
 label0:return;
 } 

