static java.lang.String org.apache.rocketmq.common.utils.NetworkUtil:socketAddress2String (
  java.net.SocketAddress addr) {
  guard = tt;
  ret(level()) = obj_level(addr);
  ret(obj_level()) = obj_level(addr);
  ret(obj_level(addr)) = obj_level(addr);
}