static byte[] org.apache.rocketmq.remoting.protocol.body.RegisterBrokerBody:convertIntToByteArray(int n)
{
java.nio.ByteBuffer byteBuffer;
  byte[] $stack4;
  byteBuffer = java.nio.ByteBuffer.allocate(4);
 byteBuffer.putInt(n);
 $stack4 = byteBuffer.array();
 return  $stack4;
 } 

