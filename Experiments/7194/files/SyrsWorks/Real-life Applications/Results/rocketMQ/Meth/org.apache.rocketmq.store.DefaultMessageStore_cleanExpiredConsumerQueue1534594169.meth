void org.apache.rocketmq.store.DefaultMessageStore:cleanExpiredConsumerQueue()
{
long minCommitLogOffset;
  org.apache.rocketmq.store.CommitLog $stack3;
  org.apache.rocketmq.store.queue.ConsumeQueueStore $stack5;
  $stack3=this.commitLog;
 minCommitLogOffset = $stack3.getMinOffset();
 $stack5=this.consumeQueueStore;
 $stack5.cleanExpired(minCommitLogOffset);
 return;
 } 

