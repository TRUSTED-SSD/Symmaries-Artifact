void org.apache.rocketmq.store.queue.ConsumeQueueStore:assignQueueOffset(org.apache.rocketmq.common.message.MessageExtBrokerInner msg,short messageNum)
{
org.apache.rocketmq.store.queue.ConsumeQueueInterface consumeQueue;
  java.lang.String $stack4;
  int $stack5;
  org.apache.rocketmq.store.queue.QueueOffsetAssigner $stack7;
  $stack4 = msg.getTopic();
 $stack5 = msg.getQueueId();
 consumeQueue = this.findOrCreateConsumeQueue($stack4,$stack5);
 $stack7=this.queueOffsetAssigner;
 consumeQueue.assignQueueOffset($stack7,msg,messageNum);
 return;
 } 

