static boolean org.apache.rocketmq.proxy.service.route.TopicRouteHelper:isTopicNotExistError (
  java.lang.Throwable @e) {
  guard = tt;
  ret(level()) = pc ⊔ level(@e) ⊔ obj_level(@e);
  ret(obj_level(@e)) = if obj_level(@e) = ⊤ ∨ pc ≠ ⊤ ∧ level(@e) = ⊤ ∧ obj_level(@e) = ⊥ then ⊤ else ⊥;
}