static void org.apache.rocketmq.common.message.MessageDecoder:decodeMessage(org.apache.rocketmq.common.message.MessageExt messageExt,java.util.List list)
{
java.util.List messages;
  int i;
  org.apache.rocketmq.common.message.Message message;
  org.apache.rocketmq.common.message.MessageClientExt messageClientExt;
  byte[] $stack6;
  java.nio.ByteBuffer $stack7;
  int $stack9;
  java.lang.Object $stack10;
  java.lang.String $stack12;
  long $stack13;
  long $stack14;
  java.util.Map $stack18;
  java.net.SocketAddress $stack20;
  boolean $stack26;
  $stack6 = messageExt.getBody();
 $stack7 = java.nio.ByteBuffer.wrap($stack6);
 messages = org.apache.rocketmq.common.message.MessageDecoder.decodeMessages($stack7);
 i=0;
 label0:$stack9 = messages.size();
 if (i>=$stack9) goto label1;
 $stack10 = messages.get(i);
 message=(org.apache.rocketmq.common.message.Message) $stack10;
 messageClientExt=new org.apache.rocketmq.common.message.MessageClientExt;
 messageClientExt#org.apache.rocketmq.common.message.MessageClientExt();
 $stack12 = messageExt.getTopic();
 messageClientExt.setTopic($stack12);
 $stack13 = messageExt.getQueueOffset();
 $stack14=(long) i;
 $stack13=($stack13+$stack14);
 messageClientExt.setQueueOffset($stack13);
 $stack9 = messageExt.getQueueId();
 messageClientExt.setQueueId($stack9);
 $stack9 = message.getFlag();
 messageClientExt.setFlag($stack9);
 $stack18 = message.getProperties();
 org.apache.rocketmq.common.message.MessageAccessor.setProperties((org.apache.rocketmq.common.message.Message)messageClientExt,$stack18);
 $stack6 = message.getBody();
 messageClientExt.setBody($stack6);
 $stack20 = messageExt.getStoreHost();
 messageClientExt.setStoreHost($stack20);
 $stack20 = messageExt.getBornHost();
 messageClientExt.setBornHost($stack20);
 $stack13 = messageExt.getBornTimestamp();
 messageClientExt.setBornTimestamp($stack13);
 $stack13 = messageExt.getStoreTimestamp();
 messageClientExt.setStoreTimestamp($stack13);
 $stack9 = messageExt.getSysFlag();
 messageClientExt.setSysFlag($stack9);
 $stack13 = messageExt.getCommitLogOffset();
 messageClientExt.setCommitLogOffset($stack13);
 $stack26 = messageExt.isWaitStoreMsgOK();
 messageClientExt.setWaitStoreMsgOK($stack26);
 list.add((java.lang.Object)messageClientExt);
 i=(i+1);
 goto label0;
 label1:return;
 } 

