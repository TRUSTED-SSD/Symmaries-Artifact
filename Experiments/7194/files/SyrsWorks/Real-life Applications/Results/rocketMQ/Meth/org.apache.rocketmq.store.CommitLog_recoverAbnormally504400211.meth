void org.apache.rocketmq.store.CommitLog:recoverAbnormally(long maxPhyOffsetOfConsumeQueue)
{
boolean checkCRCOnRecover;
  boolean checkDupInfo;
  java.util.List mappedFiles;
  int index;
  org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  java.nio.ByteBuffer byteBuffer;
  long processOffset;
  long mappedFileOffset;
  long lastValidMsgPhyOffset;
  boolean doDispatch;
  org.apache.rocketmq.store.DispatchRequest dispatchRequest;
  int size;
  org.apache.rocketmq.store.DefaultMessageStore $stack18;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack19;
  org.apache.rocketmq.store.MappedFileQueue $stack24;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack27;
  java.lang.StringBuilder $stack38;
  java.lang.String $stack41;
  byte $stack50;
  java.lang.Long $stack53;
  java.lang.Long $stack54;
  long $stack56;
  org.apache.rocketmq.store.MessageStore $stack70;
  java.lang.Object $stack72;
  long $stack107;
  $stack18=this.defaultMessageStore;
 $stack19 = $stack18.getMessageStoreConfig();
 checkCRCOnRecover = $stack19.isCheckCRCOnRecover();
 $stack18=this.defaultMessageStore;
 $stack19 = $stack18.getMessageStoreConfig();
 checkDupInfo = $stack19.isDuplicationEnable();
 $stack24=this.mappedFileQueue;
 mappedFiles = $stack24.getMappedFiles();
 doDispatch = mappedFiles.isEmpty();
 if (doDispatch!=0) goto label0;
 index = mappedFiles.size();
 index=(index-1);
 mappedFile=null;
 label1:if (index<0) goto label2;
 $stack72 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack72;
 doDispatch = this#org.apache.rocketmq.store.CommitLog:isMappedFileMatchedRecover(mappedFile);
 if (doDispatch==0) goto label3;
 $stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack38=new java.lang.StringBuilder;
 $stack38#java.lang.StringBuilder();
 $stack38 = $stack38.append("recover from this mapped file ");
 $stack41 = mappedFile.getFileName();
 $stack38 = $stack38.append($stack41);
 $stack41 = $stack38.toString();
 $stack27.info($stack41);
 goto label2;
 label3:index=(index+-1);
 goto label1;
 label2:if (index>=0) goto label4;
 index=0;
 $stack72 = mappedFiles.get(0);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack72;
 label4:byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 lastValidMsgPhyOffset = this.getConfirmOffset();
 doDispatch=1;
 label5:dispatchRequest = this.checkMessageAndReturnSize(byteBuffer,checkCRCOnRecover,checkDupInfo);
 size = dispatchRequest.getMsgSize();
 doDispatch = dispatchRequest.isSuccess();
 if (doDispatch==0) goto label6;
 if (size<=0) goto label7;
 lastValidMsgPhyOffset=(processOffset+mappedFileOffset);
 $stack56=(long) size;
 mappedFileOffset=(mappedFileOffset+$stack56);
 $stack18=this.defaultMessageStore;
 $stack19 = $stack18.getMessageStoreConfig();
 doDispatch = $stack19.isDuplicationEnable();
 if (doDispatch==0) goto label8;
 $stack56 = dispatchRequest.getCommitLogOffset();
 $stack18=this.defaultMessageStore;
 $stack107 = $stack18.getConfirmOffset();
 $stack50=($stack56 cmp $stack107);
 if ($stack50>=0) goto label9;
 $stack70 = this.getMessageStore();
 $stack70.onCommitLogDispatch(dispatchRequest,(boolean)1,mappedFile,(boolean)1,(boolean)0);
 goto label9;
 label8:$stack70 = this.getMessageStore();
 $stack70.onCommitLogDispatch(dispatchRequest,(boolean)1,mappedFile,(boolean)1,(boolean)0);
 goto label9;
 label7:if (size!=0) goto label9;
 $stack70 = this.getMessageStore();
 $stack70.onCommitLogDispatch(dispatchRequest,(boolean)1,mappedFile,(boolean)1,(boolean)1);
 index=(index+1);
 size = mappedFiles.size();
 if (index<size) goto label10;
 $stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack38=new java.lang.StringBuilder;
 $stack38#java.lang.StringBuilder();
 $stack38 = $stack38.append("recover physics file over, last mapped file ");
 $stack41 = mappedFile.getFileName();
 $stack38 = $stack38.append($stack41);
 $stack41 = $stack38.toString();
 $stack27.info($stack41);
 goto label11;
 label10:$stack72 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack72;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 $stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack38=new java.lang.StringBuilder;
 $stack38#java.lang.StringBuilder();
 $stack38 = $stack38.append("recover next physics file, ");
 $stack41 = mappedFile.getFileName();
 $stack38 = $stack38.append($stack41);
 $stack41 = $stack38.toString();
 $stack27.info($stack41);
 goto label9;
 label6:if (size<=0) goto label12;
 $stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack56=(processOffset+mappedFileOffset);
 $stack53 = java.lang.Long.valueOf($stack56);
 $stack27.warn("found a half message at {}, it will be truncated.",(java.lang.Object)$stack53);
 label12:$stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack38=new java.lang.StringBuilder;
 $stack38#java.lang.StringBuilder();
 $stack38 = $stack38.append("recover physics file end, ");
 $stack41 = mappedFile.getFileName();
 $stack38 = $stack38.append($stack41);
 $stack38 = $stack38.append(" pos=");
 index = byteBuffer.position();
 $stack38 = $stack38.append(index);
 $stack41 = $stack38.toString();
 $stack27.info($stack41);
 goto label11;
 label9:goto label5;
 label11:processOffset=(processOffset+mappedFileOffset);
 this.setConfirmOffset(lastValidMsgPhyOffset);
 $stack24=this.mappedFileQueue;
 $stack24.setFlushedWhere(processOffset);
 $stack24=this.mappedFileQueue;
 $stack24.setCommittedWhere(processOffset);
 $stack24=this.mappedFileQueue;
 $stack24.truncateDirtyFiles(processOffset);
 $stack50=(maxPhyOffsetOfConsumeQueue cmp processOffset);
 if ($stack50<0) goto label13;
 $stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack53 = java.lang.Long.valueOf(maxPhyOffsetOfConsumeQueue);
 $stack54 = java.lang.Long.valueOf(processOffset);
 $stack27.warn("maxPhyOffsetOfConsumeQueue({}) >= processOffset({}), truncate dirty logic files",(java.lang.Object)$stack53,(java.lang.Object)$stack54);
 $stack18=this.defaultMessageStore;
 $stack18.truncateDirtyLogicFiles(processOffset);
 label13:goto label14;
 label0:$stack27=org.apache.rocketmq.store.CommitLog.log;
 $stack27.warn("The commitlog files are deleted, and delete the consume queue files");
 $stack24=this.mappedFileQueue;
 $stack24.setFlushedWhere(0L);
 $stack24=this.mappedFileQueue;
 $stack24.setCommittedWhere(0L);
 $stack18=this.defaultMessageStore;
 $stack18.destroyLogics();
 label14:return;
 } 

