org.apache.rocketmq.remoting.netty.RequestTask org.apache.rocketmq.proxy.remoting.RemotingProtocolServer:castRunnable (
  java.lang.Runnable runnable) {
  guard = tt;
  ret(level()) = pc ⊔ level(runnable) ⊔ obj_level(runnable);
  ret(obj_level()) = pc ⊔ level(runnable) ⊔ obj_level(runnable);
  ret(obj_level(runnable)) = if obj_level(runnable) = ⊤ ∨ pc ≠ ⊤ ∧ level(runnable) = ⊤ ∧ obj_level(runnable) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(runnable) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
}