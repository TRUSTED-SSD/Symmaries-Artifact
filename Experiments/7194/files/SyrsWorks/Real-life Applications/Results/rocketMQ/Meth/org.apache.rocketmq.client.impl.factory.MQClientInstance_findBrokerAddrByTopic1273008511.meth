java.lang.String org.apache.rocketmq.client.impl.factory.MQClientInstance:findBrokerAddrByTopic(java.lang.String @topic)
{
org.apache.rocketmq.remoting.protocol.route.TopicRouteData topicRouteData;
  java.util.List brokers;
  int index;
  org.apache.rocketmq.remoting.protocol.route.BrokerData bd;
  java.util.concurrent.ConcurrentMap $stack6;
  java.lang.Object $stack7;
  boolean $stack9;
  java.util.Random $stack11;
  int $stack14;
  java.lang.String topic;
topic=@topic;
$stack6=this.topicRouteTable;
 $stack7 = $stack6.get((java.lang.Object)topic);
 topicRouteData=(org.apache.rocketmq.remoting.protocol.route.TopicRouteData) $stack7;
 if (topicRouteData==null) goto label0;
 brokers = topicRouteData.getBrokerDatas();
 $stack9 = brokers.isEmpty();
 if ($stack9!=0) goto label0;
 $stack11=this.random;
 index = brokers.size();
 index = $stack11.nextInt(index);
 $stack14 = brokers.size();
 index=(index%$stack14);
 $stack7 = brokers.get(index);
 bd=(org.apache.rocketmq.remoting.protocol.route.BrokerData) $stack7;
 topic = bd.selectBrokerAddr();
 return  topic;
 label0:return  null;
 } 

