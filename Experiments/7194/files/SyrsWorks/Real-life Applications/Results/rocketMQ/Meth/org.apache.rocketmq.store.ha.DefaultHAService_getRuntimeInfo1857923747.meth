org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo org.apache.rocketmq.store.ha.DefaultHAService:getRuntimeInfo(long masterPutWhere)
{
org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo info;
  int inSyncNums;
  java.util.Iterator l5;
  org.apache.rocketmq.store.ha.HAConnection conn;
  org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo$HAConnectionRuntimeInfo cInfo;
  long slaveAckOffset;
  boolean isInSync;
  org.apache.rocketmq.store.config.BrokerRole $stack12;
  org.apache.rocketmq.store.DefaultMessageStore $stack13;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack14;
  org.apache.rocketmq.store.config.BrokerRole $stack15;
  java.util.List $stack17;
  java.lang.Object $stack20;
  java.lang.String $stack24;
  org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo$HAClientRuntimeInfo $stack36;
  org.apache.rocketmq.store.ha.HAClient $stack37;
  info=new org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo;
 info#org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo();
 $stack12=org.apache.rocketmq.store.config.BrokerRole.SLAVE;
 $stack13 = this.getDefaultMessageStore();
 $stack14 = $stack13.getMessageStoreConfig();
 $stack15 = $stack14.getBrokerRole();
 isInSync = $stack12.equals((java.lang.Object)$stack15);
 if (isInSync==0) goto label0;
 info.setMaster((boolean)0);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack37=this.haClient;
 $stack24 = $stack37.getHaMasterAddress();
 $stack36.setMasterAddr($stack24);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack13 = this.getDefaultMessageStore();
 masterPutWhere = $stack13.getMaxPhyOffset();
 $stack36.setMaxOffset(masterPutWhere);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack37=this.haClient;
 masterPutWhere = $stack37.getLastReadTimestamp();
 $stack36.setLastReadTimestamp(masterPutWhere);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack37=this.haClient;
 masterPutWhere = $stack37.getLastWriteTimestamp();
 $stack36.setLastWriteTimestamp(masterPutWhere);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack37=this.haClient;
 masterPutWhere = $stack37.getTransferredByteInSecond();
 $stack36.setTransferredByteInSecond(masterPutWhere);
 $stack36 = info.getHaClientRuntimeInfo();
 $stack13=this.defaultMessageStore;
 masterPutWhere = $stack13.getMasterFlushedOffset();
 $stack36.setMasterFlushOffset(masterPutWhere);
 goto label1;
 label0:info.setMaster((boolean)1);
 inSyncNums=0;
 info.setMasterCommitLogMaxOffset(masterPutWhere);
 $stack17=this.connectionList;
 l5 = $stack17.iterator();
 label2:isInSync = l5.hasNext();
 if (isInSync==0) goto label3;
 $stack20 = l5.next();
 conn=(org.apache.rocketmq.store.ha.HAConnection) $stack20;
 cInfo=new org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo$HAConnectionRuntimeInfo;
 cInfo#org.apache.rocketmq.remoting.protocol.body.HARuntimeInfo$HAConnectionRuntimeInfo();
 slaveAckOffset = conn.getSlaveAckOffset();
 cInfo.setSlaveAckOffset(slaveAckOffset);
 slaveAckOffset=(masterPutWhere-slaveAckOffset);
 cInfo.setDiff(slaveAckOffset);
 $stack24 = conn.getClientAddress();
 $stack24 = $stack24.substring(1);
 cInfo.setAddr($stack24);
 slaveAckOffset = conn.getTransferredByteInSecond();
 cInfo.setTransferredByteInSecond(slaveAckOffset);
 slaveAckOffset = conn.getTransferFromWhere();
 cInfo.setTransferFromWhere(slaveAckOffset);
 isInSync = this.isInSyncSlave(masterPutWhere,conn);
 if (isInSync==0) goto label4;
 inSyncNums=(inSyncNums+1);
 label4:cInfo.setInSync(isInSync);
 $stack17 = info.getHaConnectionInfo();
 $stack17.add((java.lang.Object)cInfo);
 goto label2;
 label3:info.setInSyncSlaveNums(inSyncNums);
 label1:return  info;
 } 

