void org.apache.rocketmq.proxy.grpc.v2.producer.SendMessageActivity:fillDelayMessageProperty(apache.rocketmq.v2.Message message,org.apache.rocketmq.common.message.Message messageWithHeader)
{
com.google.protobuf.Timestamp deliveryTimestamp;
  long deliveryTimestampMs;
  org.apache.rocketmq.proxy.config.ProxyConfig config;
  int delayLevel;
  apache.rocketmq.v2.SystemProperties $stack8;
  boolean $stack9;
  java.lang.String $stack18;
  $stack8 = message.getSystemProperties();
 $stack9 = $stack8.hasDeliveryTimestamp();
 if ($stack9==0) goto label0;
 $stack8 = message.getSystemProperties();
 deliveryTimestamp = $stack8.getDeliveryTimestamp();
 deliveryTimestampMs = com.google.protobuf.util.Timestamps.toMillis(deliveryTimestamp);
 this.validateDelayTime(deliveryTimestampMs);
 config = org.apache.rocketmq.proxy.config.ConfigurationManager.getProxyConfig();
 $stack9 = config.isUseDelayLevel();
 if ($stack9==0) goto label1;
 delayLevel = config.computeDelayLevel(deliveryTimestampMs);
 $stack18 = java.lang.String.valueOf(delayLevel);
 org.apache.rocketmq.common.message.MessageAccessor.putProperty(messageWithHeader,"DELAY",$stack18);
 label1:$stack18 = java.lang.String.valueOf(deliveryTimestampMs);
 org.apache.rocketmq.common.message.MessageAccessor.putProperty(messageWithHeader,"TIMER_DELIVER_MS",$stack18);
 label0:return;
 } 

