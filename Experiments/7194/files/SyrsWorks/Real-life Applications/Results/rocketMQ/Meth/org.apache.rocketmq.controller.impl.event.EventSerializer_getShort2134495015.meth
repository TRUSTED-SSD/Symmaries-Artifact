short org.apache.rocketmq.controller.impl.event.EventSerializer:getShort(byte[] memory,int index)
{
byte $stack3;
  short $stack6;
  int $stack7;
  $stack3=memory[index];
 $stack7=($stack3<<8);
 index=(index+1);
 $stack3=memory[index];
 $stack6=($stack3 & 255);
 index=($stack7 | $stack6);
 $stack6=(short) index;
 return  $stack6;
 } 

