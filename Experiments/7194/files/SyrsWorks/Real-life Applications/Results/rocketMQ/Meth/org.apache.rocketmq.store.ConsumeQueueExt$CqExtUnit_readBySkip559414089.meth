void org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit:readBySkip(java.nio.ByteBuffer buffer)
{
java.nio.ByteBuffer temp;
  short tempSize;
  int $stack6;
  temp = buffer.slice();
 tempSize = temp.getShort();
 this.size=tempSize;
 if (tempSize<=0) goto label0;
 $stack6 = buffer.position();
 tempSize=this.size;
 $stack6=($stack6+tempSize);
 buffer.position($stack6);
 label0:return;
 } 

