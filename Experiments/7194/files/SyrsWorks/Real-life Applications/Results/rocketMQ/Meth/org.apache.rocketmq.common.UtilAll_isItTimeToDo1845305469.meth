static boolean org.apache.rocketmq.common.UtilAll:isItTimeToDo(java.lang.String @when)
{
java.lang.String[] whiles;
  java.util.Calendar now;
  int l4;
  int l5;
  int nowHour;
  int $stack13;
  java.lang.String when;
when=@when;
whiles = when.split(";");
 l4=#whiles;
 if (l4<=0) goto label0;
 now = java.util.Calendar.getInstance();
 l4=#whiles;
 l5=0;
 label1:if (l5>=l4) goto label0;
 when=whiles[l5];
 nowHour = java.lang.Integer.parseInt(when);
 $stack13 = now.get(11);
 if (nowHour!=$stack13) goto label2;
 return  1;
 label2:l5=(l5+1);
 goto label1;
 label0:return  0;
 } 

