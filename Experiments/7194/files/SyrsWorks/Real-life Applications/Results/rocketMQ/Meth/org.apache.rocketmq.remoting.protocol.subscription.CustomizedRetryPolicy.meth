org.apache.rocketmq.remoting.protocol.subscription.CustomizedRetryPolicy()
{
long[] $stack1;
  java.util.concurrent.TimeUnit $stack2;
  long $stack4;
  this#java.lang.Object();
 $stack1=new long[18];
 $stack2=java.util.concurrent.TimeUnit.SECONDS;
 $stack4 = $stack2.toMillis(1L);
 $stack1[0]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.SECONDS;
 $stack4 = $stack2.toMillis(5L);
 $stack1[1]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.SECONDS;
 $stack4 = $stack2.toMillis(10L);
 $stack1[2]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.SECONDS;
 $stack4 = $stack2.toMillis(30L);
 $stack1[3]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(1L);
 $stack1[4]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(2L);
 $stack1[5]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(3L);
 $stack1[6]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(4L);
 $stack1[7]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(5L);
 $stack1[8]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(6L);
 $stack1[9]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(7L);
 $stack1[10]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(8L);
 $stack1[11]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(9L);
 $stack1[12]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(10L);
 $stack1[13]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(20L);
 $stack1[14]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.MINUTES;
 $stack4 = $stack2.toMillis(30L);
 $stack1[15]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.HOURS;
 $stack4 = $stack2.toMillis(1L);
 $stack1[16]=$stack4;
 $stack2=java.util.concurrent.TimeUnit.HOURS;
 $stack4 = $stack2.toMillis(2L);
 $stack1[17]=$stack4;
 this.next=$stack1;
 return;
 } 

