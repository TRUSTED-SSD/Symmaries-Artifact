org.apache.rocketmq.store.util.PerfCounter org.apache.rocketmq.store.util.PerfCounter$Ticks:makeSureExists(java.lang.String key)
{
java.util.Map $stack2;
  java.lang.Object $stack3;
  org.apache.rocketmq.store.util.PerfCounter $stack4;
  int $stack9;
  int $stack10;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack15;
  $stack2=this.perfs;
 $stack3 = $stack2.get((java.lang.Object)key);
 if ($stack3!=null) goto label0;
 $stack2=this.perfs;
 $stack9 = $stack2.size();
 $stack10=this.maxKeyNumPerf;
 $stack10=($stack10+100);
 if ($stack9<$stack10) goto label1;
 $stack4=this.defaultPerf;
 return  $stack4;
 label1:$stack2=this.perfs;
 $stack4=new org.apache.rocketmq.store.util.PerfCounter;
 $stack15=this.logger;
 $stack10=this.maxNumPerCount;
 $stack9=this.maxTimeMsPerCount;
 $stack4#org.apache.rocketmq.store.util.PerfCounter(3001,$stack15,key,$stack10,$stack9);
 $stack2.put((java.lang.Object)key,(java.lang.Object)$stack4);
 label0:$stack2=this.perfs;
 $stack4=this.defaultPerf;
 $stack3 = $stack2.getOrDefault((java.lang.Object)key,(java.lang.Object)$stack4);
 $stack4=(org.apache.rocketmq.store.util.PerfCounter) $stack3;
 return  $stack4;
 } 

