boolean org.apache.rocketmq.store.CommitLog:getLastMappedFile(long startOffset)
{
org.apache.rocketmq.store.logfile.MappedFile lastMappedFile;
  org.apache.rocketmq.store.MappedFileQueue $stack4;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack6;
  java.lang.Long $stack8;
  $stack4=this.mappedFileQueue;
 lastMappedFile = $stack4.getLastMappedFile(startOffset);
 if (null!=lastMappedFile) goto label0;
 $stack6=org.apache.rocketmq.store.CommitLog.log;
 $stack8 = java.lang.Long.valueOf(startOffset);
 $stack6.error("getLastMappedFile error. offset:{}",(java.lang.Object)$stack8);
 return  0;
 label0:return  1;
 } 

