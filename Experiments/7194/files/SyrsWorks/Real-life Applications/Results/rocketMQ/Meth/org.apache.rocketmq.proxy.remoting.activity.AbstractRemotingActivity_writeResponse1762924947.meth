void org.apache.rocketmq.proxy.remoting.activity.AbstractRemotingActivity:writeResponse(io.netty.channel.ChannelHandlerContext ctx,org.apache.rocketmq.proxy.common.ProxyContext context,org.apache.rocketmq.remoting.protocol.RemotingCommand request,org.apache.rocketmq.remoting.protocol.RemotingCommand response,java.lang.Throwable t)
{
org.apache.rocketmq.proxy.config.ProxyConfig config;
  boolean $stack7;
  io.netty.channel.Channel $stack8;
  int $stack11;
  java.lang.String $stack13;
  $stack7 = request.isOnewayRPC();
 if ($stack7==0) goto label0;
 return;
 label0:$stack8 = ctx.channel();
 $stack7 = $stack8.isWritable();
 if ($stack7!=0) goto label1;
 return;
 label1:config = org.apache.rocketmq.proxy.config.ConfigurationManager.getProxyConfig();
 $stack11 = request.getOpaque();
 response.setOpaque($stack11);
 response.markResponseType();
 $stack13 = config.getRegionId();
 response.addExtField("MSG_REGION",$stack13);
 $stack7 = config.isTraceOn();
 $stack13 = java.lang.String.valueOf($stack7);
 response.addExtField("TRACE_ON",$stack13);
 if (t==null) goto label2;
 $stack13 = t.getMessage();
 response.setRemark($stack13);
 label2:ctx.writeAndFlush((java.lang.Object)response);
 return;
 } 

