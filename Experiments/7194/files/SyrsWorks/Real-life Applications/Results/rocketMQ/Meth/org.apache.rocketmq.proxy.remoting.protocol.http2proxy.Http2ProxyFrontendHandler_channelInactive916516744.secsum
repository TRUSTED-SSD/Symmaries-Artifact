void org.apache.rocketmq.proxy.remoting.protocol.http2proxy.Http2ProxyFrontendHandler:channelInactive (
  io.netty.channel.ChannelHandlerContext ctx) {
  guard = tt;
  ret(obj_level(ctx)) = if obj_level(ctx) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(ctx) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = obj_level(this);
}