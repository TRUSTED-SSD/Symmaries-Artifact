long org.apache.rocketmq.store.StoreCheckpoint:getMinTimestamp()
{
long min;
  long $stack4;
  byte $stack6;
  $stack4=this.physicMsgTimestamp;
 min=this.logicsMsgTimestamp;
 min = java.lang.Math.min($stack4,min);
 min=(min-3000L);
 $stack6=(min cmp 0L);
 if ($stack6>=0) goto label0;
 min=0L;
 label0:return  min;
 } 

