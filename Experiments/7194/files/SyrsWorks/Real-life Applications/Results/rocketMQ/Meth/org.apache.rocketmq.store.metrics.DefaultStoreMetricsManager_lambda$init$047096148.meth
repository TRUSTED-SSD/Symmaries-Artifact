static void org.apache.rocketmq.store.metrics.DefaultStoreMetricsManager:lambda$init$0(io.opentelemetry.api.metrics.ObservableLongMeasurement measurement)
{
java.io.File storeDir;
  long totalSpace;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack5;
  java.lang.String $stack6;
  boolean $stack7;
  byte $stack10;
  long $stack11;
  io.opentelemetry.api.common.AttributesBuilder $stack13;
  io.opentelemetry.api.common.Attributes $stack14;
  storeDir=new java.io.File;
 $stack5=org.apache.rocketmq.store.metrics.DefaultStoreMetricsManager.messageStoreConfig;
 $stack6 = $stack5.getStorePathRootDir();
 storeDir#java.io.File($stack6);
 $stack7 = storeDir.exists();
 if ($stack7==0) goto label0;
 $stack7 = storeDir.isDirectory();
 if ($stack7==0) goto label0;
 totalSpace = storeDir.getTotalSpace();
 $stack10=(totalSpace cmp 0L);
 if ($stack10<=0) goto label0;
 $stack11 = storeDir.getFreeSpace();
 totalSpace=(totalSpace-$stack11);
 $stack13 = org.apache.rocketmq.store.metrics.DefaultStoreMetricsManager.newAttributesBuilder();
 $stack14 = $stack13.build();
 measurement.record(totalSpace,$stack14);
 label0:return;
 } 

