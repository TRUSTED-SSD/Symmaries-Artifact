void org.apache.rocketmq.remoting.protocol.header.namesrv.RegisterBrokerRequestHeader:setHeartbeatTimeoutMillis (
  java.lang.Long heartbeatTimeoutMillis) {
  guard = tt;
  ret(obj_level(heartbeatTimeoutMillis)) = obj_level(heartbeatTimeoutMillis);
  ret(obj_level(this)) = level(heartbeatTimeoutMillis) ⊔ obj_level(heartbeatTimeoutMillis) ⊔ obj_level(this);
}