static double org.apache.rocketmq.store.StoreStatsService$CallSnapshot:getTPS(org.apache.rocketmq.store.StoreStatsService$CallSnapshot begin,org.apache.rocketmq.store.StoreStatsService$CallSnapshot end)
{
long total;
  java.lang.Long time;
  double tps;
  long $stack8;
  long $stack10;
  double $stack14;
  $stack8=end.callTimesTotal;
 total=begin.callTimesTotal;
 total=($stack8-total);
 $stack10=end.timestamp;
 $stack8=begin.timestamp;
 $stack8=($stack10-$stack8);
 time = java.lang.Long.valueOf($stack8);
 tps=(double) total;
 $stack14 = time.doubleValue();
 tps=(tps/$stack14);
 tps=(tps * 1000.0);
 return  tps;
 } 

