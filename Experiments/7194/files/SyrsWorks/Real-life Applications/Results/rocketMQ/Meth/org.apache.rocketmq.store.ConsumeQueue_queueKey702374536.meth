java.lang.String org.apache.rocketmq.store.ConsumeQueue:queueKey(java.lang.String @queueName,org.apache.rocketmq.common.message.MessageExtBrokerInner msgInner)
{
java.lang.StringBuilder keyBuilder;
  int queueId;
  org.apache.rocketmq.store.MessageStore $stack9;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack10;
  boolean $stack11;
  java.lang.String queueName;
queueName=@queueName;
keyBuilder=new java.lang.StringBuilder;
 keyBuilder#java.lang.StringBuilder();
 keyBuilder.append(queueName);
 keyBuilder.append((char)45);
 queueId = msgInner.getQueueId();
 $stack9=this.messageStore;
 $stack10 = $stack9.getMessageStoreConfig();
 $stack11 = $stack10.isEnableLmq();
 if ($stack11==0) goto label0;
 $stack11 = org.apache.rocketmq.common.MixAll.isLmq(queueName);
 if ($stack11==0) goto label0;
 queueId=0;
 label0:keyBuilder.append(queueId);
 queueName = keyBuilder.toString();
 return  queueName;
 } 

