static java.lang.String org.apache.rocketmq.common.MixAll:getReplyTopic (
  java.lang.String @clusterName) {
  guard = tt;
  ret(level()) = level(@clusterName) ⊔ obj_level(@clusterName);
  ret(obj_level()) = level(@clusterName) ⊔ obj_level(@clusterName);
  ret(obj_level(@clusterName)) = level(@clusterName) ⊔ obj_level(@clusterName);
}