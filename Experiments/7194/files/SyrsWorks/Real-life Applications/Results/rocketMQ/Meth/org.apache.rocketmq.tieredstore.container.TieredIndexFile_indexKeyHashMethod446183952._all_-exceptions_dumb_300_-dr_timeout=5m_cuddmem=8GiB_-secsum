static int org.apache.rocketmq.tieredstore.container.TieredIndexFile:indexKeyHashMethod (
  java.lang.String key) {
  guard = tt;
  ret(level()) = level(key) ⊔ obj_level(key);
  ret(obj_level(key)) = level(key) ⊔ obj_level(key);
}