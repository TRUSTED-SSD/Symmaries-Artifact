boolean org.apache.rocketmq.proxy.remoting.RemotingProtocolServer$ThreadPoolHeadSlowTimeMillsMonitor:needPrintJstack (
  java.util.concurrent.ThreadPoolExecutor executor, double value) {
  guard = tt;
  ret(level()) = pc ⊔ level(this) ⊔ level(value) ⊔ obj_level(this);
  ret(obj_level(executor)) = if obj_level(executor) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(executor) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(this) = ⊥ ∧ obj_level(executor) ⊓ (level(this) ⊔ level(value)) = ⊤ then ⊤ else ⊥;
}