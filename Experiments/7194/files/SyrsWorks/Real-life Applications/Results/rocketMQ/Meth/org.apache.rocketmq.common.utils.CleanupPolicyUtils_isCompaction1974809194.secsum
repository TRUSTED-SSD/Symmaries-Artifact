static boolean org.apache.rocketmq.common.utils.CleanupPolicyUtils:isCompaction (
  java.util.Optional topicConfig) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(topicConfig);
  ret(obj_level(topicConfig)) = pc ⊔ obj_level(topicConfig);
}