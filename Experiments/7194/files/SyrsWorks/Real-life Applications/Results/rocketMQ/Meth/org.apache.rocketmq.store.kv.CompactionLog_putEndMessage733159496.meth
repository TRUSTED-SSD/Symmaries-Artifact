void org.apache.rocketmq.store.kv.CompactionLog:putEndMessage(org.apache.rocketmq.store.MappedFileQueue mappedFileQueue)
{
org.apache.rocketmq.store.logfile.MappedFile lastFile;
  boolean $stack4;
  java.nio.ByteBuffer $stack5;
  org.apache.rocketmq.store.CompactionAppendMsgCallback $stack6;
  lastFile = mappedFileQueue.getLastMappedFile();
 $stack4 = lastFile.isFull();
 if ($stack4!=0) goto label0;
 $stack5 = java.nio.ByteBuffer.allocate(0);
 $stack6=this.endMsgCallback;
 lastFile.appendMessage($stack5,$stack6);
 label0:return;
 } 

