org.apache.rocketmq.remoting.protocol.RemotingCommand org.apache.rocketmq.broker.processor.AdminBrokerProcessor:ViewBrokerStatsData(io.netty.channel.ChannelHandlerContext ctx,org.apache.rocketmq.remoting.protocol.RemotingCommand @request)
{
org.apache.rocketmq.remoting.protocol.header.ViewBrokerStatsDataRequestHeader requestHeader;
  org.apache.rocketmq.store.MessageStore messageStore;
  org.apache.rocketmq.common.stats.StatsItem statsItem;
  org.apache.rocketmq.remoting.protocol.body.BrokerStatsData brokerStatsData;
  org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem it;
  org.apache.rocketmq.common.stats.StatsSnapshot ss;
  org.apache.rocketmq.remoting.CommandCustomHeader $stack10;
  org.apache.rocketmq.broker.BrokerController $stack12;
  org.apache.rocketmq.store.stats.BrokerStatsManager $stack14;
  java.lang.String $stack15;
  java.lang.String $stack16;
  long $stack21;
  double $stack22;
  byte[] $stack34;
  java.lang.Object[] $stack35;
  org.apache.rocketmq.remoting.protocol.RemotingCommand request;
request=@request;
$stack10 = request.decodeCommandCustomHeader(org.apache.rocketmq.remoting.protocol.header.ViewBrokerStatsDataRequestHeader);
 requestHeader=(org.apache.rocketmq.remoting.protocol.header.ViewBrokerStatsDataRequestHeader) $stack10;
 request = org.apache.rocketmq.remoting.protocol.RemotingCommand.createResponseCommand(null);
 $stack12=this.brokerController;
 messageStore = $stack12.getMessageStore();
 $stack14 = messageStore.getBrokerStatsManager();
 $stack15 = requestHeader.getStatsName();
 $stack16 = requestHeader.getStatsKey();
 statsItem = $stack14.getStatsItem($stack15,$stack16);
 if (null!=statsItem) goto label0;
 request.setCode(1);
 $stack35=new java.lang.Object[2];
 $stack15 = requestHeader.getStatsName();
 $stack35[0]=$stack15;
 $stack15 = requestHeader.getStatsKey();
 $stack35[1]=$stack15;
 $stack15 = java.lang.String.format("The stats <%s> <%s> not exist",$stack35);
 request.setRemark($stack15);
 return  request;
 label0:brokerStatsData=new org.apache.rocketmq.remoting.protocol.body.BrokerStatsData;
 brokerStatsData#org.apache.rocketmq.remoting.protocol.body.BrokerStatsData();
 it=new org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem;
 it#org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem();
 ss = statsItem.getStatsDataInMinute();
 $stack21 = ss.getSum();
 it.setSum($stack21);
 $stack22 = ss.getTps();
 it.setTps($stack22);
 $stack22 = ss.getAvgpt();
 it.setAvgpt($stack22);
 brokerStatsData.setStatsMinute(it);
 it=new org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem;
 it#org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem();
 ss = statsItem.getStatsDataInHour();
 $stack21 = ss.getSum();
 it.setSum($stack21);
 $stack22 = ss.getTps();
 it.setTps($stack22);
 $stack22 = ss.getAvgpt();
 it.setAvgpt($stack22);
 brokerStatsData.setStatsHour(it);
 it=new org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem;
 it#org.apache.rocketmq.remoting.protocol.body.BrokerStatsItem();
 ss = statsItem.getStatsDataInDay();
 $stack21 = ss.getSum();
 it.setSum($stack21);
 $stack22 = ss.getTps();
 it.setTps($stack22);
 $stack22 = ss.getAvgpt();
 it.setAvgpt($stack22);
 brokerStatsData.setStatsDay(it);
 $stack34 = brokerStatsData.encode();
 request.setBody($stack34);
 request.setCode(0);
 request.setRemark(null);
 return  request;
 } 

