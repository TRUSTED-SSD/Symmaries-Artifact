long org.apache.rocketmq.store.timer.TimerWheel:checkPhyPos(long timeStartMs,long maxOffset)
{
long minFirst;
  int firstSlotIndex;
  int i;
  int slotIndex;
  long first;
  long last;
  int $stack17;
  java.lang.ThreadLocal $stack20;
  java.lang.Object $stack21;
  java.nio.ByteBuffer $stack23;
  byte $stack36;
  minFirst=9223372036854775807L;
 firstSlotIndex = this.getSlotIndex(timeStartMs);
 i=0;
 label0:slotIndex=this.slotsTotal;
 slotIndex=(slotIndex * 2);
 if (i>=slotIndex) goto label1;
 slotIndex=(firstSlotIndex+i);
 $stack17=this.slotsTotal;
 $stack17=($stack17 * 2);
 slotIndex=(slotIndex%$stack17);
 $stack20=this.localBuffer;
 $stack21 = $stack20.get();
 $stack23=(java.nio.ByteBuffer) $stack21;
 slotIndex=(slotIndex * 32);
 $stack23.position(slotIndex);
 slotIndex=this.precisionMs;
 slotIndex=(i * slotIndex);
 first=(long) slotIndex;
 last=(timeStartMs+first);
 slotIndex=this.precisionMs;
 first=(long) slotIndex;
 first=(last/first);
 $stack20=this.localBuffer;
 $stack21 = $stack20.get();
 $stack23=(java.nio.ByteBuffer) $stack21;
 last = $stack23.getLong();
 $stack36=(first cmp last);
 if ($stack36==0) goto label2;
 goto label3;
 label2:$stack20=this.localBuffer;
 $stack21 = $stack20.get();
 $stack23=(java.nio.ByteBuffer) $stack21;
 first = $stack23.getLong();
 $stack20=this.localBuffer;
 $stack21 = $stack20.get();
 $stack23=(java.nio.ByteBuffer) $stack21;
 last = $stack23.getLong();
 $stack36=(last cmp maxOffset);
 if ($stack36<=0) goto label3;
 $stack36=(first cmp minFirst);
 if ($stack36>=0) goto label3;
 minFirst=first;
 label3:i=(i+1);
 goto label0;
 label1:return  minFirst;
 } 

