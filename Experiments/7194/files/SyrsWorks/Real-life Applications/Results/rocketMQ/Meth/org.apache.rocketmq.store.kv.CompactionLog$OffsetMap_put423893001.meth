void org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:put(java.lang.String key,long offset)
{
int tryNum;
  int index;
  byte[] $stack8;
  boolean $stack11;
  java.nio.ByteBuffer $stack12;
  byte[] $stack27;
  java.lang.IllegalArgumentException $stack35;
  index=this.entryNum;
 tryNum=this.capacity;
 if (index<tryNum) goto label0;
 $stack35=new java.lang.IllegalArgumentException;
 $stack35#java.lang.IllegalArgumentException("offset map is full");
 throw $stack35;
 label0:$stack8=this.hash1;
 this#org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:hashInto(key,$stack8);
 tryNum=0;
 $stack8=this.hash1;
 index = this#org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:indexOf($stack8,0);
 label1:$stack11 = this#org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:isEmpty(index);
 if ($stack11!=0) goto label2;
 $stack12=this.dataBytes;
 $stack12.position(index);
 $stack12=this.dataBytes;
 $stack8=this.hash2;
 $stack12.get($stack8);
 $stack27=this.hash1;
 $stack8=this.hash2;
 $stack11 = java.util.Arrays.equals($stack27,$stack8);
 if ($stack11==0) goto label3;
 $stack12=this.dataBytes;
 $stack12.putLong(offset);
 this.lastOffset=offset;
 return;
 label3:tryNum=(tryNum+1);
 $stack8=this.hash1;
 index = this#org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:indexOf($stack8,tryNum);
 goto label1;
 label2:$stack12=this.dataBytes;
 $stack12.position(index);
 $stack12=this.dataBytes;
 $stack8=this.hash1;
 $stack12.put($stack8);
 $stack12=this.dataBytes;
 $stack12.putLong(offset);
 this.lastOffset=offset;
 tryNum=this.entryNum;
 tryNum=(tryNum+1);
 this.entryNum=tryNum;
 return;
 } 

