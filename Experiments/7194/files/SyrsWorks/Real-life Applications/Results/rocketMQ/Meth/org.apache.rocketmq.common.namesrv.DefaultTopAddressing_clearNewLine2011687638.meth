static java.lang.String org.apache.rocketmq.common.namesrv.DefaultTopAddressing:clearNewLine(java.lang.String @str)
{
int index;
  java.lang.String str;
str=@str;
str = str.trim();
 index = str.indexOf("\r");
 if (index==-1) goto label0;
 str = str.substring(0,index);
 return  str;
 label0:index = str.indexOf("\n");
 if (index==-1) goto label1;
 str = str.substring(0,index);
 return  str;
 label1:return  str;
 } 

