long org.apache.rocketmq.broker.transaction.queue.TransactionalMessageBridge:fetchConsumeOffset(org.apache.rocketmq.common.message.MessageQueue mq)
{
long offset;
  org.apache.rocketmq.broker.BrokerController $stack4;
  org.apache.rocketmq.broker.offset.ConsumerOffsetManager $stack5;
  java.lang.String $stack6;
  java.lang.String $stack7;
  int $stack8;
  byte $stack10;
  org.apache.rocketmq.store.MessageStore $stack11;
  $stack4=this.brokerController;
 $stack5 = $stack4.getConsumerOffsetManager();
 $stack6 = org.apache.rocketmq.broker.transaction.queue.TransactionalMessageUtil.buildConsumerGroup();
 $stack7 = mq.getTopic();
 $stack8 = mq.getQueueId();
 offset = $stack5.queryOffset($stack6,$stack7,$stack8);
 $stack10=(offset cmp -1L);
 if ($stack10!=0) goto label0;
 $stack11=this.store;
 $stack6 = mq.getTopic();
 $stack8 = mq.getQueueId();
 offset = $stack11.getMinOffsetInQueue($stack6,$stack8);
 label0:return  offset;
 } 

