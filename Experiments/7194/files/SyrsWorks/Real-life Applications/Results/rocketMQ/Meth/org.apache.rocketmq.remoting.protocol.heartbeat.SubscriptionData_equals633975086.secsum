boolean org.apache.rocketmq.remoting.protocol.heartbeat.SubscriptionData:equals (
  java.lang.Object obj) {
  guard = tt;
  ret(level()) = pc ⊔ level(obj) ⊔ level(this) ⊔ obj_level(obj);
  ret(obj_level(obj)) = if obj_level(obj) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(obj) = ⊥ ∧ level(obj) ⊔ level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(obj) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(obj) = ⊥ ∧ level(obj) ⊔ level(this) = ⊤ then ⊤ else ⊥;
}