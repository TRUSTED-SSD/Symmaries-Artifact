long org.apache.rocketmq.store.timer.TimerMetrics:updateDistPair(int period,int value)
{
org.apache.rocketmq.store.timer.TimerMetrics$Metric distPair;
  java.lang.Integer $stack4;
  java.util.concurrent.atomic.AtomicLong $stack6;
  long $stack7;
  $stack4 = java.lang.Integer.valueOf(period);
 distPair = this.getDistPair($stack4);
 $stack6 = distPair.getCount();
 $stack7=(long) value;
 $stack7 = $stack6.addAndGet($stack7);
 return  $stack7;
 } 

