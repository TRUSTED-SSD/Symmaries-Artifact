long org.apache.rocketmq.store.CommitLog:flush()
{
org.apache.rocketmq.store.MappedFileQueue $stack1;
  long $stack6;
  $stack1=this.mappedFileQueue;
 $stack1.commit(0);
 $stack1=this.mappedFileQueue;
 $stack1.flush(0);
 $stack1=this.mappedFileQueue;
 $stack6 = $stack1.getFlushedWhere();
 return  $stack6;
 } 

