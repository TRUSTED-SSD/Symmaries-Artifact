long org.apache.rocketmq.store.queue.BatchConsumeQueue:getLastOffset()
{
org.apache.rocketmq.store.queue.CqUnit latestUnit;
  long $stack3;
  int $stack4;
  long $stack5;
  latestUnit = this.getLatestUnit();
 $stack3 = latestUnit.getPos();
 $stack4 = latestUnit.getSize();
 $stack5=(long) $stack4;
 $stack3=($stack3+$stack5);
 return  $stack3;
 } 

