boolean org.apache.rocketmq.store.DefaultMessageStore:checkInStoreByConsumeOffset(java.lang.String topic,int queueId,long consumeOffset)
{
boolean $stack8;
  consumeOffset = this.getCommitLogOffsetInQueue(topic,queueId,consumeOffset);
 $stack8 = this.checkInDiskByCommitOffset(consumeOffset);
 return  $stack8;
 } 

