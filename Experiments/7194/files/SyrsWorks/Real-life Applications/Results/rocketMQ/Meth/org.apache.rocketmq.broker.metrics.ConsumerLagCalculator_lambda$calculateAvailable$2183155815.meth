void org.apache.rocketmq.broker.metrics.ConsumerLagCalculator:lambda$calculateAvailable$2(java.util.function.Consumer availableRecorder,org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$ProcessGroupInfo info)
{
org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$CalculateAvailableResult result;
  long retryAvailable;
  java.lang.String $stack7;
  java.lang.String $stack8;
  boolean $stack9;
  result=new org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$CalculateAvailableResult;
 $stack8=info.group;
 $stack7=info.topic;
 result#org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$CalculateAvailableResult($stack8,$stack7,0);
 $stack8=info.group;
 $stack7=info.topic;
 $stack9=info.isPop;
 retryAvailable = this.getAvailableMsgCount($stack8,$stack7,$stack9);
 result.available=retryAvailable;
 availableRecorder.accept((java.lang.Object)result);
 $stack9=info.isPop;
 if ($stack9==0) goto label0;
 $stack8=info.group;
 $stack7=info.retryTopic;
 retryAvailable = this.getAvailableMsgCount($stack8,$stack7,(boolean)1);
 result=new org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$CalculateAvailableResult;
 $stack8=info.group;
 $stack7=info.topic;
 result#org.apache.rocketmq.broker.metrics.ConsumerLagCalculator$CalculateAvailableResult($stack8,$stack7,1);
 result.available=retryAvailable;
 availableRecorder.accept((java.lang.Object)result);
 label0:return;
 } 

