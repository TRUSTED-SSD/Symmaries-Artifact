long org.apache.rocketmq.store.CommitLog:getLastFileFromOffset()
{
org.apache.rocketmq.store.logfile.MappedFile lastMappedFile;
  org.apache.rocketmq.store.MappedFileQueue $stack2;
  boolean $stack4;
  long $stack6;
  $stack2=this.mappedFileQueue;
 lastMappedFile = $stack2.getLastMappedFile();
 if (lastMappedFile==null) goto label0;
 $stack4 = lastMappedFile.isAvailable();
 if ($stack4==0) goto label0;
 $stack6 = lastMappedFile.getFileFromOffset();
 return  $stack6;
 label0:return  -1L;
 } 

