static java.util.List org.apache.rocketmq.common.message.MessageDecoder:decodes (
  java.nio.ByteBuffer byteBuffer) {
  guard = tt;
  ret(level()) = level(byteBuffer) ⊔ obj_level(byteBuffer);
  ret(obj_level()) = level(byteBuffer) ⊔ obj_level(byteBuffer);
  ret(obj_level(byteBuffer)) = obj_level(byteBuffer);
}