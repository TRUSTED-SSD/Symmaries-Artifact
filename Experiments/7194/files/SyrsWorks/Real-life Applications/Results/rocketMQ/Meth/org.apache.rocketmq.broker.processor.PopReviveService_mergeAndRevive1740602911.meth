void org.apache.rocketmq.broker.processor.PopReviveService:mergeAndRevive(org.apache.rocketmq.broker.processor.PopReviveService$ConsumeReviveObj consumeReviveObj)
{
java.util.ArrayList sortList;
  long newOffset;
  java.util.Iterator l5;
  org.apache.rocketmq.store.pop.PopCheckPoint popCheckPoint;
  java.lang.String normalTopic;
  org.apache.rocketmq.common.Pair pair;
  org.apache.rocketmq.store.pop.PopCheckPoint oldCK;
  int $stack11;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack12;
  java.lang.Integer $stack14;
  java.lang.Integer $stack16;
  boolean $stack19;
  long $stack20;
  byte $stack21;
  org.apache.rocketmq.broker.BrokerController $stack23;
  org.apache.rocketmq.broker.offset.ConsumerOffsetManager $stack24;
  java.lang.Object $stack36;
  long $stack39;
  java.lang.String $stack45;
  org.apache.rocketmq.broker.topic.TopicConfigManager $stack48;
  org.apache.rocketmq.common.TopicConfig $stack49;
  org.apache.rocketmq.broker.subscription.SubscriptionGroupManager $stack52;
  org.apache.rocketmq.remoting.protocol.subscription.SubscriptionGroupConfig $stack54;
  java.util.NavigableMap $stack55;
  java.util.Map$Entry $stack60;
  java.lang.Boolean $stack63;
  java.lang.Long $stack69;
  java.lang.Object[] $stack96;
  sortList = consumeReviveObj.genSortList();
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 $stack11 = sortList.size();
 $stack16 = java.lang.Integer.valueOf($stack11);
 $stack12.info("reviveQueueId={},ck listSize={}",(java.lang.Object)$stack14,(java.lang.Object)$stack16);
 $stack11 = sortList.size();
 if ($stack11==0) goto label0;
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 $stack96=new java.lang.Object[5];
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 $stack96[0]=$stack14;
 $stack36 = sortList.get(0);
 popCheckPoint=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 newOffset = popCheckPoint.getStartOffset();
 $stack69 = java.lang.Long.valueOf(newOffset);
 $stack96[1]=$stack69;
 $stack36 = sortList.get(0);
 popCheckPoint=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 newOffset = popCheckPoint.getReviveOffset();
 $stack69 = java.lang.Long.valueOf(newOffset);
 $stack96[2]=$stack69;
 $stack11 = sortList.size();
 $stack11=($stack11-1);
 $stack36 = sortList.get($stack11);
 popCheckPoint=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 newOffset = popCheckPoint.getStartOffset();
 $stack69 = java.lang.Long.valueOf(newOffset);
 $stack96[3]=$stack69;
 $stack11 = sortList.size();
 $stack11=($stack11-1);
 $stack36 = sortList.get($stack11);
 popCheckPoint=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 newOffset = popCheckPoint.getReviveOffset();
 $stack69 = java.lang.Long.valueOf(newOffset);
 $stack96[4]=$stack69;
 $stack12.info("reviveQueueId={}, 1st ck, startOffset={}, reviveOffset={} ; last ck, startOffset={}, reviveOffset={}",$stack96);
 label0:newOffset=consumeReviveObj.oldOffset;
 l5 = sortList.iterator();
 label1:$stack19 = l5.hasNext();
 if ($stack19==0) goto label2;
 $stack36 = l5.next();
 popCheckPoint=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 $stack19=this.shouldRunPopRevive;
 if ($stack19!=0) goto label3;
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 normalTopic=this.reviveTopic;
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 $stack12.info("slave skip ck process , revive topic={}, reviveQueueId={}",(java.lang.Object)normalTopic,(java.lang.Object)$stack14);
 goto label2;
 label3:$stack20=consumeReviveObj.endTime;
 $stack39 = popCheckPoint.getReviveTime();
 $stack20=($stack20-$stack39);
 $stack39=org.apache.rocketmq.common.PopAckConstants.ackTimeInterval;
 $stack39=($stack39+1000L);
 $stack21=($stack20 cmp $stack39);
 if ($stack21>0) goto label4;
 goto label2;
 label4:normalTopic = popCheckPoint.getTopic();
 $stack45 = popCheckPoint.getCId();
 normalTopic = org.apache.rocketmq.common.KeyBuilder.parseNormalTopic(normalTopic,$stack45);
 $stack23=this.brokerController;
 $stack48 = $stack23.getTopicConfigManager();
 $stack49 = $stack48.selectTopicConfig(normalTopic);
 if ($stack49!=null) goto label5;
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 normalTopic = popCheckPoint.getTopic();
 $stack12.warn("reviveQueueId={},can not get normal topic {} , then continue ",(java.lang.Object)$stack14,(java.lang.Object)normalTopic);
 newOffset = popCheckPoint.getReviveOffset();
 goto label1;
 label5:$stack23=this.brokerController;
 $stack52 = $stack23.getSubscriptionGroupManager();
 normalTopic = popCheckPoint.getCId();
 $stack54 = $stack52.findSubscriptionGroupConfig(normalTopic);
 if (null!=$stack54) goto label6;
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 normalTopic = popCheckPoint.getCId();
 $stack12.warn("reviveQueueId={},can not get cid {} , then continue ",(java.lang.Object)$stack14,(java.lang.Object)normalTopic);
 newOffset = popCheckPoint.getReviveOffset();
 goto label1;
 label6:$stack55=this.inflightReviveRequestMap;
 $stack11 = $stack55.size();
 if ($stack11<=3) goto label7;
 this.waitForRunning(100L);
 $stack55=this.inflightReviveRequestMap;
 $stack60 = $stack55.firstEntry();
 $stack36 = $stack60.getValue();
 pair=(org.apache.rocketmq.common.Pair) $stack36;
 $stack36 = pair.getObject2();
 $stack63=(java.lang.Boolean) $stack36;
 $stack19 = $stack63.booleanValue();
 if ($stack19!=0) goto label8;
 newOffset = java.lang.System.currentTimeMillis();
 $stack36 = pair.getObject1();
 $stack69=(java.lang.Long) $stack36;
 $stack20 = $stack69.longValue();
 newOffset=(newOffset-$stack20);
 $stack21=(newOffset cmp 30000L);
 if ($stack21<=0) goto label8;
 $stack55=this.inflightReviveRequestMap;
 $stack36 = $stack55.firstKey();
 oldCK=(org.apache.rocketmq.store.pop.PopCheckPoint) $stack36;
 this#org.apache.rocketmq.broker.processor.PopReviveService:rePutCK(oldCK,pair);
 $stack55=this.inflightReviveRequestMap;
 $stack55.remove((java.lang.Object)oldCK);
 label8:goto label6;
 label7:this#org.apache.rocketmq.broker.processor.PopReviveService(popCheckPoint);
 newOffset = popCheckPoint.getReviveOffset();
 goto label1;
 label2:$stack20=consumeReviveObj.oldOffset;
 $stack21=(newOffset cmp $stack20);
 if ($stack21<=0) goto label9;
 $stack19=this.shouldRunPopRevive;
 if ($stack19!=0) goto label10;
 $stack12=org.apache.rocketmq.broker.processor.PopReviveService.POP_LOGGER;
 normalTopic=this.reviveTopic;
 $stack11=this.queueId;
 $stack14 = java.lang.Integer.valueOf($stack11);
 $stack12.info("slave skip commit, revive topic={}, reviveQueueId={}",(java.lang.Object)normalTopic,(java.lang.Object)$stack14);
 return;
 label10:$stack23=this.brokerController;
 $stack24 = $stack23.getConsumerOffsetManager();
 normalTopic=this.reviveTopic;
 $stack11=this.queueId;
 $stack24.commitOffset("127.0.0.1","CID_RMQ_SYS_REVIVE_GROUP",normalTopic,$stack11,newOffset);
 label9:this.reviveOffset=newOffset;
 consumeReviveObj.newOffset=newOffset;
 return;
 } 

