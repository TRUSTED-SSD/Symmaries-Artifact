void org.apache.rocketmq.filter.util.BitsArray:or(org.apache.rocketmq.filter.util.BitsArray other)
{
int minByteLength;
  int i;
  byte[] $stack7;
  byte[] $stack8;
  byte $stack9;
  byte $stack10;
  this.checkInitialized(this);
 this.checkInitialized(other);
 minByteLength = this.byteLength();
 i = other.byteLength();
 minByteLength = java.lang.Math.min(minByteLength,i);
 i=0;
 label0:if (i>=minByteLength) goto label1;
 $stack8=this.bytes;
 $stack7=this.bytes;
 $stack9=$stack7[i];
 $stack10 = other.getByte(i);
 $stack9=($stack9 | $stack10);
 $stack9=(byte) $stack9;
 $stack8[i]=$stack9;
 i=(i+1);
 goto label0;
 label1:return;
 } 

