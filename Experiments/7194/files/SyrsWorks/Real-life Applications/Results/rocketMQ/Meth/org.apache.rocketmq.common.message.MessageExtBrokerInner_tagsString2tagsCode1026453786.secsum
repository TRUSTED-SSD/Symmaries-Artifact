static long org.apache.rocketmq.common.message.MessageExtBrokerInner:tagsString2tagsCode (
  java.lang.String tags) {
  guard = tt;
  ret(level()) = pc ⊔ level(tags) ⊔ obj_level(tags);
  ret(obj_level(tags)) = obj_level(tags);
}