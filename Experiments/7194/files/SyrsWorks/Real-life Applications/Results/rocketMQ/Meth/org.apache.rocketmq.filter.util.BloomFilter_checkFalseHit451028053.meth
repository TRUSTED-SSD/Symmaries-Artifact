boolean org.apache.rocketmq.filter.util.BloomFilter:checkFalseHit(int[] bitPositions,org.apache.rocketmq.filter.util.BitsArray bits)
{
int j;
  int pos;
  boolean $stack6;
  j=0;
 label0:pos=#bitPositions;
 if (j>=pos) goto label1;
 pos=bitPositions[j];
 $stack6 = bits.getBit(pos);
 if ($stack6!=0) goto label2;
 return  0;
 label2:j=(j+1);
 goto label0;
 label1:return  1;
 } 

