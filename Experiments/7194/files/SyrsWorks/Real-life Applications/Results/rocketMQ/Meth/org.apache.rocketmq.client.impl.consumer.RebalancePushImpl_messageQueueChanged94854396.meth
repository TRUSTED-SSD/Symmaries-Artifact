void org.apache.rocketmq.client.impl.consumer.RebalancePushImpl:messageQueueChanged(java.lang.String topic,java.util.Set mqAll,java.util.Set mqDivided)
{
org.apache.rocketmq.remoting.protocol.heartbeat.SubscriptionData subscriptionData;
  long newVersion;
  int currentQueueCount;
  int pullThresholdForTopic;
  java.util.concurrent.ConcurrentMap $stack11;
  java.lang.Object $stack12;
  java.lang.Object[] $stack14;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack15;
  long $stack18;
  java.lang.Long $stack19;
  org.apache.rocketmq.client.impl.factory.MQClientInstance $stack24;
  org.apache.rocketmq.client.impl.consumer.DefaultMQPushConsumerImpl $stack25;
  org.apache.rocketmq.client.consumer.DefaultMQPushConsumer $stack26;
  java.lang.Integer $stack39;
  java.lang.Integer $stack40;
  int $stack49;
  $stack11=this.subscriptionInner;
 $stack12 = $stack11.get((java.lang.Object)topic);
 subscriptionData=(org.apache.rocketmq.remoting.protocol.heartbeat.SubscriptionData) $stack12;
 newVersion = java.lang.System.currentTimeMillis();
 $stack15=org.apache.rocketmq.client.impl.consumer.RebalanceImpl.log;
 $stack14=new java.lang.Object[3];
 $stack14[0]=topic;
 $stack18 = subscriptionData.getSubVersion();
 $stack19 = java.lang.Long.valueOf($stack18);
 $stack14[1]=$stack19;
 $stack19 = java.lang.Long.valueOf(newVersion);
 $stack14[2]=$stack19;
 $stack15.info("{} Rebalance changed, also update version: {}, {}",$stack14);
 subscriptionData.setSubVersion(newVersion);
 $stack11=this.processQueueTable;
 currentQueueCount = $stack11.size();
 if (currentQueueCount==0) goto label0;
 $stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 pullThresholdForTopic = $stack26.getPullThresholdForTopic();
 if (pullThresholdForTopic==-1) goto label1;
 pullThresholdForTopic=(pullThresholdForTopic/currentQueueCount);
 pullThresholdForTopic = java.lang.Math.max(1,pullThresholdForTopic);
 $stack15=org.apache.rocketmq.client.impl.consumer.RebalanceImpl.log;
 $stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 $stack49 = $stack26.getPullThresholdForQueue();
 $stack39 = java.lang.Integer.valueOf($stack49);
 $stack40 = java.lang.Integer.valueOf(pullThresholdForTopic);
 $stack15.info("The pullThresholdForQueue is changed from {} to {}",(java.lang.Object)$stack39,(java.lang.Object)$stack40);
 $stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 $stack26.setPullThresholdForQueue(pullThresholdForTopic);
 label1:$stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 pullThresholdForTopic = $stack26.getPullThresholdSizeForTopic();
 if (pullThresholdForTopic==-1) goto label0;
 currentQueueCount=(pullThresholdForTopic/currentQueueCount);
 currentQueueCount = java.lang.Math.max(1,currentQueueCount);
 $stack15=org.apache.rocketmq.client.impl.consumer.RebalanceImpl.log;
 $stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 pullThresholdForTopic = $stack26.getPullThresholdSizeForQueue();
 $stack39 = java.lang.Integer.valueOf(pullThresholdForTopic);
 $stack40 = java.lang.Integer.valueOf(currentQueueCount);
 $stack15.info("The pullThresholdSizeForQueue is changed from {} to {}",(java.lang.Object)$stack39,(java.lang.Object)$stack40);
 $stack25=this.defaultMQPushConsumerImpl;
 $stack26 = $stack25.getDefaultMQPushConsumer();
 $stack26.setPullThresholdSizeForQueue(currentQueueCount);
 label0:$stack24 = this.getmQClientFactory();
 $stack24.sendHeartbeatToAllBrokerWithLock();
 return;
 } 

