long org.apache.rocketmq.store.ConsumeQueueExt:getMaxAddress()
{
org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  org.apache.rocketmq.store.MappedFileQueue $stack2;
  long $stack4;
  int $stack5;
  long $stack6;
  $stack2=this.mappedFileQueue;
 mappedFile = $stack2.getLastMappedFile();
 if (mappedFile!=null) goto label0;
 $stack4 = this.decorate(0L);
 return  $stack4;
 label0:$stack4 = mappedFile.getFileFromOffset();
 $stack5 = mappedFile.getWrotePosition();
 $stack6=(long) $stack5;
 $stack4=($stack4+$stack6);
 $stack4 = this.decorate($stack4);
 return  $stack4;
 } 

