org.apache.rocketmq.store.queue.CqUnit org.apache.rocketmq.store.ConsumeQueue:getEarliestUnit()
{
org.apache.rocketmq.store.queue.ReferredIterator it;
  long $stack2;
  java.lang.Object $stack5;
  org.apache.rocketmq.store.queue.CqUnit $stack6;
  $stack2=this.minLogicOffset;
 $stack2=($stack2/20L);
 it = this.iterateFrom($stack2);
 if (it!=null) goto label0;
 return  null;
 label0:$stack5 = it.nextAndRelease();
 $stack6=(org.apache.rocketmq.store.queue.CqUnit) $stack5;
 return  $stack6;
 } 

