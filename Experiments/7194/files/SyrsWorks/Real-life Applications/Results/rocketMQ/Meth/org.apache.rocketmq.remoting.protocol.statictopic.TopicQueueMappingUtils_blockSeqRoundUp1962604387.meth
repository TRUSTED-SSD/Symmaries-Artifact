static long org.apache.rocketmq.remoting.protocol.statictopic.TopicQueueMappingUtils:blockSeqRoundUp(long offset,long blockSeqSize)
{
long num;
  long $stack8;
  byte $stack9;
  num=(offset/blockSeqSize);
 offset=(offset%blockSeqSize);
 $stack8=(blockSeqSize/2L);
 $stack9=(offset cmp $stack8);
 if ($stack9>=0) goto label0;
 offset=(num+1L);
 offset=(offset * blockSeqSize);
 return  offset;
 label0:offset=(num+2L);
 offset=(offset * blockSeqSize);
 return  offset;
 } 

