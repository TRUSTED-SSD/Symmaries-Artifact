java.net.URL org.apache.rocketmq.common.logging.DefaultJoranConfiguratorExt:findConfigFileURLFromSystemProperties (
  java.lang.ClassLoader classLoader, boolean updateStatus) {
  guard = tt;
  ret(level()) = pc;
  ret(obj_level()) = pc;
  ret(obj_level(classLoader)) = if if level(updateStatus) = ⊤ then obj_level(classLoader) ⊔ obj_level(this) else level(updateStatus) ⊔ obj_level(classLoader) = ⊥ then pc else if if level(updateStatus) = ⊤ then obj_level(classLoader) ⊔ obj_level(this) = ⊤ else level(updateStatus) = ⊥ ∧ obj_level(classLoader) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if if level(updateStatus) = ⊤ then obj_level(classLoader) ⊔ obj_level(this) else level(updateStatus) ⊔ obj_level(this) = ⊥ then pc else if if level(updateStatus) = ⊤ then obj_level(classLoader) ⊔ obj_level(this) = ⊤ else level(updateStatus) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
}