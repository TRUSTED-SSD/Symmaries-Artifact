int org.apache.rocketmq.store.ConsumeQueue:deleteExpiredFile(long offset)
{
int cnt;
  org.apache.rocketmq.store.MappedFileQueue $stack4;
  $stack4=this.mappedFileQueue;
 cnt = $stack4.deleteExpiredFileByOffset(offset,20);
 this.correctMinOffset(offset);
 return  cnt;
 } 

