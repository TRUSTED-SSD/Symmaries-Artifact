long org.apache.rocketmq.store.queue.ConsumeQueueStore:rollNextFile (
  org.apache.rocketmq.store.queue.ConsumeQueueInterface consumeQueue,
  long offset) {
  guard = tt;
  ret(level()) = level(consumeQueue) ⊔ level(offset) ⊔ level(this) ⊔ obj_level(consumeQueue) ⊔ obj_level(this);
  ret(obj_level(consumeQueue)) = obj_level(consumeQueue);
  ret(obj_level(this)) = obj_level(this);
}