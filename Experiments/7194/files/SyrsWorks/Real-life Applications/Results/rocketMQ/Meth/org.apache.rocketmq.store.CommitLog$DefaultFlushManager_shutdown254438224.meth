void org.apache.rocketmq.store.CommitLog$DefaultFlushManager:shutdown()
{
org.apache.rocketmq.store.CommitLog $stack1;
  org.apache.rocketmq.store.DefaultMessageStore $stack2;
  boolean $stack3;
  org.apache.rocketmq.store.CommitLog$FlushCommitLogService $stack4;
  $stack1=this.this$0;
 $stack2=$stack1.defaultMessageStore;
 $stack3 = $stack2.isTransientStorePoolEnable();
 if ($stack3==0) goto label0;
 $stack4=this.commitRealTimeService;
 $stack4.shutdown();
 label0:$stack4=this.flushCommitLogService;
 $stack4.shutdown();
 return;
 } 

