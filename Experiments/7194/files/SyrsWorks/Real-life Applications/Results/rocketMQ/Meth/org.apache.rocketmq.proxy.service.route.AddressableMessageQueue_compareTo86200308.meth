int org.apache.rocketmq.proxy.service.route.AddressableMessageQueue:compareTo(org.apache.rocketmq.proxy.service.route.AddressableMessageQueue o)
{
org.apache.rocketmq.common.message.MessageQueue $stack2;
  org.apache.rocketmq.common.message.MessageQueue $stack3;
  int $stack4;
  $stack3=this.messageQueue;
 $stack2=o.messageQueue;
 $stack4 = $stack3.compareTo($stack2);
 return  $stack4;
 } 

