static boolean org.apache.rocketmq.tieredstore.util.TieredStoreUtil:isSystemTopic (
  java.lang.String topic) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(topic)) = level(topic) ⊔ obj_level(topic);
}