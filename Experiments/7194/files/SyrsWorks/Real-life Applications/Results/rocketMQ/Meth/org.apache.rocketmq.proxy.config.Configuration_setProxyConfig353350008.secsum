void org.apache.rocketmq.proxy.config.Configuration:setProxyConfig (org.apache.rocketmq.proxy.config.ProxyConfig
                                                                    proxyConfig) {
  guard = tt;
  ret(obj_level(proxyConfig)) = ⊤;
  ret(obj_level(this)) = ⊤;
}