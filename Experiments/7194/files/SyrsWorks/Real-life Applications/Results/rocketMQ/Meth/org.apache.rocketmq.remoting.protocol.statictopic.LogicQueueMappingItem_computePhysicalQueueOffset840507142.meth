long org.apache.rocketmq.remoting.protocol.statictopic.LogicQueueMappingItem:computePhysicalQueueOffset(long staticQueueOffset)
{
long $stack3;
  $stack3=this.logicOffset;
 $stack3=(staticQueueOffset-$stack3);
 staticQueueOffset=this.startOffset;
 staticQueueOffset=($stack3+staticQueueOffset);
 return  staticQueueOffset;
 } 

