int org.apache.rocketmq.store.util.PerfCounter:convert(int index)
{
if (index>=1000) goto label0;
 return  index;
 label0:if (index<1000) goto label1;
 if (index>=2000) goto label1;
 index=(index-1000);
 index=(index * 10);
 index=(index+1000);
 return  index;
 label1:index=(index-2000);
 index=(index * 100);
 index=(index+10000);
 index=(index+1000);
 return  index;
 } 

