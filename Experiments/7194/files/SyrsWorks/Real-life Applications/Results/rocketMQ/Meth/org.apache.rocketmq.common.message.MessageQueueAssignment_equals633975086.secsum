boolean org.apache.rocketmq.common.message.MessageQueueAssignment:equals (
  java.lang.Object obj) {
  guard = tt;
  ret(level()) = pc ⊔ level(obj) ⊔ level(this) ⊔ obj_level(obj);
  ret(obj_level(obj)) = if obj_level(obj) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(this) = ⊤ then obj_level(obj) = ⊥ else level(obj) = ⊤ ∧ level(this) = ⊥ ∧ obj_level(obj) = ⊥) then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(obj) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(this) = ⊤ then obj_level(obj) = ⊥ else level(obj) = ⊤ ∧ level(this) = ⊥ ∧ obj_level(obj) = ⊥) then ⊤ else ⊥;
}