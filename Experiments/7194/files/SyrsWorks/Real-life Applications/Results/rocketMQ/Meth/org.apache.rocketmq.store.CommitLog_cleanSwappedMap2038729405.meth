void org.apache.rocketmq.store.CommitLog:cleanSwappedMap(long forceCleanSwapIntervalMs)
{
org.apache.rocketmq.store.MappedFileQueue $stack3;
  $stack3 = this.getMappedFileQueue();
 $stack3.cleanSwappedMap(forceCleanSwapIntervalMs);
 return;
 } 

