static int org.apache.rocketmq.remoting.protocol.RemotingCommand:markProtocolType(int source,org.apache.rocketmq.remoting.protocol.SerializeType type)
{
byte $stack2;
  int $stack4;
  $stack2 = type.getCode();
 $stack4=($stack2<<24);
 source=(source & 16777215);
 source=($stack4 | source);
 return  source;
 } 

