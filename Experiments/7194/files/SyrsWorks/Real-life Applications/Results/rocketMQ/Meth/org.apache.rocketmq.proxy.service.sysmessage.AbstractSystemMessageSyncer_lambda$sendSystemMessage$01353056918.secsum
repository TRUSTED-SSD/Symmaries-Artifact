void org.apache.rocketmq.proxy.service.sysmessage.AbstractSystemMessageSyncer:lambda$sendSystemMessage$0 (
  java.lang.Object data,
  org.apache.rocketmq.client.producer.SendResult result,
  java.lang.Throwable throwable) {
  guard = tt;
  ret(obj_level(data)) = pc ⊔ level(data) ⊔ level(result) ⊔ level(throwable) ⊔ obj_level(data);
  ret(obj_level(result)) = if obj_level(result) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(throwable) = ⊤ then obj_level(result) = ⊥ else level(result) = ⊤ ∧ level(throwable) = ⊥ ∧ obj_level(result) = ⊥) then ⊤ else ⊥;
  ret(obj_level(this)) = pc ⊔ level(data) ⊔ level(result) ⊔ level(throwable) ⊔ obj_level(data);
  ret(obj_level(throwable)) = if obj_level(result) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(throwable) = ⊤ then obj_level(result) = ⊥ else level(result) = ⊤ ∧ level(throwable) = ⊥ ∧ obj_level(result) = ⊥) then ⊤ else ⊥;
}