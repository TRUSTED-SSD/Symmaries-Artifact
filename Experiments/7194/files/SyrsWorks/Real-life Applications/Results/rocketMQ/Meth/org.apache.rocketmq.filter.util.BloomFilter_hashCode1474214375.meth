int org.apache.rocketmq.filter.util.BloomFilter:hashCode()
{
int result;
  int $stack3;
  result=this.f;
 $stack3=(31 * result);
 result=this.n;
 result=($stack3+result);
 $stack3=(31 * result);
 result=this.k;
 result=($stack3+result);
 $stack3=(31 * result);
 result=this.m;
 result=($stack3+result);
 return  result;
 } 

