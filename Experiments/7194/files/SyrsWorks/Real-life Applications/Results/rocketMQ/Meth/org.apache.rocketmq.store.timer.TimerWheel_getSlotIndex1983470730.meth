int org.apache.rocketmq.store.timer.TimerWheel:getSlotIndex(long timeMs)
{
int $stack3;
  long $stack4;
  $stack3=this.precisionMs;
 $stack4=(long) $stack3;
 $stack4=(timeMs/$stack4);
 $stack3=this.slotsTotal;
 $stack3=($stack3 * 2);
 timeMs=(long) $stack3;
 timeMs=($stack4%timeMs);
 $stack3=(int) timeMs;
 return  $stack3;
 } 

