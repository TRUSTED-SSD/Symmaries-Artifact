java.util.Map org.apache.rocketmq.broker.topic.TopicConfigManager:request (
  org.apache.rocketmq.common.TopicConfig topicConfig) {
  guard = tt;
  ret(level()) = level(topicConfig) ⊔ obj_level(topicConfig);
  ret(obj_level()) = obj_level(topicConfig);
  ret(obj_level(this)) = obj_level(this);
  ret(obj_level(topicConfig)) = obj_level(topicConfig);
}