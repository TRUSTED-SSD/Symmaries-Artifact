long org.apache.rocketmq.store.plugin.AbstractPluginMessageStore:getMessageStoreTimeStamp(java.lang.String topic,int queueId,long consumeQueueOffset)
{
org.apache.rocketmq.store.MessageStore $stack5;
  $stack5=this.next;
 consumeQueueOffset = $stack5.getMessageStoreTimeStamp(topic,queueId,consumeQueueOffset);
 return  consumeQueueOffset;
 } 

