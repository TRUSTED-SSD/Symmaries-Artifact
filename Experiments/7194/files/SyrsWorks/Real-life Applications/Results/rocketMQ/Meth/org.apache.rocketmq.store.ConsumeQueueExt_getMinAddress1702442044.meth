long org.apache.rocketmq.store.ConsumeQueueExt:getMinAddress()
{
org.apache.rocketmq.store.logfile.MappedFile firstFile;
  org.apache.rocketmq.store.MappedFileQueue $stack2;
  long $stack4;
  $stack2=this.mappedFileQueue;
 firstFile = $stack2.getFirstMappedFile();
 if (firstFile!=null) goto label0;
 $stack4 = this.decorate(0L);
 return  $stack4;
 label0:$stack4 = firstFile.getFileFromOffset();
 $stack4 = this.decorate($stack4);
 return  $stack4;
 } 

