void org.apache.rocketmq.proxy.remoting.RemotingProtocolServer:registerRemotingServer(org.apache.rocketmq.remoting.RemotingServer remotingServer)
{
java.util.concurrent.ThreadPoolExecutor $stack2;
  org.apache.rocketmq.proxy.remoting.activity.SendMessageActivity $stack3;
  org.apache.rocketmq.proxy.remoting.activity.TransactionActivity $stack11;
  org.apache.rocketmq.proxy.remoting.activity.ClientManagerActivity $stack13;
  org.apache.rocketmq.proxy.remoting.activity.PullMessageActivity $stack19;
  org.apache.rocketmq.proxy.remoting.activity.ConsumerManagerActivity $stack25;
  org.apache.rocketmq.proxy.remoting.activity.GetTopicRouteActivity $stack45;
  $stack3=this.sendMessageActivity;
 $stack2=this.sendMessageExecutor;
 remotingServer.registerProcessor(10,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack3,(java.util.concurrent.ExecutorService)$stack2);
 $stack3=this.sendMessageActivity;
 $stack2=this.sendMessageExecutor;
 remotingServer.registerProcessor(310,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack3,(java.util.concurrent.ExecutorService)$stack2);
 $stack3=this.sendMessageActivity;
 $stack2=this.sendMessageExecutor;
 remotingServer.registerProcessor(320,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack3,(java.util.concurrent.ExecutorService)$stack2);
 $stack3=this.sendMessageActivity;
 $stack2=this.sendMessageExecutor;
 remotingServer.registerProcessor(36,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack3,(java.util.concurrent.ExecutorService)$stack2);
 $stack11=this.transactionActivity;
 $stack2=this.sendMessageExecutor;
 remotingServer.registerProcessor(37,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack11,(java.util.concurrent.ExecutorService)$stack2);
 $stack13=this.clientManagerActivity;
 $stack2=this.heartbeatExecutor;
 remotingServer.registerProcessor(34,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack13,(java.util.concurrent.ExecutorService)$stack2);
 $stack13=this.clientManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(35,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack13,(java.util.concurrent.ExecutorService)$stack2);
 $stack13=this.clientManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(46,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack13,(java.util.concurrent.ExecutorService)$stack2);
 $stack19=this.pullMessageActivity;
 $stack2=this.pullMessageExecutor;
 remotingServer.registerProcessor(11,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack19,(java.util.concurrent.ExecutorService)$stack2);
 $stack19=this.pullMessageActivity;
 $stack2=this.pullMessageExecutor;
 remotingServer.registerProcessor(361,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack19,(java.util.concurrent.ExecutorService)$stack2);
 $stack19=this.pullMessageActivity;
 $stack2=this.pullMessageExecutor;
 remotingServer.registerProcessor(200050,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack19,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.updateOffsetExecutor;
 remotingServer.registerProcessor(15,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.updateOffsetExecutor;
 remotingServer.registerProcessor(200051,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.updateOffsetExecutor;
 remotingServer.registerProcessor(200053,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(38,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(30,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(31,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(14,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(29,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(41,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack25=this.consumerManagerActivity;
 $stack2=this.defaultExecutor;
 remotingServer.registerProcessor(42,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack25,(java.util.concurrent.ExecutorService)$stack2);
 $stack45=this.getTopicRouteActivity;
 $stack2=this.topicRouteExecutor;
 remotingServer.registerProcessor(105,(org.apache.rocketmq.remoting.netty.NettyRequestProcessor)$stack45,(java.util.concurrent.ExecutorService)$stack2);
 return;
 } 

