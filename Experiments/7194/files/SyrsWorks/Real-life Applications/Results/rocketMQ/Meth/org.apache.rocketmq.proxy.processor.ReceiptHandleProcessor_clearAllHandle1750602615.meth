void org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor:clearAllHandle()
{
java.util.Set keySet;
  java.util.Iterator l2;
  org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor$ReceiptHandleGroupKey key;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack4;
  java.util.concurrent.ConcurrentMap $stack5;
  boolean $stack8;
  java.lang.Object $stack10;
  $stack4=org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor.log;
 $stack4.info("start clear all handle in receiptHandleProcessor");
 $stack5=this.receiptHandleGroupMap;
 keySet = $stack5.keySet();
 l2 = keySet.iterator();
 label0:$stack8 = l2.hasNext();
 if ($stack8==0) goto label1;
 $stack10 = l2.next();
 key=(org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor$ReceiptHandleGroupKey) $stack10;
 this#org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor(key);
 goto label0;
 label1:$stack4=org.apache.rocketmq.proxy.processor.ReceiptHandleProcessor.log;
 $stack4.info("clear all handle in receiptHandleProcessor done");
 return;
 } 

