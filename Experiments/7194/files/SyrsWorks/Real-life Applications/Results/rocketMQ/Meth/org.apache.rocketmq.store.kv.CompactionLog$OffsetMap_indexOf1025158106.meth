int org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:indexOf(byte[] hash,int tryNum)
{
int $stack5;
  int $stack9;
  $stack5=this.hashSize;
 $stack5=($stack5-4);
 $stack5 = java.lang.Math.min(tryNum,$stack5);
 $stack5 = this#org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:readInt(hash,$stack5);
 $stack9=this.hashSize;
 tryNum=(tryNum-$stack9);
 tryNum=(tryNum+4);
 tryNum = java.lang.Math.max(0,tryNum);
 tryNum=($stack5+tryNum);
 tryNum = java.lang.Math.abs(tryNum);
 $stack5=this.capacity;
 tryNum=(tryNum%$stack5);
 $stack5=this.entrySize;
 tryNum=(tryNum * $stack5);
 return  tryNum;
 } 

