java.util.concurrent.CompletableFuture org.apache.rocketmq.proxy.processor.ConsumerProcessor:getMaxOffset(org.apache.rocketmq.proxy.common.ProxyContext ctx,org.apache.rocketmq.common.message.MessageQueue messageQueue,long timeoutMillis)
{
java.util.concurrent.CompletableFuture future;
  org.apache.rocketmq.proxy.service.route.AddressableMessageQueue addressableMessageQueue;
  org.apache.rocketmq.remoting.protocol.header.GetMaxOffsetRequestHeader requestHeader;
  org.apache.rocketmq.proxy.service.ServiceManager $stack9;
  org.apache.rocketmq.proxy.service.route.TopicRouteService $stack10;
  java.lang.String $stack13;
  int $stack14;
  java.lang.Integer $stack15;
  org.apache.rocketmq.proxy.service.message.MessageService $stack17;
  java.util.concurrent.ExecutorService $stack19;
  java.lang.Throwable $stack21;
  future=new java.util.concurrent.CompletableFuture;
 future#java.util.concurrent.CompletableFuture();
 label0:$stack9=this.serviceManager;
 $stack10 = $stack9.getTopicRouteService();
 addressableMessageQueue = $stack10.buildAddressableMessageQueue(messageQueue);
 requestHeader=new org.apache.rocketmq.remoting.protocol.header.GetMaxOffsetRequestHeader;
 requestHeader#org.apache.rocketmq.remoting.protocol.header.GetMaxOffsetRequestHeader();
 $stack13 = addressableMessageQueue.getTopic();
 requestHeader.setTopic($stack13);
 $stack14 = addressableMessageQueue.getQueueId();
 $stack15 = java.lang.Integer.valueOf($stack14);
 requestHeader.setQueueId($stack15);
 $stack9=this.serviceManager;
 $stack17 = $stack9.getMessageService();
 future = $stack17.getMaxOffset(ctx,addressableMessageQueue,requestHeader,timeoutMillis);
 label1:goto label2;
 label3:$stack21 = catch;
 future.completeExceptionally($stack21);
 label2:$stack19=this.executor;
 future = org.apache.rocketmq.proxy.common.utils.FutureUtils.addExecutor(future,$stack19);
 return  future;
 catch java.lang.Throwable: label0 - label1 : label3;
} 

