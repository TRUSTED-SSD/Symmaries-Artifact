float org.apache.rocketmq.store.util.PerfCounter:getRate(int from,int to)
{
long tmp;
  float $stack6;
  java.util.concurrent.atomic.AtomicLong $stack8;
  float $stack12;
  tmp = this.getCount(from,to);
 $stack6=(float) tmp;
 $stack6=($stack6+0.0F);
 $stack6=($stack6 * 100.0F);
 $stack8=this.allCount;
 tmp = $stack8.get();
 tmp=(tmp+1L);
 $stack12=(float) tmp;
 $stack6=($stack6/$stack12);
 return  $stack6;
 } 

