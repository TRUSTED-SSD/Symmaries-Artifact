void org.apache.rocketmq.store.kv.CompactionLog$TopicPartitionLog:shutdown()
{
org.apache.rocketmq.store.MappedFileQueue $stack1;
  org.apache.rocketmq.store.queue.SparseConsumeQueue $stack2;
  $stack1=this.mappedFileQueue;
 $stack1.shutdown(30000L);
 $stack2=this.consumeQueue;
 $stack1 = $stack2.getMappedFileQueue();
 $stack1.shutdown(30000L);
 return;
 } 

