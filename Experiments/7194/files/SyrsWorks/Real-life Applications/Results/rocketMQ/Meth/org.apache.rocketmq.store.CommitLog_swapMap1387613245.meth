void org.apache.rocketmq.store.CommitLog:swapMap(int reserveNum,long forceSwapIntervalMs,long normalSwapIntervalMs)
{
org.apache.rocketmq.store.MappedFileQueue $stack6;
  $stack6 = this.getMappedFileQueue();
 $stack6.swapMap(reserveNum,forceSwapIntervalMs,normalSwapIntervalMs);
 return;
 } 

