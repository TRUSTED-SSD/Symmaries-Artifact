void org.apache.rocketmq.client.consumer.DefaultLitePullConsumer:seek(org.apache.rocketmq.common.message.MessageQueue @messageQueue,long offset)
{
org.apache.rocketmq.client.impl.consumer.DefaultLitePullConsumerImpl $stack4;
  org.apache.rocketmq.common.message.MessageQueue messageQueue;
messageQueue=@messageQueue;
$stack4=this.defaultLitePullConsumerImpl;
 messageQueue = this.queueWithNamespace(messageQueue);
 $stack4.seek(messageQueue,offset);
 return;
 } 

