boolean org.apache.rocketmq.broker.transaction.queue.TransactionalMessageServiceImpl:needSkip(org.apache.rocketmq.common.message.MessageExt msgExt)
{
long valueOfCurrentMinusBorn;
  long $stack5;
  org.apache.rocketmq.broker.transaction.queue.TransactionalMessageBridge $stack6;
  org.apache.rocketmq.broker.BrokerController $stack7;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack8;
  int $stack9;
  byte $stack13;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack14;
  java.lang.String $stack16;
  java.lang.Long $stack18;
  valueOfCurrentMinusBorn = java.lang.System.currentTimeMillis();
 $stack5 = msgExt.getBornTimestamp();
 valueOfCurrentMinusBorn=(valueOfCurrentMinusBorn-$stack5);
 $stack6=this.transactionalMessageBridge;
 $stack7 = $stack6.getBrokerController();
 $stack8 = $stack7.getMessageStoreConfig();
 $stack9 = $stack8.getFileReservedTime();
 $stack5=(long) $stack9;
 $stack5=($stack5 * 3600L);
 $stack5=($stack5 * 1000L);
 $stack13=(valueOfCurrentMinusBorn cmp $stack5);
 if ($stack13<=0) goto label0;
 $stack14=org.apache.rocketmq.broker.transaction.queue.TransactionalMessageServiceImpl.log;
 $stack16 = msgExt.getMsgId();
 valueOfCurrentMinusBorn = msgExt.getBornTimestamp();
 $stack18 = java.lang.Long.valueOf(valueOfCurrentMinusBorn);
 $stack14.info("Half message exceed file reserved time ,so skip it.messageId {},bornTime {}",(java.lang.Object)$stack16,(java.lang.Object)$stack18);
 return  1;
 label0:return  0;
 } 

