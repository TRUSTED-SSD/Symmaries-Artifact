void org.apache.rocketmq.client.impl.consumer.PopProcessQueue:fillPopProcessQueueInfo(org.apache.rocketmq.remoting.protocol.body.PopProcessQueueInfo info)
{
int $stack2;
  boolean $stack3;
  long $stack4;
  $stack2 = this.getWaiAckMsgCount();
 info.setWaitAckCount($stack2);
 $stack3 = this.isDropped();
 info.setDroped($stack3);
 $stack4 = this.getLastPopTimestamp();
 info.setLastPopTimestamp($stack4);
 return;
 } 

