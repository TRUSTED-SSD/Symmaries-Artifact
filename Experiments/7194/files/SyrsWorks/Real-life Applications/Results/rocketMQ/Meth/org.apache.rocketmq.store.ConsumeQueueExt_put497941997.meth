long org.apache.rocketmq.store.ConsumeQueueExt:put(org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit cqExtUnit)
{
byte retryTimes;
  int size;
  int i;
  org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  int wrotePosition;
  int blankSize;
  org.apache.rocketmq.store.MappedFileQueue $stack9;
  long $stack10;
  long $stack11;
  java.nio.ByteBuffer $stack14;
  byte[] $stack24;
  boolean $stack25;
  java.lang.Object[] $stack32;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack33;
  java.lang.Integer $stack36;
  java.lang.String $stack40;
  java.lang.Long $stack55;
  java.lang.Throwable $stack60;
  java.lang.StringBuilder $stack61;
  retryTimes=3;
 label0:size = org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit.access$100(cqExtUnit);
 if (size<=32767) goto label1;
 $stack33=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack36 = java.lang.Integer.valueOf(32767);
 $stack33.error("Size of cq ext unit is greater than {}, {}",(java.lang.Object)$stack36,(java.lang.Object)cqExtUnit);
 label2:return  1L;
 label1:$stack9=this.mappedFileQueue;
 $stack10 = $stack9.getMaxOffset();
 $stack11=(long) size;
 $stack10=($stack10+$stack11);
 retryTimes=($stack10 cmp 9223372034707292159L);
 if (retryTimes<=0) goto label3;
 $stack33=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack9=this.mappedFileQueue;
 $stack10 = $stack9.getMaxOffset();
 $stack55 = java.lang.Long.valueOf($stack10);
 $stack36 = java.lang.Integer.valueOf(size);
 $stack33.warn("Capacity of ext is maximum!{}, {}",(java.lang.Object)$stack55,(java.lang.Object)$stack36);
 label4:return  1L;
 label3:$stack14=this.tempContainer;
 if ($stack14==null) goto label5;
 $stack14=this.tempContainer;
 i = $stack14.capacity();
 if (i>=size) goto label6;
 label5:$stack14 = java.nio.ByteBuffer.allocate(size);
 this.tempContainer=$stack14;
 label6:i=0;
 label7:if (i>=3) goto label8;
 $stack9=this.mappedFileQueue;
 mappedFile = $stack9.getLastMappedFile();
 if (mappedFile==null) goto label9;
 $stack25 = mappedFile.isFull();
 if ($stack25==0) goto label10;
 label9:$stack9=this.mappedFileQueue;
 mappedFile = $stack9.getLastMappedFile(0L);
 label10:if (mappedFile!=null) goto label11;
 $stack33=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack33.error("Create mapped file when save consume queue extend, {}",(java.lang.Object)cqExtUnit);
 goto label12;
 label11:wrotePosition = mappedFile.getWrotePosition();
 blankSize=this.mappedFileSize;
 blankSize=(blankSize-wrotePosition);
 blankSize=(blankSize-4);
 if (size<=blankSize) goto label13;
 this.fullFillToEnd(mappedFile,wrotePosition);
 $stack33=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack32=new java.lang.Object[3];
 $stack36 = java.lang.Integer.valueOf(size);
 $stack32[0]=$stack36;
 $stack36 = java.lang.Integer.valueOf(blankSize);
 $stack32[1]=$stack36;
 $stack40 = mappedFile.getFileName();
 $stack32[2]=$stack40;
 $stack33.info("No enough space(need:{}, has:{}) of file {}, so fill to end",$stack32);
 goto label12;
 label13:$stack14=this.tempContainer;
 $stack24 = org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit.access$200(cqExtUnit,$stack14);
 $stack25 = mappedFile.appendMessage($stack24,0,size);
 if ($stack25==0) goto label12;
 $stack10=(long) wrotePosition;
 $stack11 = mappedFile.getFileFromOffset();
 $stack10=($stack10+$stack11);
 $stack10 = this.decorate($stack10);
 label14:return  $stack10;
 label12:i=(i+1);
 goto label7;
 label8:goto label15;
 label16:$stack60 = catch;
 $stack33=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack61=new java.lang.StringBuilder;
 $stack61#java.lang.StringBuilder();
 $stack61 = $stack61.append("Save consume queue extend error, ");
 $stack61 = $stack61.append((java.lang.Object)cqExtUnit);
 $stack40 = $stack61.toString();
 $stack33.error($stack40,$stack60);
 label15:return  1L;
 catch java.lang.Throwable: label0 - label2 : label16;
catch java.lang.Throwable: label1 - label4 : label16;
catch java.lang.Throwable: label3 - label14 : label16;
catch java.lang.Throwable: label12 - label8 : label16;
} 

