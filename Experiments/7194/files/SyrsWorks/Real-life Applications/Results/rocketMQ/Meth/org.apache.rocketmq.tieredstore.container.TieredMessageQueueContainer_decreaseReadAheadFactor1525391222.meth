void org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:decreaseReadAheadFactor()
{
int $stack1;
  org.apache.rocketmq.tieredstore.common.TieredMessageStoreConfig $stack2;
  int $stack4;
  $stack1=this.readAheadFactor;
 $stack1=($stack1-1);
 $stack2=this.storeConfig;
 $stack4 = $stack2.getReadAheadMinFactor();
 $stack1 = java.lang.Math.max($stack1,$stack4);
 this.readAheadFactor=$stack1;
 return;
 } 

