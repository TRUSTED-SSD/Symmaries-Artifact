static boolean org.apache.rocketmq.tieredstore.container.TieredFileQueue:lambda$getFileListByTime$3(long beginTime,long endTime,org.apache.rocketmq.tieredstore.provider.TieredFileSegment segment)
{
long $stack5;
  byte $stack9;
  boolean $stack10;
  $stack5 = segment.getBeginTimestamp();
 beginTime = java.lang.Math.max(beginTime,$stack5);
 $stack5 = segment.getEndTimestamp();
 endTime = java.lang.Math.min(endTime,$stack5);
 $stack9=(beginTime cmp endTime);
 if ($stack9>0) goto label0;
 $stack10=1;
 goto label1;
 label0:$stack10=0;
 label1:return  $stack10;
 } 

