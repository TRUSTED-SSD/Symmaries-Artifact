static int org.apache.rocketmq.remoting.protocol.RemotingCommand:markProtocolType (
  int source, org.apache.rocketmq.remoting.protocol.SerializeType type) {
  guard = tt;
  ret(level()) = level(source) ⊔ level(type) ⊔ obj_level(type);
  ret(obj_level(type)) = obj_level(type);
}