void org.apache.rocketmq.store.config.MessageStoreConfig:setTimerPrecisionMs(int timerPrecisionMs)
{
int[] candidates;
  int i;
  int $stack5;
  candidates=new int[4];
 candidates[0]=100;
 candidates[1]=200;
 candidates[2]=500;
 candidates[3]=1000;
 i=1;
 label0:$stack5=#candidates;
 if (i>=$stack5) goto label1;
 $stack5=candidates[i];
 if (timerPrecisionMs>=$stack5) goto label2;
 timerPrecisionMs=(i-1);
 timerPrecisionMs=candidates[timerPrecisionMs];
 this.timerPrecisionMs=timerPrecisionMs;
 return;
 label2:i=(i+1);
 goto label0;
 label1:timerPrecisionMs=#candidates;
 timerPrecisionMs=(timerPrecisionMs-1);
 timerPrecisionMs=candidates[timerPrecisionMs];
 this.timerPrecisionMs=timerPrecisionMs;
 return;
 } 

