static boolean org.apache.rocketmq.common.utils.NetworkUtil:isBridge (
  java.net.NetworkInterface networkInterface) {
  guard = tt;
  ret(level()) = obj_level(networkInterface);
  ret(obj_level(networkInterface)) = obj_level(networkInterface);
}