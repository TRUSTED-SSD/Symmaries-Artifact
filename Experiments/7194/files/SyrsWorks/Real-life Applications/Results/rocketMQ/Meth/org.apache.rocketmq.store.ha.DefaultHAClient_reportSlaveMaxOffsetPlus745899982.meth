boolean org.apache.rocketmq.store.ha.DefaultHAClient:reportSlaveMaxOffsetPlus()
{
boolean result;
  long currentPhyOffset;
  org.apache.rocketmq.store.DefaultMessageStore $stack4;
  long $stack6;
  byte $stack7;
  java.lang.StringBuilder $stack11;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack12;
  java.lang.String $stack16;
  result=1;
 $stack4=this.defaultMessageStore;
 currentPhyOffset = $stack4.getMaxPhyOffset();
 $stack6=this.currentReportedOffset;
 $stack7=(currentPhyOffset cmp $stack6);
 if ($stack7<=0) goto label0;
 this.currentReportedOffset=currentPhyOffset;
 currentPhyOffset=this.currentReportedOffset;
 result = this#org.apache.rocketmq.store.ha.DefaultHAClient:reportSlaveMaxOffset(currentPhyOffset);
 if (result!=0) goto label0;
 this.closeMaster();
 $stack12=org.apache.rocketmq.store.ha.DefaultHAClient.log;
 $stack11=new java.lang.StringBuilder;
 $stack11#java.lang.StringBuilder();
 $stack11 = $stack11.append("HAClient, reportSlaveMaxOffset error, ");
 currentPhyOffset=this.currentReportedOffset;
 $stack11 = $stack11.append(currentPhyOffset);
 $stack16 = $stack11.toString();
 $stack12.error($stack16);
 label0:return  result;
 } 

