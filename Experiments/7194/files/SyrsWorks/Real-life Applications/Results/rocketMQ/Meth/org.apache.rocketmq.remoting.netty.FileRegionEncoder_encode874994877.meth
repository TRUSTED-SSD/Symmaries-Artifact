void org.apache.rocketmq.remoting.netty.FileRegionEncoder:encode(io.netty.channel.ChannelHandlerContext ctx,io.netty.channel.FileRegion msg,io.netty.buffer.ByteBuf out)
{
org.apache.rocketmq.remoting.netty.FileRegionEncoder$1 writableByteChannel;
  long toTransfer;
  long transferred;
  long $stack12;
  byte $stack13;
  writableByteChannel=new org.apache.rocketmq.remoting.netty.FileRegionEncoder$1;
 writableByteChannel#org.apache.rocketmq.remoting.netty.FileRegionEncoder$1(this,out);
 toTransfer = msg.count();
 label0:transferred = msg.transfered();
 $stack12=(toTransfer-transferred);
 $stack13=($stack12 cmp 0L);
 if ($stack13>0) goto label1;
 goto label2;
 label1:transferred = msg.transferTo((java.nio.channels.WritableByteChannel)writableByteChannel,transferred);
 goto label0;
 label2:return;
 } 

