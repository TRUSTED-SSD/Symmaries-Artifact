static long org.apache.rocketmq.tieredstore.util.TieredStoreUtil:fileName2Offset(java.lang.String @fileName)
{
int $stack1;
  long $stack4;
  java.lang.String fileName;
fileName=@fileName;
$stack1 = fileName.length();
 $stack1=($stack1-20);
 fileName = fileName.substring($stack1);
 $stack4 = java.lang.Long.parseLong(fileName);
 return  $stack4;
 } 

