boolean org.apache.rocketmq.broker.processor.PopMessageProcessor:notifyMessageArriving(java.lang.String @topic,java.lang.String cid,int queueId)
{
java.util.concurrent.ConcurrentSkipListSet remotingCommands;
  org.apache.rocketmq.broker.longpolling.PopRequest popRequest;
  com.googlecode.concurrentlinkedhashmap.ConcurrentLinkedHashMap $stack6;
  java.lang.Object $stack8;
  boolean $stack9;
  java.util.concurrent.atomic.AtomicLong $stack11;
  org.apache.rocketmq.broker.BrokerController $stack13;
  org.apache.rocketmq.common.BrokerConfig $stack14;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack17;
  io.netty.channel.Channel $stack20;
  java.lang.String topic;
topic=@topic;
$stack6=this.pollingMap;
 topic = org.apache.rocketmq.common.KeyBuilder.buildPollingKey(topic,cid,queueId);
 $stack8 = $stack6.get((java.lang.Object)topic);
 remotingCommands=(java.util.concurrent.ConcurrentSkipListSet) $stack8;
 if (remotingCommands==null) goto label0;
 $stack9 = remotingCommands.isEmpty();
 if ($stack9==0) goto label1;
 label0:return  0;
 label1:$stack8 = remotingCommands.pollFirst();
 popRequest=(org.apache.rocketmq.broker.longpolling.PopRequest) $stack8;
 label2:if (popRequest==null) goto label3;
 $stack20 = popRequest.getChannel();
 $stack9 = $stack20.isActive();
 if ($stack9!=0) goto label3;
 $stack11=this.totalPollingNum;
 $stack11.decrementAndGet();
 $stack8 = remotingCommands.pollFirst();
 popRequest=(org.apache.rocketmq.broker.longpolling.PopRequest) $stack8;
 goto label2;
 label3:if (popRequest!=null) goto label4;
 return  0;
 label4:$stack11=this.totalPollingNum;
 $stack11.decrementAndGet();
 $stack13=this.brokerController;
 $stack14 = $stack13.getBrokerConfig();
 $stack9 = $stack14.isEnablePopLog();
 if ($stack9==0) goto label5;
 $stack17=org.apache.rocketmq.broker.processor.PopMessageProcessor.POP_LOGGER;
 $stack17.info("lock release , new msg arrive , wakeUp : {}",(java.lang.Object)popRequest);
 label5:$stack9 = this#org.apache.rocketmq.broker.processor.PopMessageProcessor:wakeUp(popRequest);
 return  $stack9;
 } 

