boolean org.apache.rocketmq.broker.client.rebalance.RebalanceLockManager:isLocked(java.lang.String group,org.apache.rocketmq.common.message.MessageQueue mq,java.lang.String clientId)
{
java.util.concurrent.ConcurrentHashMap groupValue;
  org.apache.rocketmq.broker.client.rebalance.RebalanceLockManager$LockEntry lockEntry;
  boolean locked;
  java.util.concurrent.ConcurrentMap $stack7;
  java.lang.Object $stack8;
  long $stack12;
  $stack7=this.mqLockTable;
 $stack8 = $stack7.get((java.lang.Object)group);
 groupValue=(java.util.concurrent.ConcurrentHashMap) $stack8;
 if (groupValue==null) goto label0;
 $stack8 = groupValue.get((java.lang.Object)mq);
 lockEntry=(org.apache.rocketmq.broker.client.rebalance.RebalanceLockManager$LockEntry) $stack8;
 if (lockEntry==null) goto label0;
 locked = lockEntry.isLocked(clientId);
 if (locked==0) goto label1;
 $stack12 = java.lang.System.currentTimeMillis();
 lockEntry.setLastUpdateTimestamp($stack12);
 label1:return  locked;
 label0:return  0;
 } 

