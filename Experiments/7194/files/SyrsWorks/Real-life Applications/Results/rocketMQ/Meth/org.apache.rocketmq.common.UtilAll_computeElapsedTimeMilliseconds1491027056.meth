static long org.apache.rocketmq.common.UtilAll:computeElapsedTimeMilliseconds(long beginTime)
{
long $stack2;
  $stack2 = java.lang.System.currentTimeMillis();
 beginTime=($stack2-beginTime);
 return  beginTime;
 } 

