org.apache.rocketmq.store.logfile.MappedFile org.apache.rocketmq.store.queue.SparseConsumeQueue:getPreFile(org.apache.rocketmq.store.logfile.MappedFile @file)
{
int index;
  org.apache.rocketmq.store.MappedFileQueue $stack3;
  java.util.List $stack4;
  java.lang.Object $stack9;
  org.apache.rocketmq.store.logfile.MappedFile file;
file=@file;
$stack3=this.mappedFileQueue;
 $stack4 = $stack3.getMappedFiles();
 index = $stack4.indexOf((java.lang.Object)file);
 if (index>=1) goto label0;
 return  null;
 label0:$stack3=this.mappedFileQueue;
 $stack4 = $stack3.getMappedFiles();
 index=(index-1);
 $stack9 = $stack4.get(index);
 file=(org.apache.rocketmq.store.logfile.MappedFile) $stack9;
 return  file;
 } 

