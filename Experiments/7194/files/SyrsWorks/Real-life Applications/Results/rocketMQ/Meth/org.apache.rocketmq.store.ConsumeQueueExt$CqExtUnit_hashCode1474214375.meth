int org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit:hashCode()
{
int result;
  long $stack2;
  long $stack4;
  int $stack7;
  short $stack14;
  byte[] $stack16;
  result=this.size;
 $stack7=(31 * result);
 $stack4=this.tagsCode;
 $stack2=this.tagsCode;
 $stack2=($stack2>>>32);
 $stack4=(!$stack4 & $stack2) |(!$stack2 & $stack4);
 result=(int) $stack4;
 result=($stack7+result);
 $stack7=(31 * result);
 $stack4=this.msgStoreTime;
 $stack2=this.msgStoreTime;
 $stack2=($stack2>>>32);
 $stack4=(!$stack4 & $stack2) |(!$stack2 & $stack4);
 result=(int) $stack4;
 result=($stack7+result);
 result=(31 * result);
 $stack14=this.bitMapSize;
 result=(result+$stack14);
 result=(31 * result);
 $stack16=this.filterBitMap;
 if ($stack16==null) goto label0;
 $stack16=this.filterBitMap;
 $stack7 = java.util.Arrays.hashCode($stack16);
 goto label1;
 label0:$stack7=0;
 label1:result=(result+$stack7);
 return  result;
 } 

