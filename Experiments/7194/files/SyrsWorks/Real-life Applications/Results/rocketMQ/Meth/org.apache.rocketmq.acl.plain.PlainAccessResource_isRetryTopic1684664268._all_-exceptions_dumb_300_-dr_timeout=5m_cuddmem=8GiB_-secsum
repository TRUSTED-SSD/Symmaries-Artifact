static boolean org.apache.rocketmq.acl.plain.PlainAccessResource:isRetryTopic (
  java.lang.String topic) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(topic)) = level(topic) ⊔ obj_level(topic);
}