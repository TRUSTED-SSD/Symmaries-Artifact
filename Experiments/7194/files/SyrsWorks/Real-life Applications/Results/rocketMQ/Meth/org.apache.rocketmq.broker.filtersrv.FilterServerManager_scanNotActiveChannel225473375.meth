void org.apache.rocketmq.broker.filtersrv.FilterServerManager:scanNotActiveChannel()
{
java.util.Iterator it;
  java.util.Map$Entry next;
  long timestamp;
  io.netty.channel.Channel channel;
  java.util.concurrent.ConcurrentMap $stack6;
  java.util.Set $stack7;
  boolean $stack9;
  java.lang.Object $stack10;
  org.apache.rocketmq.broker.filtersrv.FilterServerManager$FilterServerInfo $stack12;
  long $stack15;
  byte $stack17;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack19;
  $stack6=this.filterServerTable;
 $stack7 = $stack6.entrySet();
 it = $stack7.iterator();
 label0:$stack9 = it.hasNext();
 if ($stack9==0) goto label1;
 $stack10 = it.next();
 next=(java.util.Map$Entry) $stack10;
 $stack10 = next.getValue();
 $stack12=(org.apache.rocketmq.broker.filtersrv.FilterServerManager$FilterServerInfo) $stack10;
 timestamp = $stack12.getLastUpdateTimestamp();
 $stack10 = next.getKey();
 channel=(io.netty.channel.Channel) $stack10;
 $stack15 = java.lang.System.currentTimeMillis();
 timestamp=($stack15-timestamp);
 $stack17=(timestamp cmp 30000L);
 if ($stack17<=0) goto label2;
 $stack19=org.apache.rocketmq.broker.filtersrv.FilterServerManager.log;
 $stack10 = next.getKey();
 $stack19.info("The Filter Server<{}> expired, remove it",$stack10);
 it.remove();
 org.apache.rocketmq.remoting.common.RemotingHelper.closeChannel(channel);
 label2:goto label0;
 label1:return;
 } 

