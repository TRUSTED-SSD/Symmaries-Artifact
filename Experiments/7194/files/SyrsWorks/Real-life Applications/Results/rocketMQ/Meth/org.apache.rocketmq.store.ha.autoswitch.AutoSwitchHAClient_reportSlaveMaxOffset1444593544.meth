boolean org.apache.rocketmq.store.ha.autoswitch.AutoSwitchHAClient:reportSlaveMaxOffset()
{
boolean result;
  long maxPhyOffset;
  org.apache.rocketmq.store.DefaultMessageStore $stack4;
  long $stack6;
  byte $stack7;
  result=1;
 $stack4=this.messageStore;
 maxPhyOffset = $stack4.getMaxPhyOffset();
 $stack6=this.currentReportedOffset;
 $stack7=(maxPhyOffset cmp $stack6);
 if ($stack7<=0) goto label0;
 this.currentReportedOffset=maxPhyOffset;
 maxPhyOffset=this.currentReportedOffset;
 result = this#org.apache.rocketmq.store.ha.autoswitch.AutoSwitchHAClient:reportSlaveOffset(maxPhyOffset);
 label0:return  result;
 } 

