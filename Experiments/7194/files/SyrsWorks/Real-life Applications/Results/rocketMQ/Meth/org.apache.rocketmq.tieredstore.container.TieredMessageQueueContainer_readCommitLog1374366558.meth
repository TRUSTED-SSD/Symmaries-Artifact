java.util.concurrent.CompletableFuture org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:readCommitLog(long offset,int length)
{
org.apache.rocketmq.tieredstore.container.TieredCommitLog $stack4;
  java.util.concurrent.CompletableFuture $stack5;
  $stack4=this.commitLog;
 $stack5 = $stack4.readAsync(offset,length);
 return  $stack5;
 } 

