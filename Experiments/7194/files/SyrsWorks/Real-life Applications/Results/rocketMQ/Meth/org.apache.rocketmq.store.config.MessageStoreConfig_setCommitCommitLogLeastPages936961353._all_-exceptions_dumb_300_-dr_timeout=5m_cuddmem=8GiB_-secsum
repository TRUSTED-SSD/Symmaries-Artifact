void org.apache.rocketmq.store.config.MessageStoreConfig:setCommitCommitLogLeastPages (
  int commitCommitLogLeastPages) {
  guard = tt;
  ret(obj_level(this)) = level(commitCommitLogLeastPages) ⊔ obj_level(this);
}