void org.apache.rocketmq.store.TopicQueueLock:lock(java.lang.String topicQueueKey)
{
java.util.concurrent.locks.Lock lock;
  java.util.List $stack3;
  int $stack4;
  int $stack6;
  java.lang.Object $stack8;
  $stack3=this.lockList;
 $stack4 = topicQueueKey.hashCode();
 $stack6=($stack4 & 2147483647);
 $stack4=this.size;
 $stack4=($stack6%$stack4);
 $stack8 = $stack3.get($stack4);
 lock=(java.util.concurrent.locks.Lock) $stack8;
 lock.lock();
 return;
 } 

