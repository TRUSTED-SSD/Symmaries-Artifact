void org.apache.rocketmq.store.CommitLog:setMappedFileQueueOffset(long phyOffset)
{
org.apache.rocketmq.store.MappedFileQueue $stack3;
  $stack3=this.mappedFileQueue;
 $stack3.setFlushedWhere(phyOffset);
 $stack3=this.mappedFileQueue;
 $stack3.setCommittedWhere(phyOffset);
 return;
 } 

