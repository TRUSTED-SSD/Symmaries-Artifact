long org.apache.rocketmq.store.MappedFileQueue:getMaxWrotePosition()
{
org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  long $stack3;
  int $stack4;
  long $stack5;
  mappedFile = this.getLastMappedFile();
 if (mappedFile==null) goto label0;
 $stack3 = mappedFile.getFileFromOffset();
 $stack4 = mappedFile.getWrotePosition();
 $stack5=(long) $stack4;
 $stack3=($stack3+$stack5);
 return  $stack3;
 label0:return  0L;
 } 

