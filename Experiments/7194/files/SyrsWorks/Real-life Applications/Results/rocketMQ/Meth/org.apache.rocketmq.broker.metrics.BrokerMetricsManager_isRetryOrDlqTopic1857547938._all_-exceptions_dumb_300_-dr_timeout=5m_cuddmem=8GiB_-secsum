static boolean org.apache.rocketmq.broker.metrics.BrokerMetricsManager:isRetryOrDlqTopic (
  java.lang.String topic) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(topic)) = level(topic) ⊔ obj_level(topic);
}