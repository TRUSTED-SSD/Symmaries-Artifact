void org.apache.rocketmq.broker.offset.BroadcastOffsetManager:run()
{
boolean $stack1;
  java.time.Duration $stack2;
  long $stack3;
  label0:$stack1 = this.isStopped();
 if ($stack1!=0) goto label1;
 $stack2 = java.time.Duration.ofSeconds(5L);
 $stack3 = $stack2.toMillis();
 this.waitForRunning($stack3);
 goto label0;
 label1:return;
 } 

