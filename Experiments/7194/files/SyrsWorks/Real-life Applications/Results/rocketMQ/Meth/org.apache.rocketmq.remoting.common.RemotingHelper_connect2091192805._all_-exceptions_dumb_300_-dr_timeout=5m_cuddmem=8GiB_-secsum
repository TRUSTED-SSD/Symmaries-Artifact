static java.nio.channels.SocketChannel org.apache.rocketmq.remoting.common.RemotingHelper:connect (
  java.net.SocketAddress remote) {
  guard = tt;
  ret(level()) = level(remote) ⊔ obj_level(remote);
  ret(obj_level()) = level(remote) ⊔ obj_level(remote);
  ret(obj_level(remote)) = obj_level(remote);
}