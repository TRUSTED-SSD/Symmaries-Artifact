static java.lang.ClassLoader org.apache.rocketmq.common.utils.ServiceProvider:getClassLoader (
  java.lang.Class clazz) {
  guard = tt;
  ret(level()) = obj_level(clazz);
  ret(obj_level()) = obj_level(clazz);
  ret(obj_level(clazz)) = obj_level(clazz);
}