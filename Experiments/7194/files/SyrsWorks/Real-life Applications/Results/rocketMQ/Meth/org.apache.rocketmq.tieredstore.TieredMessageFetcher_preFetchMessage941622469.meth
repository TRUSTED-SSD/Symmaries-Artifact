void org.apache.rocketmq.tieredstore.TieredMessageFetcher:preFetchMessage(org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer container,java.lang.String group,int maxMsgNums,long nextBeginOffset)
{
org.apache.rocketmq.common.message.MessageQueue mq;
  int prefetchBatchSize;
  org.apache.rocketmq.tieredstore.common.InflightRequestFuture inflightRequest;
  org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer l9;
  long maxOffsetOfLastRequest;
  boolean lastRequestIsExpired;
  int flag;
  int concurrency;
  int requestBatchSize;
  java.util.ArrayList futureList;
  long nextQueueOffset;
  int firstBatchSize;
  java.util.concurrent.CompletableFuture future;
  java.lang.Throwable l26;
  org.apache.rocketmq.tieredstore.common.TieredMessageStoreConfig $stack30;
  java.lang.Object $stack40;
  java.lang.Long $stack41;
  org.apache.rocketmq.tieredstore.common.SelectMappedBufferResultWrapper $stack43;
  java.lang.Object[] $stack45;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack46;
  java.lang.Boolean $stack53;
  java.lang.Integer $stack55;
  int $stack67;
  long $stack68;
  byte $stack69;
  org.apache.commons.lang3.tuple.Pair $stack92;
  java.util.ArrayList tmp$112462960;
  if (maxMsgNums==1) goto label0;
 prefetchBatchSize = container.getReadAheadFactor();
 if (prefetchBatchSize!=1) goto label1;
 label0:return;
 label1:mq = container.getMessageQueue();
 prefetchBatchSize = container.getReadAheadFactor();
 prefetchBatchSize=(maxMsgNums * prefetchBatchSize);
 $stack30=this.storeConfig;
 flag = $stack30.getReadAheadMessageCountThreshold();
 prefetchBatchSize = java.lang.Math.min(prefetchBatchSize,flag);
 inflightRequest = container.getInflightRequest(group,nextBeginOffset,prefetchBatchSize);
 lastRequestIsExpired = inflightRequest.isAllDone();
 if (lastRequestIsExpired!=0) goto label2;
 return;
 label2:l9=container;
 monitorenter container;
 label3:inflightRequest = container.getInflightRequest(nextBeginOffset,maxMsgNums);
 lastRequestIsExpired = inflightRequest.isAllDone();
 if (lastRequestIsExpired!=0) goto label4;
 monitorexit container;
 label5:return;
 label4:future = inflightRequest.getLastFuture();
 $stack40 = future.join();
 $stack41=(java.lang.Long) $stack40;
 maxOffsetOfLastRequest = $stack41.longValue();
 $stack43 = this.getMessageFromCache(container,nextBeginOffset);
 if ($stack43!=null) goto label6;
 lastRequestIsExpired=1;
 goto label7;
 label6:lastRequestIsExpired=0;
 label7:nextQueueOffset=(maxOffsetOfLastRequest-nextBeginOffset);
 $stack46=org.apache.rocketmq.tieredstore.TieredMessageFetcher.LOGGER;
 $stack45=new java.lang.Object[5];
 $stack45[0]=group;
 $stack41 = java.lang.Long.valueOf(nextBeginOffset);
 $stack45[1]=$stack41;
 $stack41 = java.lang.Long.valueOf(maxOffsetOfLastRequest);
 $stack45[2]=$stack41;
 $stack53 = java.lang.Boolean.valueOf(lastRequestIsExpired);
 $stack45[3]=$stack53;
 prefetchBatchSize=(int) nextQueueOffset;
 $stack55 = java.lang.Integer.valueOf(prefetchBatchSize);
 $stack45[4]=$stack55;
 $stack46.debug("TieredMessageFetcher#preFetchMessage: group={}, nextBeginOffset={}, maxOffsetOfLastRequest={}, lastRequestIsExpired={}, cacheRemainCount={}",$stack45);
 if (lastRequestIsExpired!=0) goto label8;
 $stack69=(maxOffsetOfLastRequest cmp -1L);
 if ($stack69==0) goto label9;
 nextQueueOffset = inflightRequest.getStartOffset();
 $stack69=(nextBeginOffset cmp nextQueueOffset);
 if ($stack69<0) goto label9;
 label8:if (lastRequestIsExpired==0) goto label10;
 maxOffsetOfLastRequest=nextBeginOffset;
 container.decreaseReadAheadFactor();
 goto label11;
 label10:maxOffsetOfLastRequest=(maxOffsetOfLastRequest+1L);
 container.increaseReadAheadFactor();
 label11:prefetchBatchSize = container.getReadAheadFactor();
 $stack30=this.storeConfig;
 flag = $stack30.getReadAheadMessageCountThreshold();
 flag=(flag/maxMsgNums);
 prefetchBatchSize = java.lang.Math.min(prefetchBatchSize,flag);
 flag=0;
 concurrency=1;
 $stack30=this.storeConfig;
 requestBatchSize = $stack30.getReadAheadBatchSizeFactorThreshold();
 if (prefetchBatchSize<=requestBatchSize) goto label12;
 $stack30=this.storeConfig;
 flag = $stack30.getReadAheadBatchSizeFactorThreshold();
 flag=(prefetchBatchSize%flag);
 if (flag!=0) goto label13;
 flag=0;
 goto label14;
 label13:flag=1;
 label14:$stack30=this.storeConfig;
 concurrency = $stack30.getReadAheadBatchSizeFactorThreshold();
 concurrency=(prefetchBatchSize/concurrency);
 concurrency=(concurrency+flag);
 label12:$stack30=this.storeConfig;
 requestBatchSize = $stack30.getReadAheadBatchSizeFactorThreshold();
 requestBatchSize = java.lang.Math.min(prefetchBatchSize,requestBatchSize);
 requestBatchSize=(maxMsgNums * requestBatchSize);
 tmp$112462960=new java.util.ArrayList;
 futureList=tmp$112462960;
 tmp$112462960#java.util.ArrayList();
 futureList=tmp$112462960;
 nextQueueOffset=maxOffsetOfLastRequest;
 if (flag!=1) goto label15;
 $stack30=this.storeConfig;
 firstBatchSize = $stack30.getReadAheadBatchSizeFactorThreshold();
 firstBatchSize=(prefetchBatchSize%firstBatchSize);
 firstBatchSize=(firstBatchSize * maxMsgNums);
 future = this#org.apache.rocketmq.tieredstore.TieredMessageFetcher:prefetchAndPutMsgToCache(container,mq,maxOffsetOfLastRequest,firstBatchSize);
 $stack55 = java.lang.Integer.valueOf(firstBatchSize);
 $stack92 = org.apache.commons.lang3.tuple.Pair.of((java.lang.Object)$stack55,(java.lang.Object)future);
 tmp$112462960.add((java.lang.Object)$stack92);
 $stack68=(long) firstBatchSize;
 nextQueueOffset=(maxOffsetOfLastRequest+$stack68);
 label15:firstBatchSize=(int) 0L;
 label16:$stack67=(concurrency-flag);
 $stack68=(long) $stack67;
 $stack69=(firstBatchSize cmp $stack68);
 if ($stack69>=0) goto label17;
 $stack68=(long) requestBatchSize;
 $stack67=(int) $stack68;
 $stack67=(firstBatchSize * $stack67);
 $stack68=(nextQueueOffset+$stack67);
 future = this#org.apache.rocketmq.tieredstore.TieredMessageFetcher:prefetchAndPutMsgToCache(container,mq,$stack68,requestBatchSize);
 $stack55 = java.lang.Integer.valueOf(requestBatchSize);
 $stack92 = org.apache.commons.lang3.tuple.Pair.of((java.lang.Object)$stack55,(java.lang.Object)future);
 tmp$112462960.add((java.lang.Object)$stack92);
 $stack67=(int) 1L;
 firstBatchSize=(firstBatchSize+$stack67);
 goto label16;
 label17:maxMsgNums=(maxMsgNums * prefetchBatchSize);
 container.putInflightRequest(group,maxOffsetOfLastRequest,maxMsgNums,(java.util.List)tmp$112462960);
 $stack46=org.apache.rocketmq.tieredstore.TieredMessageFetcher.LOGGER;
 $stack45=new java.lang.Object[6];
 $stack41 = java.lang.Long.valueOf(nextBeginOffset);
 $stack45[0]=$stack41;
 $stack41 = java.lang.Long.valueOf(maxOffsetOfLastRequest);
 $stack45[1]=$stack41;
 $stack55 = java.lang.Integer.valueOf(prefetchBatchSize);
 $stack45[2]=$stack55;
 $stack55 = java.lang.Integer.valueOf(flag);
 $stack45[3]=$stack55;
 $stack55 = java.lang.Integer.valueOf(requestBatchSize);
 $stack45[4]=$stack55;
 $stack55 = java.lang.Integer.valueOf(concurrency);
 $stack45[5]=$stack55;
 $stack46.debug("TieredMessageFetcher#preFetchMessage: try to prefetch messages for later requests: next begin offset: {}, request offset: {}, factor: {}, flag: {}, request batch: {}, concurrency: {}",$stack45);
 label9:monitorexit container;
 label18:goto label19;
 label20:l26 = catch;
 monitorexit container;
 label21:throw l26;
 label19:return;
 catch java.lang.Throwable: label3 - label5 : label20;
catch java.lang.Throwable: label4 - label18 : label20;
catch java.lang.Throwable: label20 - label21 : label20;
} 

