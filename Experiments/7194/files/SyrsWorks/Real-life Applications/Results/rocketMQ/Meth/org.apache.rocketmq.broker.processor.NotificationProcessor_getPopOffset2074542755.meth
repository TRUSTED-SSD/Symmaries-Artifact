long org.apache.rocketmq.broker.processor.NotificationProcessor:getPopOffset(java.lang.String topic,java.lang.String cid,int queueId)
{
long offset;
  long bufferOffset;
  org.apache.rocketmq.broker.BrokerController $stack8;
  org.apache.rocketmq.broker.offset.ConsumerOffsetManager $stack9;
  byte $stack11;
  org.apache.rocketmq.broker.processor.PopMessageProcessor $stack13;
  org.apache.rocketmq.broker.processor.PopBufferMergeService $stack14;
  org.apache.rocketmq.store.MessageStore $stack20;
  $stack8=this.brokerController;
 $stack9 = $stack8.getConsumerOffsetManager();
 offset = $stack9.queryOffset(cid,topic,queueId);
 $stack11=(offset cmp 0L);
 if ($stack11>=0) goto label0;
 $stack8=this.brokerController;
 $stack20 = $stack8.getMessageStore();
 offset = $stack20.getMinOffsetInQueue(topic,queueId);
 label0:$stack8=this.brokerController;
 $stack13 = $stack8.getPopMessageProcessor();
 $stack14 = $stack13.getPopBufferMergeService();
 bufferOffset = $stack14.getLatestOffset(topic,cid,queueId);
 $stack11=(bufferOffset cmp 0L);
 if ($stack11>=0) goto label1;
 return  offset;
 label1:$stack11=(bufferOffset cmp offset);
 if ($stack11<=0) goto label2;
 offset=bufferOffset;
 goto label2;
 label2:return  offset;
 } 

