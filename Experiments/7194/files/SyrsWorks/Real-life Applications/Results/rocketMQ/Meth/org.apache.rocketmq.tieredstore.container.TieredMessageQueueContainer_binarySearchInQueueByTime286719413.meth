long org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:binarySearchInQueueByTime(long timestamp,org.apache.rocketmq.tieredstore.common.BoundaryType boundaryType)
{
org.apache.commons.lang3.tuple.Pair pair;
  long minQueueOffset;
  long maxQueueOffset;
  long low;
  long high;
  long offset;
  long storeTime;
  java.nio.ByteBuffer message;
  long midOffset;
  long targetOffset;
  long leftOffset;
  long rightOffset;
  org.apache.rocketmq.tieredstore.container.TieredConsumeQueue $stack30;
  java.lang.Object $stack32;
  java.lang.Long $stack33;
  byte $stack38;
  java.util.concurrent.CompletableFuture $stack40;
  int[] $stack50;
  int $stack51;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack54;
  $stack30=this.consumeQueue;
 pair = $stack30.getQueueOffsetInFileByTime(timestamp,boundaryType);
 $stack32 = pair.getLeft();
 $stack33=(java.lang.Long) $stack32;
 minQueueOffset = $stack33.longValue();
 $stack32 = pair.getRight();
 $stack33=(java.lang.Long) $stack32;
 maxQueueOffset = $stack33.longValue();
 $stack38=(maxQueueOffset cmp -1L);
 if ($stack38==0) goto label0;
 $stack38=(maxQueueOffset cmp minQueueOffset);
 if ($stack38>=0) goto label1;
 label0:return  -1L;
 label1:low=minQueueOffset;
 high=maxQueueOffset;
 offset=0L;
 $stack40 = this.getMessageAsync(maxQueueOffset);
 $stack32 = $stack40.join();
 message=(java.nio.ByteBuffer) $stack32;
 storeTime = org.apache.rocketmq.tieredstore.util.MessageBufferUtil.getStoreTimeStamp(message);
 $stack38=(storeTime cmp timestamp);
 if ($stack38>=0) goto label2;
 $stack50=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer$1.$SwitchMap$org$apache$rocketmq$tieredstore$common$BoundaryType;
 $stack51 = boundaryType.ordinal();
 $stack51=$stack50[$stack51];
 switch [$stack51]
if 0 goto label3;
if 1 goto label4;
goto label5;
 label3:timestamp=(maxQueueOffset+1L);
 return  timestamp;
 label4:return  maxQueueOffset;
 label5:$stack54=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer.LOGGER;
 $stack54.warn("TieredMessageQueueContainer#getQueueOffsetByTime: unknown boundary boundaryType");
 label2:$stack40 = this.getMessageAsync(minQueueOffset);
 $stack32 = $stack40.join();
 message=(java.nio.ByteBuffer) $stack32;
 storeTime = org.apache.rocketmq.tieredstore.util.MessageBufferUtil.getStoreTimeStamp(message);
 $stack38=(storeTime cmp timestamp);
 if ($stack38<=0) goto label6;
 $stack50=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer$1.$SwitchMap$org$apache$rocketmq$tieredstore$common$BoundaryType;
 $stack51 = boundaryType.ordinal();
 $stack51=$stack50[$stack51];
 switch [$stack51]
if 0 goto label7;
if 1 goto label8;
goto label9;
 label7:return  minQueueOffset;
 label8:return  0L;
 label9:$stack54=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer.LOGGER;
 $stack54.warn("TieredMessageQueueContainer#getQueueOffsetByTime: unknown boundary boundaryType");
 label6:midOffset=-1L;
 targetOffset=-1L;
 leftOffset=-1L;
 rightOffset=-1L;
 label10:$stack38=(high cmp low);
 if ($stack38<0) goto label11;
 storeTime=(low+high);
 midOffset=(storeTime/2L);
 $stack40 = this.getMessageAsync(midOffset);
 $stack32 = $stack40.join();
 message=(java.nio.ByteBuffer) $stack32;
 storeTime = org.apache.rocketmq.tieredstore.util.MessageBufferUtil.getStoreTimeStamp(message);
 $stack38=(storeTime cmp timestamp);
 if ($stack38!=0) goto label12;
 targetOffset=midOffset;
 goto label11;
 label12:$stack38=(storeTime cmp timestamp);
 if ($stack38<=0) goto label13;
 high=(midOffset-1L);
 rightOffset=midOffset;
 goto label10;
 label13:low=(midOffset+1L);
 leftOffset=midOffset;
 goto label10;
 label11:$stack38=(targetOffset cmp -1L);
 if ($stack38==0) goto label14;
 offset=targetOffset;
 low=targetOffset;
 $stack50=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer$1.$SwitchMap$org$apache$rocketmq$tieredstore$common$BoundaryType;
 $stack51 = boundaryType.ordinal();
 $stack51=$stack50[$stack51];
 switch [$stack51]
if 0 goto label15;
if 1 goto label16;
goto label17;
 label15:offset=(low-1L);
 $stack38=(offset cmp minQueueOffset);
 if ($stack38>=0) goto label18;
 goto label19;
 label18:$stack40 = this.getMessageAsync(offset);
 $stack32 = $stack40.join();
 message=(java.nio.ByteBuffer) $stack32;
 storeTime = org.apache.rocketmq.tieredstore.util.MessageBufferUtil.getStoreTimeStamp(message);
 $stack38=(storeTime cmp timestamp);
 if ($stack38!=0) goto label19;
 low=offset;
 goto label15;
 label19:offset=low;
 goto label20;
 label16:offset=(low+1L);
 $stack38=(offset cmp maxQueueOffset);
 if ($stack38<=0) goto label21;
 goto label22;
 label21:$stack40 = this.getMessageAsync(offset);
 $stack32 = $stack40.join();
 message=(java.nio.ByteBuffer) $stack32;
 storeTime = org.apache.rocketmq.tieredstore.util.MessageBufferUtil.getStoreTimeStamp(message);
 $stack38=(storeTime cmp timestamp);
 if ($stack38!=0) goto label22;
 low=offset;
 goto label16;
 label22:offset=low;
 goto label20;
 label17:$stack54=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer.LOGGER;
 $stack54.warn("TieredMessageQueueContainer#getQueueOffsetByTime: unknown boundary boundaryType");
 label20:goto label23;
 label14:$stack50=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer$1.$SwitchMap$org$apache$rocketmq$tieredstore$common$BoundaryType;
 $stack51 = boundaryType.ordinal();
 $stack51=$stack50[$stack51];
 switch [$stack51]
if 0 goto label24;
if 1 goto label25;
goto label26;
 label24:offset=rightOffset;
 goto label23;
 label25:offset=leftOffset;
 goto label23;
 label26:$stack54=org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer.LOGGER;
 $stack54.warn("TieredMessageQueueContainer#getQueueOffsetByTime: unknown boundary boundaryType");
 label23:return  offset;
 } 

