boolean org.apache.rocketmq.common.CountDownLatch2:await(long timeout,java.util.concurrent.TimeUnit unit)
{
org.apache.rocketmq.common.CountDownLatch2$Sync $stack4;
  boolean $stack7;
  $stack4=this.sync;
 timeout = unit.toNanos(timeout);
 $stack7 = $stack4.tryAcquireSharedNanos(1,timeout);
 return  $stack7;
 } 

