static int org.apache.rocketmq.proxy.grpc.interceptor.RequestMapping:map (
  java.lang.String rpcFullName) {
  guard = tt;
  ret(level()) = level(rpcFullName) ⊔ obj_level(rpcFullName);
  ret(obj_level(rpcFullName)) = level(rpcFullName) ⊔ obj_level(rpcFullName);
}