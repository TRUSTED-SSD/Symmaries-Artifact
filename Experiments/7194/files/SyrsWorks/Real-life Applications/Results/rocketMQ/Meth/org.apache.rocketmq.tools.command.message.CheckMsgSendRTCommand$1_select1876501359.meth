org.apache.rocketmq.common.message.MessageQueue org.apache.rocketmq.tools.command.message.CheckMsgSendRTCommand$1:select(java.util.List mqs,org.apache.rocketmq.common.message.Message msg,java.lang.Object @arg)
{
int queueIndex;
  org.apache.rocketmq.common.message.MessageQueue queue;
  java.lang.Integer $stack6;
  int $stack8;
  java.lang.String $stack10;
  java.lang.Object arg;
arg=@arg;
$stack6=(java.lang.Integer) arg;
 queueIndex = $stack6.intValue();
 $stack8 = mqs.size();
 queueIndex=(queueIndex%$stack8);
 arg = mqs.get(queueIndex);
 queue=(org.apache.rocketmq.common.message.MessageQueue) arg;
 $stack10 = queue.getBrokerName();
 org.apache.rocketmq.tools.command.message.CheckMsgSendRTCommand.access$002($stack10);
 queueIndex = queue.getQueueId();
 org.apache.rocketmq.tools.command.message.CheckMsgSendRTCommand.access$102(queueIndex);
 return  queue;
 } 

