void org.apache.rocketmq.store.kv.CompactionLog$OffsetMap:hashInto(java.lang.String key,byte[] buf)
{
java.nio.charset.Charset $stack3;
  java.security.MessageDigest $stack4;
  byte[] $stack5;
  int $stack6;
  $stack4=this.digest;
 $stack3=java.nio.charset.StandardCharsets.UTF_8;
 $stack5 = key.getBytes($stack3);
 $stack4.update($stack5);
 $stack4=this.digest;
 $stack6=this.hashSize;
 $stack4.digest(buf,0,$stack6);
 return;
 } 

