void org.apache.rocketmq.broker.latency.BrokerFastFailure:cleanExpiredRequestInQueue (
  java.util.concurrent.BlockingQueue blockingQueue,
  long maxWaitTimeMillsInQueue) {
  guard = tt;
  ret(obj_level(blockingQueue)) = level(blockingQueue) ⊔ obj_level(blockingQueue);
  ret(obj_level(this)) = level(blockingQueue) ⊔ obj_level(blockingQueue) ⊔ obj_level(this);
}