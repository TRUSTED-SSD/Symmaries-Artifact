void org.apache.rocketmq.store.DefaultMessageStore$ReputMessageService:notifyMessageArrive4MultiQueue(org.apache.rocketmq.store.DispatchRequest dispatchRequest)
{
java.util.Map prop;
  java.lang.String multiDispatchQueue;
  java.lang.String multiQueueOffset;
  java.lang.String[] queues;
  java.lang.String[] queueOffsets;
  int i;
  long queueOffset;
  int queueId;
  java.lang.Object $stack13;
  boolean $stack15;
  org.apache.rocketmq.store.DefaultMessageStore $stack25;
  org.apache.rocketmq.store.config.MessageStoreConfig $stack26;
  org.apache.rocketmq.store.MessageArrivingListener $stack29;
  long $stack31;
  long $stack32;
  byte[] $stack33;
  prop = dispatchRequest.getPropertiesMap();
 if (prop!=null) goto label0;
 return;
 label0:$stack13 = prop.get((java.lang.Object)"INNER_MULTI_DISPATCH");
 multiDispatchQueue=(java.lang.String) $stack13;
 $stack13 = prop.get((java.lang.Object)"INNER_MULTI_QUEUE_OFFSET");
 multiQueueOffset=(java.lang.String) $stack13;
 $stack15 = org.apache.commons.lang3.StringUtils.isBlank((java.lang.CharSequence)multiDispatchQueue);
 if ($stack15!=0) goto label1;
 $stack15 = org.apache.commons.lang3.StringUtils.isBlank((java.lang.CharSequence)multiQueueOffset);
 if ($stack15==0) goto label2;
 label1:return;
 label2:queues = multiDispatchQueue.split(",");
 queueOffsets = multiQueueOffset.split(",");
 queueId=#queues;
 i=#queueOffsets;
 if (queueId==i) goto label3;
 return;
 label3:i=0;
 label4:queueId=#queues;
 if (i>=queueId) goto label5;
 multiDispatchQueue=queues[i];
 multiQueueOffset=queueOffsets[i];
 queueOffset = java.lang.Long.parseLong(multiQueueOffset);
 queueId = dispatchRequest.getQueueId();
 $stack25=this.this$0;
 $stack26 = $stack25.getMessageStoreConfig();
 $stack15 = $stack26.isEnableLmq();
 if ($stack15==0) goto label6;
 $stack15 = org.apache.rocketmq.common.MixAll.isLmq(multiDispatchQueue);
 if ($stack15==0) goto label6;
 queueId=0;
 label6:$stack25=this.this$0;
 $stack29 = org.apache.rocketmq.store.DefaultMessageStore.access$1200($stack25);
 queueOffset=(queueOffset+1L);
 $stack31 = dispatchRequest.getTagsCode();
 $stack32 = dispatchRequest.getStoreTimestamp();
 $stack33 = dispatchRequest.getBitMap();
 prop = dispatchRequest.getPropertiesMap();
 $stack29.arriving(multiDispatchQueue,queueId,queueOffset,$stack31,$stack32,$stack33,prop);
 i=(i+1);
 goto label4;
 label5:return;
 } 

