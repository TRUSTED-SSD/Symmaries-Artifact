static java.lang.String org.apache.rocketmq.common.UtilAll:offset2FileName(long offset)
{
java.text.NumberFormat nf;
  java.lang.String $stack4;
  nf = java.text.NumberFormat.getInstance();
 nf.setMinimumIntegerDigits(20);
 nf.setMaximumFractionDigits(0);
 nf.setGroupingUsed((boolean)0);
 $stack4 = nf.format(offset);
 return  $stack4;
 } 

