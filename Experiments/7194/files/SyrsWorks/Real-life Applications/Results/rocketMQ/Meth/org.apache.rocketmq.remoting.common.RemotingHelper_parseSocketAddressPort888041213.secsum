static int org.apache.rocketmq.remoting.common.RemotingHelper:parseSocketAddressPort (
  java.net.SocketAddress socketAddress) {
  guard = tt;
  ret(level()) = pc ⊔ level(socketAddress) ⊔ obj_level(socketAddress);
  ret(obj_level(socketAddress)) = obj_level(socketAddress);
}