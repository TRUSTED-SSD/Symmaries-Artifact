boolean org.apache.rocketmq.filter.util.BitsArray:getBit(int bitPos)
{
byte[] $stack2;
  int $stack3;
  byte $stack4;
  boolean $stack7;
  this.checkBitPosition(bitPos,this);
 $stack2=this.bytes;
 $stack3 = this.subscript(bitPos);
 $stack4=$stack2[$stack3];
 bitPos = this.position(bitPos);
 bitPos=($stack4 & bitPos);
 if (bitPos==0) goto label0;
 $stack7=1;
 goto label1;
 label0:$stack7=0;
 label1:return  $stack7;
 } 

