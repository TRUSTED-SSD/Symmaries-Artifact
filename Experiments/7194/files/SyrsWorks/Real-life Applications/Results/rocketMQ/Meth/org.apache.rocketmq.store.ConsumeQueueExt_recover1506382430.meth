void org.apache.rocketmq.store.ConsumeQueueExt:recover()
{
java.util.List mappedFiles;
  int index;
  org.apache.rocketmq.store.logfile.MappedFile mappedFile;
  java.nio.ByteBuffer byteBuffer;
  long processOffset;
  long mappedFileOffset;
  org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit extUnit;
  org.apache.rocketmq.store.MappedFileQueue $stack10;
  boolean $stack12;
  java.lang.Object $stack13;
  short $stack17;
  int $stack18;
  java.lang.StringBuilder $stack19;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack20;
  java.lang.String $stack22;
  long $stack41;
  $stack10=this.mappedFileQueue;
 mappedFiles = $stack10.getMappedFiles();
 if (mappedFiles==null) goto label0;
 $stack12 = mappedFiles.isEmpty();
 if ($stack12==0) goto label1;
 label0:return;
 label1:index=0;
 $stack13 = mappedFiles.get(0);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack13;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 extUnit=new org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit;
 extUnit#org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit();
 label2:org.apache.rocketmq.store.ConsumeQueueExt$CqExtUnit.access$300(extUnit,byteBuffer);
 $stack17 = extUnit.getSize();
 if ($stack17<=0) goto label3;
 $stack17 = extUnit.getSize();
 $stack41=(long) $stack17;
 mappedFileOffset=(mappedFileOffset+$stack41);
 goto label2;
 label3:index=(index+1);
 $stack18 = mappedFiles.size();
 if (index>=$stack18) goto label4;
 $stack13 = mappedFiles.get(index);
 mappedFile=(org.apache.rocketmq.store.logfile.MappedFile) $stack13;
 byteBuffer = mappedFile.sliceByteBuffer();
 processOffset = mappedFile.getFileFromOffset();
 mappedFileOffset=0L;
 $stack20=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack19=new java.lang.StringBuilder;
 $stack19#java.lang.StringBuilder();
 $stack19 = $stack19.append("Recover next consume queue extend file, ");
 $stack22 = mappedFile.getFileName();
 $stack19 = $stack19.append($stack22);
 $stack22 = $stack19.toString();
 $stack20.info($stack22);
 goto label2;
 label4:$stack20=org.apache.rocketmq.store.ConsumeQueueExt.log;
 $stack19=new java.lang.StringBuilder;
 $stack19#java.lang.StringBuilder();
 $stack19 = $stack19.append("All files of consume queue extend has been recovered over, last mapped file ");
 $stack22 = mappedFile.getFileName();
 $stack19 = $stack19.append($stack22);
 $stack22 = $stack19.toString();
 $stack20.info($stack22);
 goto label5;
 label5:processOffset=(processOffset+mappedFileOffset);
 $stack10=this.mappedFileQueue;
 $stack10.setFlushedWhere(processOffset);
 $stack10=this.mappedFileQueue;
 $stack10.setCommittedWhere(processOffset);
 $stack10=this.mappedFileQueue;
 $stack10.truncateDirtyFiles(processOffset);
 return;
 } 

