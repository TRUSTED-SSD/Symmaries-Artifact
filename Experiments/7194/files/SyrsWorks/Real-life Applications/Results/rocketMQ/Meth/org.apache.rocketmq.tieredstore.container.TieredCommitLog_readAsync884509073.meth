java.util.concurrent.CompletableFuture org.apache.rocketmq.tieredstore.container.TieredCommitLog:readAsync(long offset,int length)
{
org.apache.rocketmq.tieredstore.container.TieredFileQueue $stack4;
  java.util.concurrent.CompletableFuture $stack5;
  $stack4=this.fileQueue;
 $stack5 = $stack4.readAsync(offset,length);
 return  $stack5;
 } 

