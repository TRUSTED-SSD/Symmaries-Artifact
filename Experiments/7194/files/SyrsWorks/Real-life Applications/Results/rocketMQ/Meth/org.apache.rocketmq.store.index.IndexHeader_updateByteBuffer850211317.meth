void org.apache.rocketmq.store.index.IndexHeader:updateByteBuffer()
{
java.util.concurrent.atomic.AtomicLong $stack1;
  java.nio.ByteBuffer $stack2;
  int $stack3;
  long $stack4;
  java.util.concurrent.atomic.AtomicInteger $stack21;
  int $stack24;
  $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.beginTimestampIndex;
 $stack1=this.beginTimestamp;
 $stack4 = $stack1.get();
 $stack2.putLong($stack3,$stack4);
 $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.endTimestampIndex;
 $stack1=this.endTimestamp;
 $stack4 = $stack1.get();
 $stack2.putLong($stack3,$stack4);
 $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.beginPhyoffsetIndex;
 $stack1=this.beginPhyOffset;
 $stack4 = $stack1.get();
 $stack2.putLong($stack3,$stack4);
 $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.endPhyoffsetIndex;
 $stack1=this.endPhyOffset;
 $stack4 = $stack1.get();
 $stack2.putLong($stack3,$stack4);
 $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.hashSlotcountIndex;
 $stack21=this.hashSlotCount;
 $stack24 = $stack21.get();
 $stack2.putInt($stack3,$stack24);
 $stack2=this.byteBuffer;
 $stack3=org.apache.rocketmq.store.index.IndexHeader.indexCountIndex;
 $stack21=this.indexCount;
 $stack24 = $stack21.get();
 $stack2.putInt($stack3,$stack24);
 return;
 } 

