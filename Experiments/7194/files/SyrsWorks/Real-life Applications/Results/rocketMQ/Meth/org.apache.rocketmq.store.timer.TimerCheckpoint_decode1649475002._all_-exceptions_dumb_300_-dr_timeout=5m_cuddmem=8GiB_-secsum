static org.apache.rocketmq.store.timer.TimerCheckpoint org.apache.rocketmq.store.timer.TimerCheckpoint:decode (
  java.nio.ByteBuffer byteBuffer) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = obj_level(byteBuffer);
  ret(obj_level(byteBuffer)) = obj_level(byteBuffer);
}