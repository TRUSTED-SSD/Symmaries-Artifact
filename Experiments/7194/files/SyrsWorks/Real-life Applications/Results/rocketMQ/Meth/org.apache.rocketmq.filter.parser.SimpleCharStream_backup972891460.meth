void org.apache.rocketmq.filter.parser.SimpleCharStream:backup(int amount)
{
int $stack2;
  $stack2=this.inBuf;
 $stack2=($stack2+amount);
 this.inBuf=$stack2;
 $stack2=this.bufpos;
 amount=($stack2-amount);
 this.bufpos=amount;
 if (amount>=0) goto label0;
 $stack2=this.bufpos;
 amount=this.bufsize;
 amount=($stack2+amount);
 this.bufpos=amount;
 label0:return;
 } 

