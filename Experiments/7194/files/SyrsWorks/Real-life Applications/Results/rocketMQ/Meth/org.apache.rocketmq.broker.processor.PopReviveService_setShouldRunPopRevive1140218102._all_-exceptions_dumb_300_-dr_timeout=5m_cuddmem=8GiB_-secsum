void org.apache.rocketmq.broker.processor.PopReviveService:setShouldRunPopRevive (
  boolean shouldRunPopRevive) {
  guard = tt;
  ret(obj_level(this)) = level(shouldRunPopRevive) ⊔ obj_level(this);
}