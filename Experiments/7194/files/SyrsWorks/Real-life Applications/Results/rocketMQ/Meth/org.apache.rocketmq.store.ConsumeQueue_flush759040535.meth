boolean org.apache.rocketmq.store.ConsumeQueue:flush(int flushLeastPages)
{
boolean result;
  org.apache.rocketmq.store.MappedFileQueue $stack3;
  boolean $stack5;
  org.apache.rocketmq.store.ConsumeQueueExt $stack6;
  $stack3=this.mappedFileQueue;
 result = $stack3.flush(flushLeastPages);
 $stack5 = this.isExtReadEnable();
 if ($stack5==0) goto label0;
 $stack6=this.consumeQueueExt;
 $stack5 = $stack6.flush(flushLeastPages);
 result=(result & $stack5);
 label0:return  result;
 } 

