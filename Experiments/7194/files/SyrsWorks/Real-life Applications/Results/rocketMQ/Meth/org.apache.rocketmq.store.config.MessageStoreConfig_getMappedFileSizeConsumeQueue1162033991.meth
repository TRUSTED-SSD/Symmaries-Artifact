int org.apache.rocketmq.store.config.MessageStoreConfig:getMappedFileSizeConsumeQueue()
{
int factor;
  double $stack3;
  factor=this.mappedFileSizeConsumeQueue;
 $stack3=(double) factor;
 $stack3=($stack3/20.0);
 $stack3 = java.lang.Math.ceil($stack3);
 factor=(int) $stack3;
 factor=(factor * 20);
 return  factor;
 } 

