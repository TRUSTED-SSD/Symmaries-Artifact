static java.lang.String org.apache.rocketmq.remoting.protocol.filter.FilterAPI:simpleClassName(java.lang.String className)
{
java.lang.String simple;
  int index;
  simple=className;
 index = className.lastIndexOf(".");
 if (index<0) goto label0;
 index=(index+1);
 simple = className.substring(index);
 label0:return  simple;
 } 

