void org.apache.rocketmq.store.queue.BatchConsumeQueue:swapMap(int reserveNum,long forceSwapIntervalMs,long normalSwapIntervalMs)
{
org.apache.rocketmq.store.MappedFileQueue $stack6;
  $stack6=this.mappedFileQueue;
 $stack6.swapMap(reserveNum,forceSwapIntervalMs,normalSwapIntervalMs);
 return;
 } 

