void org.apache.rocketmq.client.consumer.DefaultLitePullConsumer:setAutoCommitIntervalMillis(long autoCommitIntervalMillis)
{
byte $stack3;
  $stack3=(autoCommitIntervalMillis cmp 1000L);
 if ($stack3<0) goto label0;
 this.autoCommitIntervalMillis=autoCommitIntervalMillis;
 label0:return;
 } 

