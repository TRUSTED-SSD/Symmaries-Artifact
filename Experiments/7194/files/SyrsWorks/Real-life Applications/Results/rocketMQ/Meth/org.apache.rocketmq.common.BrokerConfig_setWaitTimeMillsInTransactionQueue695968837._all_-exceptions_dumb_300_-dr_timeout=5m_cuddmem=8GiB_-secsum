void org.apache.rocketmq.common.BrokerConfig:setWaitTimeMillsInTransactionQueue (
  long waitTimeMillsInTransactionQueue) {
  guard = tt;
  ret(obj_level(this)) = level(waitTimeMillsInTransactionQueue) ⊔ obj_level(this);
}