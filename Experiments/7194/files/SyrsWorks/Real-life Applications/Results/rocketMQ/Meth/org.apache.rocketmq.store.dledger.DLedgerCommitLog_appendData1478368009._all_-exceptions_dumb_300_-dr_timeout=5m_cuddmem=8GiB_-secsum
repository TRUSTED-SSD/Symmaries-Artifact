boolean org.apache.rocketmq.store.dledger.DLedgerCommitLog:appendData (
  long startOffset, byte[] data, int dataStart, int dataLength) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level(data)) = obj_level(data);
  ret(obj_level(this)) = obj_level(this);
}