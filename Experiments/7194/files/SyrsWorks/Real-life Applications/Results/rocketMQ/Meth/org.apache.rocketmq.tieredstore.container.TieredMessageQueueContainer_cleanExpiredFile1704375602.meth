void org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:cleanExpiredFile(long expireTimestamp)
{
org.apache.rocketmq.tieredstore.container.TieredCommitLog $stack3;
  org.apache.rocketmq.tieredstore.container.TieredConsumeQueue $stack4;
  $stack3=this.commitLog;
 $stack3.cleanExpiredFile(expireTimestamp);
 $stack4=this.consumeQueue;
 $stack4.cleanExpiredFile(expireTimestamp);
 return;
 } 

