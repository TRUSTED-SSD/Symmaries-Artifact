void org.apache.rocketmq.store.timer.TimerMessageStore:calcTimerDistribution()
{
long startTime;
  java.util.List timerDist;
  long currTime;
  int i;
  int slotTotalNum;
  int periodTotal;
  int j;
  org.apache.rocketmq.store.timer.Slot slotEach;
  org.apache.rocketmq.store.timer.TimerMetrics $stack13;
  long $stack17;
  org.apache.rocketmq.logging.org.slf4j.Logger $stack24;
  java.lang.Long $stack26;
  java.lang.Object $stack28;
  java.lang.Integer $stack29;
  int $stack49;
  org.apache.rocketmq.store.timer.TimerWheel $stack53;
  startTime = java.lang.System.currentTimeMillis();
 $stack13=this.timerMetrics;
 timerDist = $stack13.getTimerDistList();
 currTime = java.lang.System.currentTimeMillis();
 i=this.precisionMs;
 $stack17=(long) i;
 $stack17=(currTime/$stack17);
 i=this.precisionMs;
 currTime=(long) i;
 currTime=($stack17 * currTime);
 i=0;
 label0:periodTotal = timerDist.size();
 if (i>=periodTotal) goto label1;
 if (i!=0) goto label2;
 j=0;
 goto label3;
 label2:periodTotal=(i-1);
 $stack28 = timerDist.get(periodTotal);
 $stack29=(java.lang.Integer) $stack28;
 periodTotal = $stack29.intValue();
 slotTotalNum=(periodTotal * 1000);
 periodTotal=this.precisionMs;
 j=(slotTotalNum/periodTotal);
 label3:$stack28 = timerDist.get(i);
 $stack29=(java.lang.Integer) $stack28;
 periodTotal = $stack29.intValue();
 slotTotalNum=(periodTotal * 1000);
 periodTotal=this.precisionMs;
 slotTotalNum=(slotTotalNum/periodTotal);
 periodTotal=0;
 label4:if (j>=slotTotalNum) goto label5;
 $stack53=this.timerWheel;
 $stack49=this.precisionMs;
 $stack49=(j * $stack49);
 $stack17=(long) $stack49;
 $stack17=(currTime+$stack17);
 slotEach = $stack53.getSlot($stack17);
 $stack49=slotEach.num;
 periodTotal=(periodTotal+$stack49);
 j=(j+1);
 goto label4;
 label5:$stack24=org.apache.rocketmq.store.timer.TimerMessageStore.LOGGER;
 $stack28 = timerDist.get(i);
 $stack29 = java.lang.Integer.valueOf(periodTotal);
 $stack24.debug("{} period\'s total num: {}",$stack28,(java.lang.Object)$stack29);
 $stack13=this.timerMetrics;
 $stack28 = timerDist.get(i);
 $stack29=(java.lang.Integer) $stack28;
 slotTotalNum = $stack29.intValue();
 $stack13.updateDistPair(slotTotalNum,periodTotal);
 i=(i+1);
 goto label0;
 label1:currTime = java.lang.System.currentTimeMillis();
 i=(int) currTime;
 $stack24=org.apache.rocketmq.store.timer.TimerMessageStore.LOGGER;
 periodTotal=(int) startTime;
 i=(i-periodTotal);
 startTime=(long) i;
 $stack26 = java.lang.Long.valueOf(startTime);
 $stack24.debug("Total cost Time: {}",(java.lang.Object)$stack26);
 return;
 } 

