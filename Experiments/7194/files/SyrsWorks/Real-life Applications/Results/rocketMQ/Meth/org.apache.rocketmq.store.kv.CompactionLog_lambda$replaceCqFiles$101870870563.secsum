static boolean org.apache.rocketmq.store.kv.CompactionLog:lambda$replaceCqFiles$10 (
  java.util.List fileListToDelete,
  org.apache.rocketmq.store.logfile.MappedFile m) {
  guard = tt;
  ret(level()) = pc ⊔ level(fileListToDelete) ⊔ obj_level(fileListToDelete) ⊔ obj_level(m);
  ret(obj_level(fileListToDelete)) = if obj_level(fileListToDelete) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(fileListToDelete) = ⊥ ∧ obj_level(m) = ⊤ then ⊤ else ⊥;
  ret(obj_level(m)) = if obj_level(m) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(fileListToDelete) = ⊤ ∧ obj_level(m) = ⊥ then ⊤ else ⊥;
}