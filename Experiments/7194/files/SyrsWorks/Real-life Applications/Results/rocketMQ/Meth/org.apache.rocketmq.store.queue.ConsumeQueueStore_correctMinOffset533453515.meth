void org.apache.rocketmq.store.queue.ConsumeQueueStore:correctMinOffset(org.apache.rocketmq.store.queue.ConsumeQueueInterface consumeQueue,long minCommitLogOffset)
{
consumeQueue.correctMinOffset(minCommitLogOffset);
 return;
 } 

