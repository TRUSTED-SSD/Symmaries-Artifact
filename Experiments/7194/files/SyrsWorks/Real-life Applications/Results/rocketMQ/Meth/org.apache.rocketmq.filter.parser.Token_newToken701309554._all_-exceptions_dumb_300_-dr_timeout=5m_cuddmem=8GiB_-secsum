static org.apache.rocketmq.filter.parser.Token org.apache.rocketmq.filter.parser.Token:newToken (
  int ofKind, java.lang.String image) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(image) ⊔ level(ofKind) ⊔ obj_level(image);
  ret(obj_level(image)) = level(image) ⊔ level(ofKind) ⊔ obj_level(image);
}