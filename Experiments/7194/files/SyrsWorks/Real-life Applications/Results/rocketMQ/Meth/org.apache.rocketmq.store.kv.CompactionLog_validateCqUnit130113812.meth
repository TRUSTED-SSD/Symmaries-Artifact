boolean org.apache.rocketmq.store.kv.CompactionLog:validateCqUnit(org.apache.rocketmq.store.queue.CqUnit cqUnit)
{
long $stack2;
  byte $stack3;
  int $stack4;
  boolean $stack5;
  short $stack8;
  $stack2 = cqUnit.getPos();
 $stack3=($stack2 cmp 0L);
 if ($stack3<0) goto label0;
 $stack4 = cqUnit.getSize();
 if ($stack4<=0) goto label0;
 $stack2 = cqUnit.getQueueOffset();
 $stack3=($stack2 cmp 0L);
 if ($stack3<0) goto label0;
 $stack8 = cqUnit.getBatchNum();
 if ($stack8<=0) goto label0;
 $stack5=1;
 goto label1;
 label0:$stack5=0;
 label1:return  $stack5;
 } 

