void org.apache.rocketmq.store.config.MessageStoreConfig:setTimerProgressLogIntervalMs (
  int timerProgressLogIntervalMs) {
  guard = tt;
  ret(obj_level(this)) = level(timerProgressLogIntervalMs) ⊔ obj_level(this);
}