static org.apache.rocketmq.common.message.MessageBatch org.apache.rocketmq.common.message.MessageBatch:generateFromList (
  java.util.Collection messages) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(messages) ⊔ obj_level(messages);
  ret(obj_level(messages)) = level(messages) ⊔ obj_level(messages);
}