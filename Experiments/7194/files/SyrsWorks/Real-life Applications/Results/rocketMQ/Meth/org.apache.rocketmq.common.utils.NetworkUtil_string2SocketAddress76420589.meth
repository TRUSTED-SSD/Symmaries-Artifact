static java.net.SocketAddress org.apache.rocketmq.common.utils.NetworkUtil:string2SocketAddress(java.lang.String @addr)
{
int split;
  java.lang.String host;
  java.net.InetSocketAddress $stack9;
  java.lang.String addr;
addr=@addr;
split = addr.lastIndexOf(":");
 host = addr.substring(0,split);
 split=(split+1);
 addr = addr.substring(split);
 $stack9=new java.net.InetSocketAddress;
 split = java.lang.Integer.parseInt(addr);
 $stack9#java.net.InetSocketAddress(host,split);
 return  $stack9;
 } 

