java.util.concurrent.CompletableFuture org.apache.rocketmq.tieredstore.container.TieredMessageQueueContainer:readConsumeQueue(long queueOffset,int count)
{
org.apache.rocketmq.tieredstore.container.TieredConsumeQueue $stack6;
  java.util.concurrent.CompletableFuture $stack7;
  $stack6=this.consumeQueue;
 queueOffset=(queueOffset * 20L);
 count=(count * 20);
 $stack7 = $stack6.readAsync(queueOffset,count);
 return  $stack7;
 } 

