long org.apache.rocketmq.store.ConsumeQueue:rollNextFile(long nextBeginOffset)
{
int mappedFileSize;
  long $stack5;
  long $stack6;
  mappedFileSize=this.mappedFileSize;
 mappedFileSize=(mappedFileSize/20);
 $stack5=(long) mappedFileSize;
 $stack5=(nextBeginOffset+$stack5);
 $stack6=(long) mappedFileSize;
 nextBeginOffset=(nextBeginOffset%$stack6);
 nextBeginOffset=($stack5-nextBeginOffset);
 return  nextBeginOffset;
 } 

