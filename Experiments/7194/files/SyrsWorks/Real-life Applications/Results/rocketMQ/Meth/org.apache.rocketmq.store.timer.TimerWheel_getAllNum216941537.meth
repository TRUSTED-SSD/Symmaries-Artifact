long org.apache.rocketmq.store.timer.TimerWheel:getAllNum(long timeStartMs)
{
int allNum;
  int firstSlotIndex;
  int i;
  int slotIndex;
  int $stack11;
  java.lang.ThreadLocal $stack14;
  java.lang.Object $stack15;
  java.nio.ByteBuffer $stack17;
  long $stack21;
  long $stack24;
  byte $stack30;
  allNum=0;
 firstSlotIndex = this.getSlotIndex(timeStartMs);
 i=0;
 label0:slotIndex=this.slotsTotal;
 slotIndex=(slotIndex * 2);
 if (i>=slotIndex) goto label1;
 slotIndex=(firstSlotIndex+i);
 $stack11=this.slotsTotal;
 $stack11=($stack11 * 2);
 slotIndex=(slotIndex%$stack11);
 $stack14=this.localBuffer;
 $stack15 = $stack14.get();
 $stack17=(java.nio.ByteBuffer) $stack15;
 slotIndex=(slotIndex * 32);
 $stack17.position(slotIndex);
 slotIndex=this.precisionMs;
 slotIndex=(i * slotIndex);
 $stack21=(long) slotIndex;
 $stack24=(timeStartMs+$stack21);
 slotIndex=this.precisionMs;
 $stack21=(long) slotIndex;
 $stack21=($stack24/$stack21);
 $stack14=this.localBuffer;
 $stack15 = $stack14.get();
 $stack17=(java.nio.ByteBuffer) $stack15;
 $stack24 = $stack17.getLong();
 $stack30=($stack21 cmp $stack24);
 if ($stack30!=0) goto label2;
 $stack14=this.localBuffer;
 $stack15 = $stack14.get();
 $stack17=(java.nio.ByteBuffer) $stack15;
 $stack17.getLong();
 $stack14=this.localBuffer;
 $stack15 = $stack14.get();
 $stack17=(java.nio.ByteBuffer) $stack15;
 $stack17.getLong();
 $stack14=this.localBuffer;
 $stack15 = $stack14.get();
 $stack17=(java.nio.ByteBuffer) $stack15;
 slotIndex = $stack17.getInt();
 allNum=(allNum+slotIndex);
 label2:i=(i+1);
 goto label0;
 label1:timeStartMs=(long) allNum;
 return  timeStartMs;
 } 

