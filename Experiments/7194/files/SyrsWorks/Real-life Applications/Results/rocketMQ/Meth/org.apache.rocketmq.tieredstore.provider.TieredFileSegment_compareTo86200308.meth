int org.apache.rocketmq.tieredstore.provider.TieredFileSegment:compareTo(org.apache.rocketmq.tieredstore.provider.TieredFileSegment o)
{
long $stack2;
  long $stack3;
  int $stack4;
  $stack3=this.baseOffset;
 $stack2=o.baseOffset;
 $stack4 = java.lang.Long.compare($stack3,$stack2);
 return  $stack4;
 } 

