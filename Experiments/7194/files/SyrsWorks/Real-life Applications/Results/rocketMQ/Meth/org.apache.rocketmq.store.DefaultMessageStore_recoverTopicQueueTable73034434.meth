void org.apache.rocketmq.store.DefaultMessageStore:recoverTopicQueueTable()
{
long minPhyOffset;
  org.apache.rocketmq.store.CommitLog $stack3;
  org.apache.rocketmq.store.queue.ConsumeQueueStore $stack5;
  $stack3=this.commitLog;
 minPhyOffset = $stack3.getMinOffset();
 $stack5=this.consumeQueueStore;
 $stack5#org.apache.rocketmq.store.queue.ConsumeQueueStore(minPhyOffset);
 return;
 } 

