long org.apache.rocketmq.store.plugin.AbstractPluginMessageStore:getCommitLogOffsetInQueue(java.lang.String topic,int queueId,long consumeQueueOffset)
{
org.apache.rocketmq.store.MessageStore $stack5;
  $stack5=this.next;
 consumeQueueOffset = $stack5.getCommitLogOffsetInQueue(topic,queueId,consumeQueueOffset);
 return  consumeQueueOffset;
 } 

