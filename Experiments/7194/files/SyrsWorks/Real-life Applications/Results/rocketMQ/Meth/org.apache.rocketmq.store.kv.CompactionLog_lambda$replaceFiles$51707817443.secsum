static boolean org.apache.rocketmq.store.kv.CompactionLog:lambda$replaceFiles$5 (
  java.util.List mappedFileList,
  org.apache.rocketmq.store.logfile.MappedFile m) {
  guard = tt;
  ret(level()) = pc ⊔ level(mappedFileList) ⊔ obj_level(m) ⊔ obj_level(mappedFileList);
  ret(obj_level(m)) = if obj_level(m) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(m) = ⊥ ∧ obj_level(mappedFileList) = ⊤ then ⊤ else ⊥;
  ret(obj_level(mappedFileList)) = if obj_level(mappedFileList) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(m) = ⊤ ∧ obj_level(mappedFileList) = ⊥ then ⊤ else ⊥;
}