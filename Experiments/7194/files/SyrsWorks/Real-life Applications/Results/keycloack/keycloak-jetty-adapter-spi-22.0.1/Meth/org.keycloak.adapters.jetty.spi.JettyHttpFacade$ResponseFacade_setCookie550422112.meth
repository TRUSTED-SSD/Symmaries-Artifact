void org.keycloak.adapters.jetty.spi.JettyHttpFacade$ResponseFacade:setCookie(java.lang.String name,java.lang.String value,java.lang.String path,java.lang.String domain,int maxAge,boolean secure,boolean httpOnly)
{
javax.servlet.http.Cookie cookie;
  org.keycloak.adapters.jetty.spi.JettyHttpFacade $stack10;
  javax.servlet.http.HttpServletResponse $stack11;
  cookie=new javax.servlet.http.Cookie;
 cookie#javax.servlet.http.Cookie(name,value);
 if (domain==null) goto label0;
 cookie.setDomain(domain);
 label0:if (path==null) goto label1;
 cookie.setPath(path);
 label1:if (secure==0) goto label2;
 cookie.setSecure((boolean)1);
 label2:if (httpOnly==0) goto label3;
 cookie.setHttpOnly(httpOnly);
 label3:cookie.setMaxAge(maxAge);
 $stack10=this.this$0;
 $stack11=$stack10.response;
 $stack11.addCookie(cookie);
 return;
 } 

