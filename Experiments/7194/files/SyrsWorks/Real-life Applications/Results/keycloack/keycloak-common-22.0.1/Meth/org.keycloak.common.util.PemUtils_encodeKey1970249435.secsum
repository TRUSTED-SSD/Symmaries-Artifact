static java.lang.String org.keycloak.common.util.PemUtils:encodeKey (
  java.security.Key key) {
  guard = tt;
  ret(level()) = pc ⊔ level(key) ⊔ obj_level(key);
  ret(obj_level()) = pc ⊔ level(key) ⊔ obj_level(key);
  ret(obj_level(key)) = obj_level(key);
}