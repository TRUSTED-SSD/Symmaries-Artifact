static void org.keycloak.common.util.Base64:encode (java.nio.ByteBuffer raw,
                                                    java.nio.ByteBuffer
                                                      encoded) {
  guard = tt;
  ret(obj_level(encoded)) = ⊤;
  ret(obj_level(raw)) = ⊤;
}