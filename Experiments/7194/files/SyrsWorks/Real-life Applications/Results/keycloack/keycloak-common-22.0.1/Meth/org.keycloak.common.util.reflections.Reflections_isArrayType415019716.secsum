static boolean org.keycloak.common.util.reflections.Reflections:isArrayType (
  java.lang.Class rawType) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(rawType);
  ret(obj_level(rawType)) = obj_level(rawType);
}