boolean org.keycloak.common.util.PathMatcher:endsWithWildcard(java.lang.String expectedUri)
{
int length;
  char $stack6;
  boolean $stack7;
  length = expectedUri.length();
 if (length<=0) goto label0;
 length=(length-1);
 $stack6 = expectedUri.charAt(length);
 if (42!=$stack6) goto label0;
 $stack7=1;
 goto label1;
 label0:$stack7=0;
 label1:return  $stack7;
 } 

