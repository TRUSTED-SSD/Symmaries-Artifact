static org.keycloak.common.util.MimeTypeUtil
{
jakarta.activation.MimetypesFileTypeMap $stack0;
  $stack0=new jakarta.activation.MimetypesFileTypeMap;
 $stack0#jakarta.activation.MimetypesFileTypeMap();
 org.keycloak.common.util.MimeTypeUtil.map=$stack0;
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("text/css css CSS");
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("text/javascript js JS");
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("text/javascript js JS");
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("image/png png PNG");
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("image/svg+xml svg SVG");
 $stack0=org.keycloak.common.util.MimeTypeUtil.map;
 $stack0.addMimeTypes("text/html html htm HTML HTM");
 return;
 } 

