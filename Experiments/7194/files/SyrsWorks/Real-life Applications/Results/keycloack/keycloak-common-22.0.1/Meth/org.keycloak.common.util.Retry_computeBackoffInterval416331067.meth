static int org.keycloak.common.util.Retry:computeBackoffInterval(int base,int iteration)
{
java.util.Random $stack2;
  $stack2=new java.util.Random;
 $stack2#java.util.Random();
 base = org.keycloak.common.util.Retry.computeIterationBase(base,iteration);
 base = $stack2.nextInt(base);
 return  base;
 } 

