static boolean org.keycloak.common.util.reflections.Reflections:containsWildcards (
  java.lang.reflect.Type[] types) {
  guard = tt;
  ret(level()) = pc ⊔ level(types) ⊔ obj_level(types);
  ret(obj_level(types)) = obj_level(types);
}