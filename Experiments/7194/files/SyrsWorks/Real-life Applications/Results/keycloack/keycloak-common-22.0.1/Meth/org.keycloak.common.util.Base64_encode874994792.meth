static void org.keycloak.common.util.Base64:encode(java.nio.ByteBuffer raw,java.nio.ByteBuffer encoded)
{
byte[] raw3;
  byte[] enc4;
  int rem;
  boolean $stack5;
  raw3=new byte[3];
 enc4=new byte[4];
 label0:$stack5 = raw.hasRemaining();
 if ($stack5==0) goto label1;
 rem = raw.remaining();
 rem = java.lang.Math.min(3,rem);
 raw.get(raw3,0,rem);
 org.keycloak.common.util.Base64.encode3to4(enc4,raw3,rem,0);
 encoded.put(enc4);
 goto label0;
 label1:return;
 } 

