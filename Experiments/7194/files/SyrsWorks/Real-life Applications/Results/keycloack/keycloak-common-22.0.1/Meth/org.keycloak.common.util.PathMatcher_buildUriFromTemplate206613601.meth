java.lang.String org.keycloak.common.util.PathMatcher:buildUriFromTemplate(java.lang.String @template,java.lang.String @targetUri,boolean onlyFirstParam)
{
java.lang.StringBuilder uri;
  int patternStartIndex;
  char[] expectedUriChars;
  char[] matchingUri;
  int matchingUriLastIndex;
  java.lang.String targetUriParams;
  int paramIndex;
  int lastPattern;
  char c;
  java.lang.String[] params;
  int k;
  int paramLength;
  java.lang.StringBuilder value;
  int j;
  char valueChar;
  boolean $stack24;
  char[] $stack33;
  int $stack56;
  java.lang.String template;
java.lang.String targetUri;
targetUri=@targetUri;
template=@template;
uri=new java.lang.StringBuilder;
 uri#java.lang.StringBuilder(template);
 template = uri.toString();
 patternStartIndex = template.indexOf("{");
 $stack24 = template.endsWith("/*");
 if ($stack24==0) goto label0;
 matchingUriLastIndex = template.length();
 matchingUriLastIndex=(matchingUriLastIndex-2);
 template = template.substring(0,matchingUriLastIndex);
 label0:if (patternStartIndex==-1) goto label1;
 matchingUriLastIndex = targetUri.length();
 if (patternStartIndex<matchingUriLastIndex) goto label2;
 label1:return  null;
 label2:params = template.split("/");
 matchingUriLastIndex=#params;
 params = targetUri.split("/");
 paramIndex=#params;
 if (matchingUriLastIndex<=paramIndex) goto label3;
 return  null;
 label3:expectedUriChars = template.toCharArray();
 matchingUri = java.util.Arrays.copyOfRange(expectedUriChars,0,patternStartIndex);
 matchingUriLastIndex=#matchingUri;
 targetUriParams = targetUri.substring(patternStartIndex);
 $stack33 = targetUri.toCharArray();
 paramIndex=#matchingUri;
 $stack33 = java.util.Arrays.copyOf($stack33,paramIndex);
 $stack24 = java.util.Arrays.equals(matchingUri,$stack33);
 if ($stack24==0) goto label4;
 paramIndex = targetUri.length();
 matchingUri = java.util.Arrays.copyOf(matchingUri,paramIndex);
 paramIndex=0;
 lastPattern=0;
 label5:k=#expectedUriChars;
 if (patternStartIndex>=k) goto label6;
 k=#matchingUri;
 if (matchingUriLastIndex<k) goto label7;
 goto label6;
 label7:c=expectedUriChars[patternStartIndex];
 if (c==123) goto label8;
 if (c!=42) goto label9;
 label8:params = targetUriParams.split("/");
 k=paramIndex;
 label10:j=k;
 if (c!=42) goto label11;
 paramLength=#params;
 goto label12;
 label11:paramLength=paramIndex;
 label12:if (j>paramLength) goto label13;
 paramLength=#params;
 if (k!=paramLength) goto label14;
 goto label13;
 label14:targetUri=params[k];
 paramLength = targetUri.length();
 $stack56=(matchingUriLastIndex+paramLength);
 j=#matchingUri;
 if ($stack56<=j) goto label15;
 return  null;
 label15:value=new java.lang.StringBuilder;
 value#java.lang.StringBuilder();
 j=0;
 label16:if (j>=paramLength) goto label17;
 targetUri=params[k];
 valueChar = targetUri.charAt(j);
 value.append(valueChar);
 $stack56=matchingUriLastIndex;
 matchingUriLastIndex=(matchingUriLastIndex+1);
 matchingUri[$stack56]=valueChar;
 j=(j+1);
 goto label16;
 label17:if (c!=123) goto label18;
 paramLength = uri.indexOf("{",lastPattern);
 j = uri.indexOf("}",lastPattern);
 j=(j+1);
 targetUri = value.toString();
 uri.replace(paramLength,j,targetUri);
 label18:paramLength = value.length();
 paramLength=(paramLength-1);
 valueChar = value.charAt(paramLength);
 if (valueChar!=125) goto label19;
 targetUri = value.toString();
 lastPattern = uri.indexOf(targetUri);
 paramLength = value.length();
 lastPattern=(lastPattern+paramLength);
 label19:if (c!=42) goto label20;
 paramLength=#matchingUri;
 if (matchingUriLastIndex>=paramLength) goto label20;
 paramLength=matchingUriLastIndex;
 matchingUriLastIndex=(matchingUriLastIndex+1);
 matchingUri[paramLength]=47;
 label20:k=(k+1);
 goto label10;
 label13:if (c!=123) goto label21;
 patternStartIndex = template.indexOf(125,patternStartIndex);
 label21:k = template.lastIndexOf(125);
 if (patternStartIndex!=k) goto label22;
 if (onlyFirstParam==0) goto label22;
 template = java.lang.String.valueOf(matchingUri);
 template = template.substring(0,matchingUriLastIndex);
 return  template;
 label22:goto label23;
 label9:if (c!=47) goto label24;
 paramIndex=(paramIndex+1);
 label24:k=matchingUriLastIndex;
 matchingUriLastIndex=(matchingUriLastIndex+1);
 matchingUri[k]=c;
 label23:patternStartIndex=(patternStartIndex+1);
 goto label5;
 label6:template = uri.toString();
 return  template;
 label4:return  null;
 } 

