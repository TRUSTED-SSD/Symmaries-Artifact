static java.lang.String org.keycloak.common.util.NetworkUtils:formatAddress(java.net.InetAddress inet)
{
byte[] byteRepresentation;
  int[] hexRepresentation;
  int i;
  boolean $stack4;
  java.lang.String $stack6;
  int $stack8;
  byte $stack11;
  short $stack12;
  int $stack13;
  java.lang.NullPointerException $stack22;
  if (inet!=null) goto label0;
 $stack22=new java.lang.NullPointerException;
 $stack22#java.lang.NullPointerException();
 throw $stack22;
 label0:$stack4=inet instanceof java.net.Inet4Address;
 if ($stack4==0) goto label1;
 $stack6 = inet.getHostAddress();
 return  $stack6;
 label1:$stack4=inet instanceof java.net.Inet6Address;
 if ($stack4==0) goto label2;
 byteRepresentation = inet.getAddress();
 hexRepresentation=new int[8];
 i=0;
 label3:$stack8=#hexRepresentation;
 if (i>=$stack8) goto label4;
 $stack8=(2 * i);
 $stack11=byteRepresentation[$stack8];
 $stack12=($stack11 & 255);
 $stack8=($stack12<<8);
 $stack13=(2 * i);
 $stack13=($stack13+1);
 $stack11=byteRepresentation[$stack13];
 $stack12=($stack11 & 255);
 $stack8=($stack8 | $stack12);
 hexRepresentation[i]=$stack8;
 i=(i+1);
 goto label3;
 label4:org.keycloak.common.util.NetworkUtils.compactLongestZeroSequence(hexRepresentation);
 $stack6 = org.keycloak.common.util.NetworkUtils.formatAddress6(hexRepresentation);
 return  $stack6;
 label2:$stack6 = inet.getHostAddress();
 return  $stack6;
 } 

