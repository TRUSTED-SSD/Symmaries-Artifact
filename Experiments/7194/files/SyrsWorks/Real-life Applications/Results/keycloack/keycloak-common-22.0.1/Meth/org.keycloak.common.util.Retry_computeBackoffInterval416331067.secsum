static int org.keycloak.common.util.Retry:computeBackoffInterval (int base,
                                                                  int
                                                                    iteration) {
  guard = tt;
  ret(level()) = pc ⊔ level(base) ⊔ level(iteration);
}