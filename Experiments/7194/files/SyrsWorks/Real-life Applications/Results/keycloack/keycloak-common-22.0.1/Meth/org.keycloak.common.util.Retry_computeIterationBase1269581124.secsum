static int org.keycloak.common.util.Retry:computeIterationBase (int base,
                                                                int iteration) {
  guard = tt;
  ret(level()) = pc ⊔ level(base) ⊔ level(iteration);
}