static boolean org.keycloak.common.util.reflections.Reflections:isParameterizedType (
  java.lang.Class type) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(type);
  ret(obj_level(type)) = obj_level(type);
}