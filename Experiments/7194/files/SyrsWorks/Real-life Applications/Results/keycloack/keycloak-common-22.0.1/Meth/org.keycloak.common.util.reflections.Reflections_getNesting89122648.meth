static int org.keycloak.common.util.reflections.Reflections:getNesting(java.lang.Class @clazz)
{
boolean $stack1;
  int $stack6;
  java.lang.Class clazz;
clazz=@clazz;
$stack1 = clazz.isMemberClass();
 if ($stack1==0) goto label0;
 $stack1 = org.keycloak.common.util.reflections.Reflections.isStatic(clazz);
 if ($stack1!=0) goto label0;
 clazz = clazz.getDeclaringClass();
 $stack6 = org.keycloak.common.util.reflections.Reflections.getNesting(clazz);
 $stack6=(1+$stack6);
 return  $stack6;
 label0:return  0;
 } 

