static int org.keycloak.common.util.reflections.Reflections:getNesting (
  java.lang.Class @clazz) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(@clazz);
  ret(obj_level(@clazz)) = obj_level(@clazz);
}