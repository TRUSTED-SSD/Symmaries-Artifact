static void org.keycloak.common.crypto.CryptoIntegration:init (java.lang.ClassLoader
                                                                 classLoader) {
  guard = tt;
  ret(obj_level(classLoader)) = pc ⊔ level(classLoader) ⊔ obj_level(classLoader);
}