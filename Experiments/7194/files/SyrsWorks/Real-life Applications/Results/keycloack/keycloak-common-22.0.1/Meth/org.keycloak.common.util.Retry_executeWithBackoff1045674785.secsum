static int org.keycloak.common.util.Retry:executeWithBackoff (org.keycloak.common.util.Retry$AdvancedRunnable
                                                                runnable,
                                                              int
                                                                attemptsCount,
                                                              int
                                                                intervalBaseMillis) {
  guard = tt;
  ret(level()) = pc ⊔ level(attemptsCount) ⊔ level(intervalBaseMillis) ⊔ level(runnable) ⊔ obj_level(runnable);
  ret(obj_level(runnable)) = obj_level(runnable);
}