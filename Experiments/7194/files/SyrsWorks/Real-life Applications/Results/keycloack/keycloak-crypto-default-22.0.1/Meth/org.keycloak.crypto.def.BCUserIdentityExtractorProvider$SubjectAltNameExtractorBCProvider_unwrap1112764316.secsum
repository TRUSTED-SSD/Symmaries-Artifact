org.bouncycastle.asn1.ASN1Encodable org.keycloak.crypto.def.BCUserIdentityExtractorProvider$SubjectAltNameExtractorBCProvider:unwrap (
  java.lang.Object @encodable) {
  guard = tt;
  ret(level()) = pc ⊔ level(@encodable) ⊔ obj_level(@encodable);
  ret(obj_level()) = pc ⊔ level(@encodable) ⊔ obj_level(@encodable);
  ret(obj_level(@encodable)) = if obj_level(@encodable) = ⊤ ∨ pc ≠ ⊤ ∧ level(@encodable) = ⊤ ∧ obj_level(@encodable) = ⊥ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(@encodable) = ⊤ ∨ pc ≠ ⊤ ∧ level(@encodable) = ⊤ ∧ obj_level(@encodable) = ⊥ then ⊤ else ⊥;
}