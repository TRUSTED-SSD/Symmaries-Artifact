byte[] org.keycloak.crypto.def.DefaultRsaKeyEncryptionJWEAlgorithmProvider:decodeCek(byte[] @encodedCek,java.security.Key privateKey)
{
javax.crypto.Cipher cipher;
  byte[] encodedCek;
encodedCek=@encodedCek;
cipher = this#org.keycloak.crypto.def.DefaultRsaKeyEncryptionJWEAlgorithmProvider:getCipherProvider();
 this.initCipher(cipher,2,privateKey);
 encodedCek = cipher.doFinal(encodedCek);
 return  encodedCek;
 } 

