void org.keycloak.adapters.spi.InMemorySessionIdMapper:clear()
{
java.util.concurrent.ConcurrentHashMap $stack1;
  $stack1=this.ssoToSession;
 $stack1.clear();
 $stack1=this.sessionToSso;
 $stack1.clear();
 $stack1=this.principalToSession;
 $stack1.clear();
 $stack1=this.sessionToPrincipal;
 $stack1.clear();
 return;
 } 

