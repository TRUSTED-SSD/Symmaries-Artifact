org.keycloak.TokenVerifier org.keycloak.TokenVerifier:checkActive(boolean checkActive)
{
org.keycloak.TokenVerifier $this;
  org.keycloak.TokenVerifier$Predicate[] $stack2;
  org.keycloak.TokenVerifier$Predicate $stack3;
  org.keycloak.TokenVerifier$Predicate $stack4;
  $this= this;
 $stack4=org.keycloak.TokenVerifier.IS_ACTIVE;
 $stack2=new org.keycloak.TokenVerifier$Predicate[1];
 $stack3=org.keycloak.TokenVerifier.IS_ACTIVE;
 $stack2[0]=$stack3;
 $this = $this#org.keycloak.TokenVerifier:replaceCheck($stack4,checkActive,$stack2);
 return  $this;
 } 

