byte[] org.keycloak.jose.jwe.enc.AesGcmEncryptionProvider:encryptBytes(byte[] contentBytes,byte[] @ivBytes,java.security.Key aesKey,byte[] aad)
{
javax.crypto.Cipher cipher;
  org.keycloak.common.crypto.CryptoProvider $stack8;
  javax.crypto.spec.GCMParameterSpec $stack10;
  int $stack11;
  byte[] ivBytes;
ivBytes=@ivBytes;
$stack8 = org.keycloak.common.crypto.CryptoIntegration.getProvider();
 cipher = $stack8.getAesGcmCipher();
 $stack10=new javax.crypto.spec.GCMParameterSpec;
 $stack10#javax.crypto.spec.GCMParameterSpec(128,ivBytes);
 cipher.init(1,aesKey,(java.security.spec.AlgorithmParameterSpec)$stack10);
 cipher.updateAAD(aad);
 $stack11=#contentBytes;
 $stack11 = cipher.getOutputSize($stack11);
 ivBytes=new byte[$stack11];
 $stack11=#contentBytes;
 cipher.doFinal(contentBytes,0,$stack11,ivBytes);
 return  ivBytes;
 } 

