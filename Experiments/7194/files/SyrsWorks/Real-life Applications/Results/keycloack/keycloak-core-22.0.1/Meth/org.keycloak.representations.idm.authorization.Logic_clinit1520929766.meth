static org.keycloak.representations.idm.authorization.Logic
{
org.keycloak.representations.idm.authorization.Logic $stack0;
  org.keycloak.representations.idm.authorization.Logic[] $stack2;
  java.util.Map $stack4;
  $stack0=new org.keycloak.representations.idm.authorization.Logic;
 $stack0#org.keycloak.representations.idm.authorization.Logic("POSITIVE",0,0);
 org.keycloak.representations.idm.authorization.Logic.POSITIVE=$stack0;
 $stack0=new org.keycloak.representations.idm.authorization.Logic;
 $stack0#org.keycloak.representations.idm.authorization.Logic("NEGATIVE",1,1);
 org.keycloak.representations.idm.authorization.Logic.NEGATIVE=$stack0;
 $stack2 = org.keycloak.representations.idm.authorization.Logic.$values();
 org.keycloak.representations.idm.authorization.Logic.$VALUES=$stack2;
 $stack2 = org.keycloak.representations.idm.authorization.Logic.values();
 $stack4 = org.keycloak.util.EnumWithStableIndex.getReverseIndex((org.keycloak.util.EnumWithStableIndex[])$stack2);
 org.keycloak.representations.idm.authorization.Logic.BY_ID=$stack4;
 return;
 } 

