void org.keycloak.representations.adapters.config.AdapterConfig:setConnectionTimeout (
  long connectionTimeout) {
  guard = tt;
  ret(obj_level(this)) = pc ⊔ level(connectionTimeout) ⊔ obj_level(this);
}