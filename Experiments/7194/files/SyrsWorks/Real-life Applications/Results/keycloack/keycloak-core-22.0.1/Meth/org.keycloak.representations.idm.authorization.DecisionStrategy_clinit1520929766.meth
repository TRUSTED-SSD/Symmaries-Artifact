static org.keycloak.representations.idm.authorization.DecisionStrategy
{
org.keycloak.representations.idm.authorization.DecisionStrategy $stack0;
  org.keycloak.representations.idm.authorization.DecisionStrategy[] $stack3;
  java.util.Map $stack5;
  $stack0=new org.keycloak.representations.idm.authorization.DecisionStrategy;
 $stack0#org.keycloak.representations.idm.authorization.DecisionStrategy("AFFIRMATIVE",0,0);
 org.keycloak.representations.idm.authorization.DecisionStrategy.AFFIRMATIVE=$stack0;
 $stack0=new org.keycloak.representations.idm.authorization.DecisionStrategy;
 $stack0#org.keycloak.representations.idm.authorization.DecisionStrategy("UNANIMOUS",1,1);
 org.keycloak.representations.idm.authorization.DecisionStrategy.UNANIMOUS=$stack0;
 $stack0=new org.keycloak.representations.idm.authorization.DecisionStrategy;
 $stack0#org.keycloak.representations.idm.authorization.DecisionStrategy("CONSENSUS",2,2);
 org.keycloak.representations.idm.authorization.DecisionStrategy.CONSENSUS=$stack0;
 $stack3 = org.keycloak.representations.idm.authorization.DecisionStrategy.$values();
 org.keycloak.representations.idm.authorization.DecisionStrategy.$VALUES=$stack3;
 $stack3 = org.keycloak.representations.idm.authorization.DecisionStrategy.values();
 $stack5 = org.keycloak.util.EnumWithStableIndex.getReverseIndex((org.keycloak.util.EnumWithStableIndex[])$stack3);
 org.keycloak.representations.idm.authorization.DecisionStrategy.BY_ID=$stack5;
 return;
 } 

