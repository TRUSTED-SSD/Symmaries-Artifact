org.keycloak.RSATokenVerifier org.keycloak.RSATokenVerifier:checkActive(boolean checkActive)
{
org.keycloak.TokenVerifier $stack2;
  $stack2=this.tokenVerifier;
 $stack2.checkActive(checkActive);
 return  this;
 } 

