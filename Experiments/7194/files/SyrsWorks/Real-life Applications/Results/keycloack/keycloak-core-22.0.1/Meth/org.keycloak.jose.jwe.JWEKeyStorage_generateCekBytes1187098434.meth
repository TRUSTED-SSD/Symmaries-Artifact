void org.keycloak.jose.jwe.JWEKeyStorage:generateCekBytes()
{
int cekLength;
  org.keycloak.jose.jwe.enc.JWEEncryptionProvider $stack2;
  byte[] $stack4;
  $stack2=this.encryptionProvider;
 cekLength = $stack2.getExpectedCEKLength();
 $stack4 = org.keycloak.jose.jwe.JWEUtils.generateSecret(cekLength);
 this.cekBytes=$stack4;
 return;
 } 

