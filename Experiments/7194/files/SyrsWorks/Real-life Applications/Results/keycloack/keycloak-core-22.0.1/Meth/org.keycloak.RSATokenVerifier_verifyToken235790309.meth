static org.keycloak.representations.AccessToken org.keycloak.RSATokenVerifier:verifyToken(java.lang.String tokenString,java.security.PublicKey publicKey,java.lang.String realmUrl)
{
org.keycloak.RSATokenVerifier $stack3;
  org.keycloak.representations.AccessToken $stack7;
  $stack3 = org.keycloak.RSATokenVerifier.create(tokenString);
 $stack3 = $stack3.publicKey(publicKey);
 $stack3 = $stack3.realmUrl(realmUrl);
 $stack3 = $stack3.verify();
 $stack7 = $stack3.getToken();
 return  $stack7;
 } 

