void org.keycloak.representations.idm.AbstractAuthenticationExecutionRepresentation:setAuthenticator (
  java.lang.String authenticator) {
  guard = tt;
  ret(obj_level(authenticator)) = obj_level(authenticator);
  ret(obj_level(this)) = pc ⊔ level(authenticator) ⊔ obj_level(authenticator) ⊔ obj_level(this);
}