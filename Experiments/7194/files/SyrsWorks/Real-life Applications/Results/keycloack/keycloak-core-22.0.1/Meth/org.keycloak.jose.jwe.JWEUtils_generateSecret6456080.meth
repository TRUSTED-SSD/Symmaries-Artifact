static byte[] org.keycloak.jose.jwe.JWEUtils:generateSecret(int bytes)
{
byte[] buf;
  java.security.SecureRandom $stack2;
  buf=new byte[bytes];
 $stack2=new java.security.SecureRandom;
 $stack2#java.security.SecureRandom();
 dummyClass.highSourceMut(buf);
$stack2.nextBytes(buf);
 return  buf;
 } 

