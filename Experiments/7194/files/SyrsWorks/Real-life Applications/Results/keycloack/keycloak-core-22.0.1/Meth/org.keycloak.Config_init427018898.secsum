static void org.keycloak.Config:init (org.keycloak.Config$ConfigProvider
                                        configProvider) {
  guard = tt;
  ret(obj_level(configProvider)) = obj_level(configProvider);
}