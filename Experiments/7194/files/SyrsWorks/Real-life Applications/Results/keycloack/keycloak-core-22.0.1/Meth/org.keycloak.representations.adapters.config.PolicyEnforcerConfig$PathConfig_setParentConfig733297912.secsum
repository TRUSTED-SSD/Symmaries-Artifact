void org.keycloak.representations.adapters.config.PolicyEnforcerConfig$PathConfig:setParentConfig (
  org.keycloak.representations.adapters.config.PolicyEnforcerConfig$PathConfig
    parentConfig) {
  guard = tt;
  ret(obj_level(parentConfig)) = pc ⊔ level(parentConfig) ⊔ obj_level(parentConfig);
  ret(obj_level(this)) = pc ⊔ level(parentConfig) ⊔ obj_level(parentConfig);
}