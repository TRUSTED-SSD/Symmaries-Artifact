org.keycloak.RSATokenVerifier org.keycloak.RSATokenVerifier:checkRealmUrl(boolean checkRealmUrl)
{
org.keycloak.TokenVerifier $stack2;
  $stack2=this.tokenVerifier;
 $stack2.checkRealmUrl(checkRealmUrl);
 return  this;
 } 

