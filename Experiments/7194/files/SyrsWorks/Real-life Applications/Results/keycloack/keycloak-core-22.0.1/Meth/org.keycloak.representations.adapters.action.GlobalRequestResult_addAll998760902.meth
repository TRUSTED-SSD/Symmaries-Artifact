void org.keycloak.representations.adapters.action.GlobalRequestResult:addAll(org.keycloak.representations.adapters.action.GlobalRequestResult merged)
{
java.util.List $stack2;
  int $stack5;
  $stack2 = merged.getSuccessRequests();
 if ($stack2==null) goto label0;
 $stack2 = merged.getSuccessRequests();
 $stack5 = $stack2.size();
 if ($stack5<=0) goto label0;
 $stack2 = merged.getSuccessRequests();
 this.addAllSuccessRequests($stack2);
 label0:$stack2 = merged.getFailedRequests();
 if ($stack2==null) goto label1;
 $stack2 = merged.getFailedRequests();
 $stack5 = $stack2.size();
 if ($stack5<=0) goto label1;
 $stack2 = merged.getFailedRequests();
 this.addAllFailedRequests($stack2);
 label1:return;
 } 

