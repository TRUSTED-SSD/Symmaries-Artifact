boolean org.keycloak.TokenVerifier$2:test (org.keycloak.representations.JsonWebToken
                                             t) {
  guard = tt;
  ret(level()) = pc ⊔ level(t) ⊔ obj_level(t);
  ret(obj_level(t)) = if obj_level(t) = ⊤ ∨ pc ≠ ⊤ ∧ level(t) = ⊤ ∧ obj_level(t) = ⊥ then ⊤ else ⊥;
  ret(obj_level(this)) = obj_level(this);
}