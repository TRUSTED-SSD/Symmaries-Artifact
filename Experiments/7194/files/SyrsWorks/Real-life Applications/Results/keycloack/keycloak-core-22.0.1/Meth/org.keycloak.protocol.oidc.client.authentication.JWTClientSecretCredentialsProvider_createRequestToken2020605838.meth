org.keycloak.representations.JsonWebToken org.keycloak.protocol.oidc.client.authentication.JWTClientSecretCredentialsProvider:createRequestToken(java.lang.String clientId,java.lang.String realmInfoUrl)
{
org.keycloak.representations.JsonWebToken reqToken;
  int now;
  java.util.UUID $stack6;
  java.lang.String $stack7;
  java.lang.String[] $stack11;
  int $stack15;
  reqToken=new org.keycloak.representations.JsonWebToken;
 reqToken#org.keycloak.representations.JsonWebToken();
 $stack6 = java.util.UUID.randomUUID();
 $stack7 = $stack6.toString();
 reqToken.id($stack7);
 reqToken.issuer(clientId);
 reqToken.subject(clientId);
 $stack11=new java.lang.String[1];
 $stack11[0]=realmInfoUrl;
 reqToken.audience($stack11);
 now = org.keycloak.common.util.Time.currentTime();
 reqToken.issuedAt(now);
 $stack15=(now+10);
 reqToken.expiration($stack15);
 reqToken.notBefore(now);
 return  reqToken;
 } 

