void org.keycloak.jose.jws.JWSBuilder:encode(java.lang.String @sigAlgName,byte[] data,java.lang.StringBuilder encoding)
{
java.lang.String sigAlgName;
sigAlgName=@sigAlgName;
sigAlgName = this.encodeHeader(sigAlgName);
 encoding.append(sigAlgName);
 encoding.append((char)46);
 sigAlgName = org.keycloak.common.util.Base64Url.encode(data);
 encoding.append(sigAlgName);
 return;
 } 

