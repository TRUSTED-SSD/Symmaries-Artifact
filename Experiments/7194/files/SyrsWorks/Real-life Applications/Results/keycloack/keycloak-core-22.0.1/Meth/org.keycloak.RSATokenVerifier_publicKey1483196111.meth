org.keycloak.RSATokenVerifier org.keycloak.RSATokenVerifier:publicKey(java.security.PublicKey publicKey)
{
org.keycloak.TokenVerifier $stack2;
  $stack2=this.tokenVerifier;
 $stack2.publicKey(publicKey);
 return  this;
 } 

