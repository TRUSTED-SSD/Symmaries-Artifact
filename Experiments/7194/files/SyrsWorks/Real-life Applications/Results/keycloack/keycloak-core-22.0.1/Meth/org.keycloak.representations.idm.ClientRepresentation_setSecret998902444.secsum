void org.keycloak.representations.idm.ClientRepresentation:setSecret (
  java.lang.String secret) {
  guard = tt;
  ret(obj_level(secret)) = obj_level(secret);
  ret(obj_level(this)) = pc ⊔ level(secret) ⊔ obj_level(secret) ⊔ obj_level(this);
}