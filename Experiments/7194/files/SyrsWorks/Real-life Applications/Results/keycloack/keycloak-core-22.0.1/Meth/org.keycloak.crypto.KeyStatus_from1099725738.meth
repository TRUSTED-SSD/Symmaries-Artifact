static org.keycloak.crypto.KeyStatus org.keycloak.crypto.KeyStatus:from(boolean active,boolean enabled)
{
org.keycloak.crypto.KeyStatus $stack2;
  if (enabled!=0) goto label0;
 $stack2=org.keycloak.crypto.KeyStatus.DISABLED;
 return  $stack2;
 label0:if (active==0) goto label1;
 $stack2=org.keycloak.crypto.KeyStatus.ACTIVE;
 goto label2;
 label1:$stack2=org.keycloak.crypto.KeyStatus.PASSIVE;
 label2:return  $stack2;
 } 

