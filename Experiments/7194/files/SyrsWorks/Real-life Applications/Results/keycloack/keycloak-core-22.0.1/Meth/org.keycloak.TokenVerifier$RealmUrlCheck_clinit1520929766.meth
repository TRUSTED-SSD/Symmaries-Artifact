static org.keycloak.TokenVerifier$RealmUrlCheck
{
org.keycloak.TokenVerifier$RealmUrlCheck $stack0;
  $stack0=new org.keycloak.TokenVerifier$RealmUrlCheck;
 $stack0#org.keycloak.TokenVerifier$RealmUrlCheck(null);
 org.keycloak.TokenVerifier$RealmUrlCheck.NULL_INSTANCE=$stack0;
 return;
 } 

