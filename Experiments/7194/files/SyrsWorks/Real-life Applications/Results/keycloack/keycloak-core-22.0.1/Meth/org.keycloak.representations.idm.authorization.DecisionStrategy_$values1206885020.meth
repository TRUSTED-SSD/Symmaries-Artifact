static org.keycloak.representations.idm.authorization.DecisionStrategy[] org.keycloak.representations.idm.authorization.DecisionStrategy:$values()
{
org.keycloak.representations.idm.authorization.DecisionStrategy[] $stack0;
  org.keycloak.representations.idm.authorization.DecisionStrategy $stack1;
  $stack0=new org.keycloak.representations.idm.authorization.DecisionStrategy[3];
 $stack1=org.keycloak.representations.idm.authorization.DecisionStrategy.AFFIRMATIVE;
 $stack0[0]=$stack1;
 $stack1=org.keycloak.representations.idm.authorization.DecisionStrategy.UNANIMOUS;
 $stack0[1]=$stack1;
 $stack1=org.keycloak.representations.idm.authorization.DecisionStrategy.CONSENSUS;
 $stack0[2]=$stack1;
 return  $stack0;
 } 

