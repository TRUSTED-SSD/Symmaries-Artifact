boolean org.keycloak.representations.JsonWebToken:hasAnyAudience (java.util.List
                                                                    audiences) {
  guard = tt;
  ret(level()) = pc ⊔ level(this) ⊔ obj_level(audiences) ⊔ obj_level(this);
  ret(obj_level(audiences)) = if obj_level(audiences) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(audiences) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(audiences) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
}