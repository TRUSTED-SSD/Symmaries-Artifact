void org.keycloak.jose.jwe.JWE:setupJWEHeader(java.lang.String @jweStr)
{
java.lang.String[] parts;
  int $stack4;
  byte[] $stack8;
  org.keycloak.jose.JOSEHeader $stack13;
  org.keycloak.jose.jwe.JWEHeader $stack14;
  java.lang.IllegalStateException $stack15;
  java.lang.String jweStr;
jweStr=@jweStr;
parts = jweStr.split("\\.");
 $stack4=#parts;
 if ($stack4==5) goto label0;
 $stack15=new java.lang.IllegalStateException;
 $stack15#java.lang.IllegalStateException("Not a JWE String");
 throw $stack15;
 label0:jweStr=parts[0];
 this.base64Header=jweStr;
 jweStr=parts[1];
 this.base64Cek=jweStr;
 jweStr=parts[2];
 $stack8 = org.keycloak.common.util.Base64Url.decode(jweStr);
 this.initializationVector=$stack8;
 jweStr=parts[3];
 $stack8 = org.keycloak.common.util.Base64Url.decode(jweStr);
 this.encryptedContent=$stack8;
 jweStr=parts[4];
 $stack8 = org.keycloak.common.util.Base64Url.decode(jweStr);
 this.authenticationTag=$stack8;
 $stack13 = this.getHeader();
 $stack14=(org.keycloak.jose.jwe.JWEHeader) $stack13;
 this.header=$stack14;
 return;
 } 

