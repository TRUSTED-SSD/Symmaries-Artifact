static org.keycloak.RSATokenVerifier org.keycloak.RSATokenVerifier:create(java.lang.String tokenString)
{
org.keycloak.RSATokenVerifier $stack1;
  $stack1=new org.keycloak.RSATokenVerifier;
 $stack1#org.keycloak.RSATokenVerifier(tokenString);
 return  $stack1;
 } 

