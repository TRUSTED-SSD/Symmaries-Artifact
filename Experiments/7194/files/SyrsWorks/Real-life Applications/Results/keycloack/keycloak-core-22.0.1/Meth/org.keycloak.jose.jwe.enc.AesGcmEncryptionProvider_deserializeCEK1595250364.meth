void org.keycloak.jose.jwe.enc.AesGcmEncryptionProvider:deserializeCEK(org.keycloak.jose.jwe.JWEKeyStorage @keyStorage)
{
byte[] cekBytes;
  javax.crypto.spec.SecretKeySpec $stack5;
  org.keycloak.jose.jwe.JWEKeyStorage$KeyUse $stack6;
  org.keycloak.jose.jwe.JWEKeyStorage keyStorage;
keyStorage=@keyStorage;
cekBytes = keyStorage.getCekBytes();
 $stack5=new javax.crypto.spec.SecretKeySpec;
 $stack5#javax.crypto.spec.SecretKeySpec(cekBytes,"AES");
 $stack6=org.keycloak.jose.jwe.JWEKeyStorage$KeyUse.ENCRYPTION;
 keyStorage = keyStorage.setCEKKey((java.security.Key)$stack5,$stack6);
 return;
 } 

