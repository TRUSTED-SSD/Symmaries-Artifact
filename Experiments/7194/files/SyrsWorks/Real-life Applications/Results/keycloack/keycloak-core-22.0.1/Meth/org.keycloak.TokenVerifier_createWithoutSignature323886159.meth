static org.keycloak.TokenVerifier org.keycloak.TokenVerifier:createWithoutSignature(org.keycloak.representations.JsonWebToken token)
{
org.keycloak.TokenVerifier $stack1;
  $stack1=new org.keycloak.TokenVerifier;
 $stack1#org.keycloak.TokenVerifier(token);
 return  $stack1;
 } 

