byte[] org.keycloak.jose.jwe.alg.DirectAlgorithmProvider:decodeCek(byte[] @encodedCek,java.security.Key encryptionKey)
{
byte[] encodedCek;
encodedCek=@encodedCek;
encodedCek=new byte[0];
 return  encodedCek;
 } 

