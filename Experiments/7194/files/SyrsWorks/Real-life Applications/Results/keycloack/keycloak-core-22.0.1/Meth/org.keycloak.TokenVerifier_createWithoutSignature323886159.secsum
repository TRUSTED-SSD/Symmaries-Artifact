static org.keycloak.TokenVerifier org.keycloak.TokenVerifier:createWithoutSignature (
  org.keycloak.representations.JsonWebToken token) {
  guard = tt;
  ret(level()) = pc;
  ret(obj_level()) = pc;
  ret(obj_level(token)) = obj_level(token);
}