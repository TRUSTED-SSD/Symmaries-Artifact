static org.keycloak.crypto.KeyStatus org.keycloak.crypto.KeyStatus:from (
  boolean active, boolean enabled) {
  guard = tt;
  ret(level()) = pc ⊔ level(active) ⊔ level(enabled);
  ret(obj_level()) = ⊥;
}