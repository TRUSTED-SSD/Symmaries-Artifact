static void org.keycloak.adapters.ServerRequest:error (int status,
                                                       org.apache.http.HttpEntity
                                                         entity) {
  guard = tt;
  ret(obj_level(entity)) = obj_level(entity);
}