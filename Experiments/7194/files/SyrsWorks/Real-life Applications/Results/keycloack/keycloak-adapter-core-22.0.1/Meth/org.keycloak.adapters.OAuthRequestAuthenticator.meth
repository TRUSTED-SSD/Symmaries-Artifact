org.keycloak.adapters.OAuthRequestAuthenticator(org.keycloak.adapters.RequestAuthenticator requestAuthenticator,org.keycloak.adapters.spi.HttpFacade facade,org.keycloak.adapters.KeycloakDeployment deployment,int sslRedirectPort,org.keycloak.adapters.spi.AdapterSessionStore tokenStore)
{
int $stack6;
  this#java.lang.Object();
 this.reqAuthenticator=requestAuthenticator;
 this.facade=facade;
 this.deployment=deployment;
 $stack6 = deployment.getConfidentialPort();
 if ($stack6==-1) goto label0;
 sslRedirectPort = deployment.getConfidentialPort();
 goto label0;
 label0:this.sslRedirectPort=sslRedirectPort;
 this.tokenStore=tokenStore;
 return;
 } 

