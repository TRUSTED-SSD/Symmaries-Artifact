static void org.keycloak.adapters.ServerRequest:invokeRegisterNode(org.keycloak.adapters.KeycloakDeployment deployment,java.lang.String host)
{
java.lang.String registerNodeUrl;
  registerNodeUrl = deployment.getRegisterNodeUrl();
 org.keycloak.adapters.ServerRequest.invokeClientManagementRequest(deployment,host,registerNodeUrl);
 return;
 } 

