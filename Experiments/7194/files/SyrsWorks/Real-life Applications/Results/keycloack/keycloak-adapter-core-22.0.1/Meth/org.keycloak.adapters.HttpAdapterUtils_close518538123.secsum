static void org.keycloak.adapters.HttpAdapterUtils:close (org.apache.http.HttpResponse
                                                            response) {
  guard = tt;
  ret(obj_level(response)) = obj_level(response);
}