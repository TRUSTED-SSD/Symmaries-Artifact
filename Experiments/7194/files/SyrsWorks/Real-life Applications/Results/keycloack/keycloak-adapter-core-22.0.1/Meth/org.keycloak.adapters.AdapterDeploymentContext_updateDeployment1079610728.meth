void org.keycloak.adapters.AdapterDeploymentContext:updateDeployment(org.keycloak.representations.adapters.config.AdapterConfig config)
{
org.keycloak.adapters.KeycloakConfigResolver $stack2;
  org.keycloak.adapters.KeycloakDeployment $stack3;
  java.lang.IllegalStateException $stack4;
  $stack2=this.configResolver;
 if (null==$stack2) goto label0;
 $stack4=new java.lang.IllegalStateException;
 $stack4#java.lang.IllegalStateException("Cannot parse an adapter config and build an updated deployment when on a multi-tenant scenario.");
 throw $stack4;
 label0:$stack3 = org.keycloak.adapters.KeycloakDeploymentBuilder.build(config);
 this.deployment=$stack3;
 return;
 } 

