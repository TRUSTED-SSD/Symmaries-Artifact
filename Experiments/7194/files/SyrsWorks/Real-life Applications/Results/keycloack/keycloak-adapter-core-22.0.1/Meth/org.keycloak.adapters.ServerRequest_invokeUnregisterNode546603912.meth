static void org.keycloak.adapters.ServerRequest:invokeUnregisterNode(org.keycloak.adapters.KeycloakDeployment deployment,java.lang.String host)
{
java.lang.String unregisterNodeUrl;
  unregisterNodeUrl = deployment.getUnregisterNodeUrl();
 org.keycloak.adapters.ServerRequest.invokeClientManagementRequest(deployment,host,unregisterNodeUrl);
 return;
 } 

