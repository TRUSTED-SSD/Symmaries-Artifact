static void org.keycloak.adapters.ServerRequest:invokeUnregisterNode (
  org.keycloak.adapters.KeycloakDeployment deployment, java.lang.String host) {
  guard = tt;
  ret(obj_level(deployment)) = obj_level(deployment);
  ret(obj_level(host)) = obj_level(host);
}