void org.keycloak.adapters.rotation.HardcodedPublicKeyLocator:reset (
  org.keycloak.adapters.KeycloakDeployment deployment) {
  guard = tt;
  ret(obj_level(deployment)) = obj_level(deployment);
  ret(obj_level(this)) = obj_level(this);
}