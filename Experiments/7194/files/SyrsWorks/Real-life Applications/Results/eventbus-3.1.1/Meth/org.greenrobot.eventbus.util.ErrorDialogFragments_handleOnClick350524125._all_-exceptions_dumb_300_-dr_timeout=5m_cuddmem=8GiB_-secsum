static void org.greenrobot.eventbus.util.ErrorDialogFragments:handleOnClick (
  android.content.DialogInterface dialog, int which,
  android.app.Activity activity, android.os.Bundle arguments) {
  guard = tt;
  ret(obj_level(activity)) = level(activity) ⊔ obj_level(activity);
  ret(obj_level(arguments)) = level(arguments) ⊔ obj_level(arguments);
  ret(obj_level(dialog)) = obj_level(dialog);
}