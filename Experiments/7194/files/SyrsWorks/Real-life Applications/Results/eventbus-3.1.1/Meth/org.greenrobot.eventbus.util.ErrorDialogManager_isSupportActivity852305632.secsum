static boolean org.greenrobot.eventbus.util.ErrorDialogManager:isSupportActivity (
  android.app.Activity activity) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(activity);
  ret(obj_level(activity)) = obj_level(activity);
}