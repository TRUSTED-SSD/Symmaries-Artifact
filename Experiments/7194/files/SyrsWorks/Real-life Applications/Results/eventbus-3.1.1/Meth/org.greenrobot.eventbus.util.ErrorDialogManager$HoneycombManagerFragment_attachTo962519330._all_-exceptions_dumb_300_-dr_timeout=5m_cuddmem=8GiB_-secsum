static void org.greenrobot.eventbus.util.ErrorDialogManager$HoneycombManagerFragment:attachTo (
  android.app.Activity activity, java.lang.Object executionScope,
  boolean finishAfterDialog, android.os.Bundle argumentsForErrorDialog) {
  guard = tt;
  ret(obj_level(activity)) = obj_level(activity);
  ret(obj_level(argumentsForErrorDialog)) = obj_level(argumentsForErrorDialog);
  ret(obj_level(executionScope)) = level(argumentsForErrorDialog) ⊔ level(executionScope) ⊔ level(finishAfterDialog) ⊔ obj_level(activity);
}