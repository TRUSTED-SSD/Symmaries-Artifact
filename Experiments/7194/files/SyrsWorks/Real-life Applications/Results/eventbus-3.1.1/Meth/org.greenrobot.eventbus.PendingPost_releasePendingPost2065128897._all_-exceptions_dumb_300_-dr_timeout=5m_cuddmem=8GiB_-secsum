static void org.greenrobot.eventbus.PendingPost:releasePendingPost (org.greenrobot.eventbus.PendingPost
                                                                    pendingPost) {
  guard = tt;
  ret(obj_level(pendingPost)) = level(pendingPost) ⊔ obj_level(pendingPost);
}