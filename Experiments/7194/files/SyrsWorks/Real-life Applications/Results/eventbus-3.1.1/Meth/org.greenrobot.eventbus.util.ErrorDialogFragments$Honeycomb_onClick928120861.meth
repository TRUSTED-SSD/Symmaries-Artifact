void org.greenrobot.eventbus.util.ErrorDialogFragments$Honeycomb:onClick(android.content.DialogInterface dialog,int which)
{
android.app.Activity $stack3;
  android.os.Bundle $stack4;
  $stack3 = this.getActivity();
 $stack4 = this.getArguments();
 org.greenrobot.eventbus.util.ErrorDialogFragments.handleOnClick(dialog,which,$stack3,$stack4);
 return;
 } 

