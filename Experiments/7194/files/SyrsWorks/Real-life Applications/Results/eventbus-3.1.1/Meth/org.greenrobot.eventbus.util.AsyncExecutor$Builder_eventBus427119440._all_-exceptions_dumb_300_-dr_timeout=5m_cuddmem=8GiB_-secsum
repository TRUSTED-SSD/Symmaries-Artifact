org.greenrobot.eventbus.util.AsyncExecutor$Builder org.greenrobot.eventbus.util.AsyncExecutor$Builder:eventBus (
  org.greenrobot.eventbus.EventBus eventBus) {
  guard = tt;
  ret(level()) = level(this);
  ret(obj_level()) = level(eventBus) ⊔ obj_level(eventBus) ⊔ obj_level(this);
  ret(obj_level(eventBus)) = level(eventBus) ⊔ obj_level(eventBus);
  ret(obj_level(this)) = level(eventBus) ⊔ obj_level(eventBus) ⊔ obj_level(this);
}