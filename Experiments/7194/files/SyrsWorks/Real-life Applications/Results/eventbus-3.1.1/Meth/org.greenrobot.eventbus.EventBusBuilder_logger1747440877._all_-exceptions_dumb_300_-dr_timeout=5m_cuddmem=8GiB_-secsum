org.greenrobot.eventbus.EventBusBuilder org.greenrobot.eventbus.EventBusBuilder:logger (
  org.greenrobot.eventbus.Logger logger) {
  guard = tt;
  ret(level()) = level(this);
  ret(obj_level()) = level(logger) ⊔ obj_level(logger) ⊔ obj_level(this);
  ret(obj_level(logger)) = level(logger) ⊔ obj_level(logger);
  ret(obj_level(this)) = level(logger) ⊔ obj_level(logger) ⊔ obj_level(this);
}