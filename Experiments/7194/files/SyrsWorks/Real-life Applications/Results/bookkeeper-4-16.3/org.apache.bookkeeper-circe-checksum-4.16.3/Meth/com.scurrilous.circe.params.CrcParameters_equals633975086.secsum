boolean com.scurrilous.circe.params.CrcParameters:equals (java.lang.Object
                                                            obj) {
  guard = tt;
  ret(level()) = pc ⊔ level(obj) ⊔ level(this) ⊔ obj_level(obj);
  ret(obj_level(obj)) = if obj_level(obj) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(this) = ⊥ then level(obj) = ⊤ ∧ obj_level(obj) = ⊥ else level(this) = ⊤ ∧ obj_level(obj) = ⊥) then ⊤ else ⊥;
  ret(obj_level(this)) = obj_level(obj);
}