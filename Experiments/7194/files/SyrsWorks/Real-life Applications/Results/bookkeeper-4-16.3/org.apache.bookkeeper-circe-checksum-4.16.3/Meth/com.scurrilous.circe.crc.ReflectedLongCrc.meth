com.scurrilous.circe.crc.ReflectedLongCrc(java.lang.String algorithm,int width,long poly,long init,long xorOut)
{
int j;
  long[] $stack13;
  byte $stack19;
  this#com.scurrilous.circe.crc.AbstractLongCrc(algorithm,width,init,xorOut);
 $stack13=new long[256];
 this.table=$stack13;
 poly = this.reflect(poly);
 width=0;
 label0:if (width>=256) goto label1;
 init=(long) width;
 j=0;
 label2:if (j>=8) goto label3;
 xorOut=(init & 1L);
 $stack19=(xorOut cmp 0L);
 if ($stack19==0) goto label4;
 init=(init>>>1);
 init=(!init & poly) |(!poly & init);
 goto label5;
 label4:init=(init>>>1);
 label5:j=(j+1);
 goto label2;
 label3:$stack13=this.table;
 $stack13[width]=init;
 width=(width+1);
 goto label0;
 label1:return;
 } 

