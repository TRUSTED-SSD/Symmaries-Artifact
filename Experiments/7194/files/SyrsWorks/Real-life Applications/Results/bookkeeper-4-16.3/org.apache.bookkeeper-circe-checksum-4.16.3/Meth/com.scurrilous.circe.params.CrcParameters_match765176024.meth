boolean com.scurrilous.circe.params.CrcParameters:match(int bitWidth,long polynomial,long initial,long xorOut,boolean reflected)
{
int $stack9;
  long $stack10;
  byte $stack11;
  boolean $stack17;
  $stack9=this.bitWidth;
 if (bitWidth!=$stack9) goto label0;
 $stack10=this.polynomial;
 $stack11=(polynomial cmp $stack10);
 if ($stack11!=0) goto label0;
 polynomial=this.initial;
 $stack11=(initial cmp polynomial);
 if ($stack11!=0) goto label0;
 polynomial=this.xorOut;
 $stack11=(xorOut cmp polynomial);
 if ($stack11!=0) goto label0;
 $stack17=this.reflected;
 if (reflected!=$stack17) goto label0;
 reflected=1;
 goto label1;
 label0:reflected=0;
 label1:return  reflected;
 } 

