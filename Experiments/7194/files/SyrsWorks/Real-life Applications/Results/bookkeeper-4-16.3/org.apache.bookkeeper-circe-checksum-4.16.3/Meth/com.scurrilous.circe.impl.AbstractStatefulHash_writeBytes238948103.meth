void com.scurrilous.circe.impl.AbstractStatefulHash:writeBytes(byte[] output,int index,int length)
{
long temp;
  int i;
  int $stack8;
  byte $stack9;
  int $stack10;
  temp = this.getLong();
 i=0;
 label0:if (i>=length) goto label1;
 $stack10=(index+i);
 $stack8=(int) temp;
 $stack9=(byte) $stack8;
 output[$stack10]=$stack9;
 temp=(temp>>>8);
 i=(i+1);
 goto label0;
 label1:return;
 } 

