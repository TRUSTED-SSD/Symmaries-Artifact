com.scurrilous.circe.crc.ReflectedIntCrc(java.lang.String algorithm,int width,int poly,int init,int xorOut)
{
int[] $stack9;
  int $stack14;
  this#com.scurrilous.circe.crc.AbstractIntCrc(algorithm,width,init,xorOut);
 $stack9=new int[256];
 this.table=$stack9;
 poly = this.reflect(poly);
 width=0;
 label0:if (width>=256) goto label1;
 xorOut=width;
 init=0;
 label2:if (init>=8) goto label3;
 $stack14=(xorOut & 1);
 if ($stack14==0) goto label4;
 xorOut=(xorOut>>>1);
 xorOut=(!xorOut & poly) |(!poly & xorOut);
 goto label5;
 label4:xorOut=(xorOut>>>1);
 label5:init=(init+1);
 goto label2;
 label3:$stack9=this.table;
 $stack9[width]=xorOut;
 width=(width+1);
 goto label0;
 label1:return;
 } 

