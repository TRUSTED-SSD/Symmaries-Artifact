java.io.File[] org.apache.bookkeeper.stream.storage.conf.StorageConfiguration:getRangeStoreDirs()
{
java.lang.String[] rangeStoreDirNames;
  java.io.File[] rangeStoreDirs;
  int i;
  int $stack6;
  java.io.File $stack7;
  java.lang.String $stack8;
  rangeStoreDirNames = this#org.apache.bookkeeper.stream.storage.conf.StorageConfiguration:getRangeStoreDirNames();
 i=#rangeStoreDirNames;
 rangeStoreDirs=new java.io.File[i];
 i=0;
 label0:$stack6=#rangeStoreDirNames;
 if (i>=$stack6) goto label1;
 $stack7=new java.io.File;
 $stack8=rangeStoreDirNames[i];
 $stack7#java.io.File($stack8);
 rangeStoreDirs[i]=$stack7;
 i=(i+1);
 goto label0;
 label1:return  rangeStoreDirs;
 } 

