static io.grpc.ServerServiceDefinition org.apache.bookkeeper.stream.proto.storage.MetaRangeServiceGrpc:bindService (
  org.apache.bookkeeper.stream.proto.storage.MetaRangeServiceGrpc$AsyncService
    service) {
  guard = tt;
  ret(level()) = level(service) ⊔ obj_level(service);
  ret(obj_level()) = level(service) ⊔ obj_level(service);
  ret(obj_level(service)) = level(service) ⊔ obj_level(service);
}