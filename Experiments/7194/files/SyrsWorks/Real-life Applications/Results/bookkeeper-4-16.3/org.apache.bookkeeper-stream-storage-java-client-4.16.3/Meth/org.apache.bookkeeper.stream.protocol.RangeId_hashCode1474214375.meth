int org.apache.bookkeeper.stream.protocol.RangeId:hashCode()
{
byte PRIME;
  int result;
  long $streamId;
  long $stack8;
  int $stack11;
  PRIME=59;
 $streamId = this.getStreamId();
 $stack11=(1 * 59);
 $stack8=($streamId>>>32);
 $streamId=(!$stack8 & $streamId) |(!$streamId & $stack8);
 result=(int) $streamId;
 result=($stack11+result);
 $streamId = this.getRangeId();
 $stack11=(result * 59);
 $stack8=($streamId>>>32);
 $streamId=(!$stack8 & $streamId) |(!$streamId & $stack8);
 result=(int) $streamId;
 result=($stack11+result);
 return  result;
 } 

