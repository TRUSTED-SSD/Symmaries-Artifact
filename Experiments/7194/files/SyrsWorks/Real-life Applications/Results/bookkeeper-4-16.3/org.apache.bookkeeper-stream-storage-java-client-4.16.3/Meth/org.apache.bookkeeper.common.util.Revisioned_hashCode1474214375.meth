int org.apache.bookkeeper.common.util.Revisioned:hashCode()
{
byte PRIME;
  int result;
  long $revision;
  java.lang.Object $value;
  long $stack7;
  int $stack10;
  PRIME=59;
 $revision = this.getRevision();
 $stack10=(1 * 59);
 $stack7=($revision>>>32);
 $revision=(!$stack7 & $revision) |(!$revision & $stack7);
 result=(int) $revision;
 result=($stack10+result);
 $value = this.getValue();
 result=(result * 59);
 if ($value!=null) goto label0;
 $stack10=43;
 goto label1;
 label0:$stack10 = $value.hashCode();
 label1:result=(result+$stack10);
 return  result;
 } 

