static java.net.InetSocketAddress org.apache.bookkeeper.clients.utils.NetUtils:of (
  org.apache.bookkeeper.stream.proto.common.Endpoint endpoint) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = level(endpoint) ⊔ obj_level(endpoint);
  ret(obj_level(endpoint)) = level(endpoint) ⊔ obj_level(endpoint);
}