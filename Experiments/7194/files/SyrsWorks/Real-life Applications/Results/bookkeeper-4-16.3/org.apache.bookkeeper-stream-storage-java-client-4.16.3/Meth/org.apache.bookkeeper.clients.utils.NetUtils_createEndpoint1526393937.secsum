static org.apache.bookkeeper.stream.proto.common.Endpoint org.apache.bookkeeper.clients.utils.NetUtils:createEndpoint (
  java.lang.String hostname, int port) {
  guard = tt;
  ret(level()) = ⊥;
  ret(obj_level()) = ⊥;
  ret(obj_level(hostname)) = level(hostname) ⊔ level(port) ⊔ obj_level(hostname);
}