static long org.apache.bookkeeper.common.util.Bytes:toLong (byte[] memory,
                                                            int index) {
  guard = tt;
  ret(level()) = level(index) ⊔ level(memory) ⊔ obj_level(memory);
  ret(obj_level(memory)) = obj_level(memory);
}