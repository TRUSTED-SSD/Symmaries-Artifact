io.netty.buffer.ByteBuf org.apache.bookkeeper.common.router.StringUtf8HashRouter:getRoutingKeyData(java.lang.String key)
{
int keyLen;
  io.netty.buffer.ByteBuf keyBuf;
  io.netty.buffer.PooledByteBufAllocator $stack5;
  java.nio.charset.Charset $stack7;
  keyLen = key.length();
 $stack5=io.netty.buffer.PooledByteBufAllocator.DEFAULT;
 keyBuf = $stack5.buffer(keyLen);
 $stack7=java.nio.charset.StandardCharsets.UTF_8;
 keyLen = keyBuf.writeCharSequence((java.lang.CharSequence)key,$stack7);
 return  keyBuf;
 } 

