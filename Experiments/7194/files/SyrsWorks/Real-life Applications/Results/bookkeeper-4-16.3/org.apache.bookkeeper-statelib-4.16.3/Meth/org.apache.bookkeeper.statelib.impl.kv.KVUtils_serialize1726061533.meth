static io.netty.buffer.ByteBuf org.apache.bookkeeper.statelib.impl.kv.KVUtils:serialize(io.netty.buffer.ByteBuf valBuf,long revision)
{
int serializedSize;
  io.netty.buffer.ByteBuf buffer;
  io.netty.buffer.PooledByteBufAllocator $stack6;
  serializedSize = valBuf.readableBytes();
dummyClass.highSourceMutint(serializedSize);
 serializedSize=(serializedSize+8);
 $stack6=io.netty.buffer.PooledByteBufAllocator.DEFAULT;
 buffer = $stack6.heapBuffer(serializedSize);
 buffer.writeLong(revision);
 buffer.writeBytes(valBuf);
 return  buffer;
 } 

