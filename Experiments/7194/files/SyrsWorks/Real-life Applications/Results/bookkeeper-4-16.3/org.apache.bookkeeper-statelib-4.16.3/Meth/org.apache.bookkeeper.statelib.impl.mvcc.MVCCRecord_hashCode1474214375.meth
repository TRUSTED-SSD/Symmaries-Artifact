int org.apache.bookkeeper.statelib.impl.mvcc.MVCCRecord:hashCode()
{
byte PRIME;
  int result;
  long $number;
  io.netty.buffer.ByteBuf $value;
  io.netty.util.Recycler$Handle $handle;
  org.apache.bookkeeper.stream.proto.kv.store.ValueType $valueType;
  long $stack17;
  int $stack20;
  PRIME=59;
 $number = this.getNumber();
 $stack20=(1 * 59);
 $stack17=($number>>>32);
 $number=(!$stack17 & $number) |(!$number & $stack17);
 result=(int) $number;
 result=($stack20+result);
 $number = this.getCreateRev();
 $stack20=(result * 59);
 $stack17=($number>>>32);
 $number=(!$stack17 & $number) |(!$number & $stack17);
 result=(int) $number;
 result=($stack20+result);
 $number = this.getModRev();
 $stack20=(result * 59);
 $stack17=($number>>>32);
 $number=(!$stack17 & $number) |(!$number & $stack17);
 result=(int) $number;
 result=($stack20+result);
 $number = this.getVersion();
 $stack20=(result * 59);
 $stack17=($number>>>32);
 $number=(!$stack17 & $number) |(!$number & $stack17);
 result=(int) $number;
 result=($stack20+result);
 $number = this.getExpireTime();
 $stack20=(result * 59);
 $stack17=($number>>>32);
 $number=(!$stack17 & $number) |(!$number & $stack17);
 result=(int) $number;
 result=($stack20+result);
 $value = this.getValue();
 result=(result * 59);
 if ($value!=null) goto label0;
 $stack20=43;
 goto label1;
 label0:$stack20 = $value.hashCode();
 label1:$handle = this.getHandle();
 result=(result+$stack20);
 result=(result * 59);
 if ($handle!=null) goto label2;
 $stack20=43;
 goto label3;
 label2:$stack20 = $handle.hashCode();
 label3:$valueType = this.getValueType();
 result=(result+$stack20);
 result=(result * 59);
 if ($valueType!=null) goto label4;
 $stack20=43;
 goto label5;
 label4:$stack20 = $valueType.hashCode();
 label5:result=(result+$stack20);
 return  result;
 } 

