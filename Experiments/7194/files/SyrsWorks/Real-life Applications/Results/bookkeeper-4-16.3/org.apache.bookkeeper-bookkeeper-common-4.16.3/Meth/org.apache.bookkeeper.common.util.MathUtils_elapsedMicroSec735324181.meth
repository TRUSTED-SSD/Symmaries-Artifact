static long org.apache.bookkeeper.common.util.MathUtils:elapsedMicroSec(long startNanoTime)
{
java.util.concurrent.TimeUnit $stack2;
  long $stack3;
  $stack2=java.util.concurrent.TimeUnit.NANOSECONDS;
 $stack3 = java.lang.System.nanoTime();
 startNanoTime=($stack3-startNanoTime);
 startNanoTime = $stack2.toMicros(startNanoTime);
 return  startNanoTime;
 } 

