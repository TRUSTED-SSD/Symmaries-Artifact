int org.apache.bookkeeper.common.util.Backoff$Constant:hashCode()
{
byte PRIME;
  int result;
  long $ms;
  long $stack8;
  int $stack11;
  PRIME=59;
 $ms = this.getMs();
 $stack11=(1 * 59);
 $stack8=($ms>>>32);
 $ms=(!$stack8 & $ms) |(!$ms & $stack8);
 result=(int) $ms;
 result=($stack11+result);
 $ms = this.getLimit();
 $stack11=(result * 59);
 $stack8=($ms>>>32);
 $ms=(!$stack8 & $ms) |(!$ms & $stack8);
 result=(int) $ms;
 result=($stack11+result);
 return  result;
 } 

