static java.lang.Long org.apache.bookkeeper.common.util.Backoff:lambda$constant$0 (
  long startMs, java.lang.Long @lastMs) {
  guard = tt;
  ret(level()) = pc ⊔ level(startMs);
  ret(obj_level()) = pc ⊔ level(startMs);
  ret(obj_level(@lastMs)) = obj_level(@lastMs);
}