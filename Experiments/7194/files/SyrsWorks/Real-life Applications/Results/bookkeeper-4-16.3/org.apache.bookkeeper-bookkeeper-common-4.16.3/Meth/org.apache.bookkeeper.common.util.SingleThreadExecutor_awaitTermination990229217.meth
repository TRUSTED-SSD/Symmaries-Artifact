boolean org.apache.bookkeeper.common.util.SingleThreadExecutor:awaitTermination(long timeout,java.util.concurrent.TimeUnit unit)
{
java.lang.Thread $stack4;
  boolean $stack7;
  $stack4=this.runner;
 timeout = unit.toMillis(timeout);
 $stack4.join(timeout);
 $stack4=this.runner;
 $stack7 = $stack4.isAlive();
 return  $stack7;
 } 

