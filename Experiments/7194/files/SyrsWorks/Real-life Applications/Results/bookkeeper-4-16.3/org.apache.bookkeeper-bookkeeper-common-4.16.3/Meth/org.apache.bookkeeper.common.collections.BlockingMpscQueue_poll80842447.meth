java.lang.Object org.apache.bookkeeper.common.collections.BlockingMpscQueue:poll(long timeout,java.util.concurrent.TimeUnit unit)
{
int idleCounter;
  java.lang.Object item;
  long $stack8;
  boolean $stack11;
  byte $stack13;
  org.jctools.queues.MessagePassingQueue$WaitStrategy $stack14;
  java.lang.InterruptedException $stack18;
  $stack8 = java.lang.System.nanoTime();
 timeout = unit.toNanos(timeout);
 timeout=($stack8+timeout);
 idleCounter=0;
 label0:item = this.relaxedPoll();
 if (item!=null) goto label1;
 $stack11 = java.lang.Thread.interrupted();
 if ($stack11==0) goto label2;
 $stack18=new java.lang.InterruptedException;
 $stack18#java.lang.InterruptedException();
 throw $stack18;
 label2:$stack8 = java.lang.System.nanoTime();
 $stack13=($stack8 cmp timeout);
 if ($stack13<=0) goto label3;
 return  null;
 label3:$stack14=org.apache.bookkeeper.common.collections.BlockingMpscQueue.WAIT_STRATEGY;
 idleCounter = $stack14.idle(idleCounter);
 goto label0;
 label1:return  item;
 } 

