static org.apache.bookkeeper.common.util.Backoff$Exponential org.apache.bookkeeper.common.util.Backoff$Exponential:of (
  long startMs, long maxMs, int multiplier) {
  guard = tt;
  ret(level()) = pc ⊔ level(maxMs) ⊔ level(multiplier) ⊔ level(startMs);
  ret(obj_level()) = level(maxMs) ⊔ level(multiplier) ⊔ level(startMs);
}