void org.apache.bookkeeper.common.stats.BroadCastStatsLogger$Two$2:addLatency(long eventLatency,java.util.concurrent.TimeUnit unit)
{
org.apache.bookkeeper.stats.Counter $stack7;
  eventLatency = unit.toMillis(eventLatency);
 $stack7=this.val$firstCounter;
 $stack7.addCount(eventLatency);
 $stack7=this.val$secondCounter;
 $stack7.addCount(eventLatency);
 return;
 } 

