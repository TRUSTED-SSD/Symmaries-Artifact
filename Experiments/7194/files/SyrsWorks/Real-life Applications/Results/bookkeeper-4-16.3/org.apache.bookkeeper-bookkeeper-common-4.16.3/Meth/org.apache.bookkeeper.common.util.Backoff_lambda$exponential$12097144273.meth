static java.lang.Long org.apache.bookkeeper.common.util.Backoff:lambda$exponential$1(int multiplier,long maxMs,java.lang.Long @lastMs)
{
long $stack4;
  long $stack5;
  java.lang.Long lastMs;
lastMs=@lastMs;
$stack4 = lastMs.longValue();
 $stack5=(long) multiplier;
 $stack4=($stack4 * $stack5);
 maxMs = java.lang.Math.min($stack4,maxMs);
 lastMs = java.lang.Long.valueOf(maxMs);
 return  lastMs;
 } 

