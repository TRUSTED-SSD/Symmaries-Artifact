int org.apache.bookkeeper.common.util.Backoff$Exponential:hashCode()
{
byte PRIME;
  int result;
  long $startMs;
  long $stack8;
  int $stack11;
  PRIME=59;
 $startMs = this.getStartMs();
 $stack11=(1 * 59);
 $stack8=($startMs>>>32);
 $startMs=(!$stack8 & $startMs) |(!$startMs & $stack8);
 result=(int) $startMs;
 result=($stack11+result);
 $startMs = this.getMaxMs();
 $stack11=(result * 59);
 $stack8=($startMs>>>32);
 $startMs=(!$stack8 & $startMs) |(!$startMs & $stack8);
 result=(int) $startMs;
 result=($stack11+result);
 result=(result * 59);
 $stack11 = this.getMultiplier();
 result=(result+$stack11);
 result=(result * 59);
 $stack11 = this.getLimit();
 result=(result+$stack11);
 return  result;
 } 

