static int org.apache.bookkeeper.common.util.MathUtils:signSafeMod(long dividend,int divisor)
{
int mod;
  long $stack4;
  $stack4=(long) divisor;
 dividend=(dividend%$stack4);
 mod=(int) dividend;
 if (mod>=0) goto label0;
 mod=(mod+divisor);
 label0:return  mod;
 } 

