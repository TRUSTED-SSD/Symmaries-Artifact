void org.apache.bookkeeper.common.conf.ConfigDef:writeSentence(java.io.PrintStream stream,java.lang.String prefix,java.lang.String @sentence)
{
byte max;
  java.lang.String[] words;
  int i;
  int current;
  int $stack11;
  java.lang.String sentence;
sentence=@sentence;
max=80;
 words = sentence.split(" ");
 i=0;
 dummyClass.lowSink(prefix);
stream.print(prefix);
 current = prefix.length();
 label0:$stack11=#words;
 if (i>=$stack11) goto label1;
 sentence=words[i];
 $stack11 = sentence.length();
 if ($stack11>80) goto label2;
 $stack11 = sentence.length();
 $stack11=(current+$stack11);
 if ($stack11>80) goto label3;
 label2:if (i==0) goto label4;
 dummyClass.lowSink(" ");
stream.print(" ");
 label4:dummyClass.lowSink(sentence);
stream.print(sentence);
 $stack11 = sentence.length();
 $stack11=($stack11+1);
 current=(current+$stack11);
 goto label5;
 label3:stream.println();
 dummyClass.lowSink(prefix);
stream.print(prefix);
 dummyClass.lowSink(sentence);
stream.print(sentence);
 current = prefix.length();
 $stack11 = sentence.length();
 current=(current+$stack11);
 label5:i=(i+1);
 goto label0;
 label1:stream.println();
 return;
 } 

