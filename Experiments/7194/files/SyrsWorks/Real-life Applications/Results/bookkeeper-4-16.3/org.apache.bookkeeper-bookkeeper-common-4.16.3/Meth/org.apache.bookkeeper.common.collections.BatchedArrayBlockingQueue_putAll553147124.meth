void org.apache.bookkeeper.common.collections.BatchedArrayBlockingQueue:putAll(java.lang.Object[] a,int offset,int len)
{
int published;
  label0:if (len<=0) goto label1;
 published = this#org.apache.bookkeeper.common.collections.BatchedArrayBlockingQueue:internalPutAll(a,offset,len);
 offset=(offset+published);
 len=(len-published);
 goto label0;
 label1:return;
 } 

