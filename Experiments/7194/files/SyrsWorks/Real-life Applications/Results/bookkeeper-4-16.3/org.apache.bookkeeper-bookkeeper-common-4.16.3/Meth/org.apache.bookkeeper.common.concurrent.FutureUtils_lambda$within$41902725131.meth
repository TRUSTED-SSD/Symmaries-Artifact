static void org.apache.bookkeeper.common.concurrent.FutureUtils:lambda$within$4(java.util.concurrent.ScheduledFuture task,java.lang.Object value,java.lang.Throwable throwable)
{
boolean $stack3;
  org.slf4j.Logger $stack4;
  $stack3 = task.cancel((boolean)1);
 if ($stack3!=0) goto label0;
 $stack4=org.apache.bookkeeper.common.concurrent.FutureUtils.log;
 $stack3 = $stack4.isDebugEnabled();
 if ($stack3==0) goto label0;
 $stack4=org.apache.bookkeeper.common.concurrent.FutureUtils.log;
 dummyClass.lowSink("Failed to cancel the timeout task");
$stack4.debug("Failed to cancel the timeout task");
 label0:return;
 } 

