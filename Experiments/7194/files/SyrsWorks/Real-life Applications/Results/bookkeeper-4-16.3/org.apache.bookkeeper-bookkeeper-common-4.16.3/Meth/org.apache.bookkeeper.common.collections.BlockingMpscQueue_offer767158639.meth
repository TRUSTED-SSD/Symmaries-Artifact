boolean org.apache.bookkeeper.common.collections.BlockingMpscQueue:offer(java.lang.Object e,long timeout,java.util.concurrent.TimeUnit unit)
{
long $stack7;
  boolean $stack9;
  byte $stack11;
  java.lang.InterruptedException $stack15;
  $stack7 = java.lang.System.nanoTime();
 timeout = unit.toNanos(timeout);
 timeout=($stack7+timeout);
 label0:$stack9 = this.relaxedOffer(e);
 if ($stack9!=0) goto label1;
 $stack7 = java.lang.System.nanoTime();
 $stack11=($stack7 cmp timeout);
 if ($stack11<=0) goto label2;
 return  0;
 label2:$stack9 = java.lang.Thread.interrupted();
 if ($stack9==0) goto label0;
 $stack15=new java.lang.InterruptedException;
 $stack15#java.lang.InterruptedException();
 throw $stack15;
 label1:return  1;
 } 

