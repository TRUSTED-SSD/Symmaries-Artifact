long org.apache.bookkeeper.common.util.nativeio.NativeIOImpl:lseek (int fd,
                                                                    long
                                                                    offset,
                                                                    int
                                                                    whence) {
  guard = tt;
  ret(level()) = pc ⊔ level(fd) ⊔ level(offset) ⊔ level(whence);
  ret(obj_level(this)) = obj_level(this);
}