int org.apache.bookkeeper.common.util.nativeio.NativeIOImpl:pwrite (int fd,
                                                                    long
                                                                    pointer,
                                                                    int count,
                                                                    long
                                                                    offset) {
  guard = tt;
  ret(level()) = pc ⊔ level(count) ⊔ level(fd) ⊔ level(offset) ⊔ level(pointer);
  ret(obj_level(this)) = obj_level(this);
}