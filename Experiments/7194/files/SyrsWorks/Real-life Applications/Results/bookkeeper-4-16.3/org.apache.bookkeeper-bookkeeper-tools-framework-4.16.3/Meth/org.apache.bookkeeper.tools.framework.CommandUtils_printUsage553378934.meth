static void org.apache.bookkeeper.tools.framework.CommandUtils:printUsage(java.io.PrintStream printer,java.lang.String usage)
{
int indent;
  int firstIndent;
  java.lang.String $stack4;
  $stack4="Usage:";
 indent = $stack4.length();
 indent=(indent/4);
 indent=(indent+1);
 indent=(indent * 4);
 $stack4="Usage:";
 firstIndent = $stack4.length();
 firstIndent=(indent-firstIndent);
 dummyClass.lowSink("Usage:");
printer.print("Usage:");
 org.apache.bookkeeper.tools.framework.CommandUtils.printDescription(printer,firstIndent,indent,usage);
 printer.println();
 return;
 } 

