static java.util.SortedSet org.apache.bookkeeper.common.util.affinity.impl.IsolatedProcessors:parseProcessorRange(java.lang.String @range)
{
java.util.TreeSet processors;
  java.lang.String[] l2;
  int l3;
  int l4;
  java.lang.String[] parts;
  int first;
  int last;
  boolean $stack13;
  java.lang.Integer $stack18;
  java.lang.String range;
range=@range;
processors=new java.util.TreeSet;
 processors#java.util.TreeSet();
 range = org.apache.commons.lang3.StringUtils.trim(range);
 l2 = range.split(",");
 l3=#l2;
 l4=0;
 label0:if (l4>=l3) goto label1;
 range=l2[l4];
 $stack13 = range.contains((java.lang.CharSequence)"-");
 if ($stack13==0) goto label2;
 parts = range.split("-");
 range=parts[0];
 first = java.lang.Integer.parseInt(range);
 range=parts[1];
 last = java.lang.Integer.parseInt(range);
 label3:if (first>last) goto label4;
 $stack18 = java.lang.Integer.valueOf(first);
 processors.add((java.lang.Object)$stack18);
 first=(first+1);
 goto label3;
 label4:goto label5;
 label2:$stack13 = range.isEmpty();
 if ($stack13!=0) goto label5;
 last = java.lang.Integer.parseInt(range);
 $stack18 = java.lang.Integer.valueOf(last);
 processors.add((java.lang.Object)$stack18);
 label5:l4=(l4+1);
 goto label0;
 label1:return  processors;
 } 

