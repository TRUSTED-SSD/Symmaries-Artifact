static java.lang.String org.apache.bookkeeper.stats.prometheus.PrometheusTextFormat:getTypeNameSuffix (
  io.prometheus.client.Collector$Type type) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(type);
  ret(obj_level()) = pc ⊔ obj_level(type);
  ret(obj_level(type)) = obj_level(type);
}