void org.apache.bookkeeper.stats.prometheus.LongAdderCounter:addLatency(long eventLatency,java.util.concurrent.TimeUnit unit)
{
java.util.concurrent.atomic.LongAdder $stack7;
  eventLatency = unit.toMillis(eventLatency);
 $stack7=this.counter;
 $stack7.add(eventLatency);
 return;
 } 

