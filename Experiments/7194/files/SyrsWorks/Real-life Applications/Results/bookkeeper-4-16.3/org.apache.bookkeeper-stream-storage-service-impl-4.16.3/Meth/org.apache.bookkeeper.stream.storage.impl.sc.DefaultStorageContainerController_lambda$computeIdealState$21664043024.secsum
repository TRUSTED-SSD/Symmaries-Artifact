static boolean org.apache.bookkeeper.stream.storage.impl.sc.DefaultStorageContainerController:lambda$computeIdealState$2 (
  java.util.Set currentCluster, java.util.Map$Entry serverEntry) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(currentCluster) ⊔ obj_level(serverEntry);
  ret(obj_level(currentCluster)) = if obj_level(currentCluster) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(currentCluster) = ⊥ ∧ obj_level(serverEntry) = ⊤ then ⊤ else ⊥;
  ret(obj_level(serverEntry)) = if obj_level(serverEntry) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(currentCluster) = ⊤ ∧ obj_level(serverEntry) = ⊥ then ⊤ else ⊥;
}