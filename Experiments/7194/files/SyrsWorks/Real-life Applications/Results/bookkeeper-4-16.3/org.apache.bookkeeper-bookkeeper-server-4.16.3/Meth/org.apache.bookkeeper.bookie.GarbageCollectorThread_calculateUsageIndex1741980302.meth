int org.apache.bookkeeper.bookie.GarbageCollectorThread:calculateUsageIndex(int numBuckets,double usage)
{
double $stack4;
  int $stack6;
  $stack6=(numBuckets-1);
 $stack4=(double) numBuckets;
 usage=(usage * $stack4);
 usage = java.lang.Math.floor(usage);
 numBuckets=(int) usage;
 numBuckets = java.lang.Math.min($stack6,numBuckets);
 return  numBuckets;
 } 

