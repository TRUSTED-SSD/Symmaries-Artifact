static void org.apache.bookkeeper.client.SyncCallbackUtils:finish (int rc,
                                                                   java.lang.Object
                                                                    result,
                                                                   java.util.concurrent.CompletableFuture
                                                                    future) {
  guard = pc ⊔ level(rc) = ⊥;
  ret(obj_level(future)) = ⊤;
  ret(obj_level(result)) = ⊤;
}