static org.apache.bookkeeper.bookie.LedgerStorage org.apache.bookkeeper.bookie.BookieResources:createLedgerStorage(org.apache.bookkeeper.conf.ServerConfiguration conf,org.apache.bookkeeper.meta.LedgerManager ledgerManager,org.apache.bookkeeper.bookie.LedgerDirsManager ledgerDirsManager,org.apache.bookkeeper.bookie.LedgerDirsManager indexDirsManager,org.apache.bookkeeper.stats.StatsLogger statsLogger,io.netty.buffer.ByteBufAllocator allocator)
{
java.lang.String ledgerStorageClass;
  org.apache.bookkeeper.bookie.LedgerStorage storage;
  org.slf4j.Logger $stack9;
  org.apache.bookkeeper.bookie.CheckpointSource $stack11;
  ledgerStorageClass = conf.getLedgerStorageClass();
 $stack9=org.apache.bookkeeper.bookie.BookieResources.log;
 dummyClass.lowSink("Using ledger storage: {}");
$stack9.info("Using ledger storage: {}",(java.lang.Object)ledgerStorageClass);
 storage = org.apache.bookkeeper.bookie.LedgerStorageFactory.createLedgerStorage(ledgerStorageClass);
 storage.initialize(conf,ledgerManager,ledgerDirsManager,indexDirsManager,statsLogger,allocator);
 $stack11=org.apache.bookkeeper.bookie.CheckpointSource.DEFAULT;
 storage.setCheckpointSource($stack11);
 return  storage;
 } 

