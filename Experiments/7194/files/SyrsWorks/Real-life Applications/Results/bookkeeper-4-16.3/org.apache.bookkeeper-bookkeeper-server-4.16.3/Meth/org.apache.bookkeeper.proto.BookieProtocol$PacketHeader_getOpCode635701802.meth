static byte org.apache.bookkeeper.proto.BookieProtocol$PacketHeader:getOpCode(int packetHeader)
{
byte version;
  version = org.apache.bookkeeper.proto.BookieProtocol$PacketHeader.getVersion(packetHeader);
 if (version!=0) goto label0;
 version=(byte) packetHeader;
 return  version;
 label0:packetHeader=(packetHeader>>16);
 packetHeader=(packetHeader & 255);
 version=(byte) packetHeader;
 return  version;
 } 

