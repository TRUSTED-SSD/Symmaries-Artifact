static org.apache.bookkeeper.bookie.storage.directentrylogger.Header
{
io.netty.buffer.ByteBuf buf;
  byte[] $stack1;
  int $stack10;
  $stack1=new byte[4096];
 org.apache.bookkeeper.bookie.storage.directentrylogger.Header.EMPTY_HEADER=$stack1;
 $stack1=org.apache.bookkeeper.bookie.storage.directentrylogger.Header.EMPTY_HEADER;
 buf = io.netty.buffer.Unpooled.wrappedBuffer($stack1);
 buf.setByte(0,66);
 buf.setByte(1,75);
 buf.setByte(2,76);
 buf.setByte(3,79);
 buf.setInt(4,1);
 $stack10 = buf.capacity();
 $stack10=($stack10-1024);
 $stack10=($stack10-4);
 buf.setInt(1024,$stack10);
 buf.setLong(1028,-1L);
 return;
 } 

