void org.apache.bookkeeper.client.RoundRobinDistributionSchedule$WriteSetImpl:addMissingIndices(int maxIndex)
{
int oldSize;
  int i;
  boolean $stack6;
  i=this.size;
 if (i>=maxIndex) goto label0;
 oldSize=this.size;
 this#org.apache.bookkeeper.client.RoundRobinDistributionSchedule$WriteSetImpl(maxIndex);
 i=0;
 label1:if (i>=maxIndex) goto label0;
 if (oldSize>=maxIndex) goto label0;
 $stack6 = this.contains(i);
 if ($stack6!=0) goto label2;
 this.set(oldSize,i);
 oldSize=(oldSize+1);
 label2:i=(i+1);
 goto label1;
 label0:return;
 } 

