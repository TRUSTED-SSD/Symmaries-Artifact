boolean org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy:allowRetry(int retryCount,long elapsedRetryTime)
{
int $stack4;
  long $stack5;
  byte $stack6;
  boolean $stack7;
  $stack4=this.maxRetries;
 if (retryCount>$stack4) goto label0;
 $stack5=this.deadline;
 $stack6=(elapsedRetryTime cmp $stack5);
 if ($stack6>=0) goto label0;
 $stack7=1;
 goto label1;
 label0:$stack7=0;
 label1:return  $stack7;
 } 

