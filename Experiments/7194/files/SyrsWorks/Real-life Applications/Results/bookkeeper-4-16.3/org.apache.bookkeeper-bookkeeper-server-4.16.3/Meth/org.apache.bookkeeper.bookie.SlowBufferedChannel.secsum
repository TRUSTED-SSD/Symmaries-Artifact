org.apache.bookkeeper.bookie.SlowBufferedChannel (io.netty.buffer.ByteBufAllocator
                                                    allocator,
                                                  java.nio.channels.FileChannel
                                                    fc,
                                                  int capacity) {
  guard = tt;
  ret(obj_level(allocator)) = obj_level(allocator);
  ret(obj_level(fc)) = pc ⊔ obj_level(fc);
  ret(obj_level(this)) = pc ⊔ obj_level(this);
}