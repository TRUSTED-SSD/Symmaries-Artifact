long org.apache.bookkeeper.bookie.FileInfo:size()
{
long rc;
  byte $stack4;
  this.checkOpen((boolean)0);
 rc=this.size;
 rc=(rc-1024L);
 $stack4=(rc cmp 0L);
 if ($stack4>=0) goto label0;
 rc=0L;
 label0:return  rc;
 } 

