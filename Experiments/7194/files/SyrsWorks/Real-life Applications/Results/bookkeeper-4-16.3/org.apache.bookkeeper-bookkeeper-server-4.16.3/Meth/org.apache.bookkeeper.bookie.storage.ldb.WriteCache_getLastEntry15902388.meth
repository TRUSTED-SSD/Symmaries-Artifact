io.netty.buffer.ByteBuf org.apache.bookkeeper.bookie.storage.ldb.WriteCache:getLastEntry(long ledgerId)
{
long lastEntryId;
  org.apache.bookkeeper.util.collections.ConcurrentLongLongHashMap $stack5;
  byte $stack7;
  io.netty.buffer.ByteBuf $stack8;
  $stack5=this.lastEntryMap;
 lastEntryId = $stack5.get(ledgerId);
 $stack7=(lastEntryId cmp -1L);
 if ($stack7!=0) goto label0;
 return  null;
 label0:$stack8 = this.get(ledgerId,lastEntryId);
 return  $stack8;
 } 

