java.util.concurrent.CompletableFuture org.apache.bookkeeper.bookie.datainteg.DataIntegrityCheckImpl:runPreBootCheck(java.lang.String reason)
{
java.util.concurrent.CompletableFuture $stack2;
  $stack2=this.preBootFuture;
 if ($stack2!=null) goto label0;
 $stack2 = this#org.apache.bookkeeper.bookie.datainteg.DataIntegrityCheckImpl:runPreBootSequence(reason);
 this.preBootFuture=$stack2;
 label0:$stack2=this.preBootFuture;
 return  $stack2;
 } 

