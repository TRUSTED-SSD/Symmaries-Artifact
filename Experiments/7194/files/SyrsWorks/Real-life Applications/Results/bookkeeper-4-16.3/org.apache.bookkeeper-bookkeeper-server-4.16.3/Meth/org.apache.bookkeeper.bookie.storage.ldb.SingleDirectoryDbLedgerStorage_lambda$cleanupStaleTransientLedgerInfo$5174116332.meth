static boolean org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage:lambda$cleanupStaleTransientLedgerInfo$5(long ledgerId,org.apache.bookkeeper.bookie.storage.ldb.TransientLedgerInfo ledgerInfo)
{
boolean isStale;
  isStale = ledgerInfo.isStale();
 if (isStale==0) goto label0;
 ledgerInfo.close();
 label0:return  isStale;
 } 

