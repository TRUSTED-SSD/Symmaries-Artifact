void org.apache.bookkeeper.bookie.InterleavedLedgerStorage:setMasterKey(long ledgerId,byte[] masterKey)
{
org.apache.bookkeeper.bookie.LedgerCache $stack4;
  $stack4=this.ledgerCache;
 $stack4.setMasterKey(ledgerId,masterKey);
 return;
 } 

