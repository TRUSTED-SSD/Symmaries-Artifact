void org.apache.bookkeeper.client.LedgerHandle$4:onFailure (java.lang.Throwable
                                                              cause) {
  guard = tt;
  ret(obj_level(cause)) = if obj_level(cause) = ⊤ ∨ pc ≠ ⊤ ∧ level(cause) = ⊤ ∧ obj_level(cause) = ⊥ ∧ obj_level(this) = ⊤ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ pc ≠ ⊤ ∧ obj_level(cause) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
}