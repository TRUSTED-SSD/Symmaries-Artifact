static byte[] org.apache.bookkeeper.proto.BookieProtoEncoding$RequestEnDeCoderPreV3:readMasterKey (
  io.netty.buffer.ByteBuf packet) {
  p0: '0 -> dummyClass ('0) -> level;
  guard = tt;
  ret(level()) = pc ⊔ level(packet) ⊔ obj_level(packet);
  ret(obj_level()) = p0 ⊔ pc ⊔ level(packet) ⊔ obj_level(packet);
  ret(obj_level(packet)) = pc ⊔ level(packet) ⊔ obj_level(packet);
}