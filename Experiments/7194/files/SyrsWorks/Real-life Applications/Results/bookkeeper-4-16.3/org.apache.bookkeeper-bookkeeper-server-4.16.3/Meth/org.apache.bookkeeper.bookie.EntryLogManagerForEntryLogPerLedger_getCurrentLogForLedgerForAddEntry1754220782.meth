org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger:getCurrentLogForLedgerForAddEntry(long ledgerId,int entrySize,boolean rollLog)
{
java.util.concurrent.locks.Lock lock;
  org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger$BufferedLogChannelWithDirInfo logChannelWithDirInfo;
  org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel logChannel;
  boolean diskFull;
  boolean allDisksFull;
  java.lang.Throwable l12;
  long $stack15;
  org.apache.bookkeeper.bookie.LedgerDirsManager $stack18;
  java.lang.StringBuilder $stack20;
  java.lang.String $stack29;
  lock = this.getLock(ledgerId);
 lock.lock();
 label0:logChannelWithDirInfo = this.getCurrentLogWithDirInfoForLedger(ledgerId);
 logChannel=null;
 if (logChannelWithDirInfo==null) goto label1;
 logChannel = logChannelWithDirInfo.getLogChannel();
 label1:if (rollLog==0) goto label2;
 $stack15=(long) entrySize;
 rollLog = this.reachEntryLogLimit(logChannel,$stack15);
 goto label3;
 label2:$stack15=(long) entrySize;
 rollLog = this.readEntryLogHardLimit(logChannel,$stack15);
 label3:if (logChannel!=null) goto label4;
 diskFull=0;
 goto label5;
 label4:diskFull = org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger$BufferedLogChannelWithDirInfo.access$1400(logChannelWithDirInfo);
 label5:$stack18=this.ledgerDirsManager;
 allDisksFull = $stack18.hasWritableLedgerDirs();
 if (allDisksFull!=0) goto label6;
 allDisksFull=1;
 goto label7;
 label6:allDisksFull=0;
 label7:if (diskFull==0) goto label8;
 if (allDisksFull==0) goto label9;
 label8:if (rollLog!=0) goto label9;
 if (logChannel!=null) goto label10;
 label9:if (logChannel==null) goto label11;
 logChannel.flushAndForceWriteIfRegularFlush((boolean)0);
 label11:$stack20=new java.lang.StringBuilder;
 $stack20#java.lang.StringBuilder();
 $stack20 = $stack20.append(": diskFull = ");
 $stack20 = $stack20.append(diskFull);
 $stack20 = $stack20.append(", allDisksFull = ");
 $stack20 = $stack20.append(allDisksFull);
 $stack20 = $stack20.append(", reachEntryLogLimit = ");
 $stack20 = $stack20.append(rollLog);
 $stack20 = $stack20.append(", logChannel = ");
 $stack20 = $stack20.append((java.lang.Object)logChannel);
 $stack29 = $stack20.toString();
 this.createNewLog(ledgerId,$stack29);
 label10:logChannel = this.getCurrentLogForLedger(ledgerId);
 label12:lock.unlock();
 return  logChannel;
 label13:l12 = catch;
 label14:lock.unlock();
 throw l12;
 catch java.lang.Throwable: label0 - label12 : label13;
catch java.lang.Throwable: label13 - label14 : label13;
} 

