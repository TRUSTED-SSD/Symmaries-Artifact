void org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand:scanEntryLogForPositionRange(org.apache.bookkeeper.conf.ServerConfiguration conf,long logId,long rangeStartPos,long rangeEndPos,boolean printMsg)
{
org.apache.commons.lang.mutable.MutableBoolean entryFound;
  java.lang.Object[] $stack10;
  org.slf4j.Logger $stack11;
  java.lang.Long $stack14;
  java.lang.String $stack16;
  org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand$1 $stack22;
  $stack11=org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand.LOG;
 $stack10=new java.lang.Object[4];
 $stack14 = java.lang.Long.valueOf(logId);
 $stack10[0]=$stack14;
 $stack16 = java.lang.Long.toHexString(logId);
 $stack10[1]=$stack16;
 $stack14 = java.lang.Long.valueOf(rangeStartPos);
 $stack10[2]=$stack14;
 $stack14 = java.lang.Long.valueOf(rangeEndPos);
 $stack10[3]=$stack14;
 dummyClass.lowSink("Scan entry log {} ({}.log) for PositionRange: {} - {}");
$stack11.info("Scan entry log {} ({}.log) for PositionRange: {} - {}",$stack10);
 entryFound=new org.apache.commons.lang.mutable.MutableBoolean;
 entryFound#org.apache.commons.lang.mutable.MutableBoolean(0);
 $stack22=new org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand$1;
 $stack22#org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand$1(this,rangeEndPos,rangeStartPos,printMsg,entryFound);
 this#org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand:scanEntryLog(conf,logId,(org.apache.bookkeeper.bookie.storage.EntryLogScanner)$stack22);
 printMsg = entryFound.booleanValue();
 if (printMsg!=0) goto label0;
 $stack11=org.apache.bookkeeper.tools.cli.commands.bookie.ReadLogCommand.LOG;
 $stack10=new java.lang.Object[4];
 $stack14 = java.lang.Long.valueOf(logId);
 $stack10[0]=$stack14;
 $stack16 = java.lang.Long.toHexString(logId);
 $stack10[1]=$stack16;
 $stack14 = java.lang.Long.valueOf(rangeStartPos);
 $stack10[2]=$stack14;
 $stack14 = java.lang.Long.valueOf(rangeEndPos);
 $stack10[3]=$stack14;
 dummyClass.lowSink("Entry log {} ({}.log) doesn\'t has any entry in the range {} - {}. Probably the position range, you have provided is lesser than the LOGFILE_HEADER_SIZE (1024) or greater than the current log filesize.");
$stack11.info("Entry log {} ({}.log) doesn\'t has any entry in the range {} - {}. Probably the position range, you have provided is lesser than the LOGFILE_HEADER_SIZE (1024) or greater than the current log filesize.",$stack10);
 label0:return;
 } 

