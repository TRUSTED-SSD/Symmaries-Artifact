io.netty.buffer.ByteBuf org.apache.bookkeeper.bookie.storage.directentrylogger.DirectReader:readEntryAt(int offset)
{
int sizeOffset;
  int entrySize;
  long $stack4;
  java.io.IOException $stack8;
  org.apache.bookkeeper.common.util.ExceptionMessageHelper $stack9;
  java.lang.String $stack10;
  java.lang.Integer $stack13;
  io.netty.buffer.ByteBuf $stack24;
  $stack4=(long) offset;
 org.apache.bookkeeper.bookie.storage.directentrylogger.DirectReader.assertValidEntryOffset($stack4);
 sizeOffset=(offset-4);
 if (sizeOffset>=0) goto label0;
 $stack8=new java.io.IOException;
 $stack9 = org.apache.bookkeeper.common.util.ExceptionMessageHelper.exMsg("Invalid offset, buffer size missing");
 $stack10=this.filename;
 $stack9 = $stack9.kv("file",(java.lang.Object)$stack10);
 $stack13 = java.lang.Integer.valueOf(offset);
 $stack9 = $stack9.kv("offset",(java.lang.Object)$stack13);
 $stack10 = $stack9.toString();
 $stack8#java.io.IOException($stack10);
 throw $stack8;
 label0:$stack4=(long) sizeOffset;
 entrySize = this.readIntAt($stack4);
 if (entrySize!=0) goto label1;
 this#org.apache.bookkeeper.bookie.storage.directentrylogger.DirectReader:clearCache();
 $stack4=(long) sizeOffset;
 entrySize = this.readIntAt($stack4);
 label1:sizeOffset=this.maxSaneEntrySize;
 if (entrySize>sizeOffset) goto label2;
 if (entrySize>0) goto label3;
 label2:$stack8=new java.io.IOException;
 $stack9 = org.apache.bookkeeper.common.util.ExceptionMessageHelper.exMsg("Invalid entry size");
 $stack10=this.filename;
 $stack9 = $stack9.kv("file",(java.lang.Object)$stack10);
 $stack13 = java.lang.Integer.valueOf(offset);
 $stack9 = $stack9.kv("offset",(java.lang.Object)$stack13);
 offset=this.maxSaneEntrySize;
 $stack13 = java.lang.Integer.valueOf(offset);
 $stack9 = $stack9.kv("maxSaneEntrySize",(java.lang.Object)$stack13);
 $stack13 = java.lang.Integer.valueOf(entrySize);
 $stack9 = $stack9.kv("readEntrySize",(java.lang.Object)$stack13);
 $stack10 = $stack9.toString();
 $stack8#java.io.IOException($stack10);
 throw $stack8;
 label3:$stack4=(long) offset;
 $stack24 = this.readBufferAt($stack4,entrySize);
 return  $stack24;
 } 

