void org.apache.bookkeeper.bookie.Journal:logAddEntry(io.netty.buffer.ByteBuf entry,boolean ackBeforeSync,org.apache.bookkeeper.proto.BookkeeperInternalCallbacks$WriteCallback cb,java.lang.Object ctx)
{
long ledgerId;
  long entryId;
  int $stack9;
  $stack9 = entry.readerIndex();
 $stack9=($stack9+0);
 ledgerId = entry.getLong($stack9);
 $stack9 = entry.readerIndex();
 $stack9=($stack9+8);
 entryId = entry.getLong($stack9);
 this#org.apache.bookkeeper.bookie.Journal(ledgerId,entryId,entry,ackBeforeSync,cb,ctx);
 return;
 } 

