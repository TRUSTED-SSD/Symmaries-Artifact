static boolean org.apache.bookkeeper.proto.LocalBookiesRegistry:isLocalBookie (
  org.apache.bookkeeper.net.BookieId address) {
  guard = tt;
  ret(level()) = pc ⊔ obj_level(address);
  ret(obj_level(address)) = obj_level(address);
}