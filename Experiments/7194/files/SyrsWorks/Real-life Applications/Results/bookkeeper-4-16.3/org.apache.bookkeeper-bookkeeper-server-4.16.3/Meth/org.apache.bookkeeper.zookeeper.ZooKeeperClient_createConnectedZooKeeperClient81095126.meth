static org.apache.bookkeeper.zookeeper.ZooKeeperClient org.apache.bookkeeper.zookeeper.ZooKeeperClient:createConnectedZooKeeperClient(java.lang.String connectString,int sessionTimeoutMs,java.util.Set childWatchers,org.apache.bookkeeper.zookeeper.RetryPolicy operationRetryPolicy)
{
org.apache.bookkeeper.zookeeper.ZooKeeperClient$Builder $stack4;
  org.apache.bookkeeper.zookeeper.ZooKeeperClient $stack9;
  $stack4 = org.apache.bookkeeper.zookeeper.ZooKeeperClient.newBuilder();
 $stack4 = $stack4.connectString(connectString);
 $stack4 = $stack4.sessionTimeoutMs(sessionTimeoutMs);
 $stack4 = $stack4.watchers(childWatchers);
 $stack4 = $stack4.operationRetryPolicy(operationRetryPolicy);
 $stack9 = $stack4.build();
 return  $stack9;
 } 

