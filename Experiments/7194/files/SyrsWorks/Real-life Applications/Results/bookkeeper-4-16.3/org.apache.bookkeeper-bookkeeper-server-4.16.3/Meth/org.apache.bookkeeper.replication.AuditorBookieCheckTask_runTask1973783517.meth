void org.apache.bookkeeper.replication.AuditorBookieCheckTask:runTask()
{
boolean $stack1;
  org.slf4j.Logger $stack2;
  $stack1 = this.hasBookieCheckTask();
 if ($stack1!=0) goto label0;
 this.startAudit((boolean)1);
 goto label1;
 label0:$stack2=org.apache.bookkeeper.replication.AuditorBookieCheckTask.LOG;
 dummyClass.lowSink("Audit already scheduled; skipping periodic bookie check");
$stack2.info("Audit already scheduled; skipping periodic bookie check");
 label1:return;
 } 

