static void org.apache.bookkeeper.proto.PacketProcessorBase:lambda$sendResponse$0(io.netty.util.concurrent.Future future)
{
boolean $stack1;
  org.slf4j.Logger $stack2;
  java.lang.Throwable $stack4;
  $stack1 = future.isSuccess();
 if ($stack1!=0) goto label0;
 $stack2=org.apache.bookkeeper.proto.PacketProcessorBase.logger;
 $stack4 = future.cause();
 dummyClass.lowSink("Netty channel write exception. ");
$stack2.debug("Netty channel write exception. ",$stack4);
 label0:return;
 } 

