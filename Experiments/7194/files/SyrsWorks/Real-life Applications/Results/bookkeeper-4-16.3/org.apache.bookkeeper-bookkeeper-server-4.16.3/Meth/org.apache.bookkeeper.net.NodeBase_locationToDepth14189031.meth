static int org.apache.bookkeeper.net.NodeBase:locationToDepth(java.lang.String @location)
{
int length;
  int depth;
  int i;
  char $stack7;
  java.lang.String location;
location=@location;
location = org.apache.bookkeeper.net.NodeBase.normalize(location);
 length = location.length();
 depth=0;
 i=0;
 label0:if (i>=length) goto label1;
 $stack7 = location.charAt(i);
 if ($stack7!=47) goto label2;
 depth=(depth+1);
 label2:i=(i+1);
 goto label0;
 label1:return  depth;
 } 

