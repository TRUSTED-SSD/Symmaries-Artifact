java.io.File[] org.apache.bookkeeper.conf.ServerConfiguration:getJournalDirs()
{
java.lang.String[] journalDirNames;
  java.io.File[] journalDirs;
  int i;
  int $stack6;
  java.io.File $stack7;
  java.lang.String $stack8;
  journalDirNames = this.getJournalDirNames();
 i=#journalDirNames;
 journalDirs=new java.io.File[i];
 i=0;
 label0:$stack6=#journalDirNames;
 if (i>=$stack6) goto label1;
 $stack7=new java.io.File;
 $stack8=journalDirNames[i];
 $stack7#java.io.File($stack8);
 journalDirs[i]=$stack7;
 i=(i+1);
 goto label0;
 label1:return  journalDirs;
 } 

