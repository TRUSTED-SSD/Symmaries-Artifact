java.util.List org.apache.bookkeeper.client.RackawareEnsemblePlacementPolicyImpl:revertBookieListByIndex(java.util.List @bookies,int startIndex)
{
org.apache.bookkeeper.net.BookieId[] bookieIds;
  int i;
  int $stack6;
  java.lang.Object $stack10;
  org.apache.bookkeeper.net.BookieId $stack11;
  java.util.List bookies;
bookies=@bookies;
i = bookies.size();
 bookieIds=new org.apache.bookkeeper.net.BookieId[i];
 i=0;
 label0:$stack6 = bookies.size();
 if (i>=$stack6) goto label1;
 $stack6 = bookies.size();
 if (startIndex!=$stack6) goto label2;
 startIndex=0;
 label2:$stack6=startIndex;
 startIndex=(startIndex+1);
 $stack10 = bookies.get(i);
 $stack11=(org.apache.bookkeeper.net.BookieId) $stack10;
 bookieIds[$stack6]=$stack11;
 i=(i+1);
 goto label0;
 label1:bookies = com.beust.jcommander.internal.Lists.newArrayList((java.lang.Object[])bookieIds);
 return  bookies;
 } 

