org.apache.bookkeeper.verifier.BookkeeperVerifier$LedgerInfo org.apache.bookkeeper.verifier.BookkeeperVerifier:getRandomLedger(java.util.Collection ledgerCollection)
{
int elem;
  java.util.Iterator iter;
  int i;
  java.util.Random $stack5;
  java.lang.Object $stack9;
  org.apache.bookkeeper.verifier.BookkeeperVerifier$LedgerInfo $stack10;
  $stack5=this.opRand;
 elem = ledgerCollection.size();
 elem = $stack5.nextInt(elem);
 iter = ledgerCollection.iterator();
 i=0;
 label0:if (i>=elem) goto label1;
 iter.next();
 i=(i+1);
 goto label0;
 label1:$stack9 = iter.next();
 $stack10=(org.apache.bookkeeper.verifier.BookkeeperVerifier$LedgerInfo) $stack9;
 return  $stack10;
 } 

