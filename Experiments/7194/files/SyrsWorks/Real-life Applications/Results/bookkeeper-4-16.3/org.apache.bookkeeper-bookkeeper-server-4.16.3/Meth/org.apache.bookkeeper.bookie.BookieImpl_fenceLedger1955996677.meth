java.util.concurrent.CompletableFuture org.apache.bookkeeper.bookie.BookieImpl:fenceLedger(long ledgerId,byte[] masterKey)
{
org.apache.bookkeeper.bookie.LedgerDescriptor handle;
  org.apache.bookkeeper.bookie.HandleFactory $stack5;
  org.apache.bookkeeper.bookie.Journal $stack7;
  java.util.concurrent.CompletableFuture $stack8;
  $stack5=this.handles;
 handle = $stack5.getHandle(ledgerId,masterKey);
 $stack7 = this#org.apache.bookkeeper.bookie.BookieImpl:getJournal(ledgerId);
 $stack8 = handle.fenceAndLogInJournal($stack7);
 return  $stack8;
 } 

