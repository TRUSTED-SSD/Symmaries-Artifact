static void org.apache.bookkeeper.bookie.SlowBufferedChannel:delayMs(long delay)
{
java.lang.InterruptedException l2;
  byte $stack3;
  java.util.concurrent.TimeUnit $stack4;
  $stack3=(delay cmp 1L);
 if ($stack3>=0) goto label0;
 return;
 label0:$stack4=java.util.concurrent.TimeUnit.MILLISECONDS;
 $stack4.sleep(delay);
 label1:goto label2;
 label3:l2 = catch;
 label2:return;
 catch java.lang.InterruptedException: label0 - label1 : label3;
} 

