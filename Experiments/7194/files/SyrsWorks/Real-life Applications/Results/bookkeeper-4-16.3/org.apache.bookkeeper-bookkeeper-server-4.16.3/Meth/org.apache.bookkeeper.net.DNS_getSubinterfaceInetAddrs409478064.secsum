static java.util.LinkedHashSet org.apache.bookkeeper.net.DNS:getSubinterfaceInetAddrs (
  java.net.NetworkInterface @nif) {
  guard = tt;
  ret(level()) = pc;
  ret(obj_level()) = pc ⊔ obj_level(@nif);
  ret(obj_level(@nif)) = pc ⊔ obj_level(@nif);
}