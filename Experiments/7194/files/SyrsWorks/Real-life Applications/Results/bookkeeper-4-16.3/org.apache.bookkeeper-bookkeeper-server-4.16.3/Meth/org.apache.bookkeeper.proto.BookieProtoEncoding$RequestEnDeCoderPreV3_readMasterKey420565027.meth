static byte[] org.apache.bookkeeper.proto.BookieProtoEncoding$RequestEnDeCoderPreV3:readMasterKey(io.netty.buffer.ByteBuf packet)
{
byte[] masterKey;
  boolean isEmptyKey;
  int i;
  int $stack9;
  byte $stack11;
  byte $stack13;
  masterKey=null;
 isEmptyKey=1;
 i=0;
 label0:if (i>=20) goto label1;
 $stack9 = packet.readerIndex();
 $stack9=($stack9+i);
 $stack11 = packet.getByte($stack9);
 masterKey=org.apache.bookkeeper.proto.BookieProtoEncoding$RequestEnDeCoderPreV3.emptyPasswordMasterKey;
 $stack13=masterKey[i];
 if ($stack11==$stack13) goto label2;
 isEmptyKey=0;
 goto label1;
 label2:i=(i+1);
 goto label0;
 label1:if (isEmptyKey==0) goto label3;
 masterKey=org.apache.bookkeeper.proto.BookieProtoEncoding$RequestEnDeCoderPreV3.emptyPasswordMasterKey;
 i = packet.readerIndex();
 i=(i+20);
 packet.readerIndex(i);
 goto label4;
 label3:masterKey=new byte[20];
 dummyClass.highSourceMut(masterKey);
packet.readBytes(masterKey,0,20);
 label4:return  masterKey;
 } 

