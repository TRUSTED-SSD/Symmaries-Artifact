boolean org.apache.bookkeeper.client.LedgerHandle:waitForWritable(org.apache.bookkeeper.client.DistributionSchedule$WriteSet writeSet,int allowedNonWritableCount,long durationMs)
{
long startTime;
  boolean writableResult;
  int backoff;
  byte maxBackoff;
  long maxSleep;
  java.lang.InterruptedException e;
  org.apache.bookkeeper.stats.OpStatsLogger $stack20;
  java.util.concurrent.TimeUnit $stack22;
  boolean $stack31;
  java.lang.Object[] $stack32;
  org.slf4j.Logger $stack33;
  java.lang.Long $stack37;
  java.lang.Integer $stack41;
  java.lang.Boolean $stack43;
  long $stack50;
  java.lang.Thread $stack60;
  maxBackoff=(durationMs cmp 0L);
 if (maxBackoff>=0) goto label0;
 return  1;
 label0:startTime = org.apache.bookkeeper.common.util.MathUtils.nowInNano();
 writableResult = this#org.apache.bookkeeper.client.LedgerHandle:isWriteSetWritable(writeSet,allowedNonWritableCount);
 if (writableResult!=0) goto label1;
 maxBackoff=(durationMs cmp 0L);
 if (maxBackoff<=0) goto label1;
 backoff=1;
 maxBackoff=4;
 $stack22=java.util.concurrent.TimeUnit.MILLISECONDS;
 durationMs = $stack22.toNanos(durationMs);
 durationMs=(startTime+durationMs);
 label2:$stack31 = this#org.apache.bookkeeper.client.LedgerHandle:isWriteSetWritable(writeSet,allowedNonWritableCount);
 writableResult=$stack31;
 if ($stack31!=0) goto label3;
 maxSleep = org.apache.bookkeeper.common.util.MathUtils.nowInNano();
 maxBackoff=(maxSleep cmp durationMs);
 if (maxBackoff>=0) goto label4;
 maxSleep = org.apache.bookkeeper.common.util.MathUtils.elapsedMSec(startTime);
 maxBackoff=(maxSleep cmp 0L);
 if (maxBackoff>=0) goto label5;
 maxSleep=1L;
 label5:$stack50=(long) backoff;
 maxSleep = java.lang.Math.min($stack50,maxSleep);
 label6:$stack22=java.util.concurrent.TimeUnit.MILLISECONDS;
 $stack22.sleep(maxSleep);
 label7:goto label8;
 label9:e = catch;
 $stack60 = java.lang.Thread.currentThread();
 $stack60.interrupt();
 writableResult = this#org.apache.bookkeeper.client.LedgerHandle:isWriteSetWritable(writeSet,allowedNonWritableCount);
 goto label3;
 label8:if (backoff>4) goto label10;
 backoff=(backoff+1);
 label10:goto label2;
 label4:writableResult=0;
 goto label3;
 label3:if (backoff<=1) goto label1;
 $stack33=org.apache.bookkeeper.client.LedgerHandle.LOG;
 $stack32=new java.lang.Object[3];
 durationMs = org.apache.bookkeeper.common.util.MathUtils.elapsedMSec(startTime);
 $stack37 = java.lang.Long.valueOf(durationMs);
 $stack32[0]=$stack37;
 backoff = writeSet.size();
 allowedNonWritableCount=(backoff-allowedNonWritableCount);
 $stack41 = java.lang.Integer.valueOf(allowedNonWritableCount);
 $stack32[1]=$stack41;
 $stack43 = java.lang.Boolean.valueOf(writableResult);
 $stack32[2]=$stack43;
 dummyClass.lowSink("Spent {} ms waiting for {} writable channels, writable result {}");
$stack33.info("Spent {} ms waiting for {} writable channels, writable result {}",$stack32);
 label1:if (writableResult==0) goto label11;
 $stack20=this.clientChannelWriteWaitStats;
 durationMs = org.apache.bookkeeper.common.util.MathUtils.elapsedNanos(startTime);
 $stack22=java.util.concurrent.TimeUnit.NANOSECONDS;
 $stack20.registerSuccessfulEvent(durationMs,$stack22);
 goto label12;
 label11:$stack20=this.clientChannelWriteWaitStats;
 durationMs = org.apache.bookkeeper.common.util.MathUtils.elapsedNanos(startTime);
 $stack22=java.util.concurrent.TimeUnit.NANOSECONDS;
 $stack20.registerFailedEvent(durationMs,$stack22);
 label12:return  writableResult;
 catch java.lang.InterruptedException: label6 - label7 : label9;
} 

