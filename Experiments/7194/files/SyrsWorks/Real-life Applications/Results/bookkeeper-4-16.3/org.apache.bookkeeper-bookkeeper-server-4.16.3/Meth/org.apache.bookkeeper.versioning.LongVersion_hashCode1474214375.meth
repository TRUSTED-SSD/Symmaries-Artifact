int org.apache.bookkeeper.versioning.LongVersion:hashCode()
{
byte PRIME;
  int result;
  long $version;
  long $stack5;
  int $stack8;
  PRIME=59;
 $version=this.version;
 $stack8=(1 * 59);
 $stack5=($version>>>32);
 $version=(!$stack5 & $version) |(!$version & $stack5);
 result=(int) $version;
 result=($stack8+result);
 return  result;
 } 

