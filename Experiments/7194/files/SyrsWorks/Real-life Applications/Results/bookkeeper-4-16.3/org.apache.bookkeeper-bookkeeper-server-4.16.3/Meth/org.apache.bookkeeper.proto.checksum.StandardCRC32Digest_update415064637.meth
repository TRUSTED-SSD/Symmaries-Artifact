void org.apache.bookkeeper.proto.checksum.StandardCRC32Digest:update(io.netty.buffer.ByteBuf @buf,int offset,int len)
{
java.util.zip.CRC32 $stack4;
  java.nio.ByteBuffer $stack6;
  io.netty.buffer.ByteBuf buf;
buf=@buf;
$stack4=this.crc;
 buf = buf.slice(offset,len);
 $stack6 = buf.nioBuffer();
 $stack4.update($stack6);
 return;
 } 

