void org.apache.bookkeeper.replication.ReplicationEnableCb:operationComplete(int rc,java.lang.Void result)
{
java.util.concurrent.CountDownLatch $stack3;
  org.slf4j.Logger $stack4;
  boolean $stack5;
  $stack3=this.latch;
 $stack3.countDown();
 $stack4=org.apache.bookkeeper.replication.ReplicationEnableCb.LOG;
 $stack5 = $stack4.isDebugEnabled();
 if ($stack5==0) goto label0;
 $stack4=org.apache.bookkeeper.replication.ReplicationEnableCb.LOG;
 dummyClass.lowSink("Automatic ledger re-replication is enabled");
$stack4.debug("Automatic ledger re-replication is enabled");
 label0:return;
 } 

