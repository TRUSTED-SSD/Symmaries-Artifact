void org.apache.bookkeeper.bookie.EntryLogMetadata:addLedgerSize(long ledgerId,long size)
{
long $stack5;
  org.apache.bookkeeper.util.collections.ConcurrentLongLongHashMap $stack9;
  $stack5=this.totalSize;
 $stack5=($stack5+size);
 this.totalSize=$stack5;
 $stack5=this.remainingSize;
 $stack5=($stack5+size);
 this.remainingSize=$stack5;
 $stack9=this.ledgersMap;
 ledgerId = $stack9.addAndGet(ledgerId,size);
 return;
 } 

