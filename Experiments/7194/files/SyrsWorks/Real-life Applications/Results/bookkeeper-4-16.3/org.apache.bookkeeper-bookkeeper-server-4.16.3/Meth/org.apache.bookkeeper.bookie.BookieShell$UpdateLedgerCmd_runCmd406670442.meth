int org.apache.bookkeeper.bookie.BookieShell$UpdateLedgerCmd:runCmd(org.apache.commons.cli.CommandLine cmdLine)
{
org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand cmd;
  org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand$FlipBookieIdFlags flags;
  java.lang.String bookieId;
  boolean useHostName;
  int rate;
  int maxOutstandingReads;
  int limit;
  boolean verbose;
  long printprogress;
  org.apache.bookkeeper.bookie.BookieShell $stack31;
  org.apache.bookkeeper.conf.ServerConfiguration $stack32;
  byte $stack34;
  boolean $stack35;
  org.slf4j.Logger $stack52;
  cmd=new org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand;
 cmd#org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand();
 flags=new org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand$FlipBookieIdFlags;
 flags#org.apache.bookkeeper.tools.cli.commands.bookie.FlipBookieIdCommand$FlipBookieIdFlags();
 bookieId = cmdLine.getOptionValue("bookieId");
 useHostName = org.apache.commons.lang.StringUtils.isBlank(bookieId);
 if (useHostName==0) goto label0;
 $stack52=org.apache.bookkeeper.bookie.BookieShell.LOG;
 $stack52.error("Invalid argument list!");
 this.printUsage();
 return  -1;
 label0:useHostName = org.apache.commons.lang.StringUtils.equals(bookieId,"hostname");
 if (useHostName!=0) goto label1;
 useHostName = org.apache.commons.lang.StringUtils.equals(bookieId,"ip");
 if (useHostName!=0) goto label1;
 $stack52=org.apache.bookkeeper.bookie.BookieShell.LOG;
 $stack52.error("Invalid option value {} for bookieId, expected hostname/ip",(java.lang.Object)bookieId);
 this.printUsage();
 return  -1;
 label1:useHostName = org.apache.bookkeeper.bookie.BookieShell.access$400(bookieId,"hostname");
 rate = org.apache.bookkeeper.bookie.BookieShell.access$100(cmdLine,"updatespersec",5);
 maxOutstandingReads=(rate * 5);
 maxOutstandingReads = org.apache.bookkeeper.bookie.BookieShell.access$100(cmdLine,"maxOutstandingReads",maxOutstandingReads);
 limit = org.apache.bookkeeper.bookie.BookieShell.access$100(cmdLine,"limit",-2147483648);
 verbose = org.apache.bookkeeper.bookie.BookieShell.access$500(cmdLine,"verbose",(boolean)0);
 if (verbose!=0) goto label2;
 $stack35 = cmdLine.hasOption("printprogress");
 if ($stack35==0) goto label3;
 $stack52=org.apache.bookkeeper.bookie.BookieShell.LOG;
 $stack52.warn("Ignoring option \'printprogress\', this is applicable when \'verbose\' is true");
 label3:printprogress=-2147483648L;
 goto label4;
 label2:printprogress = org.apache.bookkeeper.bookie.BookieShell.access$200(cmdLine,"printprogress",10L);
 label4:flags.hostname(useHostName);
 flags.printProgress(printprogress);
 flags.limit(limit);
 flags.updatePerSec(rate);
 flags.maxOutstandingReads(maxOutstandingReads);
 flags.verbose(verbose);
 $stack31=this.this$0;
 $stack32=$stack31.bkConf;
 useHostName = cmd.apply($stack32,flags);
 if (useHostName==0) goto label5;
 $stack34=0;
 goto label6;
 label5:$stack34=-1;
 label6:return  $stack34;
 } 

