boolean org.apache.bookkeeper.bookie.EntryLogMetadata:lambda$removeLedgerIf$0(java.util.function.LongPredicate predicate,long ledgerId,long size)
{
boolean shouldRemove;
  shouldRemove = predicate.test(ledgerId);
 if (shouldRemove==0) goto label0;
 ledgerId=this.remainingSize;
 ledgerId=(ledgerId-size);
 this.remainingSize=ledgerId;
 label0:return  shouldRemove;
 } 

