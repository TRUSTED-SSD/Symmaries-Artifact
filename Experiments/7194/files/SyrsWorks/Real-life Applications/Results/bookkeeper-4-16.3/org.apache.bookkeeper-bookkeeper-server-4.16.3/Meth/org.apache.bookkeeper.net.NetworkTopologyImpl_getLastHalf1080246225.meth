static java.lang.String org.apache.bookkeeper.net.NetworkTopologyImpl:getLastHalf(java.lang.String @networkLocation)
{
int index;
  java.lang.String networkLocation;
networkLocation=@networkLocation;
index = networkLocation.lastIndexOf("/");
 networkLocation = networkLocation.substring(index);
 return  networkLocation;
 } 

