org.apache.bookkeeper.bookie.storage.directentrylogger.DirectCompactionEntryLog(int srcLogId,int dstLogId,java.io.File @ledgerDir,org.apache.bookkeeper.slogger.Slogger @slog)
{
java.io.File $stack5;
  java.lang.Integer $stack9;
  java.io.File ledgerDir;
org.apache.bookkeeper.slogger.Slogger slog;
slog=@slog;
ledgerDir=@ledgerDir;
this#java.lang.Object();
 $stack5 = org.apache.bookkeeper.bookie.storage.directentrylogger.DirectCompactionEntryLog.compactingFile(ledgerDir,dstLogId);
 this.compactingFile=$stack5;
 $stack5 = org.apache.bookkeeper.bookie.storage.directentrylogger.DirectCompactionEntryLog.compactedFile(ledgerDir,dstLogId,srcLogId);
 this.compactedFile=$stack5;
 ledgerDir = org.apache.bookkeeper.bookie.storage.directentrylogger.DirectEntryLogger.logFile(ledgerDir,dstLogId);
 this.completeFile=ledgerDir;
 this.srcLogId=srcLogId;
 this.dstLogId=dstLogId;
 $stack9 = java.lang.Integer.valueOf(dstLogId);
 slog = slog.kv((java.lang.Object)"dstLogId",(java.lang.Object)$stack9);
 $stack9 = java.lang.Integer.valueOf(srcLogId);
 slog = slog.kv((java.lang.Object)"srcLogId",(java.lang.Object)$stack9);
 slog = slog.ctx(org.apache.bookkeeper.bookie.storage.directentrylogger.DirectCompactionEntryLog);
 this.slog=slog;
 return;
 } 

