void org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage:clearLimboState(long ledgerId)
{
org.slf4j.Logger $stack3;
  boolean $stack4;
  org.apache.bookkeeper.bookie.storage.ldb.LedgerMetadataIndex $stack5;
  java.lang.Long $stack9;
  $stack3=org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage.log;
 $stack4 = $stack3.isDebugEnabled();
 if ($stack4==0) goto label0;
 $stack3=org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage.log;
 $stack9 = java.lang.Long.valueOf(ledgerId);
 dummyClass.lowSink("clearLimboState. ledger: {}");
$stack3.debug("clearLimboState. ledger: {}",(java.lang.Object)$stack9);
 label0:$stack5=this.ledgerIndex;
 $stack5.clearLimbo(ledgerId);
 return;
 } 

