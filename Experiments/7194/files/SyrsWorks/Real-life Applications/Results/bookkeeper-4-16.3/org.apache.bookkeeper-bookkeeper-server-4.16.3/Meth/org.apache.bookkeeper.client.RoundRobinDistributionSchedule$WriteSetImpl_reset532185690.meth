void org.apache.bookkeeper.client.RoundRobinDistributionSchedule$WriteSetImpl:reset(int ensembleSize,int writeQuorumSize,long entryId)
{
int w;
  long $stack6;
  long $stack8;
  int $stack10;
  this#org.apache.bookkeeper.client.RoundRobinDistributionSchedule$WriteSetImpl(writeQuorumSize);
 w=0;
 label0:if (w>=writeQuorumSize) goto label1;
 $stack6=(long) w;
 $stack8=(entryId+$stack6);
 $stack6=(long) ensembleSize;
 $stack6=($stack8%$stack6);
 $stack10=(int) $stack6;
 this.set(w,$stack10);
 w=(w+1);
 goto label0;
 label1:return;
 } 

