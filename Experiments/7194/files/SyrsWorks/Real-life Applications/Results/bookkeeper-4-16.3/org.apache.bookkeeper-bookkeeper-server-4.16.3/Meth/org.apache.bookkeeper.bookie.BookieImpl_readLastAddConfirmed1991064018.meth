long org.apache.bookkeeper.bookie.BookieImpl:readLastAddConfirmed(long ledgerId)
{
org.apache.bookkeeper.bookie.LedgerDescriptor handle;
  org.apache.bookkeeper.bookie.HandleFactory $stack4;
  $stack4=this.handles;
 handle = $stack4.getReadOnlyHandle(ledgerId);
 ledgerId = handle.getLastAddConfirmed();
 return  ledgerId;
 } 

