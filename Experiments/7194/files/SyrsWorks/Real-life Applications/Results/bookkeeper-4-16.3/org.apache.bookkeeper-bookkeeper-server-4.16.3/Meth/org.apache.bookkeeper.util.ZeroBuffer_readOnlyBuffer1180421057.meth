static java.nio.ByteBuffer org.apache.bookkeeper.util.ZeroBuffer:readOnlyBuffer(int length)
{
java.nio.ByteBuffer buffer;
  byte[] $stack2;
  int $stack3;
  $stack2=org.apache.bookkeeper.util.ZeroBuffer.zeroBytes;
 $stack3=#$stack2;
 if (length>$stack3) goto label0;
 $stack2=org.apache.bookkeeper.util.ZeroBuffer.zeroBytes;
 buffer = java.nio.ByteBuffer.wrap($stack2,0,length);
 goto label1;
 label0:buffer = java.nio.ByteBuffer.allocate(length);
 org.apache.bookkeeper.util.ZeroBuffer.put(buffer);
 buffer.rewind();
 label1:buffer = buffer.asReadOnlyBuffer();
 return  buffer;
 } 

