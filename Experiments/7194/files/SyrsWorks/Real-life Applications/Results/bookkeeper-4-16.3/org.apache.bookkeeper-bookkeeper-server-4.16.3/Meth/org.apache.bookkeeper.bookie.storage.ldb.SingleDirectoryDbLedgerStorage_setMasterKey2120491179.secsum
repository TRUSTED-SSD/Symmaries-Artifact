void org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage:setMasterKey (
  long ledgerId, byte[] masterKey) {
  guard = pc = ⊥;
  ret(obj_level(masterKey)) = obj_level(masterKey);
  ret(obj_level(this)) = level(ledgerId) ⊔ obj_level(this);
}