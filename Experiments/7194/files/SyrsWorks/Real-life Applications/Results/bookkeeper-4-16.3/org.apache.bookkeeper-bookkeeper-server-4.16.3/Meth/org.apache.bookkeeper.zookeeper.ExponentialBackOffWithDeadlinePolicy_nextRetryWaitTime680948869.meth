long org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy:nextRetryWaitTime(int retryCount,long elapsedRetryTime)
{
int idx;
  long waitTime;
  long jitter;
  int[] $stack9;
  int $stack10;
  java.util.Random $stack15;
  long $stack19;
  long $stack21;
  byte $stack22;
  java.lang.Object[] $stack24;
  org.slf4j.Logger $stack25;
  java.lang.Integer $stack28;
  java.lang.Long $stack32;
  java.lang.Thread $stack34;
  java.lang.StackTraceElement[] $stack35;
  java.lang.String $stack36;
  idx=retryCount;
 $stack9=org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy.RETRY_BACKOFF;
 $stack10=#$stack9;
 if (retryCount<$stack10) goto label0;
 $stack9=org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy.RETRY_BACKOFF;
 idx=#$stack9;
 idx=(idx-1);
 label0:jitter=this.baseBackoffTime;
 $stack9=org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy.RETRY_BACKOFF;
 idx=$stack9[idx];
 waitTime=(long) idx;
 waitTime=(jitter * waitTime);
 $stack15=this.random;
 idx = $stack15.nextInt(10);
 jitter=(long) idx;
 jitter=(jitter * waitTime);
 jitter=(jitter/100L);
 $stack19=(elapsedRetryTime+waitTime);
 $stack21=($stack19+jitter);
 $stack19=this.deadline;
 $stack22=($stack21 cmp $stack19);
 if ($stack22<=0) goto label1;
 $stack25=org.apache.bookkeeper.zookeeper.ExponentialBackOffWithDeadlinePolicy.log;
 $stack24=new java.lang.Object[3];
 $stack28 = java.lang.Integer.valueOf(retryCount);
 $stack24[0]=$stack28;
 waitTime=this.deadline;
 waitTime=(waitTime-elapsedRetryTime);
 $stack32 = java.lang.Long.valueOf(waitTime);
 $stack24[1]=$stack32;
 $stack34 = java.lang.Thread.currentThread();
 $stack35 = $stack34.getStackTrace();
 $stack36 = java.util.Arrays.toString((java.lang.Object[])$stack35);
 $stack24[2]=$stack36;
 $stack25.warn("Final retry attempt: {}, timeleft: {}, stacktrace: {}",$stack24);
 waitTime=this.deadline;
 elapsedRetryTime=(waitTime-elapsedRetryTime);
 return  elapsedRetryTime;
 label1:elapsedRetryTime=(waitTime+jitter);
 return  elapsedRetryTime;
 } 

