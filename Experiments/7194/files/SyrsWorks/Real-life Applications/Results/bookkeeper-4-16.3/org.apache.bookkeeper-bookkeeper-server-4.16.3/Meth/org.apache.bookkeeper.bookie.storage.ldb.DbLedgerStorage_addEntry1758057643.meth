long org.apache.bookkeeper.bookie.storage.ldb.DbLedgerStorage:addEntry(io.netty.buffer.ByteBuf entry)
{
long ledgerId;
  int $stack4;
  org.apache.bookkeeper.bookie.storage.ldb.SingleDirectoryDbLedgerStorage $stack6;
  $stack4 = entry.readerIndex();
 ledgerId = entry.getLong($stack4);
 $stack6 = this#org.apache.bookkeeper.bookie.storage.ldb.DbLedgerStorage:getLedgerStorage(ledgerId);
 ledgerId = $stack6.addEntry(entry);
 return  ledgerId;
 } 

