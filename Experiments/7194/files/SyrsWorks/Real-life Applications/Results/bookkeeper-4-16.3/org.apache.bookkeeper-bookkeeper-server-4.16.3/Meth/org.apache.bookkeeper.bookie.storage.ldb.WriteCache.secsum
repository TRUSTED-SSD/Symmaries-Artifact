org.apache.bookkeeper.bookie.storage.ldb.WriteCache (io.netty.buffer.ByteBufAllocator
                                                       allocator,
                                                     long maxCacheSize) {
  guard = tt;
  ret(obj_level(allocator)) = obj_level(allocator);
  ret(obj_level(this)) = obj_level(this);
}