void org.apache.bookkeeper.bookie.LedgerEntryPage:getEntries(org.apache.bookkeeper.bookie.LedgerEntryPage$EntryVisitor vis)
{
int i;
  long offset;
  int $stack5;
  byte $stack8;
  long $stack11;
  long $stack12;
  boolean $stack14;
  i=0;
 label0:$stack5=this.entriesPerPage;
 if (i>=$stack5) goto label1;
 $stack5=(i * 8);
 offset = this.getOffset($stack5);
 $stack8=(offset cmp 0L);
 if ($stack8==0) goto label2;
 $stack11 = this.getFirstEntry();
 $stack12=(long) i;
 $stack11=($stack11+$stack12);
 $stack14 = vis.visit($stack11,offset);
 if ($stack14!=0) goto label2;
 return;
 label2:i=(i+1);
 goto label0;
 label1:return;
 } 

