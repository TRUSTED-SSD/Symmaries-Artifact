void org.apache.bookkeeper.bookie.LedgerCacheImpl:setMasterKey(long ledgerId,byte[] masterKey)
{
org.apache.bookkeeper.bookie.IndexPersistenceMgr $stack4;
  $stack4=this.indexPersistenceManager;
 $stack4.setMasterKey(ledgerId,masterKey);
 return;
 } 

