static void org.apache.bookkeeper.bookie.storage.directentrylogger.Header:writeHeader(io.netty.buffer.ByteBuf header,long ledgerMapOffset,int ledgerCount)
{
byte[] $stack4;
  $stack4=org.apache.bookkeeper.bookie.storage.directentrylogger.Header.EMPTY_HEADER;
 header.writeBytes($stack4);
 header.setLong(8,ledgerMapOffset);
 header.setInt(16,ledgerCount);
 return;
 } 

