org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel org.apache.bookkeeper.bookie.EntryLogManagerForSingleEntryLog:getCurrentLogForLedgerForAddEntry(long ledgerId,int entrySize,boolean rollLog)
{
boolean createNewLog;
  org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel $stack7;
  java.util.concurrent.atomic.AtomicBoolean $stack11;
  java.lang.StringBuilder $stack14;
  java.lang.String $stack20;
  $stack7=this.activeLogChannel;
 if (null!=$stack7) goto label0;
 this.createNewLog(-1L,"because current active log channel has not initialized yet");
 $stack7=this.activeLogChannel;
 return  $stack7;
 label0:if (rollLog==0) goto label1;
 $stack7=this.activeLogChannel;
 ledgerId=(long) entrySize;
 rollLog = this.reachEntryLogLimit($stack7,ledgerId);
 goto label2;
 label1:$stack7=this.activeLogChannel;
 ledgerId=(long) entrySize;
 rollLog = this.readEntryLogHardLimit($stack7,ledgerId);
 label2:$stack11=this.shouldCreateNewEntryLog;
 createNewLog = $stack11.get();
 if (createNewLog!=0) goto label3;
 if (rollLog==0) goto label4;
 label3:$stack7=this.activeLogChannel;
 if ($stack7==null) goto label5;
 $stack7=this.activeLogChannel;
 $stack7.flushAndForceWriteIfRegularFlush((boolean)0);
 label5:$stack14=new java.lang.StringBuilder;
 $stack14#java.lang.StringBuilder();
 $stack14 = $stack14.append(": createNewLog = ");
 $stack14 = $stack14.append(createNewLog);
 $stack14 = $stack14.append(", reachEntryLogLimit = ");
 $stack14 = $stack14.append(rollLog);
 $stack20 = $stack14.toString();
 this.createNewLog(-1L,$stack20);
 if (createNewLog==0) goto label4;
 $stack11=this.shouldCreateNewEntryLog;
 $stack11.set((boolean)0);
 label4:$stack7=this.activeLogChannel;
 return  $stack7;
 } 

