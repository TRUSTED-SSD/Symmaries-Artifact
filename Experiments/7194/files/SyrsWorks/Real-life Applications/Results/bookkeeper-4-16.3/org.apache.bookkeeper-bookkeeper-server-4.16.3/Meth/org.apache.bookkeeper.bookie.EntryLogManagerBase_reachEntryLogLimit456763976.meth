boolean org.apache.bookkeeper.bookie.EntryLogManagerBase:reachEntryLogLimit(org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel logChannel,long size)
{
long $stack4;
  byte $stack7;
  boolean $stack8;
  if (logChannel!=null) goto label0;
 return  0;
 label0:$stack4 = logChannel.position();
 $stack4=($stack4+size);
 size=this.logSizeLimit;
 $stack7=($stack4 cmp size);
 if ($stack7<=0) goto label1;
 $stack8=1;
 goto label2;
 label1:$stack8=0;
 label2:return  $stack8;
 } 

