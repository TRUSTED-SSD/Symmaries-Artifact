int org.apache.bookkeeper.bookie.SkipListArena$Chunk:alloc(int size)
{
int oldOffset;
  java.util.concurrent.atomic.AtomicInteger $stack3;
  byte[] $stack5;
  int $stack6;
  int $stack7;
  boolean $stack10;
  label0:$stack3=this.nextFreeOffset;
 oldOffset = $stack3.get();
 if (oldOffset!=-1) goto label1;
 java.lang.Thread.yield();
 goto label0;
 label1:if (oldOffset!=-2) goto label2;
 return  -1;
 label2:$stack7=(oldOffset+size);
 $stack5=this.data;
 $stack6=#$stack5;
 if ($stack7<=$stack6) goto label3;
 return  -1;
 label3:$stack3=this.nextFreeOffset;
 $stack6=(oldOffset+size);
 $stack10 = $stack3.compareAndSet(oldOffset,$stack6);
 if ($stack10==0) goto label4;
 $stack3=this.allocCount;
 $stack3.incrementAndGet();
 return  oldOffset;
 label4:goto label0;
 } 

