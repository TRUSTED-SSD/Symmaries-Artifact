org.apache.bookkeeper.util.collections.ConcurrentLongHashSet$Section(int capacity,float mapFillFactor,float mapIdleFactor,boolean autoShrink,float expandFactor,float shrinkFactor)
{
long[] $stack10;
  this#java.util.concurrent.locks.StampedLock();
 capacity = org.apache.bookkeeper.util.collections.ConcurrentLongHashSet.access$500(capacity);
 this.capacity=capacity;
 capacity=this.capacity;
 this.initCapacity=capacity;
 capacity=this.capacity;
 $stack10=new long[capacity];
 this.table=$stack10;
 this.size=0;
 this.usedBuckets=0;
 this.autoShrink=autoShrink;
 this.mapFillFactor=mapFillFactor;
 this.mapIdleFactor=mapIdleFactor;
 this.expandFactor=expandFactor;
 this.shrinkFactor=shrinkFactor;
 capacity=this.capacity;
 expandFactor=(float) capacity;
 mapFillFactor=(expandFactor * mapFillFactor);
 capacity=(int) mapFillFactor;
 this.resizeThresholdUp=capacity;
 capacity=this.capacity;
 mapFillFactor=(float) capacity;
 mapFillFactor=(mapFillFactor * mapIdleFactor);
 capacity=(int) mapFillFactor;
 this.resizeThresholdBelow=capacity;
 $stack10=this.table;
 java.util.Arrays.fill($stack10,-1L);
 return;
 } 

