void org.apache.bookkeeper.client.SyncCounter:block(int limit)
{
int prev;
  int $stack4;
  label0:prev=this.i;
 if (prev<=limit) goto label1;
 prev=this.i;
 this.wait();
 $stack4=this.i;
 if ($stack4!=prev) goto label2;
 goto label1;
 label2:goto label0;
 label1:return;
 } 

