static int org.apache.bookkeeper.util.collections.ConcurrentOpenHashMap:signSafeMod(long n,int max)
{
long $stack4;
  max=(max-1);
 $stack4=(long) max;
 n=(n & $stack4);
 max=(int) n;
 max=(max<<1);
 return  max;
 } 

