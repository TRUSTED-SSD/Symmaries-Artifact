void org.apache.bookkeeper.bookie.SortedLedgerStorage:setMasterKey(long ledgerId,byte[] masterKey)
{
org.apache.bookkeeper.bookie.InterleavedLedgerStorage $stack4;
  $stack4=this.interleavedLedgerStorage;
 $stack4.setMasterKey(ledgerId,masterKey);
 return;
 } 

