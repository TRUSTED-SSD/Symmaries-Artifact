void org.apache.bookkeeper.bookie.DefaultEntryLogger:validateEntry(long ledgerId,long entryId,long entryLogId,long pos,io.netty.buffer.ByteBuf sizeBuff)
{
int entrySize;
  long thisLedgerId;
  long thisEntryId;
  int $stack16;
  byte $stack19;
  org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$WrongEntryException $stack20;
  org.slf4j.Logger $stack28;
  java.lang.Integer $stack30;
  org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$InvalidEntryLengthException $stack31;
  java.lang.StringBuilder $stack32;
  java.lang.String $stack40;
  entrySize = sizeBuff.readInt();
dummyClass.highSourceMut(entrySize);
 $stack16=this.maxSaneEntrySize;
 if (entrySize<=$stack16) goto label0;
 $stack28=org.apache.bookkeeper.bookie.DefaultEntryLogger.LOG;
 $stack32=new java.lang.StringBuilder;
 $stack32#java.lang.StringBuilder();
 $stack32 = $stack32.append("Sanity check failed for entry size of ");
 $stack32 = $stack32.append(entrySize);
 $stack32 = $stack32.append(" at location ");
 $stack32 = $stack32.append(pos);
 $stack32 = $stack32.append(" in ");
 $stack32 = $stack32.append(entryLogId);
 $stack40 = $stack32.toString();
 $stack28.warn($stack40);
 label0:if (entrySize>=16) goto label1;
 $stack28=org.apache.bookkeeper.bookie.DefaultEntryLogger.LOG;
 $stack30 = java.lang.Integer.valueOf(entrySize);
 $stack28.error("Read invalid entry length {}",(java.lang.Object)$stack30);
 $stack31=new org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$InvalidEntryLengthException;
 $stack31#org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$InvalidEntryLengthException(ledgerId,entryId,entryLogId,pos);
 throw $stack31;
 label1:thisLedgerId = sizeBuff.getLong(4);
 thisEntryId = sizeBuff.getLong(12);
 $stack19=(thisLedgerId cmp ledgerId);
 if ($stack19!=0) goto label2;
 $stack19=(thisEntryId cmp entryId);
 if ($stack19==0) goto label3;
 label2:$stack20=new org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$WrongEntryException;
 $stack20#org.apache.bookkeeper.bookie.DefaultEntryLogger$EntryLookupException$WrongEntryException(thisEntryId,thisLedgerId,ledgerId,entryId,entryLogId,pos);
 throw $stack20;
 label3:return;
 } 

