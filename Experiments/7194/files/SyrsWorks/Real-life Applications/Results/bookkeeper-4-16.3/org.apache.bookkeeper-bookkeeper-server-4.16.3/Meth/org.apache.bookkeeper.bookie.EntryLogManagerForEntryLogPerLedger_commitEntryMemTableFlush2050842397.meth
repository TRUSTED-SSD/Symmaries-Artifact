boolean org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger:commitEntryMemTableFlush()
{
java.util.Set copyOfCurrentLogsWithDirInfo;
  java.util.Iterator l2;
  org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger$BufferedLogChannelWithDirInfo currentLogWithDirInfo;
  org.apache.bookkeeper.bookie.DefaultEntryLogger$BufferedLogChannel currentLog;
  java.lang.Long ledgerId;
  java.util.concurrent.locks.Lock lock;
  java.lang.Throwable l7;
  boolean $stack10;
  java.lang.Object $stack11;
  long $stack16;
  org.slf4j.Logger $stack19;
  copyOfCurrentLogsWithDirInfo = this.getCopyOfCurrentLogs();
 l2 = copyOfCurrentLogsWithDirInfo.iterator();
 label0:$stack10 = l2.hasNext();
 if ($stack10==0) goto label1;
 $stack11 = l2.next();
 currentLogWithDirInfo=(org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger$BufferedLogChannelWithDirInfo) $stack11;
 currentLog = currentLogWithDirInfo.getLogChannel();
 $stack10 = this.reachEntryLogLimit(currentLog,0L);
 if ($stack10==0) goto label2;
 ledgerId = currentLog.getLedgerIdAssigned();
 $stack16 = ledgerId.longValue();
 lock = this.getLock($stack16);
 lock.lock();
 label3:$stack10 = this.reachEntryLogLimit(currentLog,0L);
 if ($stack10==0) goto label4;
 $stack19=org.apache.bookkeeper.bookie.EntryLogManagerForEntryLogPerLedger.log;
 dummyClass.lowSink("Rolling entry logger since it reached size limitation for ledger: {}");
$stack19.info("Rolling entry logger since it reached size limitation for ledger: {}",(java.lang.Object)ledgerId);
 $stack16 = ledgerId.longValue();
 this.createNewLog($stack16,"after entry log file is rotated");
 label4:lock.unlock();
 goto label2;
 label5:l7 = catch;
 label6:lock.unlock();
 throw l7;
 label2:goto label0;
 label1:return  0;
 catch java.lang.Throwable: label3 - label4 : label5;
catch java.lang.Throwable: label5 - label6 : label5;
} 

