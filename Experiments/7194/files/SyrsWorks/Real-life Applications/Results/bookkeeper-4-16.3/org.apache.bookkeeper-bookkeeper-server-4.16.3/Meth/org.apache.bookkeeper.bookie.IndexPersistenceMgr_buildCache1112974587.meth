static com.google.common.cache.Cache org.apache.bookkeeper.bookie.IndexPersistenceMgr:buildCache(int concurrencyLevel,int initialCapacity,int maximumSize,long expireAfterAccessSeconds,com.google.common.cache.RemovalListener removalListener)
{
com.google.common.cache.CacheBuilder builder;
  long $stack10;
  byte $stack13;
  com.google.common.cache.Cache $stack14;
  java.util.concurrent.TimeUnit $stack15;
  builder = com.google.common.cache.CacheBuilder.newBuilder();
 builder = builder.concurrencyLevel(concurrencyLevel);
 builder = builder.initialCapacity(initialCapacity);
 $stack10=(long) maximumSize;
 builder = builder.maximumSize($stack10);
 builder = builder.removalListener(removalListener);
 $stack13=(expireAfterAccessSeconds cmp 0L);
 if ($stack13<=0) goto label0;
 $stack15=java.util.concurrent.TimeUnit.SECONDS;
 builder.expireAfterAccess(expireAfterAccessSeconds,$stack15);
 label0:$stack14 = builder.build();
 return  $stack14;
 } 

