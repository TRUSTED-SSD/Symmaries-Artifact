void org.apache.bookkeeper.client.MetadataUpdateLoop:lambda$readLoop$2(java.util.concurrent.CompletableFuture promise,org.apache.bookkeeper.versioning.Versioned current,long ledgerId,org.apache.bookkeeper.versioning.Versioned read,java.lang.Throwable exception)
{
org.apache.bookkeeper.versioning.Version $stack7;
  org.apache.bookkeeper.versioning.Version $stack8;
  org.apache.bookkeeper.versioning.Version$Occurred $stack9;
  org.apache.bookkeeper.versioning.Version$Occurred $stack10;
  org.apache.bookkeeper.client.MetadataUpdateLoop$LocalValueUpdater $stack11;
  boolean $stack12;
  java.lang.String $stack15;
  org.slf4j.Logger $stack16;
  if (exception==null) goto label0;
 $stack16=org.apache.bookkeeper.client.MetadataUpdateLoop.LOG;
 $stack15=this.logContext;
 $stack16.error("{} Failed to read metadata from store",(java.lang.Object)$stack15,(java.lang.Object)exception);
 promise.completeExceptionally(exception);
 goto label1;
 label0:$stack7 = current.getVersion();
 $stack8 = read.getVersion();
 $stack9 = $stack7.compare($stack8);
 $stack10=org.apache.bookkeeper.versioning.Version$Occurred.CONCURRENTLY;
 if ($stack9!=$stack10) goto label2;
 promise.complete((java.lang.Object)current);
 goto label1;
 label2:$stack11=this.updateLocalValue;
 $stack12 = $stack11.updateValue(current,read);
 if ($stack12==0) goto label3;
 promise.complete((java.lang.Object)read);
 goto label1;
 label3:this#org.apache.bookkeeper.client.MetadataUpdateLoop(ledgerId,promise);
 label1:return;
 } 

