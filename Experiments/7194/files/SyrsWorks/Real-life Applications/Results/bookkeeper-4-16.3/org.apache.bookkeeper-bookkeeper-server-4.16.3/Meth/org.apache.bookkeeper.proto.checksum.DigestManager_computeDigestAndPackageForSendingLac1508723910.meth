org.apache.bookkeeper.util.ByteBufList org.apache.bookkeeper.proto.checksum.DigestManager:computeDigestAndPackageForSendingLac(long lac)
{
io.netty.buffer.ByteBuf headersBuffer;
  boolean $stack5;
  int $stack6;
  long $stack9;
  org.apache.bookkeeper.util.ByteBufList $stack13;
  io.netty.buffer.ByteBufAllocator $stack16;
  $stack5=this.useV2Protocol;
 if ($stack5==0) goto label0;
 $stack16=this.allocator;
 $stack6=this.macCodeLength;
 $stack6=(16+$stack6);
 headersBuffer = $stack16.buffer($stack6);
 goto label1;
 label0:$stack6=this.macCodeLength;
 $stack6=(16+$stack6);
 headersBuffer = io.netty.buffer.Unpooled.buffer($stack6);
 label1:$stack9=this.ledgerId;
 headersBuffer.writeLong($stack9);
 headersBuffer.writeLong(lac);
 $stack6 = this.update(0,headersBuffer,0,16);
 this.populateValueAndReset($stack6,headersBuffer);
 $stack13 = org.apache.bookkeeper.util.ByteBufList.get(headersBuffer);
 return  $stack13;
 } 

