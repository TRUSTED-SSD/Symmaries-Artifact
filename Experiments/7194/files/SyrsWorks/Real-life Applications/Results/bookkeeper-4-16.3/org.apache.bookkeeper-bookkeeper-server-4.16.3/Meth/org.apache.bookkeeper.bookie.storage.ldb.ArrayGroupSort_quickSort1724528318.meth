static void org.apache.bookkeeper.bookie.storage.ldb.ArrayGroupSort:quickSort(long[] array,int low,int high)
{
int pivotIdx;
  int $stack6;
  if (low<high) goto label0;
 return;
 label0:pivotIdx=(high-low);
 if (pivotIdx>=100) goto label1;
 org.apache.bookkeeper.bookie.storage.ldb.ArrayGroupSort.insertionSort(array,low,high);
 return;
 label1:pivotIdx = org.apache.bookkeeper.bookie.storage.ldb.ArrayGroupSort.partition(array,low,high);
 $stack6=(pivotIdx-4);
 org.apache.bookkeeper.bookie.storage.ldb.ArrayGroupSort.quickSort(array,low,$stack6);
 low=(pivotIdx+4);
 org.apache.bookkeeper.bookie.storage.ldb.ArrayGroupSort.quickSort(array,low,high);
 return;
 } 

