java.io.File[] org.apache.bookkeeper.conf.ServerConfiguration:getLedgerDirs()
{
java.lang.String[] ledgerDirNames;
  java.io.File[] ledgerDirs;
  int i;
  int $stack6;
  java.io.File $stack7;
  java.lang.String $stack8;
  ledgerDirNames = this.getLedgerDirNames();
 i=#ledgerDirNames;
 ledgerDirs=new java.io.File[i];
 i=0;
 label0:$stack6=#ledgerDirNames;
 if (i>=$stack6) goto label1;
 $stack7=new java.io.File;
 $stack8=ledgerDirNames[i];
 $stack7#java.io.File($stack8);
 ledgerDirs[i]=$stack7;
 i=(i+1);
 goto label0;
 label1:return  ledgerDirs;
 } 

