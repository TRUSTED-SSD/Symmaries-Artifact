static byte[] org.apache.bookkeeper.proto.checksum.MacDigestManager:genDigest(java.lang.String pad,byte[] @passwd)
{
java.security.MessageDigest digest;
  java.nio.charset.Charset $stack4;
  byte[] $stack5;
  byte[] passwd;
passwd=@passwd;
digest = java.security.MessageDigest.getInstance("SHA-1");
 $stack4=java.nio.charset.StandardCharsets.UTF_8;
 $stack5 = pad.getBytes($stack4);
 digest.update($stack5);
 digest.update(passwd);
 passwd = digest.digest();
 return  passwd;
 } 

