long org.apache.bookkeeper.zookeeper.BoundExponentialBackoffRetryPolicy:nextRetryWaitTime(int retryCount,long elapsedRetryTime)
{
long $stack4;
  $stack4=this.maxBackoffTime;
 elapsedRetryTime = this#org.apache.bookkeeper.zookeeper.ExponentialBackoffRetryPolicy:nextRetryWaitTime(retryCount,elapsedRetryTime);
 elapsedRetryTime = java.lang.Math.min($stack4,elapsedRetryTime);
 return  elapsedRetryTime;
 } 

