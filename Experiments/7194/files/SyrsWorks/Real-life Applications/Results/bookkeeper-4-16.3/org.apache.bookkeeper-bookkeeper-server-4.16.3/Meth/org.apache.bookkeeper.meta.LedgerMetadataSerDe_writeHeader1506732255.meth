static void org.apache.bookkeeper.meta.LedgerMetadataSerDe:writeHeader(java.io.OutputStream os,int version)
{
byte[] $stack2;
  java.lang.String $stack3;
  java.nio.charset.Charset $stack4;
  $stack2=org.apache.bookkeeper.meta.LedgerMetadataSerDe.VERSION_KEY_BYTES;
 dummyClass.lowSink($stack2);
os.write($stack2);
 $stack3 = java.lang.String.valueOf(version);
 $stack4=java.nio.charset.StandardCharsets.UTF_8;
 $stack2 = $stack3.getBytes($stack4);
 dummyClass.lowSink($stack2);
os.write($stack2);
 $stack2=org.apache.bookkeeper.meta.LedgerMetadataSerDe.LINE_SPLITTER_BYTES;
 dummyClass.lowSink($stack2);
os.write($stack2);
 return;
 } 

