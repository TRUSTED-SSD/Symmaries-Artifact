java.lang.String org.apache.bookkeeper.net.ScriptBasedMapping$RawScriptBasedMapping:runResolveCommand(java.util.List args)
{
int loopCount;
  java.lang.StringBuilder allOutput;
  int numProcessed;
  int start;
  java.util.ArrayList cmdList;
  java.io.File dir;
  org.apache.bookkeeper.util.Shell$ShellCommandExecutor s;
  java.lang.String $stack15;
  int $stack20;
  java.lang.String[] $stack25;
  java.lang.Object[] $stack26;
  java.lang.StringBuilder $stack29;
  java.lang.Object $stack44;
  org.slf4j.Logger $stack50;
  java.lang.Exception $stack59;
  loopCount=0;
 numProcessed = args.size();
 if (numProcessed!=0) goto label0;
 return  null;
 label0:allOutput=new java.lang.StringBuilder;
 allOutput#java.lang.StringBuilder();
 numProcessed=0;
 start=this.maxArgs;
 if (start>=1) goto label1;
 $stack50=org.apache.bookkeeper.net.ScriptBasedMapping$RawScriptBasedMapping.LOG;
 allOutput=new java.lang.StringBuilder;
 allOutput#java.lang.StringBuilder();
 allOutput = allOutput.append("Invalid value ");
 loopCount=this.maxArgs;
 allOutput = allOutput.append(loopCount);
 allOutput = allOutput.append(" for ");
 allOutput = allOutput.append("networkTopologyScriptNumberArgs");
 allOutput = allOutput.append("; must be >= ");
 allOutput = allOutput.append(1);
 $stack15 = allOutput.toString();
 $stack50.warn($stack15);
 return  null;
 label1:start = args.size();
 if (numProcessed==start) goto label2;
 numProcessed=this.maxArgs;
 start=(numProcessed * loopCount);
 cmdList=new java.util.ArrayList;
 cmdList#java.util.ArrayList();
 $stack15=this.scriptName;
 cmdList.add((java.lang.Object)$stack15);
 numProcessed=start;
 label3:$stack20=this.maxArgs;
 $stack20=(start+$stack20);
 if (numProcessed>=$stack20) goto label4;
 $stack20 = args.size();
 if (numProcessed>=$stack20) goto label4;
 $stack44 = args.get(numProcessed);
 cmdList.add($stack44);
 numProcessed=(numProcessed+1);
 goto label3;
 label4:dir=null;
 $stack15 = java.lang.System.getProperty("user.dir");
 if ($stack15==null) goto label5;
 dir=new java.io.File;
 dir#java.io.File($stack15);
 label5:s=new org.apache.bookkeeper.util.Shell$ShellCommandExecutor;
 start = cmdList.size();
 $stack25=new java.lang.String[start];
 $stack26 = cmdList.toArray((java.lang.Object[])$stack25);
 $stack25=(java.lang.String[]) $stack26;
 s#org.apache.bookkeeper.util.Shell$ShellCommandExecutor($stack25,dir);
 label6:s.execute();
 $stack15 = s.getOutput();
 $stack29 = allOutput.append($stack15);
 $stack29.append(" ");
 label7:goto label8;
 label9:$stack59 = catch;
 $stack50=org.apache.bookkeeper.net.ScriptBasedMapping$RawScriptBasedMapping.LOG;
 $stack15 = $stack59.getMessage();
 $stack50.warn("Exception running: {} Exception message: {}",(java.lang.Object)s,(java.lang.Object)$stack15);
 return  null;
 label8:loopCount=(loopCount+1);
 goto label1;
 label2:$stack15 = allOutput.toString();
 return  $stack15;
 catch java.lang.Exception: label6 - label7 : label9;
} 

