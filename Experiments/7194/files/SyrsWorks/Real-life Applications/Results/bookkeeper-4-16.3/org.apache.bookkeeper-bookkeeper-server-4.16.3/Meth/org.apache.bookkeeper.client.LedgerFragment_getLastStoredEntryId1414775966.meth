long org.apache.bookkeeper.client.LedgerFragment:getLastStoredEntryId()
{
long lastEntry;
  java.util.Iterator l3;
  int bookieIndex;
  java.lang.Long lastStoredEntryIdForBookie;
  java.util.Set $stack6;
  boolean $stack8;
  java.lang.Object $stack9;
  java.lang.Integer $stack10;
  long $stack13;
  byte $stack14;
  lastEntry=-1L;
 $stack6=this.bookieIndexes;
 l3 = $stack6.iterator();
 label0:$stack8 = l3.hasNext();
 if ($stack8==0) goto label1;
 $stack9 = l3.next();
 $stack10=(java.lang.Integer) $stack9;
 bookieIndex = $stack10.intValue();
 lastStoredEntryIdForBookie = this.getLastStoredEntryId(bookieIndex);
 $stack13 = lastStoredEntryIdForBookie.longValue();
 $stack14=($stack13 cmp -1L);
 if ($stack14==0) goto label2;
 $stack14=(lastEntry cmp -1L);
 if ($stack14!=0) goto label3;
 lastEntry = lastStoredEntryIdForBookie.longValue();
 goto label2;
 label3:$stack13 = lastStoredEntryIdForBookie.longValue();
 lastEntry = java.lang.Math.max(lastEntry,$stack13);
 label2:goto label0;
 label1:return  lastEntry;
 } 

