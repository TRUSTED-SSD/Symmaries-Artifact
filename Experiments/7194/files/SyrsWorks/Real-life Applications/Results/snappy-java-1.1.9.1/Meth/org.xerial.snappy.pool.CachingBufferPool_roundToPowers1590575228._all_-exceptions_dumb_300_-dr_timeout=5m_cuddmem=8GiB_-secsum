static int org.xerial.snappy.pool.CachingBufferPool:roundToPowers (int l0,
                                                                   int l1) {
  guard = tt;
  ret(level()) = level(l0) ⊔ level(l1);
}