void securibench.micro.inter.Inter6:doGet (javax.servlet.http.HttpServletRequest
                                             l1,
                                           javax.servlet.http.HttpServletResponse
                                             l2) {
  guard = tt;
  ret(obj_level(l1)) = obj_level(l1);
  ret(obj_level(l2)) = if obj_level(l2) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(l2) = ⊤ then obj_level(l2) = ⊥ else level(l1) = ⊤ ∧ level(l2) = ⊥ ∧ obj_level(l2) = ⊥) then ⊤ else ⊥;
  ret(obj_level(this)) = obj_level(this);
  exc(level()) = pc ⊔ level(l1) ⊔ level(l2);
  exc(obj_level()) = pc ⊔ level(l1) ⊔ level(l2);
  exc(obj_level(l1)) = obj_level(l1);
  exc(obj_level(l2)) = if obj_level(l2) = ⊤ ∨ pc ≠ ⊤ ∧ (if level(l2) = ⊤ then obj_level(l2) = ⊥ else level(l1) = ⊤ ∧ level(l2) = ⊥ ∧ obj_level(l2) = ⊥) then ⊤ else ⊥;
  exc(obj_level(this)) = obj_level(this);
}