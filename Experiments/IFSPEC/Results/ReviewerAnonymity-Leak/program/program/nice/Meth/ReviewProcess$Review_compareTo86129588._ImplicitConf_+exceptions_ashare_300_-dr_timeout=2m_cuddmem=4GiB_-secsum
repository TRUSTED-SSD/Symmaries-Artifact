int ReviewProcess$Review:compareTo (java.lang.Object l1) {
  guard = tt;
  ret(level()) = pc ⊔ level(l1) ⊔ level(this) ⊔ obj_level(l1) ⊔ obj_level(this);
  ret(obj_level(l1)) = ⊤;
  ret(obj_level(this)) = if ¬l1~this ∧ ¬l1.><this ∧ ¬this.><l1 ∧ level(l1) = ⊥ ∧ level(this) = ⊥ ∧ obj_level(l1) = ⊥ ∧ obj_level(this) = ⊥ then pc else if if obj_level(l1) = ⊥ then if level(l1) = ⊥ then l1~this ∨ l1.><this ∨ this.><l1 ∨ level(this) = ⊤ ∨ obj_level(this) = ⊤ else level(l1) = ⊤ else obj_level(l1) = ⊤ then ⊤ else ⊥;
  ret(l1.><l1) = tt;
  ret(l1.><this) = tt;
  ret(this.><l1) = tt;
  ret(this.><this) = tt;
  exc(level()) = pc ⊔ level(l1) ⊔ obj_level(l1);
  exc(obj_level()) = pc ⊔ level(l1) ⊔ obj_level(l1);
  exc(obj_level(l1)) = ⊤;
  exc(obj_level(this)) = if ¬l1~this ∧ ¬l1.><this ∧ ¬this.><l1 ∧ level(l1) = ⊥ ∧ level(this) = ⊥ ∧ obj_level(l1) = ⊥ ∧ obj_level(this) = ⊥ then pc else if if obj_level(l1) = ⊥ then if level(l1) = ⊥ then l1~this ∨ l1.><this ∨ this.><l1 ∨ level(this) = ⊤ ∨ obj_level(this) = ⊤ else level(l1) = ⊤ else obj_level(l1) = ⊤ then ⊤ else ⊥;
  exc(~l1) = ff;
  exc(.><l1) = ff;
  exc(.><this) = ff;
  exc(l1.><l1) = tt;
  exc(l1.><this) = tt;
  exc(this.><l1) = tt;
  exc(this.><this) = tt;
}