java.lang.String securibench.micro.inter.Inter3:id (java.lang.String l1,
                                                    java.io.PrintWriter l2) {
  guard = pc ⊔ level(l1) ⊔ level(l2) ⊔ obj_level(l1) ⊔ obj_level(l2) = ⊥;
  ret(level()) = ⊥;
  ret(obj_level()) = ⊤;
  ret(obj_level(l1)) = ⊤;
  ret(obj_level(l2)) = if l1.><l2 ∨ l2.><l1 then ⊤ else ⊥;
  ret(obj_level(this)) = if ¬l1.><this ∧ ¬this.><l1 then obj_level(this) else if l1.><this ∨ this.><l1 then ⊤ else ⊥;
  ret(~l1) = tt;
  ret(.><l1) = tt;
  ret(.><l2) = tt;
  ret(.><this) = this.><l1 ∨ this.><l2;
  ret(l1.><) = tt;
  ret(l1.><l1) = tt;
  ret(l1.><l2) = tt;
  ret(l1.><this) = l1.><this ∨ l2.><this ∨ this.><l1 ∨ this.><l2;
  ret(l2.><) = tt;
  ret(l2.><l1) = tt;
  ret(l2.><l2) = tt;
  ret(l2.><this) = l1.><this ∨ l2.><this ∨ this.><l1 ∨ this.><l2;
  ret(this.><) = l1.><this ∨ l2.><this ∨ this.><l1 ∨ this.><l2;
  ret(this.><l1) = this.><l1 ∨ this.><l2;
  ret(this.><l2) = this.><l1 ∨ this.><l2;
  ret(this.><this) = this.><l1 ∨ this.><l2 ∨ this.><this;
  exc(level()) = ⊥;
  exc(obj_level()) = ⊥;
  exc(obj_level(l1)) = ⊤;
  exc(obj_level(l2)) = if l1.><l2 ∨ l2.><l1 then ⊤ else ⊥;
  exc(obj_level(this)) = if ¬l1.><this ∧ ¬this.><l1 then obj_level(this) else if l1.><this ∨ this.><l1 then ⊤ else ⊥;
  exc(.><l2) = ff;
  exc(.><this) = ff;
  exc(l1.><l1) = tt;
  exc(l1.><l2) = tt;
  exc(l1.><this) = l1.><this ∨ l2.><this ∨ this.><l1 ∨ this.><l2;
  exc(l2.><l1) = tt;
  exc(l2.><l2) = tt;
  exc(l2.><this) = l1.><this ∨ l2.><this ∨ this.><l1 ∨ this.><l2;
  exc(this.><l1) = this.><l1 ∨ this.><l2;
  exc(this.><l2) = this.><l1 ∨ this.><l2;
  exc(this.><this) = this.><l1 ∨ this.><l2 ∨ this.><this;
}