void securibench.micro.sanitizers.Sanitizers4:doGet (javax.servlet.http.HttpServletRequest
                                                       l1,
                                                     javax.servlet.http.HttpServletResponse
                                                       l2) {
  guard = ff;
  ret(obj_level(l1)) = ⊥;
  ret(obj_level(l2)) = ⊥;
  ret(obj_level(this)) = ⊥;
  ret(l1~this) = ff;
  ret(l2~this) = ff;
  exc(level()) = ⊥;
  exc(obj_level()) = ⊥;
  exc(obj_level(l1)) = ⊥;
  exc(obj_level(l2)) = ⊥;
  exc(obj_level(this)) = ⊥;
  exc(~this) = ff;
  exc(l1~this) = ff;
  exc(l2~this) = ff;
}