void securibench.micro.inter.Inter3:f8 (java.lang.String l1) {
  guard = pc ⊔ level(l1) ⊔ level(this) ⊔ obj_level(l1) ⊔ obj_level(this) = ⊥;
  ret(obj_level(l1)) = ⊤;
  ret(obj_level(this)) = if l1.><this ∨ this.><l1 then ⊤ else ⊥;
  ret(l1.><l1) = tt;
  ret(l1.><this) = tt;
  ret(this.><l1) = tt;
  ret(this.><this) = tt;
  exc(level()) = ⊥;
  exc(obj_level()) = ⊥;
  exc(obj_level(l1)) = ⊤;
  exc(obj_level(this)) = if l1.><this ∨ this.><l1 then ⊤ else ⊥;
  exc(.><this) = ff;
  exc(l1.><l1) = tt;
  exc(l1.><this) = tt;
  exc(this.><l1) = tt;
  exc(this.><this) = tt;
}