void org.hamcrest.core.DescribedAs:describeTo(org.hamcrest.Description description)
{
java.util.regex.Matcher arg;
  int textStart;
  java.lang.String $stack4;
  java.util.regex.Pattern $stack5;
  boolean $stack7;
  int $stack9;
  java.lang.Object[] $stack17;
  java.lang.Object $stack20;
  $stack5=org.hamcrest.core.DescribedAs.ARG_PATTERN;
 $stack4=this.descriptionTemplate;
 arg = $stack5.matcher((java.lang.CharSequence)$stack4);
 textStart=0;
 label0:$stack7 = arg.find();
 if ($stack7==0) goto label1;
 $stack4=this.descriptionTemplate;
 $stack9 = arg.start();
 $stack4 = $stack4.substring(textStart,$stack9);
 description.appendText($stack4);
 $stack17=this.values;
 $stack4 = arg.group(1);
 textStart = java.lang.Integer.parseInt($stack4);
 $stack20=$stack17[textStart];
 description.appendValue($stack20);
 textStart = arg.end();
 goto label0;
 label1:$stack4=this.descriptionTemplate;
 $stack9 = $stack4.length();
 if (textStart>=$stack9) goto label2;
 $stack4=this.descriptionTemplate;
 $stack4 = $stack4.substring(textStart);
 description.appendText($stack4);
 label2:return;
 } 

