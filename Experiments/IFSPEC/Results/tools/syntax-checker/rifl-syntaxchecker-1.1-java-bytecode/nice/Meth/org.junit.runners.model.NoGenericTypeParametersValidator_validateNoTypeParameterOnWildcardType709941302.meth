void org.junit.runners.model.NoGenericTypeParametersValidator:validateNoTypeParameterOnWildcardType(java.lang.reflect.WildcardType wildcard,java.util.List errors)
{
java.lang.reflect.Type[] arr$;
  int len$;
  int i$;
  java.lang.reflect.Type each;
  arr$ = wildcard.getUpperBounds();
 len$=#arr$;
 i$=0;
 label0:if (i$>=len$) goto label1;
 each=arr$[i$];
 this#org.junit.runners.model.NoGenericTypeParametersValidator(each,errors);
 i$=(i$+1);
 goto label0;
 label1:arr$ = wildcard.getLowerBounds();
 len$=#arr$;
 i$=0;
 label2:if (i$>=len$) goto label3;
 each=arr$[i$];
 this#org.junit.runners.model.NoGenericTypeParametersValidator(each,errors);
 i$=(i$+1);
 goto label2;
 label3:return;
 } 

