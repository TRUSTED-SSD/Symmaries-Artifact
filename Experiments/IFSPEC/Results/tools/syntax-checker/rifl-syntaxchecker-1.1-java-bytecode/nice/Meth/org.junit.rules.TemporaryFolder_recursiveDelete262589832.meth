void org.junit.rules.TemporaryFolder:recursiveDelete(java.io.File file)
{
java.io.File[] files;
  int len$;
  int i$;
  java.io.File each;
  files = file.listFiles();
 if (files==null) goto label0;
 len$=#files;
 i$=0;
 label1:if (i$>=len$) goto label0;
 each=files[i$];
 this#org.junit.rules.TemporaryFolder(each);
 i$=(i$+1);
 goto label1;
 label0:file.delete();
 return;
 } 

