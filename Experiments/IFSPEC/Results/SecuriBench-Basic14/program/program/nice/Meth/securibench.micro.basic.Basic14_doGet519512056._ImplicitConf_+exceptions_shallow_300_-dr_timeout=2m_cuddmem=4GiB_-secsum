void securibench.micro.basic.Basic14:doGet (javax.servlet.http.HttpServletRequest
                                              l1,
                                            javax.servlet.http.HttpServletResponse
                                              l2) {
  p0: javax.servlet.ServletConfig () -> level;
  guard = p0 ⊔ pc ⊔ level(l2) ⊔ obj_level(l2) ⊔ obj_level(this) = ⊥;
  ret(obj_level(l1)) = obj_level(l1);
  ret(obj_level(l2)) = ⊥;
  ret(obj_level(this)) = ⊥;
  exc(level()) = ⊥;
  exc(obj_level()) = ⊥;
  exc(obj_level(l1)) = obj_level(l1);
  exc(obj_level(l2)) = ⊥;
  exc(obj_level(this)) = ⊥;
}