java.lang.String securibench.micro.sanitizers.Sanitizers2:clean (java.lang.String
                                                                   @l1) {
  guard = tt;
  ret(level()) = pc ⊔ level(@l1) ⊔ obj_level(@l1);
  ret(obj_level()) = pc ⊔ level(@l1) ⊔ obj_level(@l1);
  ret(obj_level(@l1)) = if obj_level(@l1) = ⊤ ∨ pc ≠ ⊤ ∧ level(@l1) = ⊤ ∧ obj_level(@l1) = ⊥ then ⊤ else ⊥;
  ret(obj_level(this)) = if obj_level(this) = ⊤ ∨ this.><@l1 ∧ pc ≠ ⊤ ∧ level(@l1) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
  ret(~@l1) = tt;
  ret(.><@l1) = @l1.><@l1;
  ret(.><this) = this.><@l1;
  ret(@l1.><) = @l1.><@l1;
  ret(@l1.><@l1) = @l1.><@l1;
  ret(@l1.><this) = @l1.><this;
  ret(this.><) = @l1.><this;
  ret(this.><@l1) = this.><@l1;
  ret(this.><this) = this.><this;
  exc(level()) = pc ⊔ level(@l1) ⊔ obj_level(@l1);
  exc(obj_level()) = pc ⊔ level(@l1) ⊔ obj_level(@l1);
  exc(obj_level(@l1)) = if obj_level(@l1) = ⊤ ∨ pc ≠ ⊤ ∧ level(@l1) = ⊤ ∧ obj_level(@l1) = ⊥ then ⊤ else ⊥;
  exc(obj_level(this)) = if obj_level(this) = ⊤ ∨ this.><@l1 ∧ pc ≠ ⊤ ∧ level(@l1) = ⊤ ∧ obj_level(this) = ⊥ then ⊤ else ⊥;
  exc(.><this) = ff;
  exc(@l1.><@l1) = @l1.><@l1;
  exc(@l1.><this) = @l1.><this;
  exc(this.><@l1) = this.><@l1;
  exc(this.><this) = this.><this;
}