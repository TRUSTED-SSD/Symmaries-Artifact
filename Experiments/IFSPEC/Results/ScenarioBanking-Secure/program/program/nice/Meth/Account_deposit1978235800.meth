void Account:deposit(double l1)
{
byte $stack3;
  double $stack4;
  $stack3=(l1 cmpl 0.0);
 if ($stack3<=0) goto label0;
 $stack4=dummyClass.highSource();
 l1=($stack4+l1);
 this.balance=l1;
 this#Account:logTransaction((boolean)1);
 goto label1;
 label0:this.logError("Cannot deposit a non-positive amount.");
 label1:return;
 } 

